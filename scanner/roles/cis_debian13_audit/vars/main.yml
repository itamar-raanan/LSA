---
# Remediation guidance extracted from CIS Debian Linux 13 Benchmark v1.0.0.
# Review impact and test changes before applying them to production systems.
cis_remediations:
  '1.1.1.1': !unsafe |-
    Unload and disable the cramfs kernel module.

                     1. Run the following commands to unload the cramfs kernel module:

                # modprobe -r cramfs 2>/dev/null
                # rmmod cramfs 2>/dev/null

                     2. Perform the following to disable the cramfs kernel module:

                Create a file ending in .conf with install cramfs /bin/false in the
                /etc/modprobe.d/ directory.
                Example:
                # printf '%s\n' "" "install cramfs /bin/false" >> /etc/modprobe.d/60-
                cramfs.conf
                Create a file ending in .conf with blacklist cramfs in the /etc/modprobe.d/
                directory.
                Example:
                # printf '%s\n' "" "blacklist cramfs" >> /etc/modprobe.d/60-cramfs.conf

                References:

                     1. CCI-000381
                     2. NIST SP 800-53: CM-7 a
                     3. NIST SP 800-53A :: CM-7.1 (ii)
                     4. STIG ID: RHEL-08-040025 | RULE ID: SV-230498r1069314 | CAT III
                     5. STIG ID: OL08-00-040025 | RULE ID: SV-248833r958478 | CAT III
                     6. STIG ID: RHEL-09-231195 | RULE ID: SV-257880r1044951 | CAT III
                     7. STIG ID: ALMA-09-029940 | RULE ID: SV-269344r1050226 | CAT II
                     8. STIG ID: OL09-00-000045 | RULE ID: SV-271448r1091056 | CAT III

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●      ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●      ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '1.1.1.2': !unsafe |-
    Unload and disable the freevxfs kernel module.

                     1. Run the following commands to unload the freevxfs kernel module:

                # modprobe -r freevxfs 2>/dev/null
                # rmmod freevxfs 2>/dev/null

                     2. Perform the following to disable the freevxfs kernel module:

                Create a file ending in .conf with install freevxfs /bin/false in the
                /etc/modprobe.d/ directory.
                Example:
                # printf '%s\n' "" "install freevxfs /bin/false" >> /etc/modprobe.d/60-
                freevxfs.conf
                Create a file ending in .conf with blacklist freevxfs in the /etc/modprobe.d/
                directory.
                Example:
                # printf '%s\n' "" "blacklist freevxfs" >> /etc/modprobe.d/60-freevxfs.conf

                References:

                     1. CCI-000381
                     2. NIST SP 800-53 :: CM-7
                     3. NIST SP 800-53A :: CM-7.1 (ii)

                CIS Controls:

                   Controls
                                                                 Control                                          IG 1 IG 2 IG 3
                   Version
                               4.8 Uninstall or Disable Unnecessary Services on
                               Enterprise Assets and Software
                        v8        Uninstall or disable unnecessary services on enterprise assets and software,          ●      ●
                               such as an unused file sharing service, web application module, or service
                               function.
                               9.2 Ensure Only Approved Ports, Protocols and Services
                        v7     Are Running                                                                              ●      ●
                                  Ensure that only network ports, protocols, and services listening on a system
                               with validated business needs, are running on each system.
  '1.1.1.3': !unsafe |-
    Unload and disable the hfs kernel module.

                     1. Run the following commands to unload the hfs kernel module:

                modprobe -r hfs 2>/dev/null
                rmmod hfs 2>/dev/null

                     2. Perform the following to disable the hfs kernel module:

                Create a file ending in .conf with install hfs /bin/false in the /etc/modprobe.d/
                directory.
                Example:
                # printf '%s\n' "" "install hfs /bin/false" >> /etc/modprobe.d/60-hfs.conf
                Create a file ending in .conf with blacklist hfs in the /etc/modprobe.d/ directory.
                Example:
                printf '%s\n' "" "blacklist hfs" >> /etc/modprobe.d/60-hfs.conf

                References:

                     1. CCI-000381
                     2. NIST SP 800-53 :: CM-7
                     3. NIST SP 800-53A :: CM-7.1 (ii)

                CIS Controls:

                   Controls
                                                                 Control                                          IG 1 IG 2 IG 3
                   Version
                               4.8 Uninstall or Disable Unnecessary Services on
                               Enterprise Assets and Software
                        v8        Uninstall or disable unnecessary services on enterprise assets and software,          ●      ●
                               such as an unused file sharing service, web application module, or service
                               function.
                               9.2 Ensure Only Approved Ports, Protocols and Services
                        v7     Are Running                                                                              ●      ●
                                  Ensure that only network ports, protocols, and services listening on a system
                               with validated business needs, are running on each system.
  '1.1.1.4': !unsafe |-
    Unload and disable the hfsplus kernel module.

                     1. Run the following commands to unload the hfsplus kernel module:

                # modprobe -r hfsplus 2>/dev/null
                # rmmod hfsplus 2>/dev/null

                     2. Perform the following to disable the hfsplus kernel module:

                Create a file ending in .conf with install hfsplus /bin/false in the
                /etc/modprobe.d/ directory.
                Example:
                # printf '%s\n' "" "install hfsplus /bin/false" >> /etc/modprobe.d/60-
                hfsplus.conf
                Create a file ending in .conf with blacklist hfsplus in the /etc/modprobe.d/
                directory.
                Example:
                # printf '%s\n' "" "blacklist hfsplus" >> /etc/modprobe.d/60-hfsplus.conf

                References:

                     1. CCI-000381
                     2. NIST SP 800-53 :: CM-7
                     3. NIST SP 800-53A :: CM-7.1 (ii)

                CIS Controls:

                   Controls
                                                                 Control                                         IG 1 IG 2 IG 3
                   Version
                               4.8 Uninstall or Disable Unnecessary Services on
                               Enterprise Assets and Software
                        v8        Uninstall or disable unnecessary services on enterprise assets and software,         ●      ●
                               such as an unused file sharing service, web application module, or service
                               function.
                               9.2 Ensure Only Approved Ports, Protocols and Services
                        v7
                               Are Running
                                                                                                                       ●      ●

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '1.1.1.5': !unsafe |-
    Unload and disable the jffs2 kernel module.

                     1. Run the following commands to unload the jffs2 kernel module:

                # modprobe -r jffs2 2>/dev/null
                # rmmod jffs2 2>/dev/null

                     2. Perform the following to disable the jffs2 kernel module:

                Create a file ending in .conf with install jffs2 /bin/false in the
                /etc/modprobe.d/ directory.
                Example:
                # printf '%s\n' "" "install jffs2 /bin/false" >> /etc/modprobe.d/60-
                jffs2.conf
                Create a file ending in .conf with blacklist jffs2 in the /etc/modprobe.d/
                directory.
                Example:
                # printf '%s\n' "" "blacklist jffs2" >> /etc/modprobe.d/60-jffs2.conf

                References:

                     1. CCI-000381
                     2. NIST SP 800-53 :: CM-7
                     3. NIST SP 800-53A :: CM-7.1 (ii)

                CIS Controls:

                   Controls
                                                                 Control                                         IG 1 IG 2 IG 3
                   Version
                               4.8 Uninstall or Disable Unnecessary Services on
                               Enterprise Assets and Software
                        v8        Uninstall or disable unnecessary services on enterprise assets and software,         ●      ●
                               such as an unused file sharing service, web application module, or service
                               function.
                               9.2 Ensure Only Approved Ports, Protocols and Services
                        v7
                               Are Running
                                                                                                                       ●      ●

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '1.1.1.6': !unsafe |-
    Unload and disable the overlay kernel module.

                     1. Run the following commands to unload the overlay kernel module:

                # modprobe -r overlay 2>/dev/null
                # rmmod overlay 2>/dev/null

                     2. Perform the following to disable the overlay kernel module:

                Create a file ending in .conf with install overlay /bin/false in the
                /etc/modprobe.d/ directory
                Example:
                # printf '%s\n' "" "install overlay /bin/false" >> /etc/modprobe.d/60-
                overlay.conf
                Create a file ending in .conf with blacklist overlay in the /etc/modprobe.d/
                directory
                Example:
                # printf '%s\n' "" "blacklist overlay" >> /etc/modprobe.d/60-overlay.conf

                References:

                     1. CCI-000381
                     2. NIST SP 800-53 :: CM-7
                     3. NIST SP 800-53A :: CM-7.1 (ii)
                     4. https://docs.kernel.org/filesystems/overlayfs.html
                     5. https://wiki.archlinux.org/title/Overlay_filesystem
                     6. https://cve.mitre.org/cgi-bin/cvekey.cgi?keyword=overlayfs

                CIS Controls:

                   Controls
                                                                 Control                                          IG 1 IG 2 IG 3
                   Version
                               4.8 Uninstall or Disable Unnecessary Services on
                               Enterprise Assets and Software
                        v8        Uninstall or disable unnecessary services on enterprise assets and software,          ●      ●
                               such as an unused file sharing service, web application module, or service
                               function.
                               9.2 Ensure Only Approved Ports, Protocols and Services
                        v7     Are Running                                                                              ●      ●
                                  Ensure that only network ports, protocols, and services listening on a system
                               with validated business needs, are running on each system.
  '1.1.1.7': !unsafe |-
    Unload and disable the squashfs kernel module.

                     1. Run the following commands to unload the squashfs kernel module:

                # modprobe -r squashfs 2>/dev/null
                # rmmod squashfs 2>/dev/null

                     2. Perform the following to disable the squashfs kernel module:

                Create a file ending in .conf with install squashfs /bin/false in the
                /etc/modprobe.d/ directory.
                Example:
                # printf '%s\n' "" "install squashfs /bin/false" >> /etc/modprobe.d/60-
                squashfs.conf
                Create a file ending in .conf with blacklist squashfs in the /etc/modprobe.d/
                directory.
                Example:
                # printf '%s\n' "" "blacklist squashfs" >> /etc/modprobe.d/60-squashfs.conf

                References:

                     1. CCI-000381
                     2. NIST SP 800-53 :: CM-7
                     3. NIST SP 800-53A :: CM-7.1 (ii)
                     4. STIG ID: ALMA-09-030160 | RULE ID: SV-269346r1050228 | CAT II

                CIS Controls:

                   Controls
                                                                 Control                                          IG 1 IG 2 IG 3
                   Version
                               4.8 Uninstall or Disable Unnecessary Services on
                               Enterprise Assets and Software
                        v8        Uninstall or disable unnecessary services on enterprise assets and software,          ●      ●
                               such as an unused file sharing service, web application module, or service
                               function.
                               9.2 Ensure Only Approved Ports, Protocols and Services
                        v7     Are Running                                                                              ●      ●
                                  Ensure that only network ports, protocols, and services listening on a system
                               with validated business needs, are running on each system.
  '1.1.1.8': !unsafe |-
    Unload and disable the udf kernel module.

                     1. Run the following commands to unload the udf kernel module:

                # modprobe -r udf 2>/dev/null
                # rmmod udf 2>/dev/null

                     2. Perform the following to disable the udf kernel module:

                Create a file ending in .conf with install udf /bin/false in the /etc/modprobe.d/
                directory
                Example:
                # printf '%s\n' "" "install udf /bin/false" >> /etc/modprobe.d/60-udf.conf
                Create a file ending in .conf with blacklist udf in the /etc/modprobe.d/ directory
                Example:
                # printf '%s\n' "" "blacklist udf" >> /etc/modprobe.d/60-udf.conf

                References:

                     1. CCI-000381
                     2. NIST SP 800-53 :: CM-7
                     3. NIST SP 800-53A :: CM-7.1 (ii)
                     4. STIG ID: ALMA-09-030380 | RULE ID: SV-269348r1050230 | CAT II

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●      ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●      ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '1.1.1.9': !unsafe |-
    Unload and disable the firewire-core kernel module.

                     1. Run the following commands to unload the firewire-core kernel module:

                # modprobe -r firewire-core 2>/dev/null
                # rmmod firewire-core 2>/dev/null

                     2. Perform the following to disable the firewire-core kernel module:

                Create a file ending in .conf with install firewire-core /bin/false in the
                /etc/modprobe.d/ directory
                Example:
                # printf '%s\n' "" "install firewire-core /bin/false" >> /etc/modprobe.d/60-
                firewire-core.conf
                Create a file ending in .conf with blacklist firewire-core in the
                /etc/modprobe.d/ directory
                Example:
                # printf '%s\n' "" "blacklist firewire-core" >> /etc/modprobe.d/60-firewire-
                core.conf

                References:

                     1. CCI-000381
                     2. NIST SP 800-53 :: CM-7
                     3. NIST SP 800-53A :: CM-7.1 (ii)
                     4. STIG ID: RHEL-08-040026 | RULE ID: SV-230499r1069315 | CAT III
                     5. STIG ID: OL08-00-040026 | RULE ID: SV-248834r958478 | CAT III
                     6. STIG ID: RHEL-09-213055 | RULE ID: SV-257806r1044859 | CAT II
                     7. STIG ID: OL09-00-000042 | RULE ID: SV-271445r1091047 | CAT II

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●      ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●      ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '1.1.1.10': !unsafe |-
    Unload and disable the usb-storage kernel module.

                     1. Run the following commands to unload the usb-storage kernel module:

                # modprobe -r usb-storage 2>/dev/null
                # rmmod usb-storage 2>/dev/null

                     2. Perform the following to disable the usb-storage kernel module:

                Create a file ending in .conf with install usb_storage /bin/false in the
                /etc/modprobe.d/ directory
                Example:
                # printf '%s\n' "" "install usb_storage /bin/false" >> /etc/modprobe.d/60-
                usb-storage.conf
                Create a file ending in .conf with blacklist usb_storage in the /etc/modprobe.d/
                directory
                Example:
                # printf '%s\n' "" "blacklist usb_storage" >> /etc/modprobe.d/60-usb-
                storage.conf

                References:

                     1. CCI-000778 | CCI-001958 | CCI-003959
                     2. NIST SP 800-53 :: IA-3 | NIST SP 800-53 :: CM-7 (9) (b)
                     3. NIST SP 800-53A :: IA-3.1 (ii)
                     4. STIG ID: RHEL-08-040080 | RULE ID: SV-230503r1069316 | CAT II
                     5. STIG ID: OL08-00-040080 | RULE ID: SV-248837r986386 | CAT II
                     6. STIG ID: RHEL-09-291010 | RULE ID: SV-258034r1051267 | CAT II
                     7. STIG ID: ALMA-09-031370 | RULE ID: SV-269357r1050240 | CAT II
                     8. STIG ID: OL09-00-000047 | RULE ID: SV-271450r1092466 | CAT II
                     9. STIG ID: OL09-00-002332 | RULE ID: SV-271702r1091818 | CAT II
                     10. STIG ID: UBTU-22-291010 | RULE ID: SV-260540r986276 | CAT II
                     11. STIG ID: UBTU-24-300039 | RULE ID: SV-270718r1067128 | CAT II
                     12. STIG ID: SLES-15-010480 | RULE ID: SV-234856r958820 | CAT II

                Additional Information:
                An alternative solution to disabling the usb-storage module may be found in USBGuard.
                Use of USBGuard and construction of USB device policies should be done in alignment
                with site policy.

                CIS Controls:

                   Controls
                                                                 Control                                         IG 1 IG 2 IG 3
                   Version
                       v8     10.3 Disable Autorun and Autoplay for Removable Media                               ●    ●      ●
                                 Disable autorun and autoplay auto-execute functionality for removable media.
                              13.7 Manage USB Devices
                       v7        If USB storage devices are required, enterprise software should be used that
                              can configure systems to allow the use of specific devices. An inventory of such
                                                                                                                       ●      ●
                              devices should be maintained.

                1.1.1.11 Ensure unused filesystems kernel modules are not
                available (Manual)
                Profile Applicability:

                • Level 1 - Server
                • Level 1 - Workstation

                Description:
                Filesystem kernel modules are pieces of code that can be dynamically loaded into the
                Linux kernel to extend its filesystem capabilities, or so-called base kernel, of an
                operating system. Filesystem kernel modules are typically used to add support for new
                hardware (as device drivers), or for adding system calls.
                Rationale:
                While loadable filesystem kernel modules are a convenient method of modifying the
                running kernel, this can be abused by attackers on a compromised system to prevent
                detection of their processes or files, allowing them to maintain control over the system.
                Many rootkits make use of loadable filesystem kernel modules in this way.
                Removing support for unneeded filesystem types reduces the local attack surface of the
                system. If this filesystem type is not needed, disable it. The following filesystem kernel
                modules have known CVE's and should be made unavailable if no dependencies exist:

                     •   ceph - CVE-2022-0670
                     •   cifs - CVE-2022-29869
                     •   exfat CVE-2022-29973
                     •   ext CVE-2022-1184
                     •   fat CVE-2022-22043
                     •   fscache CVE-2022-3630
                     •   fuse CVE-2023-0386
                     •   gfs2 CVE-2023-3212
                     •   nfs_common CVE-2023-6660
                     •   nfsd CVE-2022-43945
                     •   smbfs_common CVE-2022-2585

                Impact:
                This list may be quite extensive and covering all edges cases is difficult. Therefore, it's
                crucial to carefully consider the implications and dependencies before making any
                changes to the filesystem kernel module configurations.
                Audit:
                Verify filesystem kernel modules that are not in use on the system are disabled. Review
                currently loaded kernel modules to ensure they are needed and conform to local site
                policy.

                Note: The "Audit script" included at the bottom of the "Audit Procedure" will perform the
                steps included below.

                     1. Run the following command to generate a list of filesystem types currently
                        mounted on the system.

                # findmnt -Dkerno fstype | sort -u
                Review the output. These filesystem types should either be excluded from the audit or if
                no longer needed, unmounted and disabled.
                Example output:
                devtmpfs
                ext4
                tmpfs
                vfat

                     2. Run the following script to generate a list of all file system kernel modules
                        currently installed on the system:

                #!/usr/bin/env bash

                {
                     l_search="$(readlink -e /usr/lib/modules/ || readlink -e /lib/modules/)"
                     a_module=()
                     while IFS= read -r -d $'\0' l_module_dir; do
                        if [ ! "$(basename "$l_module_dir")" = "nls" ]; then
                           while IFS= read -r -d $'\0' l_module_file; do
                              l_mname="$(basename "$l_module_file" | cut -d'.' -f1)"
                              if [ -f "$l_module_file" ] && ! grep -Psiq -- '\b'"$l_mname"'\b'
                \
                                 <<< "${a_module[*]}"; then
                                    a_module+=("$l_mname")
                                 fi
                              done < <(find -L "$l_module_dir" -mindepth 1 -maxdepth 1 -type f -
                print0)
                      fi
                   done < <(find "$l_search"/**/kernel/fs/ -mindepth 1 -maxdepth 1 -type d !
                -empty -print0)
                   printf '%s\n' "" "${a_module[@]}"
                }
                The output is a list of the kernel modules included in the
                /usr/lib/modules/{KERNEL_VERSION}/kernel/fs/ or
                /lib/modules/{KERNEL_VERSION}/kernel/fs/ directories. This list has been sorted
                to be unique values, ignore items in the nls directory, and added to an array called
                a_module.

                     3. Run the following command to generate a list of modules currently loaded into
                        the kernel:

                # lsmod

                     4. compare the output from the list generated in step 2 to the list of loaded modules
                        generated by the lsmod command in step 3. Any module appearing in both lists
                        is currently loaded into the kernel. Review these to verify they are needed and
                        approved by local site policy
                     5. Run the following command to generate a list of modules that are set to be Deny
                        Listed: blacklist {MODULE_NAME}, and/or set be be not loadable: install
                        {MODULE_NAME} /bin/false or install {MODULE_NAME} /bin/true:

                # modprobe --showconfig | grep -Pi -- '^\h*(blacklist|install)\h+'

                     6. Verify all modules in the output from the list generated in step 2 are both Deny
                        Listed: blacklist {MODULE_NAME} and set to be not loadable: install
                        {MODULE_NAME} /bin/false or install {MODULE_NAME} /bin/true.

                Audit Script:
                #!/usr/bin/env bash

                {
                   a_check=() a_output=() a_module=() a_output2=() a_output3=()
                   l_search="$(readlink -e /usr/lib/modules/ || readlink -e /lib/modules/)"
                   IFS=$'\n' read -r -d '' -a a_mounted < <(findmnt -Dkerno fstype \
                   | sort -u && printf '\0' )
                   IFS=$'\n' read -r -d '' -a a_lsmod < <(lsmod | awk '{print $1}' && printf '\0' )
                   IFS=$'\n' read -r -d '' -a a_showconfig < <(modprobe --showconfig | \
                   grep -Pi -- '^\h*(blacklist|install)\h+' && printf '\0')
                   while IFS= read -r -d $'\0' l_module_dir; do
                      if [ ! "$(basename "$l_module_dir")" = "nls" ]; then
                         while IFS= read -r -d $'\0' l_module_file; do
                            l_mname="$(basename "$l_module_file" | cut -d'.' -f1)"
                            if [ -f "$l_module_file" ] && ! grep -Psiq -- '\b'"$l_mname"'\b' <<<
                "${a_module[*]}"; then
                               a_module+=("$l_mname")
                            fi
                         done < <(find -L "$l_module_dir" -mindepth 1 -maxdepth 1 -type f -print0)
                      fi
                   done < <(find "$l_search"/**/kernel/fs/ -mindepth 1 -maxdepth 1 -type d ! -empty -
                print0)
                   for l_module in "${a_module[@]}"; do
                      if grep -Psoiq -- '\b'"$l_module"'\b' <<< "${a_mounted[*]}"; then
                         a_output+=(" - \"$l_module\"")
                      elif grep -Psoiq -- '\b'"$l_module"'\b' <<< "${a_lsmod[*]}"; then
                         a_output2+=(" - \"$l_module\"")
                      elif ! grep -Psioq -- '\binstall\h+'"${l_module//-/_}"'\h+\H+\b' <<<
                "${a_showconfig[*]}" || \
                      ! grep -Psioq -- '\bblacklist\h+'"${l_module//-/_}"'\b' <<<
                "${a_showconfig[*]}"; then
                         a_output3+=(" - \"$l_module\"")
                      fi
                   done
                   [ "${#a_output[@]}" -gt 0 ] && printf '%s\n' "" \
                   "- There are \"${#a_output[@]}\" kernel modules currently mounted:"
                "${a_output[@]}"
                   [ "${#a_output2[@]}" -gt 0 ] && printf '%s\n' "" \
                   "- There are \"${#a_output2[@]}\" kernel modules currently loaded:"
                "${a_output2[@]}"
                   [ "${#a_output3[@]}" -gt 0 ] && printf '%s\n' "" \
                   "- There are \"${#a_output3[@]}\" Kernel modules currently loadable:"
                "${a_output3[@]}"
                }
                WARNING: disabling or denylisting filesystem modules that are in use on the system
                may be FATAL. It is extremely important to thoroughly review this list.

                Remediation:
                - IF - the module is available in the running kernel:

                     •   Unload the filesystem kernel module from the kernel
                     •   Create a file ending in .conf with install filesystem kernel modules /bin/false
                         in the /etc/modprobe.d/ directory
                     •   Create a file ending in .conf with deny list filesystem kernel modules in the
                         /etc/modprobe.d/ directory

                WARNING: unloading, disabling or denylisting filesystem modules that are in use on the
                system may be FATAL. It is extremely important to thoroughly review the filesystems
                returned by the audit before following the remediation procedure.
                Example of unloading the gfs2kernel module:
                modprobe -r gfs2 2>/dev/null
                rmmod gfs2 2>/dev/null
                Example of fully disabling the gfs2 kernel module:
                printf '%s\n' "" "blacklist gfs2" "install gfs2 /bin/false" >>
                /etc/modprobe.d/gfs2.conf
                Note:

                     •   Disable a kernel module by modifying the command above for each unused
                         filesystem kernel module
                     •   The example gfs2 must be updated with the appropriate module name for the
                         command or example script bellow to run correctly.

                Below is an example script that can be modified to use on various filesystem
                kernel modules manual remediation process:
                Example Script

                #!/usr/bin/env bash

                {
                   a_output2=(); a_output3=(); l_dl="" # Initialize arrays and clear
                variables
                   l_mod_name="gfs2" # set module name
                   l_mod_type="fs" # set module type
                   l_mod_path="$(readlink -f /usr/lib/modules/**/kernel/$l_mod_type ||
                readlink -f /lib/modules/**/kernel/$l_mod_type)"
                   f_module_fix()
                   {
                      l_dl="y" # Set to ignore duplicate checks
                      a_showconfig=() # Create array with modprobe output
                      while IFS= read -r l_showconfig; do
                          a_showconfig+=("$l_showconfig")
                      done < <(modprobe --showconfig | grep -P --
                '\b(install|blacklist)\h+'"${l_mod_name//-/_}"'\b')
                      if lsmod | grep "$l_mod_name" &> /dev/null; then # Check if the module
                is currently loaded
                          a_output2+=(" - unloading kernel module: \"$l_mod_name\"")
                          modprobe -r "$l_mod_name" 2>/dev/null; rmmod "$l_mod_name"
                2>/dev/null
                      fi
                      if ! grep -Pq -- '\binstall\h+'"${l_mod_name//-
                /_}"'\h+(\/usr)?\/bin\/(true|false)\b' <<< "${a_showconfig[*]}"; then
                          a_output2+=(" - setting kernel module: \"$l_mod_name\" to
                \"$(readlink -f /bin/false)\"")
                          printf '%s\n' "install $l_mod_name $(readlink -f /bin/false)" >>
                /etc/modprobe.d/"$l_mod_name".conf
                      fi
                      if ! grep -Pq -- '\bblacklist\h+'"${l_mod_name//-/_}"'\b' <<<
                "${a_showconfig[*]}"; then
                          a_output2+=(" - denylisting kernel module: \"$l_mod_name\"")
                          printf '%s\n' "blacklist $l_mod_name" >>
                /etc/modprobe.d/"$l_mod_name".conf
                      fi
                   }
                   for l_mod_base_directory in $l_mod_path; do # Check if the module exists
                on the system
                      if [ -d "$l_mod_base_directory/${l_mod_name/-/\/}" ] && [ -n "$(ls -A
                "$l_mod_base_directory/${l_mod_name/-/\/}")" ]; then
                          a_output3+=(" - \"$l_mod_base_directory\"")
                          [[ "$l_mod_name" =~ overlay ]] && l_mod_name="${l_mod_name::-2}"
                          [ "$l_dl" != "y" ] && f_module_fix
                      else
                          echo -e " - kernel module: \"$l_mod_name\" doesn't exist in
                \"$l_mod_base_directory\""
                      fi
                   done
                   [ "${#a_output3[@]}" -gt 0 ] && printf '%s\n' "" " -- INFO --" " - module:
                \"$l_mod_name\" exists in:" "${a_output3[@]}"
                   [ "${#a_output2[@]}" -gt 0 ] && printf '%s\n' "" "${a_output2[@]}" ||
                printf '%s\n' "" " - No changes needed"
                   printf '%s\n' "" " - remediation of kernel module: \"$l_mod_name\"
                complete" ""
                }

                References:

                     1. https://cve.mitre.org/cgi-bin/cvekey.cgi?keyword=filesystem

                CIS Controls:

                   Controls
                                                                 Control                                          IG 1 IG 2 IG 3
                   Version
                               4.8 Uninstall or Disable Unnecessary Services on
                               Enterprise Assets and Software
                        v8        Uninstall or disable unnecessary services on enterprise assets and software,          ●      ●
                               such as an unused file sharing service, web application module, or service
                               function.
                               9.2 Ensure Only Approved Ports, Protocols and Services
                        v7     Are Running                                                                              ●      ●
                                  Ensure that only network ports, protocols, and services listening on a system
                               with validated business needs, are running on each system.

                1.1.2 Configure Filesystem Partitions

                Directories that are used for system-wide functions can be further protected by placing
                them on separate partitions. This provides protection for resource exhaustion and
                enables the use of mounting options that are applicable to the directory's intended use.
                Users' data can be stored on separate partitions and have stricter mount options. A user
                partition is a filesystem that has been established for use by the users and does not
                contain software for system operations.
                The recommendations in this section are easier to perform during initial system
                installation. If the system is already installed, it is recommended that a full backup be
                performed before repartitioning the system.
                Note:
                -IF- you are repartitioning a system that has already been installed (This may require
                the system to be in single-user mode):

                     •   Mount the new partition to a temporary mountpoint e.g. mount /dev/sda2 /mnt
                     •   Copy data from the original partition to the new partition. e.g. cp -a /var/tmp/*
                         /mnt
                     •   Verify that all data is present on the new partition. e.g. ls -la /mnt
                     •   Unmount the new partition. e.g. umount /mnt
                     •   Remove the data from the original directory that was in the old partition. e.g. rm
                         -Rf /var/tmp/* Otherwise it will still consume space in the old partition that will
                         be masked when the new filesystem is mounted.
                     •   Mount the new partition to the desired mountpoint. e.g. mount /dev/sda2
                         /var/tmp
                     •   Update /etc/fstab with the new mountpoint. e.g. /dev/sda2 /var/tmp xfs
                         defaults,rw,nosuid,nodev,noexec,relatime 0 0

                1.1.2.1 Configure /tmp
                The /tmp directory is a world-writable directory used to store data used by the system
                and user applications for a short period of time. This data should have no expectation of
                surviving a reboot, as this directory is intended to be emptied after each reboot.
  '1.1.1.11': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '1.1.2.1.1': !unsafe |-
    First ensure that systemd is correctly configured to ensure that /tmp will be mounted at
                boot time.
                # systemctl unmask tmp.mount
                For specific configuration requirements of the /tmp mount for your environment, modify
                /etc/fstab.
                Example of using tmpfs with specific mount options:
                tmpfs         /tmp     tmpfs        defaults,rw,nosuid,nodev,noexec,relatime,size=2G   0
                0
                Note: the size=2G is an example of setting a specific size for tmpfs.
                Example of using a volume or disk with specific mount options. The source location of
                the volume or disk will vary depending on your environment:
                <device> /tmp            <fstype>      defaults,nodev,nosuid,noexec   0 0

                References:

                     1. https://www.freedesktop.org/wiki/Software/systemd/APIFileSystems/
                     2. https://www.freedesktop.org/software/systemd/man/systemd-fstab-generator.html
                     3. https://www.kernel.org/doc/Documentation/filesystems/tmpfs.txt
                     4. CCI-000366
                     5. NIST SP 800-53 :: CM-6 b
                     6. NIST SP 800-53A :: CM-6.1 (iv)
                     7. STIG ID: RHEL-08-010543 | RULE ID: SV-230295r1017106 | CAT II
                     8. STIG ID: RHEL-09-231015 | RULE ID: SV-257844r1044918 | CAT II
                     9. STIG ID: ALMA-09-025540 | RULE ID: SV-269305r1050187 | CAT II

                Additional Information:
                /tmp can be configured to use tmpfs.

                tmpfs puts everything into the kernel internal caches and grows and shrinks to
                accommodate the files it contains and is able to swap unneeded pages out to swap
                space. It has maximum size limits which can be adjusted on the fly via mount -o
                remount.
                Since tmpfs lives completely in the page cache and on swap, all tmpfs pages will be
                shown as "Shmem" in /proc/meminfo and "Shared" in free. Notice that these
                counters also include shared memory. The most reliable way to get the count is using
                df and du.
                tmpfs has three mount options for sizing:

                     •    size: The limit of allocated bytes for this tmpfs instance. The default is half of
                          your physical RAM without swap. If you oversize your tmpfs instances the
                          machine will deadlock since the OOM handler will not be able to free that
                          memory.
                     •    nr_blocks: The same as size, but in blocks of PAGE_SIZE.
                     •    nr_inodes: The maximum number of inodes for this instance. The default is half
                          of the number of your physical RAM pages, or (on a machine with highmem) the
                          number of lowmem RAM pages, whichever is the lower.

                These parameters accept a suffix k, m or g and can be changed on remount. The size
                parameter also accepts a suffix % to limit this tmpfs instance to that percentage of your
                physical RAM. The default, when neither size nor nr_blocks is specified, is size=50%.

                CIS Controls:

                  Controls
                                                                    Control                                          IG 1 IG 2 IG 3
                  Version
                               3.3 Configure Data Access Control Lists
                         v8       Configure data access control lists based on a user’s need to know. Apply data
                               access control lists, also known as access permissions, to local and remote file
                                                                                                                      ●    ●      ●
                               systems, databases, and applications.
                               14.6 Protect Information through Access Control Lists
                                   Protect all information stored on systems with file system, network share,
                         v7    claims, application, or database specific access control lists. These controls will
                               enforce the principle that only authorized individuals should have access to the
                                                                                                                      ●    ●      ●
                               information based on their need to access the information as a part of their
                               responsibilities.
  '1.1.2.1.2': !unsafe |-
    - IF - a separate partition exists for /tmp.
                Edit the /etc/fstab file and add nodev to the fourth field (mounting options) for the
                /tmp partition.
                Example:
                <device> /tmp       <fstype>        defaults,rw,nosuid,nodev,noexec,relatime        0 0
                Run the following command to remount /tmp with the configured options:
                # mount -o remount /tmp

                References:

                     1. CCI-001764
                     2. NIST SP 800-53 :: CM-7 (2)
                     3. STIG ID: RHEL-08-040123 | RULE ID: SV-230511r958804 | CAT II
                     4. STIG ID: RHEL-09-231125 | RULE ID: SV-257866r958804 | CAT II
                     5. STIG ID: ALMA-09-026860 | RULE ID: SV-269317r1050199 | CAT II

                Additional Information:
                See the fstab(5) manual page for more information.

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●      ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●      ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '1.1.2.1.3': !unsafe |-
    - IF - a separate partition exists for /tmp.
                Edit the /etc/fstab file and add nosuid to the fourth field (mounting options) for the
                /tmp partition.
                Example:
                <device> /tmp       <fstype>        defaults,rw,nosuid,nodev,noexec,relatime         0 0
                Run the following command to remount /tmp with the configured options:
                # mount -o remount /tmp

                References:

                     1. CCI-001764
                     2. NIST SP 800-53 :: CM-7 (2)
                     3. STIG ID: RHEL-08-040124 | RULE ID: SV-230512r958804 | CAT II
                     4. STIG ID: RHEL-09-231135 | RULE ID: SV-257868r958804 | CAT II
                     5. STIG ID: ALMA-09-027080 | RULE ID: SV-269319r1050201 | CAT II

                Additional Information:
                See the fstab(5) manual page for more information.

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●      ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●      ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '1.1.2.1.4': !unsafe |-
    - IF - a separate partition exists for /tmp.
                Edit the /etc/fstab file and add noexec to the fourth field (mounting options) for the
                /tmp partition.
                Example:
                <device> /tmp       <fstype>        defaults,rw,nosuid,nodev,noexec,relatime         0 0
                Run the following command to remount /tmp with the configured options:
                # mount -o remount /tmp

                References:

                     1. CCI-001764
                     2. NIST SP 800-53 :: CM-7 (2)
                     3. STIG ID: RHEL-08-040125 | RULE ID: SV-230513r958804 | CAT II
                     4. STIG ID: RHEL-09-231130 | RULE ID: SV-257867r958804 | CAT II
                     5. STIG ID: ALMA-09-026970 | RULE ID: SV-269318r1050200 | CAT II

                Additional Information:
                See the fstab(5) manual page for more information.
                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●      ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●      ●
                              information based on their need to access the information as a part of their
                              responsibilities.

                1.1.2.2 Configure /dev/shm
                The /dev/shm directory is a world-writable directory that can function as shared
                memory that facilitates inter process communication (IPC)
  '1.1.2.2.1': !unsafe |-
    For specific configuration requirements of the /dev/shm mount for your environment,
                modify /etc/fstab.
                Example:
                tmpfs   /dev/shm       tmpfs
                defaults,rw,nosuid,nodev,noexec,relatime,size=2G                               0 0

                References:

                     1. https://www.freedesktop.org/wiki/Software/systemd/APIFileSystems/
                     2. https://www.freedesktop.org/software/systemd/man/systemd-fstab-generator.html
                     3. NIST SP 800-53 Rev. 5: CM-7

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●      ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●      ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '1.1.2.2.2': !unsafe |-
    - IF - a separate partition exists for /dev/shm.
                Edit the /etc/fstab file and add nodev to the fourth field (mounting options) for the
                /dev/shm partition. See the fstab(5) manual page for more information.
                Example:
                tmpfs /dev/shm       tmpfs       defaults,rw,nosuid,nodev,noexec,relatime           0 0
                Run the following command to remount /dev/shm with the configured options:
                # mount -o remount /dev/shm
                Note: It is recommended to use tmpfs as the device/filesystem type as /dev/shm is
                used as shared memory space by applications.
                References:

                     1. CCI-001764
                     2. NIST SP 800-53 :: CM-7 (2)
                     3. STIG ID: RHEL-08-040120 | RULE ID: SV-230508r958804 | CAT II
                     4. STIG ID: RHEL-09-231110 | RULE ID: SV-257863r958804 | CAT II
                     5. STIG ID: ALMA-09-026530 | RULE ID: SV-269314r1050196 | CAT II

                Additional Information:
                Some distributions mount /dev/shm through other means and require /dev/shm to be
                added to /etc/fstab even though it is already being mounted on boot. Others may

                configure /dev/shm in other locations and may override /etc/fstab configuration.
                Consult the documentation appropriate for your distribution.
                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●      ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●      ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '1.1.2.2.3': !unsafe |-
    - IF - a separate partition exists for /dev/shm.
                Edit the /etc/fstab file and add nosuid to the fourth field (mounting options) for the
                /dev/shm partition. See the fstab(5) manual page for more information.
                Example:
                tmpfs /dev/shm       tmpfs       defaults,rw,nosuid,nodev,noexec,relatime          0 0
                Run the following command to remount /dev/shm with the configured options:
                # mount -o remount /dev/shm
                Note: It is recommended to use tmpfs as the device/filesystem type as /dev/shm is
                used as shared memory space by applications.
                References:

                     1. CCI-001764
                     2. NIST SP 800-53 :: CM-7 (2)
                     3. STIG ID: RHEL-08-040121 | RULE ID: SV-230509r958804 | CAT II
                     4. STIG ID: RHEL-09-231120 | RULE ID: SV-257865r1044946 | CAT II
                     5. STIG ID: ALMA-09-026750 | RULE ID: SV-269316r1050198 | CAT II

                Additional Information:
                Some distributions mount /dev/shm through other means and require /dev/shm to be
                added to /etc/fstab even though it is already being mounted on boot. Others may

                configure /dev/shm in other locations and may override /etc/fstab configuration.
                Consult the documentation appropriate for your distribution.
                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●      ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●      ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '1.1.2.2.4': !unsafe |-
    - IF - a separate partition exists for /dev/shm.
                Edit the /etc/fstab file and add noexec to the fourth field (mounting options) for the
                /dev/shm partition.
                Example:
                tmpfs /dev/shm       tmpfs       defaults,rw,nosuid,nodev,noexec,relatime          0 0
                Run the following command to remount /dev/shm with the configured options:
                # mount -o remount /dev/shm
                Note: It is recommended to use tmpfs as the device/filesystem type as /dev/shm is
                used as shared memory space by applications.
                References:

                     1. CCI-001764
                     2. NIST SP 800-53 :: CM-7 (2)
                     3. STIG ID: RHEL-08-040122 | RULE ID: SV-230510r958804 | CAT II
                     4. STIG ID: RHEL-09-231115 | RULE ID: SV-257864r958804 | CAT II
                     5. STIG ID: ALMA-09-026640 | RULE ID: SV-269315r1050197 | CAT II

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●      ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●      ●
                              information based on their need to access the information as a part of their
                              responsibilities.

                1.1.2.3 Configure /home
                Please note that home directories can be mounted anywhere and are not necessarily
                restricted to /home, nor restricted to a single location, nor is the name restricted in any
                way.
                Finding user home directories can be done by looking in /etc/passwd, looking over the
                mounted file systems with mount or querying the relevant database with getent.
                The following script can be run to find user's home directories:
                #!/usr/bin/env bash

                {
                  l_valid_shells="^($(awk -F\/ '$NF != "nologin" {print}' /etc/shells | sed -
                rn '/^\//{s,/,\\\\/,g;p}' | paste -s -d '|' - ))$"
                  awk -v pat="$l_valid_shells" -F:
                '($1!~/^(root|halt|sync|shutdown|nfsnobody)$/ && ($3>='"$(awk
                '/^\s*UID_MIN/{print $2}' /etc/login.defs)"' || $3 != 65534) && $(NF) ~ pat)
                {print $1 " - " $6}' /etc/passwd
                }
  '1.1.2.3.1': !unsafe |-
    For new installations, during installation create a custom partition setup and specify a
                separate partition for /home.
                For systems that were previously installed, create a new partition and configure
                /etc/fstab as appropriate.

                References:

                     1. AJ Lewis, "LVM HOWTO", http://tldp.org/HOWTO/LVM-HOWTO/
                     2. NIST SP 800-53 Rev. 5: CM-7

                Additional Information:
                When modifying /home it is advisable to bring the system to emergency mode (so auditd
                is not running), rename the existing directory, mount the new file system, and migrate
                the data over before returning to multi-user mode.
                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●      ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●      ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '1.1.2.3.2': !unsafe |-
    - IF - a separate partition exists for /home.
                Edit the /etc/fstab file and add nodev to the fourth field (mounting options) for the
                /home partition.
                Example:
                <device> /home           <fstype>              defaults,rw,nosuid,nodev,relatime                     0 0
                Run the following command to remount /home with the configured options:
                # mount -o remount /home

                References:

                     1. See the fstab(5) manual page for more information.
                     2. NIST SP 800-53 Rev. 5: AC-3, MP-2

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●     ●      ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                       v7         Protect all information stored on systems with file system, network share,         ●     ●      ●
                              claims, application, or database specific access control lists. These controls will

                  Controls
                                                                  Control                                        IG 1 IG 2 IG 3
                  Version
                              enforce the principle that only authorized individuals should have access to the
                              information based on their need to access the information as a part of their
                              responsibilities.
  '1.1.2.3.3': !unsafe |-
    - IF - a separate partition exists for /home.
                Edit the /etc/fstab file and add nosuid to the fourth field (mounting options) for the
                /home partition.
                Example:
                <device> /home           <fstype>              defaults,rw,nosuid,nodev,relatime                     0 0
                Run the following command to remount /home with the configured options:
                # mount -o remount /home

                References:

                     1. See the fstab(5) manual page for more information.
                     2. NIST SP 800-53 Rev. 5: AC-3, MP-2

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●     ●      ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                       v7         Protect all information stored on systems with file system, network share,         ●     ●      ●
                              claims, application, or database specific access control lists. These controls will

                  Controls
                                                                  Control                                        IG 1 IG 2 IG 3
                  Version
                              enforce the principle that only authorized individuals should have access to the
                              information based on their need to access the information as a part of their
                              responsibilities.

                1.1.2.4 Configure /var
                The /var directory is used by daemons and other system services to temporarily store
                dynamic data. Some directories created by these processes may be world-writable.
  '1.1.2.4.1': !unsafe |-
    For new installations, during installation create a custom partition setup and specify a
                separate partition for /var.
                For systems that were previously installed, create a new partition and configure
                /etc/fstab as appropriate.

                References:

                     1. AJ Lewis, "LVM HOWTO", http://tldp.org/HOWTO/LVM-HOWTO/
                     2. NIST SP 800-53 Rev. 5: CM-7
                     3. RHEL 8 STIG Vul ID: V-244529
                     4. RHEL 8 STIG Rule ID: SV-244529r902737

                Additional Information:
                When modifying /var it is advisable to bring the system to emergency mode (so auditd
                is not running), rename the existing directory, mount the new file system, and migrate
                the data over before returning to multi-user mode.
                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●      ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●      ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '1.1.2.4.2': !unsafe |-
    - IF - a separate partition exists for /var.
                Edit the /etc/fstab file and add nodev to the fourth field (mounting options) for the
                /var partition.
                Example:
                <device> /var           <fstype>             defaults,rw,nosuid,nodev,relatime                      0 0
                Run the following command to remount /var with the configured options:
                # mount -o remount /var

                References:

                     1. See the fstab(5) manual page for more information.
                     2. NIST SP 800-53 Rev. 5: AC-3, MP-2

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●      ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                       v7         Protect all information stored on systems with file system, network share,         ●    ●      ●
                              claims, application, or database specific access control lists. These controls will

                  Controls
                                                                  Control                                        IG 1 IG 2 IG 3
                  Version
                              enforce the principle that only authorized individuals should have access to the
                              information based on their need to access the information as a part of their
                              responsibilities.
  '1.1.2.4.3': !unsafe |-
    - IF - a separate partition exists for /var.
                Edit the /etc/fstab file and add nosuid to the fourth field (mounting options) for the
                /var partition.
                Example:
                <device> /var           <fstype>             defaults,rw,nosuid,nodev,relatime                      0 0
                Run the following command to remount /var with the configured options:
                # mount -o remount /var

                References:

                     1. See the fstab(5) manual page for more information.
                     2. NIST SP 800-53 Rev. 5: AC-3, MP-2

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●      ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                       v7         Protect all information stored on systems with file system, network share,         ●    ●      ●
                              claims, application, or database specific access control lists. These controls will

                  Controls
                                                                  Control                                        IG 1 IG 2 IG 3
                  Version
                              enforce the principle that only authorized individuals should have access to the
                              information based on their need to access the information as a part of their
                              responsibilities.

                1.1.2.5 Configure /var/tmp
                The /var/tmp directory is a world-writable directory used for temporary storage by all
                users and some applications. Temporary files residing in /var/tmp are to be preserved
                between reboots.
  '1.1.2.5.1': !unsafe |-
    For new installations, during installation create a custom partition setup and specify a
                separate partition for /var/tmp.
                For systems that were previously installed, create a new partition and configure
                /etc/fstab as appropriate.

                References:

                     1. AJ Lewis, "LVM HOWTO", http://tldp.org/HOWTO/LVM-HOWTO/

                     2. NIST SP 800-53 Rev. 5: CM-7
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. RHEL 8 STIG Vul ID: V-244529
                     5. RHEL 8 STIG Rule ID: SV-244529r902737

                Additional Information:
                When modifying /var/tmp it is advisable to bring the system to emergency mode (so
                auditd is not running), rename the existing directory, mount the new file system, and
                migrate the data over before returning to multi-user mode.
                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●      ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●      ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '1.1.2.5.2': !unsafe |-
    - IF - a separate partition exists for /var/tmp.
                Edit the /etc/fstab file and add nodev to the fourth field (mounting options) for the
                /var/tmp partition.
                Example:
                <device> /var/tmp         <fstype>      defaults,rw,nosuid,nodev,noexec,relatime            0
                0
                Run the following command to remount /var/tmp with the configured options:
                # mount -o remount /var/tmp

                References:

                     1. See the fstab(5) manual page for more information.
                     2. NIST SP 800-53 Rev. 5: AC-3, MP-2
                     3. NIST SP 800-53 Revision 5 :: CM-7 (2)
                     4. RHEL 8 STIG Vul ID: V-230520
                     5. RHEL 8 STIG Rule ID: SV-230520r854061

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '1.1.2.5.3': !unsafe |-
    - IF - a separate partition exists for /var/tmp.
                Edit the /etc/fstab file and add nosuid to the fourth field (mounting options) for the
                /var/tmp partition.
                Example:
                <device> /var/tmp         <fstype>      defaults,rw,nosuid,nodev,noexec,relatime         0
                0
                Run the following command to remount /var/tmp with the configured options:
                # mount -o remount /var/tmp

                References:

                     1. See the fstab(5) manual page for more information.
                     2. NIST SP 800-53 Rev. 5: AC-3, MP-2
                     3. NIST SP 800-53 Revision 5 :: CM-7 (2)
                     4. RHEL 8 STIG Vul ID: V-230521
                     5. RHEL 8 STIG STIG ID: RHEL-08-040133

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '1.1.2.5.4': !unsafe |-
    - IF - a separate partition exists for /var/tmp.
                Edit the /etc/fstab file and add noexec to the fourth field (mounting options) for the
                /var/tmp partition.
                Example:
                <device> /var/tmp         <fstype>      defaults,rw,nosuid,nodev,noexec,relatime         0
                0
                Run the following command to remount /var/tmp with the configured options:
                # mount -o remount /var/tmp

                References:

                     1. See the fstab(5) manual page for more information.
                     2. NIST SP 800-53 Rev. 5: AC-3, MP-2
                     3. NIST SP 800-53 Revision 5 :: CM-7 (2)
                     4. RHEL 8 STIG Vul ID: V-230522
                     5. RHEL 8 STIG Rule ID: SV-230522r854063

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.

                1.1.2.6 Configure /var/log
                The /var/log directory is used by system services to store log data.
  '1.1.2.6.1': !unsafe |-
    For new installations, during installation create a custom partition setup and specify a
                separate partition for /var/log .
                For systems that were previously installed, create a new partition and configure
                /etc/fstab as appropriate.

                References:

                     1. AJ Lewis, "LVM HOWTO", http://tldp.org/HOWTO/LVM-HOWTO/
                     2. NIST SP 800-53 Rev. 5:: CM-6 b CM-7
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. RHEL 8 STIG Vul ID: V-230293
                     5. RHEL 8 STIG Rule ID: SV-230293r902720

                Additional Information:
                When modifying /var/log it is advisable to bring the system to emergency mode (so
                auditd is not running), rename the existing directory, mount the new file system, and
                migrate the data over before returning to multiuser mode.
                CIS Controls:

                    Controls
                                                                 Control                                         IG 1 IG 2 IG 3
                    Version
                                8.3 Ensure Adequate Audit Log Storage
                         v8        Ensure that logging destinations maintain adequate storage to comply with      ●    ●    ●
                                the enterprise’s audit log management process.
                                6.4 Ensure adequate storage for logs
                         v7        Ensure that all systems that store logs have adequate storage space for the         ●    ●
                                logs generated.
  '1.1.2.6.2': !unsafe |-
    - IF - a separate partition exists for /var/log.
                Edit the /etc/fstab file and add nodev to the fourth field (mounting options) for the
                /var/log partition.
                Example:
                <device> /var/log         <fstype>      defaults,rw,nosuid,nodev,noexec,relatime             0
                0
                Run the following command to remount /var/log with the configured options:
                # mount -o remount /var/log

                References:

                     1. See the fstab(5) manual page for more information.
                     2. NIST SP 800-53 Rev. 5: AC-3, MP-2
                     3. NIST SP 800-53 Revision 5 :: CM-7 (2)
                     4. RHEL 8 STIG Vul ID: V-230514
                     5. RHEL 8 STIG Rule ID: SV-230514r854055

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '1.1.2.6.3': !unsafe |-
    - IF - a separate partition exists for /var/log.
                Edit the /etc/fstab file and add nosuid to the fourth field (mounting options) for the
                /var/log partition.
                Example:
                <device> /var/log         <fstype>      defaults,rw,nosuid,nodev,noexec,relatime          0
                0
                Run the following command to remount /var/log with the configured options:
                # mount -o remount /var/log

                References:

                     1. See the fstab(5) manual page for more information.
                     2. NIST SP 800-53 Rev. 5: AC-3, MP-2
                     3. NIST SP 800-53 Revision 5 :: CM-7 (2)
                     4. RHEL 8 STIG Vul ID: V-230515
                     5. RHEL 8 STIG Rule ID: SV-230515r854056

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '1.1.2.6.4': !unsafe |-
    - IF - a separate partition exists for /var/log.
                Edit the /etc/fstab file and add noexec to the fourth field (mounting options) for the
                /var/log partition.
                Example:
                <device> /var/log         <fstype>      defaults,rw,nosuid,nodev,noexec,relatime          0
                0
                Run the following command to remount /var/log with the configured options:
                # mount -o remount /var/log

                References:

                     1. See the fstab(5) manual page for more information.
                     2. NIST SP 800-53 Rev. 5: AC-3, MP-2
                     3. NIST SP 800-53 Revision 5 :: CM-7 (2)
                     4. RHEL 8 STIG Vul ID: V-230516
                     5. RHEL 8 STIG Rule ID: SV-230516r854057

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.

                1.1.2.7 Configure /var/log/audit
                The auditing daemon, auditd, stores log data in the /var/log/audit directory.
  '1.1.2.7.1': !unsafe |-
    For new installations, during installation create a custom partition setup and specify a
                separate partition for /var/log/audit.
                For systems that were previously installed, create a new partition and configure
                /etc/fstab as appropriate.

                References:

                     1. AJ Lewis, "LVM HOWTO", http://tldp.org/HOWTO/LVM-HOWTO/
                     2. NIST SP 800-53 Rev. 5: CM-7 CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. RHEL 8 STIG Vul ID: V-230294
                     5. RHEL 8 STIG Rule ID: SV-230294r627750

                Additional Information:
                When modifying /var/log/audit it is advisable to bring the system to emergency
                mode (so auditd is not running), rename the existing directory, mount the new file
                system, and migrate the data over before returning to multi-user mode.
                CIS Controls:

                    Controls
                                                                 Control                                         IG 1 IG 2 IG 3
                    Version
                                8.3 Ensure Adequate Audit Log Storage
                         v8        Ensure that logging destinations maintain adequate storage to comply with      ●    ●    ●
                                the enterprise’s audit log management process.
                                6.4 Ensure adequate storage for logs
                         v7        Ensure that all systems that store logs have adequate storage space for the         ●    ●
                                logs generated.
  '1.1.2.7.2': !unsafe |-
    - IF - a separate partition exists for /var/log/audit.
                Edit the /etc/fstab file and add nodev to the fourth field (mounting options) for the
                /var/log/audit partition.
                Example:
                <device> /var/log/audit    <fstype>
                defaults,rw,nosuid,nodev,noexec,relatime         0 0
                Run the following command to remount /var/log/audit with the configured options:
                # mount -o remount /var/log/audit

                References:

                     1. See the fstab(5) manual page for more information.
                     2. NIST SP 800-53 Revision 5 :: CM-7 (2)
                     3. STIG ID: RHEL-08-040129 | RULE ID: SV-230517r958804 | CAT II
                     4. STIG ID: RHEL-09-231175 | RULE ID: SV-257876r958804 | CAT II
                     5. STIG ID: ALMA-09-027190 | RULE ID: SV-269320r1050202 | CAT II

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '1.1.2.7.3': !unsafe |-
    - IF - a separate partition exists for /var/log/audit.
                Edit the /etc/fstab file and add nosuid to the fourth field (mounting options) for the
                /var/log/audit partition.
                Example:
                <device> /var/log/audit    <fstype>
                defaults,rw,nosuid,nodev,noexec,relatime        0 0
                Run the following command to remount /var/log/audit with the configured options:
                # mount -o remount /var/log/audit

                References:

                     1. See the fstab(5) manual page for more information.
                     2. NIST SP 800-53 Rev. 5: AC-3, MP-2
                     3. NIST SP 800-53 Revision 5 :: CM-7 (2)
                     4. STIG ID: RHEL-08-040130 | RULE ID: SV-230518r958804 | CAT II
                     5. STIG ID: RHEL-09-231170 | RULE ID: SV-257875r958804 | CAT II

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '1.1.2.7.4': !unsafe |-
    - IF - a separate partition exists for /var/log/audit.
                Edit the /etc/fstab file and add noexec to the fourth field (mounting options) for the
                /var/log/audit partition.
                Example:
                <device> /var/log/audit    <fstype>
                defaults,rw,nosuid,nodev,noexec,relatime        0 0
                Run the following command to remount /var/log/audit with the configured options:
                # mount -o remount /var/log/audit

                References:

                     1. See the fstab(5) manual page for more information.
                     2. NIST SP 800-53 Rev. 5: AC-3, MP-2
                     3. NIST SP 800-53 Revision 5 :: CM-7 (2)
                     4. STIG ID: RHEL-08-040131 | RULE ID: SV-230519r958804 | CAT II
                     5. STIG ID: RHEL-09-231165 | RULE ID: SV-257874r958804 | CAT II

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.

                1.2 Package Management

                Patch management procedures may vary widely between enterprises. Large enterprises
                may choose to install a local updates server that can be used in place of their
                distributions servers, whereas a single deployment of a system may prefer to get
                updates directly. Updates can be performed automatically or manually, depending on
                the site's policy for patch management. Organizations may prefer to test patches
                against their environment on a non-production system before rolling out to production.
                Outdated software is vulnerable to cyber criminals and hackers. Software updates help
                reduce the risk to your organization. The release of software update notes often reveals
                the patched exploitable entry points to the public. Public knowledge of these exploits
                can make your organization more vulnerable to malicious actors attempting to gain
                entry to your system's data.
                Software updates often offer new and improved features and speed enhancements.
                For the purpose of this benchmark, the requirement is to ensure that a patch
                management process is defined and maintained, the specifics of which are left to the
                organization.

                1.2.1 Configure Package Repositories

                Patch management procedures may vary widely between enterprises. Large enterprises
                may choose to install a local updates server that can be used in place of their
                distributions servers, whereas a single deployment of a system may prefer to get
                updates directly. Updates can be performed automatically or manually, depending on
                the site's policy for patch management. Organizations may prefer to test patches
                against their environment on a non-production system before rolling out to production.
                Outdated software is vulnerable to cyber criminals and hackers. Software updates help
                reduce the risk to your organization. The release of software update notes often reveals
                the patched exploitable entry points to the public. Public knowledge of these exploits
                can leave your organization more vulnerable to malicious actors attempting to gain
                access to your system's data.
                Note: Creation of an appropriate patch management policy is left to the organization.

                1.2.1.1 Ensure the source.list and .source files use the Signed-By
                option (Manual)
                Profile Applicability:

                • Level 1 - Server
                • Level 1 - Workstation

                Description:
                sources.list - List of configured APT data sources
                The source list /etc/apt/sources.list and the files contained in
                /etc/apt/sources.list.d/ are designed to support any number of active sources
                and a variety of source media.
                Signed-By (signed-by) is an option to require a repository to pass apt-secure(8)
                verification with a certain set of keys rather than all trusted keys apt has configured. It is
                specified as a list of absolute paths to keyring files (have to be accessible and readable
                for the _apt system user, so ensure everyone has read-permissions on the file) and
                fingerprints of keys to select from these keyrings. The recommended locations for
                keyrings are /usr/share/keyrings for keyrings managed by packages, and
                /etc/apt/keyrings for keyrings managed by the system operator. If no keyring files
                are specified, the default is the trusted.gpg keyring and all keyrings in the
                trusted.gpg.d/ directory. If no fingerprint is specified all keys in the keyrings are
                selected. A fingerprint will accept also all signatures by a subkey of this key, if this isn't
                desired an exclamation mark (!) can be appended to the fingerprint to disable this
                behaviour. The option defaults to the value of the option with the same name if set in
                the previously acquired Release file of this repository (only fingerprints can be specified
                there through). Otherwise, all keys in the trusted keyrings are considered valid signers
                for this repository. The option may also be set directly to an embedded GPG public key
                block.
                Rationale:
                Verifying that updates originate from a trusted and authenticated source helps mitigate
                the risk of spoofing attacks, which could otherwise lead to the inadvertent installation of
                malicious or compromised software.
                Impact:
                apt-key list is deprecated. Manage keyring files in trusted.gpg.d instead (see apt-
                key(8)). With the deprecation of apt-key it is recommended to use the Signed-By
                option in sources.list to require a repository to pass apt-secure(8) verification with a
                certain set of keys rather than all trusted keys apt has configured.
                It is important to list sources in order of preference, with the most preferred source listed
                first. Typically, this will result in sorting by speed from fastest to slowest (CD-ROM
                followed by hosts on a local network, followed by distant Internet hosts, for example).

                Audit:
                Run the following command and verify that the file /etc/apt/sources.list and files
                ending .list and .sources in the /etc/apt/sources.list.d/ directory use the
                Signed-By option:
                # grep -PRLs -- '^([^#\n\r]+)?\bSigned-By\b' /etc/apt/sources.list
                /etc/apt/sources.list.d/*.{list,sources}
                Nothing should be returned.
                Remediation:
                Update the file sources.list and any .sources files found in the
                /etc/apt/sources.list.d/ directory in accordance with site policy.
                Note:

                     •   The suggested filename for new systems is
                         /etc/apt/sources.list.d/vendor.sources, where vendor is the result of
                         dpkg-vendor --query Vendor | tr A-Z a-z, in deb822-style format. For
                         example, Ubuntu uses /etc/apt/sources.list.d/ubuntu.sources.
                     •   It is important to list sources in order of preference, with the most preferred
                         source listed first. Typically, this will result in sorting by speed from fastest to
                         slowest (CD-ROM followed by hosts on a local network, followed by distant
                         Internet hosts, for example).

                References:

                     1. NIST SP 800-53 Rev. 5: SI-2
                     2. https://manpages.debian.org/trixie/apt/sources.list.5.en.html
                     3. https://manpages.debian.org/trixie/gpg/gpg.1.en.html

                CIS Controls:

                   Controls
                                                                Control                                         IG 1 IG 2 IG 3
                   Version
                              7.3 Perform Automated Operating System Patch
                        v8    Management                                                                         ●    ●    ●
                                 Perform operating system updates on enterprise assets through automated
                              patch management on a monthly, or more frequent, basis.
                              7.4 Perform Automated Application Patch Management
                        v8      Perform application updates on enterprise assets through automated patch         ●    ●    ●
                              management on a monthly, or more frequent, basis.
                              3.4 Deploy Automated Operating System Patch
                              Management Tools
                        v7       Deploy automated software update tools in order to ensure that the operating    ●    ●    ●
                              systems are running the most recent security updates provided by the software
                              vendor.
                              3.5 Deploy Automated Software Patch Management
                              Tools
                        v7       Deploy automated software update tools in order to ensure that third-party      ●    ●    ●
                              software on all systems is running the most recent security updates provided by
                              the software vendor.
  '1.2.1.1': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '1.2.1.2': !unsafe |-
    Create a file under /etc/apt/apt.conf.d/ to disable the installation of weak
                dependencies:
                # printf '%s\n%s\n' "" 'APT::Install-Recommends "0";' 'APT::Install-Suggests
                "0";' > /etc/apt/apt.conf.d/60-no-weak-dependencies
                Note: All configuration files under /etc/apt/apt.conf.d/ are sorted in lexicographic
                order and where conflicting settings exist the last one will take precedence.
                Default Value:
                APT::Install-Recommends "1";
                APT::Install-Suggests "0";
  '1.2.1.3': !unsafe |-
    Run the following commands to update the access to any file(s) returned in the audit
                steps above:
                # chown root:root "/absolute/path/to/file"
                # chmod u-x,go-wx "/absolute/path/to/file"
                Example
                # chown root:root /usr/share/keyrings/debian-archive-trixie-security-
                automatic.gpg
                # chmod u-x,go-wx /usr/share/keyrings/debian-archive-trixie-security-
                automatic.gpg
                Note: APT first checks keys specified with the signed-by option in a repository's
                .sources or .list files. If no key is specified there, it falls back to administrator-
                managed keyrings in /etc/apt/trusted.gpg.d, then system-wide keyrings in
                /usr/share/keyrings.

                Default Value:
                Access: (644/-rw-r--r--) Uid: ( 0/ root) Gid: ( 0/ root)

                References:

                     1. https://ftp-master.debian.org/keys.html
                     2. https://wiki.debian.org/SecureApt#All_about_secure_apt
                     3. https://manpages.debian.org/stretch/apt/apt-secure.8.en.html

                1.2.1.4 Ensure access to /etc/apt/trusted.gpg.d directory is
                configured (Automated)
                Profile Applicability:

                • Level 1 - Server
                • Level 2 - Server

                Description:
                The /etc/apt/trusted.gpg.d directory stores individual GPG key files that APT uses
                to verify the authenticity of packages and repositories.
                Rationale:
                Impact:
                A non-privileged user with write access to /etc/apt/trusted.gpg.d can compromise
                the chain of trust by adding new public keys to APT trusted keys configuration.
                Audit:
                Run the following command and verify /etc/apt/trusted.gpg.d Uid and Gid are
                both 0/root and access is set to 0755 or more secure
                # stat -Lc 'Access: (%a/%A) Uid: ( %u/ %U) Gid: ( %g/ %G)'
                /etc/apt/trusted.gpg.d

                Access: (755/drwxr-xr-x) Uid: ( 0/ root) Gid: ( 0/ root)

                Remediation:
                Run the following commands to set /etc/apt/trusted.gpg.d Uid and Gid to 0/root
                and access to 0755 or more restrictive:
                # chown root:root /etc/apt/trusted.gpg.d
                # chmod u=rwx,g=rx,o=rx /etc/apt/trusted.gpg.d

                Default Value:
                Access: (755/drwxr-xr-x) Uid: ( 0/ root) Gid: ( 0/ root)

                1.2.1.5 Ensure access to /etc/apt/auth.conf.d directory is
                configured (Automated)
                Profile Applicability:

                • Level 1 - Server
                • Level 2 - Server

                Description:
                /etc/apt/auth.conf.d contains .conf files with credentials to access APT private
                repositories.
                Rationale:
                The /etc/apt/auth.conf.d directory contains APT .conf files which should have
                restricted access because they control how the package manager runs as root and may
                contain sensitive information. If writable or readable by non-root users, they could be
                altered to run malicious commands or expose credentials.
                Audit:
                Run the following command and verify /etc/apt/auth.conf.d Uid and Gid are both
                0/root and access is 0755 or more restrictive:
                # stat -Lc 'Access: (%a/%A) Uid: ( %u/ %U) Gid: ( %g/ %G)'
                /etc/apt/auth.conf.d

                Access: (755/drwxr-xr-x) Uid: ( 0/ root) Gid: ( 0/ root)

                Remediation:
                Run the following commands to set permissions on /etc/apt/auth.conf.d/ Uid and
                Gid are both 0/root and access is 0755 or more restrictive:
                # chown root:root /etc/apt/auth.conf.d
                # chmod u=rwx,g=rx,o=rx /etc/apt/auth.conf.d

                Default Value:
                Access: (755/drwxr-xr-x) Uid: ( 0/ root) Gid: ( 0/ root)
                References:

                     1. https://manpages.debian.org/trixie/apt/apt_auth.conf.5.en.html

                1.2.1.6 Ensure access to files in the /etc/apt/auth.conf.d/ directory
                is configured (Automated)
                Profile Applicability:

                • Level 1 - Server
                • Level 1 - Workstation

                Description:
                Files in the /etc/apt/auth.conf.d directory contains .conf files with configured
                credentials for private APT repositories or proxy.
                Rationale:
                Access to the .conf files in the /etc/apt/auth.conf.d directory should be restricted
                to prevent secrets leakage of Login information needed to connect to a proxy or to
                download data from a repository.
                Audit:
                Run the following command and verify all .conf files in the /etc/apt/auth.conf.d/
                directory Uid and Gid are both 0/root and mode 0640 or more restrictive:
                Run the following command:
                # stat -Lc 'Access: (%a/%A) Uid: ( %u/ %U) Gid: ( %g/ %G)'
                /etc/apt/auth.conf.d/* 2>/dev/null
                No output should be returned.
                Remediation:
                Run the following commands to set all .conf files in the /etc/apt/auth.conf.d/
                directory Uid and Gid both to0/root and mode 0640:
                # chown root:root /etc/apt/auth.conf.d/*
                # chmod u-x,g-wx,o-rwx /etc/apt/auth.conf.d/*

                Default Value:
                The /etc/apt/auth.conf.d/ directory is empty

                References:

                     1. https://manpages.debian.org/testing/apt/apt_auth.conf.5.en.html

                CIS Controls:

                  Controls
                                                                Control                                          IG 1 IG 2 IG 3
                  Version
                       v8     3.3 Configure Data Access Control Lists                                             ●    ●    ●
                                Configure data access control lists based on a user’s need to know. Apply data

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              access control lists, also known as access permissions, to local and remote file
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.

                1.2.1.7 Ensure access to /usr/share/keyrings directory is
                configured (Automated)
                Profile Applicability:

                • Level 1 - Server
                • Level 2 - Server

                Description:
                The /usr/share/keyrings directory contains trusted gpg keyring files used by APT to
                verify package signatures from repositories. It provides a centralized, read-only location
                for system-wide keys that ensures package integrity and authenticity.
                Rationale:
                A non-root user should not be able to add or remove keys from this directory.
                Misconfiguring could allow a non-root user to add repositories containing malicious
                packages.
                Audit:
                Run the following command and verify /usr/share/keyrings Uid and Gid are both
                0/root and access is 0755 or more restrictive:
                # stat -Lc 'Access: (%a/%A) Uid: ( %u/ %U) Gid: ( %g/ %G)'
                /usr/share/keyrings

                Access: (755/drwxr-xr-x) Uid: ( 0/ root) Gid: ( 0/ root)

                Remediation:
                Run the following commands to set permissions on /usr/share/keyrings Uid and
                Gid both 0/root and access is 0755 or more restrictive:
                # chown root:root /usr/share/keyrings
                # chmod u=rwx,g=rx,o=rx /usr/share/keyrings

                Default Value:
                Access: (755/drwxr-xr-x) Uid: ( 0/ root) Gid: ( 0/ root)

                1.2.1.8 Ensure access to /etc/apt/sources.list.d directory is
                configured (Automated)
                Profile Applicability:

                • Level 1 - Server
                • Level 2 - Server

                Description:
                The /etc/apt/sources.list.d directory provides a way to add sources.list entries
                in separate files.
                Rationale:
                /etc/apt/sources.list.d directory contains files with repositories to be used by
                APT. A non-root user should not be able to add or remove files from this directory.
                Misconfiguring could allow a non-root user to add repositories containing malicious
                packages.
                Audit:
                Run the following command and verify /etc/apt/sources.list.d Uid and Gid are
                both 0/root and access is set to 0755 or more restrictive:
                # stat -Lc 'Access: (%a/%A) Uid: ( %u/ %U) Gid: ( %g/ %G)'
                /etc/apt/sources.list.d

                Access: (755/drwxr-xr-x) Uid: ( 0/ root) Gid: ( 0/ root)

                Remediation:
                Run the following command to set permissions to /etc/apt/sources.list.d Uid and
                Gid to 0/root and access to 0755 or more restrictive:
                # chown root:root /etc/apt/sources.list.d
                # chmod u=rwx,g=rx,o=rx /etc/apt/sources.list.d

                Default Value:
                Access: (755/drwxr-xr-x) Uid: ( 0/ root) Gid: ( 0/ root)
                References:

                     1. https://manpages.debian.org/stretch/apt/sources.list.5.en.html

                1.2.1.9 Ensure access to files in /etc/apt/sources.list.d are
                configured (Automated)
                Profile Applicability:

                • Level 1 - Server
                • Level 2 - Server

                Description:
                Files in the /etc/apt/sources.list.d directory provides a way to add sources.list
                entries in separate files.
                Rationale:
                Files in the /etc/apt/sources.list.d directory contain information about repositories
                to be used by APT. A non-root user should not be able to add or remove files from this
                directory. Misconfiguring could allow a non-root user to add repositories containing
                malicious packages.
                Audit:
                Run the following command and verify all files in the /etc/apt/sources.list.d
                directory Uid and Gid are both 0/root and access is set to 0644 or more restrictive:
                # stat -Lc 'Access: (%a/%A) Uid: ( %u/ %U) Gid: ( %g/ %G)'
                /etc/apt/sources.list.d/*

                Access: (644/-rw-r--r--) Uid: ( 0/ root) Gid: ( 0/ root)

                Remediation:
                Run the following command to set permissions to /etc/apt/sources.list.d directory
                Uid and Gid to 0/root and access to 0644 or more restrictive:
                # chown root:root /etc/apt/sources.list.d/*
                # chmod u-x,go-wx /etc/apt/sources.list.d/*

                Default Value:
                No files under /etc/apt/sources.list.d
                References:

                     1. https://manpages.debian.org/stretch/apt/sources.list.5.en.html

                1.2.2 Configure Package Updates

                1.2.2.1 Ensure updates, patches, and additional security software
                are installed (Manual)
                Profile Applicability:

                • Level 1 - Server
                • Level 1 - Workstation

                Description:
                Periodically patches are released for included software either due to security flaws or to
                include additional functionality.
                Rationale:
                Newer patches may contain security enhancements that would not be available through
                the latest full update. As a result, it is recommended that the latest software patches be
                used to take advantage of the latest functionality. As with any software installation,
                organizations need to determine if a given update meets their requirements and verify
                the compatibility and supportability of any additional software against the update
                revision that is selected.
                Audit:
                Verify there are no updates or patches to install:
                # apt update
                # apt -s upgrade
                Check to make sure no system reboot is required:
                # if [ -f /var/run/reboot-required ]; then
                     cat /var/run/reboot-required
                  fi

                Remediation:
                Run the following commands to update all packages following local site policy guidance
                on applying updates and patches:
                Run the following command to update the system with the available patches and
                updates:
                # apt update
                Run one of the following commands to apply the updates and patches:
                # apt upgrade
                - OR -
                # apt dist-upgrade
                Note: When running the command apt dist-upgrade that apt has a "smart" conflict
                resolution system, and it will attempt to upgrade the most important packages at the
                expense of less important ones if necessary. So, dist-upgrade command may remove
                some packages.
                Once the update process is complete, verify if a reboot is required to load changes:
                # if [ -f /var/run/reboot-required ]; then
                     cat /var/run/reboot-required
                  fi

                References:

                     1. NIST SP 800-53 Rev. 5: SI-2

                Additional Information:
                Site policy may mandate a testing period before installation onto production systems for
                available updates.
                    • upgrade - is used to install the newest versions of all packages currently installed
                       on the system from the sources enumerated in /etc/apt/sources.list. Packages
                       currently installed with new versions available are retrieved and upgraded; under
                       no circumstances are currently installed packages removed, or packages not
                       already installed retrieved and installed. New versions of currently installed
                       packages that cannot be upgraded without changing the install status of another
                       package will be left at their current version. An update must be performed first so
                       that apt knows that new versions of packages are available.
                    • dist-upgrade - in addition to performing the function of upgrade, also intelligently
                       handles changing dependencies with new versions of packages; apt has a
                       "smart" conflict resolution system, and it will attempt to upgrade the most
                       important packages at the expense of less important ones if necessary. So, dist-
                       upgrade command may remove some packages. The /etc/apt/sources.list file
                       contains a list of locations from which to retrieve desired package files. See also
                       apt_preferences(5) for a mechanism for overriding the general settings for
                       individual packages.

                CIS Controls:

                   Controls
                                                                Control                                         IG 1 IG 2 IG 3
                   Version
                              7.3 Perform Automated Operating System Patch
                        v8    Management                                                                         ●    ●    ●
                                 Perform operating system updates on enterprise assets through automated
                              patch management on a monthly, or more frequent, basis.
                              7.4 Perform Automated Application Patch Management
                        v8      Perform application updates on enterprise assets through automated patch         ●    ●    ●
                              management on a monthly, or more frequent, basis.
                              3.4 Deploy Automated Operating System Patch
                              Management Tools
                        v7       Deploy automated software update tools in order to ensure that the operating    ●    ●    ●
                              systems are running the most recent security updates provided by the software
                              vendor.
                              3.5 Deploy Automated Software Patch Management
                              Tools
                        v7       Deploy automated software update tools in order to ensure that third-party      ●    ●    ●
                              software on all systems is running the most recent security updates provided by
                              the software vendor.

                1.3 Mandatory Access Control

                Mandatory Access Control (MAC) provides an additional layer of access restrictions to
                processes on top of the base Discretionary Access Controls. By restricting how
                processes can access files and resources on a system the potential impact from
                vulnerabilities in the processes can be reduced.
                Impact: Mandatory Access Control limits the capabilities of applications and daemons
                on a system, while this can prevent unauthorized access the configuration of MAC can
                be complex and difficult to implement correctly preventing legitimate access from
                occurring.

                1.3.1 Configure AppArmor

                AppArmor provides a Mandatory Access Control (MAC) system that greatly augments
                the default Discretionary Access Control (DAC) model. Under AppArmor MAC rules are
                applied by file paths instead of by security contexts as in other MAC systems. As such it
                does not require support in the filesystem and can be applied to network mounted
                filesystems for example. AppArmor security policies define what system resources
                applications can access and what privileges they can do so with. This automatically
                limits the damage that the software can do to files accessible by the calling user. The
                user does not need to take any action to gain this benefit. For an action to occur, both
                the traditional DAC permissions must be satisfied as well as the AppArmor MAC rules.
                The action will not be allowed if either one of these models does not permit the action.
                In this way, AppArmor rules can only make a system's permissions more restrictive and
                secure.
                References:

                     1. AppArmor Documentation: http://wiki.apparmor.net/index.php/Documentation
                     2. Ubuntu AppArmor Documentation: https://help.ubuntu.com/community/AppArmor
                     3. SUSE AppArmor Documentation:
                        https://www.suse.com/documentation/apparmor/
  '1.2.1.4': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '1.2.1.5': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '1.2.1.6': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '1.2.1.7': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '1.2.1.8': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '1.2.1.9': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '1.2.2.1': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '1.3.1.1': !unsafe |-
    Run the following command to install apparmor and apparmor-utils:
                # apt install apparmor apparmor-utils

                References:

                     1. NIST SP 800-53 Rev. 5: AC-3

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '1.3.1.2': !unsafe |-
    Edit /etc/default/grub of file in /etc/default/grub.d and remove the apparmor=0
                parameters to the GRUB_CMDLINE_LINUX= line
                Run the following commands to update the grub2 configuration and reboot the system:
                # update-grub
                # reboot

                References:

                     1. NIST SP 800-53 Rev. 5: AC-3

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '1.3.1.3': !unsafe |-
    Run the following command to set all profiles to enforce mode:
                # aa-enforce /etc/apparmor.d/*
                Note: Any unconfined processes may need to have a profile created or activated for
                them and then be restarted
                References:

                     1. NIST SP 800-53 Rev. 5: AC-3

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.

                1.3.1.4 Ensure apparmor_restrict_unprivileged_unconfined is
                enabled (Automated)
                Profile Applicability:

                • Level 1 - Server
                • Level 1 - Workstation

                Description:
                By making sure the kernel.apparmor_restrict_unprivileged_unconfined sysctl
                setting is enabled, a process that is unprivileged and unconfined cannot leverage aa-
                exec to change to a more favourable profile

                Rationale:
                Unprivileged user namespaces are a feature in the Linux kernel that was introduced in
                order to provide additional sandboxing functionality for programs such as container
                runtimes; it enables unprivileged users to gain administrator (root) permissions within a
                confined environment, without giving them elevated permissions on the host system.
                However, unconfined profiles that allow the use of user namespaces provide a trivial
                bypass of the unprivileged user namespace restriction from unconfined processes. All
                the unconfined application or user has to do is enter the unconfined profile. The
                unconfined user or application can execute the application associated with the
                unconfined profile. If the application provides the ability to run arbitrary code or shell
                access, that is all that needs to be done to bypass the restriction
                Audit:

                     1. Run the following script to verify
                        kernel.apparmor_restrict_unprivileged_unconfined is set to 0 in the
                        running configuration:

                #    sysctl kernel.apparmor_restrict_unprivileged_unconfined
                Verify output is:
                kernel.apparmor_restrict_unprivileged_unconfined = 1

                     2. Run the following script to verify
                        kernel.apparmor_restrict_unprivileged_unconfined is set to 1in a sysctl
                        conf file being used by systemd sysctl:

                #!/usr/bin/env bash

                {
                    l_parameter_name="kernel.apparmor_restrict_unprivileged_unconfined"
                l_grep="${l_parameter_name//./\\.}" a_output=()
                    l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl || readlink -e
                /usr/lib/systemd/systemd-sysctl)"
                    l_ufwscf="$([ -f /etc/default/ufw ] && awk -F= '/^\s*IPT_SYSCTL=/ {print
                $2}' /etc/default/ufw)"
                    l_opt="$(grep -Psoi '^\h*'"$l_grep"'\h*=\h*\H+\b' "$l_ufwscf" | tail -n
                1)"
                    l_option_value="$(cut -d= -f2 <<< "$l_opt" | xargs)"
                    [ -n "$l_option_value" ] && a_output+=(" - UFW set: \"$l_parameter_name\"
                to: \"$l_option_value\" in: \"$l_file\"")
                    while IFS= read -r l_file; do
                       l_file="${l_file//# /}"
                       l_opt="$(grep -Psoi '^\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" | tail -n
                1)"
                       l_option_value="$(cut -d= -f2 <<< "$l_opt" | xargs)"
                       [ -n "$l_option_value" ] && a_output+=(" - \"$l_parameter_name\" is set
                to: \"$l_option_value\" in: \"$l_file\"")
                    done < <("$l_systemdsysctl" --cat-config | tac | grep -Psoi
                '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                    [ "${#a_output[@]}" -gt "0" ] && printf '%s\n' "" "${a_output[@]}" ""
                }
                Example Output
                "kernel.apparmor_restrict_unprivileged_unconfined" is set to: "1" in:
                "/etc/sysctl.d/60-kernel_sysctl.conf"
                Note:

                     •   If the UFW set value is displayed, and is the correct value, this is considered a
                         passing state. If this value is incorrect, it must be updated in the file shown in the
                         audit script output.
                     •   If multiple lines are returned:
                              o The first line includes the value being used by systemd sysctl. If this is a
                                 correct value, this is considered a passing state. If the file listed is not in
                                 the /etc/sysctl.d/ directory, it is highly recommended to follow the
                                 remediation procedure to create a .conf file in the /etc/sysctl.d/
                                 directory with the correct setting to prevent a potential change due to an
                                 update to the system.
                              o Any files in the /etc/sysctl.d/ directory that include an incorrect value
                                 should be modified to correct or comment out the incorrect value to
                                 minimize the potential of the incorrect value being used by systemd
                                 sysctl do to system configuration changes.

                Remediation:

                     1. Set the kernel.apparmor_restrict_unprivileged_unconfined = kernel
                        parameter to 1 in /etc/sysctl.conf or a file in /etc/sysctl.d/ ending in
                        .conf:

                Example:
                # printf "\n%s\n" "kernel.apparmor_restrict_unprivileged_unconfined = 1" >>
                /etc/sysctl.d/60-kernel_sysctl.conf

                     2. Run the following command to set the active kernel parameter:

                # sysctl -w kernel.apparmor_restrict_unprivileged_unconfined=1

                     3. Run the following command to load all system configuration filles:

                # sysctl --system
                Note: If these settings appear in a canonically later file, or later in the same file, these
                settings will be overwritten
                References:

                     1. https://discourse.ubuntu.com/t/understanding-apparmor-user-namespace-
                        restriction/58007

                CIS Controls:

                  Controls
                                                                  Control                                             IG 1 IG 2 IG 3
                  Version
                              10.5 Enable Anti-Exploitation Features
                                 Enable anti-exploitation features on enterprise assets and software, where
                       v8     possible, such as Microsoft® Data Execution Prevention (DEP), Windows®                        ●    ●
                              Defender Exploit Guard (WDEG), or Apple® System Integrity Protection (SIP) and
                              Gatekeeper™.
                              8.3 Enable Operating System Anti-Exploitation Features/
                              Deploy Anti-Exploit Technologies
                       v7        Enable anti-exploitation features such as Data Execution Prevention (DEP) or
                              Address Space Layout Randomization (ASLR) that are available in an operating
                                                                                                                            ●    ●
                              system or deploy appropriate toolkits that can be configured to apply protection to a
                              broader set of applications and executables.

                1.4 Configure Bootloader

                The recommendations in this section focus on securing the bootloader and settings
                involved in the boot process directly.
  '1.3.1.4': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '1.4.1': !unsafe |-
    Create an encrypted password with grub-mkpasswd-pbkdf2:
                # grub-mkpasswd-pbkdf2 --iteration-count=600000 --salt=64

                Enter password: <password>
                Reenter password: <password>
                PBKDF2 hash of your password is <encrypted-password>
                Add the following into a custom /etc/grub.d configuration file:
                set superusers="<username>"
                password_pbkdf2 <username> <encrypted-password>
                The superuser/user information and password should not be contained in the
                /etc/grub.d/00_header file as this file could be overwritten in a package update.
                If there is a requirement to be able to boot/reboot without entering the password, edit
                /etc/grub.d/10_linux and add --unrestricted to the line CLASS=
                Example:
                CLASS="--class gnu-linux --class gnu --class os --unrestricted"
                Run the following command to update the grub2 configuration:
                # update-grub

                Default Value:
                This recommendation is designed around the grub bootloader, if LILO or another
                bootloader is in use in your environment enact equivalent settings.
                Replace /boot/grub/grub.cfg with the appropriate grub configuration file for your
                environment.
                References:

                     1. NIST SP 800-53 Rev. 5: AC-3
                     2. NIST SP 800-53A :: AC-3.1

                Additional Information:
                Changes to /etc/grub.d/10_linux may be overwritten during updates to the grub-
                common package. You should review any changes to this file before rebooting otherwise
                the system may unexpectedly prompt for a password on the next boot.
                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                       v7         Protect all information stored on systems with file system, network share,
                              claims, application, or database specific access control lists. These controls will
                                                                                                                     ●    ●    ●
                              enforce the principle that only authorized individuals should have access to the

                  Controls
                                                                  Control                                    IG 1 IG 2 IG 3
                  Version
                              information based on their need to access the information as a part of their
                              responsibilities.
  '1.4.2': !unsafe |-
    Run the following commands to set permissions on your grub configuration:
                # chown root:root /boot/grub/grub.cfg
                # chmod u-x,go-rwx /boot/grub/grub.cfg

                Default Value:
                Access: (0644/-rw-r--r--) Uid: ( 0/ root) Gid: ( 0/ root)
                References:

                     1. NIST SP 800-53 Rev. 5: AC-3

                Additional Information:
                This recommendation is designed around the grub bootloader, if LILO or another
                bootloader is in use in your environment enact equivalent settings.
                Replace /boot/grub/grub.cfg with the appropriate grub configuration file for your
                environment
                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.

                1.5 Configure Additional Process Hardening

                This section includes Recommendations covering kernel parameters.

                     •   sysctl settings are defined through files in /usr/local/lib, /usr/lib/, /lib/,
                         /run/, and /etc/
                     •   Files are typically placed in the sysctl.d directory within the parent directory
                     •   The paths where sysctl preload files usually exist
                             o /run/sysctl.d/*.conf
                             o /etc/sysctl.d/*.conf
                             o /usr/local/lib/sysctl.d/*.conf
                             o /usr/lib/sysctl.d/*.conf
                             o /lib/sysctl.d/*.conf
                             o /etc/sysctl.conf
                     •   Files must have the ".conf" extension
                     •   Vendors settings usually live in /usr/lib/ or /usr/local/lib/
                     •   To override a whole file, create a new file with the same name in
                         /etc/sysctl.d/ and put new settings there.
                     •   To override only specific settings, add a file with a lexically later name in
                         /etc/sysctl.d/ and put new settings there.
                     •   The command /usr/lib/systemd/systemd-sysctl --cat-config produces
                         output containing The system's loaded kernel parameters and the files they're
                         configured in:
                             o Entries listed latter in the file take precedence over the same settings
                                listed earlier in the file
                             o Files containing kernel parameters that are over-ridden by other files with
                                the same name will not be listed
                             o On systems running UncomplicatedFirewall, the kernel parameters may
                                be set or over-written. This will not be visible in the output of the command
                     •   On systems with Uncomplicated Firewall, additional settings may be configured
                         in /etc/ufw/sysctl.conf
                     •   The settings in /etc/ufw/sysctl.conf will override settings other settings and
                         will not be visible in the output of the /usr/lib/systemd/systemd-sysctl --
                         cat-config command
                     •   This behavior can be changed by updating the IPT_SYSCTL parameter in
                         /etc/default/ufw
                     •   SYSTEM FILE PRECEDENCE
                             o When using the --system option, sysctl will read files from directories in
                                the following list in given order from top to bottom. Once a file of a given
                                filename is loaded, any file of the same name in subsequent directories is
                                ignored.

                                /etc/sysctl.d/*.conf /run/sysctl.d/*.conf
                                /usr/local/lib/sysctl.d/*.conf /usr/lib/sysctl.d/*.conf
                                /lib/sysctl.d/*.conf /etc/sysctl.conf

                              o   All configuration files are sorted in lexicographic order, regardless of the
                                  directory they reside in. Configuration files can either be completely
                                  replaced (by having a new configuration file with the same name in a
                                  directory of higher priority) or partially replaced (by having a configuration
                                  file that is ordered later)--
  '1.5.1': !unsafe |-
    1. Review all files being used by systemd-sysctl and comment out or remove all
                        fs.protected_hardlinks lines that are not fs.protected_hardlinks=1.

                Example script:
                #!/usr/bin/env bash

                {
                     l_option="fs.protected_hardlinks" l_value="1"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*(0|[2-9]|1[0-9]+)/s/^/# /' "$l_file"
                     done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .conf and edit or
                        add the following line:

                fs.protected_hardlinks = 1
                Example:
                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "fs.protected_hardlinks = 1" >> /etc/sysctl.d/60-
                fs_sysctl.conf

                         Note: If the UFW file was the first file listed in the audit, the entry will be
                         commented out as part of the first step, however updating Uncomplicated
                         Firewall (UFW) may update this change. In this case the updated entry will
                         supersede the entry being created as part of this step.

                     3. Run the following command to load all sysctl configuration files:

                # sysctl --system

                Default Value:
                fs.protected_hardlinks = 0

                References:

                     1. CCI-002165 | CCI-002235
                     2. NIST SP 800-53 :: AC-3 (4) | NIST SP 800-53 :: AC-6 (10)
                     3. STIG ID: RHEL-08-010374 | RULE ID: SV-230268r1017086 | CAT II
                     4. STIG ID: OL08-00-010374 | RULE ID: SV-248578r958702 | CAT II
                     5. STIG ID: RHEL-09-213030 | RULE ID: SV-257801r958702 | CAT II
                     6. STIG ID: ALMA-09-007060 | RULE ID: SV-269144r1050026 | CAT II
                     7. STIG ID: OL09-00-002401 | RULE ID: SV-271740r1091932 | CAT II

                CIS Controls:

                     Controls Version                   Control          IG 1 IG 2 IG 3
                              v8         0.0 Explicitly Not Mapped
                                           Explicitly Not Mapped

                              v7         0.0 Explicitly Not Mapped
                                           Explicitly Not Mapped
  '1.5.2': !unsafe |-
    1. Review all files being used by systemd-sysctl and comment out or remove all
                         fs.protected_symlinks lines that are not fs.protected_symlinks=1.

                Example script:

                #!/usr/bin/env bash

                {
                     l_option="fs.protected_symlinks" l_value="1"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*(0|[2-9]|1[0-9]+)/s/^/# /' "$l_file"
                     done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .conf and edit or
                        add the following line:

                fs.protected_symlinks = 1
                Example:
                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "fs.protected_symlinks = 1" >> /etc/sysctl.d/60-
                fs_sysctl.conf

                         Note: If the UFW file was the first file listed in the audit, the entry will be
                         commented out as part of the first step, however updating Uncomplicated
                         Firewall (UFW) may update this change. In this case the updated entry will
                         supersede the entry being created as part of this step.

                     3. Run the following command to load all sysctl configuration files:

                # sysctl --system

                Default Value:
                fs.protected_symlinks = 0
                References:

                     1. CCI-002165 | CCI-002235
                     2. NIST SP 800-53 :: AC-3 (4) | NIST SP 800-53 :: AC-6 (10)
                     3. STIG ID: RHEL-08-010374 | RULE ID: SV-230268r1017086 | CAT II
                     4. STIG ID: OL08-00-010373 | RULE ID: SV-248577r958702 | CAT II

                     5. STIG ID: RHEL-09-213030 | RULE ID: SV-257801r958702 | CAT II
                     6. STIG ID: ALMA-09-007060 | RULE ID: SV-269144r1050026 | CAT II
                     7. STIG ID: OL09-00-002402 | RULE ID: SV-271741r1091935 | CAT II

                CIS Controls:

                     Controls Version                   Control          IG 1 IG 2 IG 3
                              v8         0.0 Explicitly Not Mapped
                                           Explicitly Not Mapped

                              v7         0.0 Explicitly Not Mapped
                                           Explicitly Not Mapped
  '1.5.3': !unsafe |-
    1. Review all files ending in .conf in the /etc/sysctl.d directory and comment
                        out or remove all kernel.yama.ptrace_scope lines that are not
                        kernel.yama.ptrace_scope=1, kernel.yama.ptrace_scope=2, or
                        kernel.yama.ptrace_scope=3.

                Example script:
                #!/usr/bin/env bash

                {
                   l_option="kernel.yama.ptrace_scope" l_grep="${l_option//./\\.}"
                l_value="(1|2|3)"
                   while IFS= read -r -d $'\0' l_file; do
                      grep -Pois '\h*'"$l_option"'\h*=\h*\H+\b' "$l_file" \
                      | grep -Pivq '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                      sed -ri '/^\s*kernel.yama.ptrace_scope\s*=/s/^/# /' "$l_file"
                   done < <(find /etc/sysctl.d/ -type f -name '*.conf' -print0)
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .confand edit or
                        add the following line:

                kernel.yama.ptrace_scope = 1
                Example:
                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "kernel.yama.ptrace_scope = 1" >> /etc/sysctl.d/60-
                kernel_sysctl.conf

                Note: The example uses kernel.yama.ptrace_scope = 1 but value may be set to 1,
                2, or 3

                     3. Run the following command to load all system configuration files:

                # sysctl --system

                Default Value:
                kernel.yama.ptrace_scope = 0
                References:

                     1. CCI-000366
                     2. NIST SP 800-53 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. STIG ID: RHEL-08-040282 | RULE ID: SV-230546r1017308 | CAT II
                     5. STIG ID: RHEL-09-213080 | RULE ID: SV-257811r1044872 | CAT II
                     6. STIG ID: ALMA-09-040940 | RULE ID: SV-269424r1050307 | CAT II

                Additional Information:
                Ptrace is very rarely used by regular applications and is mostly used by debuggers such
                as gdb and strace.

                CIS Controls:

                   Controls
                                                                 Control                                          IG 1 IG 2 IG 3
                   Version
                               4.8 Uninstall or Disable Unnecessary Services on
                               Enterprise Assets and Software
                        v8        Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                               such as an unused file sharing service, web application module, or service
                               function.
                               9.2 Ensure Only Approved Ports, Protocols and Services
                        v7     Are Running                                                                              ●    ●
                                  Ensure that only network ports, protocols, and services listening on a system
                               with validated business needs, are running on each system.
  '1.5.4': !unsafe |-
    1. Review all files being used by systemd-sysctl and comment out or remove all
                         fs.suid_dumpable lines that are not fs.suid_dumpable=0.

                Example script:
                #!/usr/bin/env bash

                {
                     l_option="fs.suid_dumpable" l_value="0"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*(1[0-9]*)/s/^/# /' "$l_file"
                     done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .conf and edit or
                        add the following line:

                fs.suid_dumpable = 0
                Example:
                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "fs.suid_dumpable = 0" >> /etc/sysctl.d/60-fs_sysctl.conf

                         Note: If the UFW file was the first file listed in the audit, the entry will be
                         commented out as part of the first step, however updating Uncomplicated
                         Firewall (UFW) may update this change. In this case the updated entry will
                         supersede the entry being created as part of this step.

                     3. Run the following command to load all sysctl configuration files:

                # sysctl --system

                Default Value:
                fs.suid_dumpable = 0
                CIS Controls:

                     Controls Version                       Control                 IG 1 IG 2 IG 3
                              v8             0.0 Explicitly Not Mapped
                                                Explicitly Not Mapped

                     Controls Version                  Control      IG 1 IG 2 IG 3
                              v7        0.0 Explicitly Not Mapped
                                          Explicitly Not Mapped
  '1.5.5': !unsafe |-
    1. Review all files being used by systemd-sysctl and comment out or remove all
                        kernel.dmesg_restrict lines that are not kernel.dmesg_restrict=1.

                Example script:
                #!/usr/bin/env bash

                {
                     l_option="kernel.dmesg_restrict" l_value="1"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*(0|[2-9]|1[0-9]+)/s/^/# /' "$l_file"
                     done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .conf and edit or
                        add the following line:

                kernel.dmesg_restrict = 1
                Example:
                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "kernel.dmesg_restrict = 1" >> /etc/sysctl.d/60-
                kernel_sysctl.conf

                         Note: If the UFW file was the first file listed in the audit, the entry will be
                         commented out as part of the first step, however updating Uncomplicated
                         Firewall (UFW) may update this change. In this case the updated entry will
                         supersede the entry being created as part of this step.

                     3. Run the following command to load all system configuration files:

                # sysctl --system

                Default Value:
                kernel.dmesg_restrict = 0

                References:

                     1. CCI-001082 | CCI-001090
                     2. NIST SP 800-53 :: SC-2 | NIST SP 800-53 :: SC-4
                     3. NIST SP 800-53A :: SC-2.1 | NIST SP 800-53A :: SC-4.1
                     4. STIG ID: RHEL-08-010375 | RULE ID: SV-230269r1017087 | CAT III
                     5. STIG ID: OL08-00-010375 | RULE ID: SV-248579r958524 | CAT III
                     6. STIG ID: RHEL-09-213010 | RULE ID: SV-257797r958514 | CAT II
                     7. STIG ID: ALMA-09-041050 | RULE ID: SV-269425r1050308 | CAT II
                     8. STIG ID: OL09-00-002406 | RULE ID: SV-271745r1091947 | CAT II
                     9. STIG ID: UBTU-22-213010 | RULE ID: SV-260472r958524 | CAT III
                     10. STIG ID: UBTU-24-600140 | RULE ID: SV-270749r1067179 | CAT III
                     11. STIG ID: SLES-15-010375 | RULE ID: SV-255921r958524 | CAT III

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '1.5.6': !unsafe |-
    Run the following command to restore binaries to normal:
                # prelink -ua
                Uninstall prelink using the appropriate package manager or manual installation:
                # apt purge prelink

                References:

                     1. NIST SP 800-53 Rev. 5: CM-6, CM-1, CM-3

                CIS Controls:

                   Controls
                                                                  Control                                      IG 1 IG 2 IG 3
                   Version
                        v8     3.14 Log Sensitive Data Access                                                             ●
                                  Log sensitive data access, including modification and disposal.
                               14.9 Enforce Detail Logging for Access or Changes to
                               Sensitive Data
                        v7         Enforce detailed audit logging for access to sensitive data or changes to              ●
                               sensitive data (utilizing tools such as File Integrity Monitoring or Security
                               Information and Event Monitoring).
  '1.5.7': !unsafe |-
    Edit /etc/default/apport and add or edit the enabled parameter to equal 0:
                enabled=0
                Run the following commands to stop and mask the apport service
                # systemctl stop apport.service
                # systemctl mask apport.service
                - OR -
                Run the following command to remove the apport package:
                # apt purge apport

                Default Value:
                enabled=1
                CIS Controls:

                   Controls
                                                       Control                               IG 1 IG 2 IG 3
                   Version
                               4.8 Uninstall or Disable Unnecessary Services on
                        v8
                               Enterprise Assets and Software
                                                                                                   ●    ●

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                                 Uninstall or disable unnecessary services on enterprise assets and software,
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '1.5.8': !unsafe |-
    1. Review all files being used by systemd-sysctl and comment out or remove all
                        kernel.kptr_restrict lines that are not kernel.kptr_restrict=1 or
                        kernel.kptr_restrict=2.

                Example script:
                #!/usr/bin/env bash

                {
                   l_option="kernel.kptr_restrict" l_value="(1|2)"
                   l_grep="${l_option//./(\\.|\\/)}" a_files=()
                   l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                   || readlink -e /usr/lib/systemd/systemd-sysctl)"
                   l_ufw_file="$([ -f /etc/default/ufw ] && \
                   awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                   [ -f "$(readlink -e "$l_ufw_file")" ] && \
                   a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                   while IFS= read -r l_fname; do
                      l_file="$(readlink -e "${l_fname//# /}")"
                      [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                      <<< "${a_files[*]}" && a_files+=("$l_file")
                   done < <("$l_systemdsysctl" --cat-config | tac | \
                   grep -Pios '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                   for l_file in "${a_files[@]}"; do
                      grep -Pios '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                      | grep -Pivq '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                      sed -ri '/^\s*'"$l_grep"'\s*=\s*(0|[3-9]|[1-9][0-9]+)/s/^/# /'
                "$l_file"
                   done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .conf and edit or
                        add the following line:

                kernel.kptr_restrict = {N}
                Example:
                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "kernel.kptr_restrict = 2" >> /etc/sysctl.d/60-
                kernel_sysctl.conf

                         Note: The example uses kernel.kptr_restrict = 2 but value may be
                         set to 1 or 2

                     3. Run the following command to load all system configuration files:

                # sysctl --system

                Default Value:
                kernel.kptr_restrict = 1

                References:

                     1. CCI-000366
                     2. NIST SP 800-53 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. STIG ID: RHEL-08-040283 | RULE ID: SV-230547r1017309 | CAT II
                     5. STIG ID: OL08-00-040283 | RULE ID: SV-248891r991589 | CAT II
                     6. STIG ID: RHEL-09-213025 | RULE ID: SV-257800r1044851 | CAT II
                     7. STIG ID: ALMA-09-040830 | RULE ID: SV-269423r1050306 | CAT II
                     8. STIG ID: OL09-00-002408 | RULE ID: SV-271747r1091953 | CAT II
                     9. STIG ID: SLES-15-010540 | RULE ID: SV-234861r958928 | CAT II

                CIS Controls:

                    Controls
                                                                  Control                                          IG 1 IG 2 IG 3
                    Version
                                4.8 Log and Alert on Changes to Administrative Group
                        v7      Membership                                                                               ●    ●
                                    Configure systems to issue a log entry and alert when an account is added
                                to or removed from any group assigned administrative privileges.
                                9.2 Ensure Only Approved Ports, Protocols and Services
                        v7      Are Running                                                                              ●    ●
                                   Ensure that only network ports, protocols, and services listening on a system
                                with validated business needs, are running on each system.
  '1.5.9': !unsafe |-
    1. Review all files being used by systemd sysctl and comment out or remove all
                        kernel.randomize_va_space lines that are not
                        kernel.randomize_va_space=2.

                Example script:
                #!/usr/bin/env bash

                {
                     l_option="kernel.randomize_va_space" l_value="2"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*([01]|[3-9]|1[0-9]+)/s/^/# /' "$l_file"
                     done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .conf and edit or
                        add the following line:

                kernel.randomize_va_space = 2
                Example:
                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "kernel.randomize_va_space = 2" >> /etc/sysctl.d/60-
                kernel_sysctl.conf

                         Note: If the UFW file was the first file listed in the audit, the entry will be
                         commented out as part of the first step, however updating Uncomplicated
                         Firewall (UFW) may update this change. In this case the updated entry will
                         supersede the entry being created as part of this step.

                     3. Run the following command to load all system configuration filles:

                # sysctl --system

                Default Value:
                kernel.randomize_va_space = 2

                References:

                     1. CCI-002824
                     2. NIST SP 800-53 :: SI-16
                     3. STIG ID: RHEL-08-010430 | RULE ID: SV-230280r1017093 | CAT II
                     4. STIG ID: OL08-00-010430 | RULE ID: SV-248594r958928 | CAT II
                     5. STIG ID: RHEL-09-213070 | RULE ID: SV-257809r1044866 | CAT II
                     6. STIG ID: ALMA-09-044900 | RULE ID: SV-269452r1050335 | CAT II
                     7. STIG ID: OL09-00-002423 | RULE ID: SV-271761r1091995 | CAT II
                     8. STIG ID: UBTU-22-213020 | RULE ID: SV-260474r958928 | CAT II
                     9. STIG ID: UBTU-24-700310 | RULE ID: SV-270772r1066805 | CAT II
                     10. STIG ID: SLES-15-010550 | RULE ID: SV-234862r958928 | CAT II

                CIS Controls:

                  Controls
                                                                  Control                                             IG 1 IG 2 IG 3
                  Version
                              10.5 Enable Anti-Exploitation Features
                                 Enable anti-exploitation features on enterprise assets and software, where
                       v8     possible, such as Microsoft® Data Execution Prevention (DEP), Windows®                        ●    ●
                              Defender Exploit Guard (WDEG), or Apple® System Integrity Protection (SIP) and
                              Gatekeeper™.
                              8.3 Enable Operating System Anti-Exploitation Features/
                              Deploy Anti-Exploit Technologies
                       v7        Enable anti-exploitation features such as Data Execution Prevention (DEP) or
                              Address Space Layout Randomization (ASLR) that are available in an operating
                                                                                                                            ●    ●
                              system or deploy appropriate toolkits that can be configured to apply protection to a
                              broader set of applications and executables.
  '1.5.10': !unsafe |-
    1. Review all files being used by systemd sysctl and comment out or remove all
                        kernel.yama.ptrace_scope lines that are not kernel.yama.ptrace_scope=1,
                        kernel.yama.ptrace_scope=2, or kernel.yama.ptrace_scope=3.

                Example script:
                #!/usr/bin/env bash

                {
                     l_option="kernel.yama.ptrace_scope" l_value="(1|2|3)"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*(0|4-9|1[0-9]+)/s/^/# /' "$l_file"
                     done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .conf and edit or
                        add the following line:

                kernel.yama.ptrace_scope = {N}
                Example:
                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "kernel.yama.ptrace_scope = 1" >> /etc/sysctl.d/60-
                kernel_sysctl.conf

                         Note: The example uses kernel.yama.ptrace_scope = 1 but value
                         may be set to 1, 2, or 3

                     3. Run the following command to load all system configuration filles:

                # sysctl --system

                Default Value:
                kernel.yama.ptrace_scope = 0

                References:

                     1. CCI-000366
                     2. NIST SP 800-53 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. STIG ID: RHEL-08-040282 | RULE ID: SV-230546r1017308 | CAT II
                     5. STIG ID: OL08-00-040282 | RULE ID: SV-248890r991589 | CAT II
                     6. STIG ID: RHEL-09-213080 | RULE ID: SV-257811r1044872 | CAT II
                     7. STIG ID: ALMA-09-040940 | RULE ID: SV-269424r1050307 | CAT II
                     8. STIG ID: OL09-00-002410 | RULE ID: SV-271749r1091959 | CAT II

                Additional Information:
                Ptrace is very rarely used by regular applications and is mostly used by debuggers such
                as gdb and strace.

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '1.5.11': !unsafe |-
    1. Run the following command to comment out any entries that include a hard
                        value for core greater than 0 in /etc/security/limits.conf and any file(s) in
                        the /etc/security/limits.d/ directory.

                Example:
                # sed -ri '/^\s*[^#\n\r]+\s+hard\s+core\s+([1-9][0-9]*)/s/^/# /'
                /etc/security/limits.conf /etc/security/limits.d/*

                     2. Create or edit a file in /etc/security/limits.d/ and add the following line:

                * hard core 0
                Example:
                # printf '%s\n' "" "* hard core 0" >> /etc/security/limits.d/60-limits.conf

                Default Value:
                None
                References:

                     1. limits.conf(5)
                     2. CCI-000366
                     3. NIST SP 800-53 :: CM-6 b
                     4. NIST SP 800-53A :: CM-6.1 (iv)
                     5. STIG ID: RHEL-08-010673 | RULE ID: SV-230313r1069304 | CAT II
                     6. STIG ID: RHEL-09-213095 | RULE ID: SV-257814r991589 | CAT II
                     7. STIG ID: ALMA-09-044900 | RULE ID: SV-269452r1050335 | CAT II

                CIS Controls:

                     Controls Version                     Control             IG 1 IG 2 IG 3
                              v8           0.0 Explicitly Not Mapped
                                             Explicitly Not Mapped

                              v7           0.0 Explicitly Not Mapped
                                             Explicitly Not Mapped
  '1.5.12': !unsafe |-
    1. Review all files ending in .conf in the /etc/systemd/coredump.conf.d/
                        directory and comment out or remove all ProcessSizeMax= lines in the
                        [Coredump] block that are not ProcessSizeMax=0.

                Example script:
                #!/usr/bin/env bash

                {
                   l_block="Coredump" l_option="ProcessSizeMax" l_value="0"
                   while IFS= read -r -d $'\0' l_file; do
                      awk '/\['"$l_block"'\]/{a=1;next}/\[/{a=0}a' "$l_file" 2>/dev/null | \
                      grep -Poi '\h*'"$l_option"'\h*=\h*\H+\b' | \
                      grep -Pivq '^\h*'"$l_option"'\h*=\h*'"$l_value"'\b' && \
                      sed -ri '/^\s*[Pp]rocess[Ss]ize[Mm]ax\s*=/s/^/# /' "$l_file"
                   done < <(find /etc/systemd/coredump.conf.d/ -type f -name '*.conf' -
                print0)
                }

                     2. Create or edit a file in the /etc/systemd/coredump.conf.d directory ending in
                        .conf and edit or add the following line to the [Coredump] section:

                ProcessSizeMax=0
                Example:
                #!/usr/bin/env bash

                {
                   [ ! -d /etc/systemd/coredump.conf.d/ ] && mkdir -p
                /etc/systemd/coredump.conf.d/
                   l_file="/etc/systemd/coredump.conf.d/60-coredump.conf"
                   if grep -Psq -- '^\h*\[Coredump\]' "$l_file"; then
                      sed -ri '/^\s*\[Coredump\]/a ProcessSizeMax=0' "$l_file"
                   else
                      printf '%s\n' "" "[Coredump]" "ProcessSizeMax=0" >> "$l_file"
                   fi
                }

                     3. Run the following command to load the updated configuration:

                # systemctl reload-or-restart systemd-coredump.socket

                Default Value:
                ProcessSizeMax=2G
                References:

                     1. COREDUMP.CONF(5)
                     2. CCI-000366
                     3. NIST SP 800-53 :: CM-6 b
                     4. NIST SP 800-53A :: CM-6.1 (iv)
                     5. STIG ID: RHEL-08-010675 | RULE ID: SV-230315r1017126 | CAT II
                     6. STIG ID: OL08-00-010675 | RULE ID: SV-248633r991589 | CAT II
                     7. STIG ID: RHEL-09-213085 | RULE ID: SV-257812r1051005 | CAT II
                     8. STIG ID: ALMA-09-011570 | RULE ID: SV-269181r1050063 | CAT II
                     9. STIG ID: OL09-00-002381 | RULE ID: SV-271729r1091899 | CAT II

                CIS Controls:

                     Controls Version                   Control          IG 1 IG 2 IG 3
                              v8         0.0 Explicitly Not Mapped
                                           Explicitly Not Mapped

                              v7         0.0 Explicitly Not Mapped
                                           Explicitly Not Mapped
  '1.5.13': !unsafe |-
    1. Review all files ending in .conf in the /etc/systemd/coredump.conf.d/
                        directory and comment out or remove all Storage= lines in the [Coredump]
                        block that are not Storage=none.

                Example script:
                #!/usr/bin/env bash

                {
                   l_block="Coredump" l_option="Storage" l_value="none"
                   while IFS= read -r -d $'\0' l_file; do
                      awk '/\['"$l_block"'\]/{a=1;next}/\[/{a=0}a' "$l_file" 2>/dev/null | \
                      grep -Poi '\h*'"$l_option"'\h*=\h*\H+\b' | \
                      grep -Pivq '^\h*'"$l_option"'\h*=\h*'"$l_value"'\b' && \
                      sed -ri '/^\s*[Ss]torage\s*=/s/^/# /' "$l_file"
                   done < <(find /etc/systemd/coredump.conf.d/ -type f -name '*.conf' -
                print0)
                }

                     2. Create or edit a file in the /etc/systemd/coredump.conf.d directory ending in
                        .conf and edit or add the following line to the [Coredump] section:

                Storage=none
                Example:
                #!/usr/bin/env bash

                {
                     [ ! -d /etc/systemd/coredump.conf.d/ ] && mkdir -p \
                     /etc/systemd/coredump.conf.d/
                     l_file="/etc/systemd/coredump.conf.d/60-coredump.conf"
                     if grep -Psq -- '^\h*\[Coredump\]' "$l_file"; then
                        sed -ri '/^\s*\[Coredump\]/a Storage=none' "$l_file"
                     else
                        printf '%s\n' "" "[Coredump]" "Storage=none" >> "$l_file"
                     fi
                }

                     3. Run the following command to load the updated configuration:

                # systemctl reload-or-restart systemd-coredump.socket

                Default Value:
                Storage=external
                References:

                     1. COREDUMP.CONF(5)
                     2. CCI-000366
                     3. NIST SP 800-53 :: CM-6 b
                     4. NIST SP 800-53A :: CM-6.1 (iv)
                     5. STIG ID: RHEL-08-010674 | RULE ID: SV-230314r1017125 | CAT II
                     6. STIG ID: OL08-00-010674 | RULE ID: SV-248632r991589 | CAT II
                     7. STIG ID: RHEL-09-213090 | RULE ID: SV-257813r991589 | CAT II
                     8. STIG ID: ALMA-09-011460 | RULE ID: SV-269180r1050062 | CAT II
                     9. STIG ID: OL09-00-002382 | RULE ID: SV-271730r1091902 | CAT II

                CIS Controls:

                     Controls Version                   Control          IG 1 IG 2 IG 3
                              v8         0.0 Explicitly Not Mapped
                                           Explicitly Not Mapped

                              v7         0.0 Explicitly Not Mapped
                                           Explicitly Not Mapped

                1.6 Configure Command Line Warning Banners

                Presenting a warning message prior to the normal user login may assist in the
                prosecution of trespassers on the computer system. Changing some of these login
                banners also has the side effect of hiding OS version information and other detailed
                system information from attackers attempting to target specific exploits at a system.
                Guidelines published by the US Department of Defense require that warning messages
                include at least the name of the organization that owns the system, the fact that the
                system is subject to monitoring and that such monitoring is in compliance with local
                statutes, and that use of the system implies consent to such monitoring. It is important
                that the organization's legal counsel review the content of all messages before any
                system modifications are made, as these warning messages are inherently site-specific.
                More information (including citations of relevant case law) can be found at
                http://www.justice.gov/criminal/cybercrime/
                The /etc/motd, /etc/issue, and /etc/issue.net files govern warning banners for
                standard command line logins for both local and remote users.
                Note: The text provided in the remediation actions for these items is intended as an
                example only. Please edit to include the specific text for your organization as approved
                by your legal department.
  '1.6.1': !unsafe |-
    Edit the file found in /etc/motd.d/* with the appropriate contents according to your
                site policy, remove any instances of \m , \r , \s , \v or references to the OS platform
                - OR -
                - IF - the motd is not used, this file can be removed.
                Run the following command to remove the motd file:
                # rm /etc/motd
                Run the following script and review and/or update all returned files' contents to:

                     •   Remove all system information (\v, \r; \m, \s)
                     •   Remove any refence to the operating system
                     •   Ensure contents follow local site policy

                #!/usr/bin/env bash

                {
                   a_files=()
                   for l_file in /etc/motd{,.d/*}; do
                      if grep -Psqi -- "(\\\v|\\\r|\\\m|\\\s|\b$(grep ^ID= /etc/os-release |
                cut -d= -f2 | sed -e 's/"//g')\b)" "$l_file"; then
                         echo -e "\n - File: \"$l_file\" includes system information. Edit
                this file to remove these entries"
                      else
                      a_files+=("$l_file")
                      fi
                   done
                   if [ "${#a_files[@]}" -gt 0 ]; then
                      echo -e "\n- ** Please review the following files and verify their
                contents follow local site policy **\n"
                      printf '%s\n' "${a_files[@]}"
                   fi
                }

                References:

                     1. NIST SP 800-53 Rev. 5: CM-6, CM-1, CM-3

                CIS Controls:

                     Controls Version                  Control      IG 1 IG 2 IG 3
                              v8        0.0 Explicitly Not Mapped
                                          Explicitly Not Mapped

                              v7        0.0 Explicitly Not Mapped
                                          Explicitly Not Mapped
  '1.6.2': !unsafe |-
    Edit the /etc/issue file with the appropriate contents according to your site policy,
                remove any instances of \m , \r , \s , \v or references to the OS platform
                Example:
                # echo "Authorized users only. All activity may be monitored and reported." >
                /etc/issue

                References:

                     1. NIST SP 800-53 Rev. 5: CM-6, CM-1, CM-3

                CIS Controls:

                     Controls Version                  Control      IG 1 IG 2 IG 3
                              v8        0.0 Explicitly Not Mapped
                                          Explicitly Not Mapped

                              v7        0.0 Explicitly Not Mapped
                                          Explicitly Not Mapped
  '1.6.3': !unsafe |-
    Edit the /etc/issue.net file with the appropriate contents according to your site policy,
                remove any instances of \m , \r , \s , \v or references to the OS platform
                Example:
                # echo "Authorized users only. All activity may be monitored and reported." >
                /etc/issue.net

                References:

                     1. NIST SP 800-53 Rev. 5: CM-6, CM-1, CM-3

                CIS Controls:

                     Controls Version                  Control      IG 1 IG 2 IG 3
                              v8        0.0 Explicitly Not Mapped
                                          Explicitly Not Mapped

                              v7        0.0 Explicitly Not Mapped
                                          Explicitly Not Mapped
  '1.6.4': !unsafe |-
    Run the following commands to set mode, owner, and group on /etc/motd:
                # chown root:root $(readlink -e /etc/motd)
                # chmod u-x,go-wx $(readlink -e /etc/motd)
                - OR -
                Run the following command to remove the /etc/motd file:
                # rm /etc/motd

                References:

                     1. NIST SP 800-53 Rev. 5: AC-3, MP-2

                CIS Controls:

                  Controls
                                                                  Control                                         IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                   ●    ●    ●
                              systems, databases, and applications.

                       v7     14.6 Protect Information through Access Control Lists                                ●    ●    ●
                                 Protect all information stored on systems with file system, network share,

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                              information based on their need to access the information as a part of their
                              responsibilities.
  '1.6.5': !unsafe |-
    Run the following commands to set mode, owner, and group on /etc/issue:
                # chown root:root $(readlink -e /etc/issue)
                # chmod u-x,go-wx $(readlink -e /etc/issue)

                Default Value:
                Access: (0644/-rw-r--r--) Uid: ( 0/ root) Gid: ( 0/ root)
                References:

                     1. NIST SP 800-53 Rev. 5: AC-3, MP-2

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '1.6.6': !unsafe |-
    Run the following commands to set mode, owner, and group on /etc/issue.net:
                # chown root:root $(readlink -e /etc/issue.net)
                # chmod u-x,go-wx $(readlink -e /etc/issue.net)

                Default Value:
                Access: (0644/-rw-r--r--) Uid: ( 0/ root) Gid: ( 0/ root)
                References:

                     1. NIST SP 800-53 Rev. 5: AC-3, MP-2

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.

                1.7 Configure GNOME Display Manager

                The GNOME Display Manager (GDM) is a program that manages graphical display
                servers and handles graphical user logins.
                This subsection requires user profiles to already exist on the system. A profile is a list of
                configuration databases.
                Sample profile:
                user-db:user
                system-db:local
                system-db:site
                Configuring a single user and multiple system databases allows for layering of
                preferences. Settings from the user database file take precedence over the settings in
                the local database file, and the local database file in turn takes precedence over the site
                database file.
                Note:

                     •   - IF - GDM is not installed on the system, this section can be skipped
                     •   The Remediation Procedure commands in this section MUST be done from a
                         command window on a graphical desktop or an error will be returned.
  '1.7.1': !unsafe |-
    Run the following commands to uninstall gdm3 and remove unused dependencies:
                # apt purge gdm3
                # apt autoremove gdm3

                References:

                     1. NIST SP 800-53 Rev. 5: CM-11

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '1.7.2': !unsafe |-
    - IF - A user profile is already created run the following commands to set and enable the
                text banner message on the login screen:
                # gsettings set org.gnome.login-screen banner-message-text 'Authorized uses
                only. All activity may be monitored and reported'
                # gsettings set org.gnome.login-screen banner-message-enable true
                Note:

                     •   banner-message-text may be set in accordance with local site policy
                     •   gsettings commands in this section MUST be done from a command window
                         on a graphical desktop or an error will be returned.
                     •   The system must be restarted after all gsettings configurations have been set
                         in order for CIS-CAT Assessor to appropriately assess.
                     •   If the dconf database is not updating correctly due to umask requirements
                         contain in the benchmark, then use (umask 0022 && gsetting set)
                         commands from above to temporarily set umask ensuring that any files or
                         directories created by gsettings will have the required permissions.

                - OR/IF - A user profile does not exist:

                     1. Create or edit the gdm profile in the /etc/dconf/profile/gdm with the following
                        lines:

                user-db:user
                system-db:gdm
                file-db:/usr/share/gdm/greeter-dconf-defaults
                Note: gdm is the name of a dconf database.

                     2. Create a gdm keyfile for machine-wide settings in /etc/dconf/db/gdm.d/01-
                        banner-message:

                [org/gnome/login-screen]
                banner-message-enable=true
                banner-message-text='Type the banner message here.'

                     3. Update the system databases

                # dconf update
                Note:

                     •   Users must log out and back in again before the system-wide settings take effect.
                     •   There is no character limit for the banner message. gnome-shell autodetects
                         longer stretches of text and enters two column mode.
                     •   The banner message cannot be read from an external file

                Default Value:
                disabled
                References:

                     1. https://help.gnome.org/admin/system-admin-guide/stable/login-banner.html.en
                     2. NIST SP 800-53 Rev. 5: CM-1,CM-2, CM-6, CM-7, IA-5

                Additional Information:
                Additional options and sections may appear in the /etc/dconf/db/gdm.d/01-banner-
                message file.
                If a different GUI login service is in use, consult your documentation and apply an
                equivalent banner.
  '1.7.3': !unsafe |-
    - IF - A user profile exists run the following command to enable the disable-user-
                list:
                # gsettings set org.gnome.login-screen disable-user-list true
                Note:

                     •   gsettings commands in this section MUST be done from a command window
                         on a graphical desktop or an error will be returned.
                     •   The system must be restarted after all gsettings configurations have been set
                         in order for CIS-CAT Assessor to appropriately assess.
                     •   If the dconf database is not updating correctly due to umask requirements
                         contain in the benchmark, then use (umask 0022 && gsetting set)
                         commands from above to temporarily set umask ensuring that any files or
                         directories created by gsettings will have the required permissions.

                - OR/IF - A user profile does not exist:

                     1. Create or edit the gdm profile in /etc/dconf/profile/gdm with the following
                        lines:

                user-db:user
                system-db:gdm
                file-db:/usr/share/gdm/greeter-dconf-defaults
                Note: gdm is the name of a dconf database.

                     2. Create a gdm keyfile for machine-wide settings in /etc/dconf/db/gdm.d/00-
                        login-screen:

                [org/gnome/login-screen]
                # Do not show the user list
                disable-user-list=true

                     3. Update the system databases:

                # dconf update
                Note: When the user profile is created or changed, the user will need to log out and log
                in again before the changes will be applied.
                Default Value:
                false
                References:

                     1. https://help.gnome.org/admin/system-admin-guide/stable/login-userlist-
                        disable.html.en
                     2. NIST SP 800-53 Rev. 5: CM-1,CM-2, CM-6, CM-7, IA-5

                Additional Information:
                If a different GUI login service is in use and required on the system, consult your
                documentation to disable displaying the user list
  '1.7.4': !unsafe |-
    - IF - A user profile is already created run the following commands to enable screen
                locks when the user is idle:
                # gsettings set org.gnome.desktop.screensaver lock-delay 5
                # gsettings set org.gnome.desktop.session idle-delay 900
                Note:
                     •   gsettings commands in this section MUST be done from a command window
                         on a graphical desktop or an error will be returned.
                     •   The system must be restarted after all gsettings configurations have been set
                         in order for CIS-CAT Assessor to appropriately assess.
                     •   If the dconf database is not updating correctly due to umask requirements
                         contain in the benchmark, then use (umask 0022 && gsetting set)
                         commands from above to temporarily set umask ensuring that any files or
                         directories created by gsettings will have the required permissions.
                         - OR/IF- A user profile does not exist:

                     1. Create or edit the user profile in the /etc/dconf/profile/ and verify it includes
                        the following:

                user-db:user
                system-db:{NAME_OF_DCONF_DATABASE}
                Note: local is the name of a dconf database used in the examples.

                     2. Create the directory /etc/dconf/db/local.d/ if it doesn't already exist:
                     3. Create the key file /etc/dconf/db/local.d/00-screensaver to provide
                        information for the local database:

                Example key file:
                # Specify the dconf path
                [org/gnome/desktop/session]

                # Number of seconds of inactivity before the screen goes blank
                # Set to 0 seconds if you want to deactivate the screensaver.
                idle-delay=uint32 180

                # Specify the dconf path
                [org/gnome/desktop/screensaver]

                # Number of seconds after the screen is blank before locking the screen
                lock-delay=uint32 0
                Note: You must include the uint32 along with the integer key values as shown.

                     4. Run the following command to update the system databases:

                # dconf update

                     5. Users must log out and back in again before the system-wide settings take effect.

                References:

                     1. https://help.gnome.org/admin/system-admin-guide/stable/desktop-
                        lockscreen.html.en
                     2. NIST SP 800-53 Revision 5 :: AC-11 a
                     3. NIST SP 800-53A :: AC-11.1 (ii)

                CIS Controls:

                  Controls
                                                                  Control                                           IG 1 IG 2 IG 3
                  Version
                              4.3 Configure Automatic Session Locking on Enterprise
                              Assets
                       v8         Configure automatic session locking on enterprise assets after a defined period    ●    ●    ●
                              of inactivity. For general purpose operating systems, the period must not exceed
                              15 minutes. For mobile end-user devices, the period must not exceed 2 minutes.

                       v7     16.11 Lock Workstation Sessions After Inactivity                                       ●    ●    ●
                                 Automatically lock workstation sessions after a standard period of inactivity.
  '1.7.5': !unsafe |-
    1. To prevent the user from overriding these settings, create the file
                        /etc/dconf/db/local.d/locks/00-screensaver with the following content:

                # Lock desktop screensaver settings
                /org/gnome/desktop/session/idle-delay
                /org/gnome/desktop/screensaver/lock-delay

                     2. Update the system databases:

                # dconf update
                Note:

                     •    A user profile must exist in order to apply locks.
                     •    Users must log out and back in again before the system-wide settings take effect.

                References:

                     1. https://help.gnome.org/admin/system-admin-guide/stable/desktop-
                        lockscreen.html.en
                     2. https://help.gnome.org/admin/system-admin-guide/stable/dconf-lockdown.html.en
                     3. NIST SP 800-53 Rev. 5: CM-11

                CIS Controls:

                  Controls
                                                                    Control                                           IG 1 IG 2 IG 3
                  Version
                                4.3 Configure Automatic Session Locking on Enterprise
                                Assets
                         v8         Configure automatic session locking on enterprise assets after a defined period    ●    ●    ●
                                of inactivity. For general purpose operating systems, the period must not exceed
                                15 minutes. For mobile end-user devices, the period must not exceed 2 minutes.

                         v7     16.11 Lock Workstation Sessions After Inactivity                                       ●    ●    ●
                                   Automatically lock workstation sessions after a standard period of inactivity.

                1.7.6 Ensure GDM automatic mounting of removable media is
                disabled (Automated)
                Profile Applicability:

                • Level 1 - Server
                • Level 2 - Workstation

                Description:
                By default GNOME automatically mounts removable media when inserted as a
                convenience to the user.
                Rationale:
                With automounting enabled anyone with physical access could attach a USB drive or
                disc and have its contents available in system even if they lacked permissions to mount
                it themselves.
                Impact:
                The use of portable hard drives is very common for workstation users. If your
                organization allows the use of portable storage or media on workstations and physical
                access controls to workstations is considered adequate there is little value add in
                turning off automounting.
                Audit:
                Run the following commands to verify automatic mounting is disabled:
                # gsettings get org.gnome.desktop.media-handling automount
                false
                # gsettings get org.gnome.desktop.media-handling automount-open
                false

                Remediation:
                - IF - A user profile exists run the following commands to ensure automatic mounting is
                disabled:
                # gsettings set org.gnome.desktop.media-handling automount false
                # gsettings set org.gnome.desktop.media-handling automount-open false
                Note:

                     •   gsettings commands in this section MUST be done from a command window
                         on a graphical desktop or an error will be returned.
                     •   The system must be restarted after all gsettings configurations have been set
                         in order for CIS-CAT Assessor to appropriately assess.
                     •   If the dconf database is not updating correctly due to umask requirements
                         contain in the benchmark, then use (umask 0022 && gsetting set)
                         commands from above to temporarily set umask ensuring that any files or
                         directories created by gsettings will have the required permissions.

                - OR/IF - A user profile does not exist:

                     1. Create a file /etc/dconf/db/local.d/00-media-automount with following
                        content:

                [org/gnome/desktop/media-handling]
                automount=false
                automount-open=false

                     2. After creating the file, apply the changes using below command :

                # dconf update
                Note: Users must log out and back in again before the system-wide settings take effect.
                References:

                     1. https://access.redhat.com/solutions/20107
                     2. NIST SP 800-53 Rev. 5: CM-1,CM-2, CM-6, CM-7, IA-5

                CIS Controls:

                      Controls
                                                                 Control                                     IG 1 IG 2 IG 3
                      Version
                                   10.3 Disable Autorun and Autoplay for Removable
                              v8   Media                                                                      ●    ●    ●
                                     Disable autorun and autoplay auto-execute functionality for removable
                                   media.

                              v7   8.5 Configure Devices Not To Auto-run Content                              ●    ●    ●
                                      Configure devices to not auto-run content from removable media.

                1.7.7 Ensure GDM disabling automatic mounting of removable
                media is not overridden (Automated)
                Profile Applicability:

                • Level 1 - Server
                • Level 2 - Workstation

                Description:
                By default GNOME automatically mounts removable media when inserted as a
                convenience to the user.
                By using the lockdown mode in dconf, you can prevent users from changing specific
                settings. To lock down a dconf key or subpath, create a locks subdirectory in the keyfile
                directory. The files inside this directory contain a list of keys or subpaths to lock. Just as
                with the keyfiles, you may add any number of files to this directory.
                Rationale:
                With automounting enabled anyone with physical access could attach a USB drive or
                disc and have its contents available in system even if they lacked permissions to mount
                it themselves.
                Impact:
                The use of portable hard drives is very common for workstation users

                Audit:
                Run the following script to verify automatic mounting of removable media is not
                overridden and correctly configured in a configuration file:

                     •   automount=false
                     •   automount-open=false

                #!/usr/bin/env bash

                {
                   a_output=() a_output2=()
                   check_setting()
                   {
                      grep -Psrilq "^\h*$1\h*=\h*false\b" /etc/dconf/db/local.d/locks/* 2>
                /dev/null && \
                      echo "- \"$3\" is locked and set to false" || echo "- \"$3\" is not
                locked or not set to false"
                   }
                   declare -A settings=(
                      ["automount"]="org/gnome/desktop/media-handling"
                      ["automount-open"]="org/gnome/desktop/media-handling"
                   )
                   for setting in "${!settings[@]}"; do
                      result=$(check_setting "$setting" "${settings[$setting]}" "$setting")
                      if [[ $result == *"is not locked"* || $result == *"not set to false"*
                ]]; then
                         a_output2+=("$result")
                      else
                         a_output+=("$result")
                      fi
                   done
                   printf '%s\n' "" "- Audit Result:"
                   if [ "${#a_output2[@]}" -gt 0 ]; then
                      printf '%s\n' " ** FAIL **" " - Reason(s) for audit failure:"
                "${a_output2[@]}"
                      [ "${#a_output[@]}" -gt 0 ] && printf '%s\n' "" "- Correctly set:"
                "${a_output[@]}"
                   else
                      printf '%s\n' " ** PASS **" "${a_output[@]}"
                   fi
                }

                Remediation:

                     1. To prevent the user from overriding these settings, create the file
                        /etc/dconf/db/local.d/locks/00-media-automount with the following
                        content:

                [org/gnome/desktop/media-handling]
                automount=false
                automount-open=false

                     2. Update the systems databases:

                # dconf update
                Note:

                     •   A user profile must exist in order to apply locks.
                     •   Users must log out and back in again before the system-wide settings take effect.

                References:

                     1. https://help.gnome.org/admin/system-admin-guide/stable/dconf-lockdown.html.en
                     2. NIST SP 800-53 Rev. 5: CM-1,CM-2, CM-6, CM-7, IA-5
                     3. https://manpages.ubuntu.com/manpages/trusty/man1/gsettings.1.html
                     4. https://access.redhat.com/solutions/20107
  '1.7.6': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '1.7.7': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '1.7.8': !unsafe |-
    - IF - A user profile exists run the following command to set autorun-never to true for
                GDM users:
                # gsettings set org.gnome.desktop.media-handling autorun-never true
                Note:

                     •   gsettings commands in this section MUST be done from a command window
                         on a graphical desktop or an error will be returned.
                     •   The system must be restarted after all gsettings configurations have been set
                         in order for CIS-CAT Assessor to appropriately assess.
                     •   If the dconf database is not updating correctly due to umask requirements
                         contain in the benchmark, then use (umask 0022 && gsetting set)
                         commands from above to temporarily set umask ensuring that any files or
                         directories created by gsettings will have the required permissions.

                - OR/IF - A lock does not exist:

                     1. create the file /etc/dconf/db/local.d/locks/00-media-autorun with the
                        following content:

                [org/gnome/desktop/media-handling]
                autorun-never=true

                     2. Update the systems databases:

                # dconf update
                Note: Users must log out and back in again before the system-wide settings take effect.
                Default Value:
                false
                References:

                     1. NIST SP 800-53 Rev. 5: CM-1,CM-2, CM-6, CM-7, IA-5

                CIS Controls:

                      Controls
                                                                 Control                                     IG 1 IG 2 IG 3
                      Version
                                   10.3 Disable Autorun and Autoplay for Removable
                              v8   Media                                                                      ●    ●    ●
                                     Disable autorun and autoplay auto-execute functionality for removable
                                   media.

                              v7   8.5 Configure Devices Not To Auto-run Content                              ●    ●    ●
                                      Configure devices to not auto-run content from removable media.
  '1.7.9': !unsafe |-
    1. To prevent the user from overriding these settings, create the file
                        /etc/dconf/db/local.d/locks/00-media-autorun with the following content:

                [org/gnome/desktop/media-handling]
                autorun-never=true

                     2. Update the systems databases:

                # dconf update
                Note:

                     •     A user profile must exist in order to apply locks.
                     •     Users must log out and back in again before the system-wide settings take effect.

                References:

                     1. NIST SP 800-53 Rev. 5: CM-1,CM-2, CM-6, CM-7, IA-5

                CIS Controls:

                         Controls
                                                                    Control                                     IG 1 IG 2 IG 3
                         Version
                                      10.3 Disable Autorun and Autoplay for Removable
                              v8      Media                                                                      ●    ●    ●
                                        Disable autorun and autoplay auto-execute functionality for removable
                                      media.

                              v7      8.5 Configure Devices Not To Auto-run Content                              ●    ●    ●
                                         Configure devices to not auto-run content from removable media.
  '1.7.10': !unsafe |-
    Edit all files returned by the audit and remove or commend out the Enable=true line in
                the [xdmcp] block:
                Example file:
                # GDM configuration storage
                #
                # See /usr/share/gdm/gdm.schemas for a list of available options.

                [daemon]
                # Uncomment the line below to force the login screen to use Xorg
                #WaylandEnable=false

                # Enabling automatic login
                # AutomaticLoginEnable = true
                # AutomaticLogin = user1

                # Enabling timed login
                # TimedLoginEnable = true
                # TimedLogin = user1
                # TimedLoginDelay = 10

                [security]

                [xdmcp]
                # Enable=true <- **This line should be removed or commented out**

                [chooser]

                [debug]
                # Uncomment the line below to turn on debugging
                # More verbose logs
                # Additionally lets the X server dump core if it crashes
                #Enable=true

                Default Value:
                false
                References:

                     1. NIST SP 800-53 Rev. 5: SI-4

                CIS Controls:

                   Controls
                                                                Control                                         IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,         ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '1.7.11': !unsafe |-
    Edit the file /etc/gdm/custom.conf and uncomment or add the following line in the
                [daemon] block:
                WaylandEnable=false
                Example:
                # GDM configuration storage

                [daemon]
                WaylandEnable=false

                [security]

                [xdmcp]

                [chooser]

                [debug]
                # Uncomment the line below to turn on debugging
                #Enable=true

                Default Value:
                Enabled
                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.

                2 Services
                While applying system updates and patches helps correct known vulnerabilities, one of
                the best ways to protect the system against as yet unreported vulnerabilities is to
                disable all services that are not required for normal system operation. This prevents the
                exploitation of vulnerabilities discovered at a later date. If a service is not enabled, it
                cannot be exploited. The actions in this section of the document provide guidance on
                some services which can be safely disabled and under which circumstances, greatly
                reducing the number of possible threats to the resulting system. Additionally, some
                services which should remain enabled but with secure configuration are covered as well
                as insecure service clients.

                2.1 Configure Server Services

                This section describes services that are installed on systems that specifically need to
                run these services. If any of these services are not required, it is recommended that the
                package be removed.
                - IF - the package is required for a dependency:

                     •   Ensure the dependent package is approved by local site policy
                     •   Ensure stopping and masking the service and/or socket meets local site policy
                     •   Stop and mask the service and/or socket to reduce the potential attack surface

                The following commands can be used to stop and mask the service and socket:
                # systemctl stop <service_name>.socket <service_name>.service
                # systemctl mask <service_name>.socket <service_name>.service
                Note: This should not be considered a comprehensive list of services not required for
                normal system operation. You may wish to consider additions to those listed here for
                your environment
  '2.1.1': !unsafe |-
    Run the following commands to stop autofs.service and remove the autofs
                package:
                # systemctl stop autofs.service
                # apt purge autofs
                - OR -
                - IF - the autofs package is required as a dependency:
                Run the following commands to stop and mask autofs.service:
                # systemctl stop autofs.service
                # systemctl mask autofs.service

                References:

                     1. NIST SP 800-53 Rev. 5: SI-3, MP-7
                     2. RHEL 8 STIG Vul ID: V-230502
                     3. RHEL 8 STIG Rule ID: SV-230502r627750

                Additional Information:
                This control should align with the tolerance of the use of portable drives and optical
                media in the organization. On a server, requiring an admin to manually mount media
                can be part of defense-in-depth to reduce the risk of unapproved software or information
                being introduced or proprietary software or information being exfiltrated. If admins
                commonly use flash drives and Server access has sufficient physical controls, requiring
                manual mounting may not increase security.

                CIS Controls:

                      Controls
                                                                 Control                                     IG 1 IG 2 IG 3
                      Version
                                   10.3 Disable Autorun and Autoplay for Removable
                              v8   Media                                                                      ●    ●    ●
                                     Disable autorun and autoplay auto-execute functionality for removable
                                   media.

                              v7   8.5 Configure Devices Not To Auto-run Content                              ●    ●    ●
                                      Configure devices to not auto-run content from removable media.
  '2.1.2': !unsafe |-
    Run the following commands to stop avahi-daemon.socket and avahi-
                daemon.service, and remove the avahi-daemon package:
                # systemctl stop avahi-daemon.socket avahi-daemon.service
                # apt purge avahi-daemon
                - OR -
                - IF - the avahi-daemon package is required as a dependency:
                Run the following commands to stop and mask the avahi-daemon.socket and avahi-
                daemon.service:
                # systemctl stop avahi-daemon.socket avahi-daemon.service
                # systemctl mask avahi-daemon.socket avahi-daemon.service

                References:

                     1. NIST SP 800-53 Rev. 5: SI-4

                CIS Controls:

                   Controls
                                                                   Control                                         IG 1 IG 2 IG 3
                   Version
                                 4.8 Uninstall or Disable Unnecessary Services on
                                 Enterprise Assets and Software
                         v8         Uninstall or disable unnecessary services on enterprise assets and software,         ●    ●
                                 such as an unused file sharing service, web application module, or service
                                 function.

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '2.1.3': !unsafe |-
    Run the following commands to stop kea-dhcp-ddns-server.service kea-dhcp4-
                server.service kea-dhcp6-server.service and remove kea package:
                # systemctl stop kea-dhcp-ddns-server.service kea-dhcp4-server.service kea-
                dhcp6-server.service
                # apt remove kea
                - OR -
                - IF - the kea package is required as a dependency:
                Run the following commands to stop and mask dhcpd.service and dhcpd6.service:
                # systemctl stop kea-dhcp-ddns-server.service kea-dhcp4-server.service kea-
                dhcp6-server.service
                # systemctl stop kea-dhcp-ddns-server.service kea-dhcp4-server.service kea-
                dhcp6-server.service

                References:

                     1. dhcpd(8)
                     2. NIST SP 800-53 Rev. 5: CM-7

                CIS Controls:

                   Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                   Version
                                 4.8 Uninstall or Disable Unnecessary Services on
                                 Enterprise Assets and Software
                         v8         Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                                 such as an unused file sharing service, web application module, or service
                                 function.
                                 9.2 Ensure Only Approved Ports, Protocols and Services
                         v7      Are Running                                                                              ●    ●
                                    Ensure that only network ports, protocols, and services listening on a system
                                 with validated business needs, are running on each system.
  '2.1.4': !unsafe |-
    Run the following commands to stop named.service and remove the bind9 package:
                # systemctl stop named.service
                # apt purge bind9
                - OR -
                - IF - the bind9 package is required as a dependency:
                Run the following commands to stop and mask bind9.service:
                # systemctl stop named.service
                # systemctl mask named.service

                References:

                     1. NIST SP 800-53 Rev. 5: CM-7

                CIS Controls:

                   Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                   Version
                                 4.8 Uninstall or Disable Unnecessary Services on
                                 Enterprise Assets and Software
                         v8         Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                                 such as an unused file sharing service, web application module, or service
                                 function.
                                 9.2 Ensure Only Approved Ports, Protocols and Services
                         v7      Are Running                                                                              ●    ●
                                    Ensure that only network ports, protocols, and services listening on a system
                                 with validated business needs, are running on each system.
  '2.1.5': !unsafe |-
    Run the following commands to stop dnsmasq.service and remove dnsmasq package:

                # systemctl stop dnsmasq.service
                # apt purge dnsmasq
                - OR -
                - IF - the dnsmasq package is required as a dependency:
                Run the following commands to stop and mask the dnsmasq.service:
                # systemctl stop dnsmasq.service
                # systemctl mask dnsmasq.service

                References:

                     1. NIST SP 800-53 Rev. 5: CM-7

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '2.1.6': !unsafe |-
    Run the following commands to stop vsftpd.service and remove the vsftpd
                package:
                # systemctl stop vsftpd.service
                # apt purge vsftpd
                - OR -
                - IF - the vsftpd package is required as a dependency:
                Run the following commands to stop and mask the vsftpd.service:
                # systemctl stop vsftpd.service
                # systemctl mask vsftpd.service
                Note: Other ftp server packages may exist. If not required and authorized by local site
                policy, they should also be removed. If the package is required for a dependency, the
                service should be stopped and masked.
                References:

                     1. NIST SP 800-53 Rev. 5: CM-7

                Additional Information:
                Additional FTP servers also exist and should be audited.
                CIS Controls:

                   Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                   Version
                                 4.8 Uninstall or Disable Unnecessary Services on
                                 Enterprise Assets and Software
                         v8         Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                                 such as an unused file sharing service, web application module, or service
                                 function.
                                 9.2 Ensure Only Approved Ports, Protocols and Services
                         v7      Are Running                                                                              ●    ●
                                    Ensure that only network ports, protocols, and services listening on a system
                                 with validated business needs, are running on each system.
  '2.1.7': !unsafe |-
    Run the following commands to stop slapd.service and remove the slapd package:

                # systemctl stop slapd.service
                # apt purge slapd
                - OR -
                - IF - the slapd package is required as a dependency:
                Run the following commands to stop and mask slapd.service:
                # systemctl stop slapd.service
                # systemctl mask slapd.service

                References:

                     1. For more detailed documentation on OpenLDAP, go to the project homepage at
                        http://www.openldap.org.
                     2. NIST SP 800-53 Rev. 5: CM-7

                CIS Controls:

                   Controls
                                                                 Control                                          IG 1 IG 2 IG 3
                   Version
                               4.8 Uninstall or Disable Unnecessary Services on
                               Enterprise Assets and Software
                        v8        Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                               such as an unused file sharing service, web application module, or service
                               function.
                               9.2 Ensure Only Approved Ports, Protocols and Services
                        v7     Are Running                                                                              ●    ●
                                  Ensure that only network ports, protocols, and services listening on a system
                               with validated business needs, are running on each system.
  '2.1.8': !unsafe |-
    Run one of the following commands to remove dovecot-imapd and dovecot-pop3d:
                Run the following commands to stop dovecot.socket and dovecot.service, and
                remove the dovecot-imapd and dovecot-pop3d packages:
                # systemctl stop dovecot.socket dovecot.service
                # apt purge dovecot-imapd dovecot-pop3d
                - OR -
                - IF - a package is installed and is required for dependencies:
                Run the following commands to stop and mask dovecot.socket and
                dovecot.service:
                # systemctl stop dovecot.socket dovecot.service
                # systemctl mask dovecot.socket dovecot.service

                References:

                     1. NIST SP 800-53 Rev. 5: CM-7

                Additional Information:
                Several IMAP/POP3 servers exist and can use other service names. courier-imap
                and cyrus-imap are example services that provide a mail server. These and other
                services should also be audited.
                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '2.1.9': !unsafe |-
    Run the following command to stop nfs-server.service and remove nfs-kernel-
                server package:
                # systemctl stop nfs-server.service
                # apt purge nfs-kernel-server
                - OR -
                - IF - the nfs-kernel-server package is required as a dependency:
                Run the following commands to stop and mask the nfs-server.service:
                # systemctl stop nfs-server.service
                # systemctl mask nfs-server.service

                References:

                     1. NIST SP 800-53 Rev. 5: CM-6, CM-7

                CIS Controls:

                   Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                   Version
                                 4.8 Uninstall or Disable Unnecessary Services on
                                 Enterprise Assets and Software
                         v8         Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                                 such as an unused file sharing service, web application module, or service
                                 function.
                                 9.2 Ensure Only Approved Ports, Protocols and Services
                         v7      Are Running                                                                              ●    ●
                                    Ensure that only network ports, protocols, and services listening on a system
                                 with validated business needs, are running on each system.
  '2.1.10': !unsafe |-
    Run the following commands to stop ypserv.service and remove ypserv package:
                # systemctl stop ypserv.service
                # apt purge ypserv
                - OR -
                - IF - the ypserv package is required as a dependency:
                Run the following commands to stop and mask ypserv.service:
                # systemctl stop ypserv.service
                # systemctl mask ypserv.service

                References:

                     1. NIST SP 800-53 Rev. 5: CM-7

                CIS Controls:

                   Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                   Version
                                 4.8 Uninstall or Disable Unnecessary Services on
                                 Enterprise Assets and Software
                         v8         Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                                 such as an unused file sharing service, web application module, or service
                                 function.
                                 9.2 Ensure Only Approved Ports, Protocols and Services
                         v7      Are Running                                                                              ●    ●
                                    Ensure that only network ports, protocols, and services listening on a system
                                 with validated business needs, are running on each system.
  '2.1.11': !unsafe |-
    Run the following commands to stop cups.socket and cups.service, and remove the
                cups package:
                # systemctl stop cups.socket cups.service
                # apt purge cups
                - OR -
                - IF - the cups package is required as a dependency:
                Run the following commands to stop and mask the cups.socket and cups.service:
                # systemctl stop cups.socket cups.service
                # systemctl mask cups.socket cups.service

                References:

                     1. http://www.cups.org
                     2. NIST SP 800-53 Rev. 5: CM-7

                CIS Controls:

                   Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                   Version
                                 4.8 Uninstall or Disable Unnecessary Services on
                                 Enterprise Assets and Software
                         v8         Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                                 such as an unused file sharing service, web application module, or service
                                 function.
                                 9.2 Ensure Only Approved Ports, Protocols and Services
                         v7      Are Running                                                                              ●    ●
                                    Ensure that only network ports, protocols, and services listening on a system
                                 with validated business needs, are running on each system.
  '2.1.12': !unsafe |-
    Run the following commands to stop rpcbind.socket and rpcbind.service, and
                remove the rpcbind package:
                # systemctl stop rpcbind.socket rpcbind.service
                # apt purge rpcbind
                - OR -
                - IF - the rpcbind package is required as a dependency:
                Run the following commands to stop and mask the rpcbind.socket and
                rpcbind.service:
                # systemctl stop rpcbind.socket rpcbind.service
                # systemctl mask rpcbind.socket rpcbind.service

                References:

                     1. NIST SP 800-53 Rev. 5: CM-6, CM-7

                CIS Controls:

                   Controls
                                                                   Control                                         IG 1 IG 2 IG 3
                   Version
                                 4.8 Uninstall or Disable Unnecessary Services on
                                 Enterprise Assets and Software
                         v8         Uninstall or disable unnecessary services on enterprise assets and software,         ●    ●
                                 such as an unused file sharing service, web application module, or service
                                 function.

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '2.1.13': !unsafe |-
    Run the following commands to stop rsync.service, and remove the rsync package:

                # systemctl stop rsync.service
                # apt purge rsync
                - OR -
                - IF - the rsync package is required as a dependency:
                Run the following commands to stop and mask rsync.service:
                # systemctl stop rsync.service
                # systemctl mask rsync.service

                References:

                     1. NIST SP 800-53 Rev. 5: CM-7

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '2.1.14': !unsafe |-
    Run the following commands to stop smbd.service and remove samba package:

                # systemctl stop smbd.service
                # apt purge samba
                - OR -
                - IF - the samba package is required as a dependency:
                Run the following commands to stop and mask the smbd.service:
                # systemctl stop smbd.service
                # systemctl mask smbd.service

                References:

                     1. NIST SP 800-53 Rev. 5: CM-6, CM-7

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '2.1.15': !unsafe |-
    Run the following commands to stop snmpd.service and remove the snmpd package:
                # systemctl stop snmpd.service
                # apt purge snmpd
                - OR - If the package is required for dependencies:
                Run the following commands to stop and mask the snmpd.service:
                # systemctl stop snmpd.service
                # systemctl mask snmpd.service

                References:

                     1. NIST SP 800-53 Rev. 5: CM-7

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '2.1.16': !unsafe |-
    Run the following commands to stop inetutils-inetd.service, and remove the
                telnetd package:
                # systemctl stop inetutils-inetd.service
                # apt purge telnetd
                - OR -
                - IF - the telnetd package is required as a dependency:
                Run the following commands to stop and mask inetutils-inetd.service:
                # systemctl stop inetutils-inetd.service
                # systemctl mask inetutils-inetd.service

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              2.6 Address unapproved software
                        v7       Ensure that unauthorized software is either removed or the inventory is          ●    ●    ●
                              updated in a timely manner
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '2.1.17': !unsafe |-
    Run the following commands to stop tftpd-hpa.service, and remove the tftpd-hpa
                package:
                # systemctl stop tftpd-hpa.service
                # apt purge tftpd-hpa
                - OR -
                - IF - the tftpd-hpa package is required as a dependency:
                Run the following commands to stop and mask tftpd-hpa.service:
                # systemctl stop tftpd-hpa.service
                # systemctl mask tftpd-hpa.service

                References:

                     1. NIST SP 800-53 Rev. 5: CM-7

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '2.1.18': !unsafe |-
    Run the following commands to stop squid.service and remove the squid package:

                # systemctl stop squid.service
                # apt purge squid
                - OR - If the squid package is required as a dependency:
                Run the following commands to stop and mask the squid.service:
                # systemctl stop squid.service
                # systemctl mask squid.service

                References:

                     1. NIST SP 800-53 Rev. 5: CM-7

                Additional Information:
                Several HTTP proxy servers exist. These and other services should be checked
                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '2.1.19': !unsafe |-
    Run the following commands to stop apache2.socket, apache2.service and remove
                the apache2 package:
                # systemctl stop apache2.socket apache2.service
                # apt purge apache2
                Run the following commands to stop nginx.service and remove the nginx package:
                # systemctl stop nginx.service
                # apt purge nginx
                - OR -
                - IF - a package is installed and is required for dependencies:
                - IF - the apache2 package is required for dependencies:
                Run the following commands to stop and mask apache2.socket and
                apache2.service:
                # systemctl stop apache2.socket apache2.service
                # systemctl mask apache2.socket apache2.service
                - IF - the nginx package is required for dependencies:
                Run the following commands to stop and mask nginx.service:
                # systemctl stop nginx.service
                # systemctl mask nginx.service
                Note: Other web server packages may exist. If not required and authorized by local site
                policy, they should also be removed. If the package is required for a dependency, the
                service and socket should be stopped and masked.
                References:

                     1. NIST SP 800-53 :: CM-7

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '2.1.20': !unsafe |-
    Run the following commands to stop xinetd.service, and remove the xinetd
                package:
                # systemctl stop xinetd.service
                # apt purge xinetd
                -OR-
                -IF- the xinetd package is required as a dependency:
                Run the following commands to stop and mask the xinetd.service:
                # systemctl stop xinetd.service
                # systemctl mask xinetd.service

                References:

                     1. NIST SP 800-53 Rev. 5: CM-7

                CIS Controls:

                   Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                   Version
                                 4.8 Uninstall or Disable Unnecessary Services on
                                 Enterprise Assets and Software
                         v8         Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                                 such as an unused file sharing service, web application module, or service
                                 function.
                                 9.2 Ensure Only Approved Ports, Protocols and Services
                         v7      Are Running                                                                              ●    ●
                                    Ensure that only network ports, protocols, and services listening on a system
                                 with validated business needs, are running on each system.
  '2.1.21': !unsafe |-
    - IF - a Graphical Desktop Manager or X-Windows server is not required and approved
                by local site policy:
                Run the following command to remove the X Windows Server package:
                # apt purge xserver-common

                References:

                     1. NIST SP 800-53 Rev. 5: CM-11

                CIS Controls:

                   Controls
                                                                Control                                         IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,         ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              2.6 Address unapproved software
                        v7       Ensure that unauthorized software is either removed or the inventory is         ●    ●    ●
                              updated in a timely manner

                2.1.22 Ensure mail transfer agents are configured for local-only
                mode (Automated)
                Profile Applicability:

                • Level 1 - Server
                • Level 1 - Workstation

                Description:
                Mail Transfer Agents (MTA), such as sendmail and Postfix, are used to listen for
                incoming mail and transfer the messages to the appropriate user or mail server. If the
                system is not intended to be a mail server, it is recommended that the MTA be
                configured to only process local mail.
                Rationale:
                The software for all Mail Transfer Agents is complex and most have a long history of
                security issues. While it is important to ensure that the system can process local mail
                messages, it is not necessary to have the MTA's daemon listening on a port unless the
                server is intended to be a mail server that receives and processes mail from other
                systems.
                Impact:
                This recommendation is designed around the postfix mail server. Depending on your
                environment you may have an alternative MTA installed such as exim4 or sendmail. If
                this is the case consult the documentation for your installed MTA to configure the
                recommended state.
                Audit:
                Run the following script to verify that the MTA is not listening on any non-loopback
                address ( 127.0.0.1 or ::1 )

                #!/usr/bin/env bash

                {
                   a_output=(); a_output2=(); a_port_list=("25" "465" "587")
                   for l_port_number in "${a_port_list[@]}"; do
                      if ss -plntu | grep -P -- ':'"$l_port_number"'\b' | grep -Pvq --
                '\h+(127\.0\.0\.1|\[?::1\]?):'"$l_port_number"'\b'; then
                         a_output2+=(" - Port \"$l_port_number\" is listening on a non-
                loopback network interface")
                      else
                         a_output+=(" - Port \"$l_port_number\" is not listening on a non-
                loopback network interface")
                      fi
                   done
                   if command -v postconf &> /dev/null; then
                      l_interfaces="$(postconf -n inet_interfaces)"
                   elif command -v exim &> /dev/null; then
                      l_interfaces="$(exim -bP local_interfaces)"
                   elif command -v sendmail &> /dev/null; then
                      l_interfaces="$(grep -i "O DaemonPortOptions=" /etc/mail/sendmail.cf |
                grep -oP '(?<=Addr=)[^,+]+' | grep -v '^127\.0\.0\.1$')"
                   fi
                   if [ -n "$l_interfaces" ]; then
                      if grep -Pqi '\ball\b' <<< "$l_interfaces"; then
                         a_output2+=(" - MTA is bound to all network interfaces")
                      elif ! grep -Pqi '(inet_interfaces\h*=\h*)?(0\.0\.0\.0|::1|loopback-
                only)' <<< "$l_interfaces"; then
                         a_output2+=(" - MTA is bound to a network interface" "
                \"$l_interfaces\"")
                      else
                         a_output+=(" - MTA is not bound to a non loopback network interface"
                "   \"$l_interfaces\"")
                      fi
                   else
                      a_output+=(" - MTA not detected or in use")
                   fi
                   if [ "${#a_output2[@]}" -le 0 ]; then
                      printf '%s\n' "" "- Audit Result:" " ** PASS **" "${a_output[@]}"
                   else
                      printf '%s\n' "" "- Audit Result:" " ** FAIL **" " * Reasons for
                audit failure *" "${a_output2[@]}" ""
                      [ "${#a_output[@]}" -gt 0 ] && printf '%s\n' "- Correctly set:"
                "${a_output[@]}"
                   fi
                }

                Remediation:
                Edit /etc/postfix/main.cf and add the following line to the RECEIVING MAIL
                section. If the line already exists, change it to look like the line below:
                inet_interfaces = loopback-only
                Run the following command to restart postfix:
                # systemctl restart postfix
                Note:

                     •    This recommendation is designed around the postfix mail server.
                     •    Depending on your environment you may have an alternative MTA installed such
                          as exim4 or sendmail. If this is the case consult the documentation for your
                          installed MTA to configure the recommended state.

                References:

                     1. NIST SP 800-53 Rev. 5: CM-7

                CIS Controls:

                   Controls
                                                                  Control                                          IG 1 IG 2 IG 3
                   Version
                                4.8 Uninstall or Disable Unnecessary Services on
                                Enterprise Assets and Software
                         v8        Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                                such as an unused file sharing service, web application module, or service
                                function.
                                9.2 Ensure Only Approved Ports, Protocols and Services
                         v7     Are Running                                                                              ●    ●
                                   Ensure that only network ports, protocols, and services listening on a system
                                with validated business needs, are running on each system.

                2.1.23 Ensure only approved services are listening on a network
                interface (Manual)
                Profile Applicability:

                • Level 1 - Server
                • Level 1 - Workstation

                Description:
                A network port is identified by its number, the associated IP address, and the type of the
                communication protocol such as TCP or UDP.
                A listening port is a network port on which an application or process listens on, acting as
                a communication endpoint.
                Each listening port can be open or closed (filtered) using a firewall. In general terms, an
                open port is a network port that accepts incoming packets from remote locations.
                Rationale:
                Services listening on the system pose a potential risk as an attack vector. These
                services should be reviewed, and if not required, the service should be stopped, and the
                package containing the service should be removed. If required packages have a
                dependency, the service should be stopped and masked to reduce the attack surface of
                the system.
                Impact:
                There may be packages that are dependent on the service's package. If the service's
                package is removed, these dependent packages will be removed as well. Before
                removing the service's package, review any dependent packages to determine if they
                are required on the system.
                - IF - a dependent package is required: stop and mask the <service_name>.socket
                and <service_name>.service leaving the service's package installed.

                Audit:
                Run the following command:
                # ss -plntu
                Review the output to ensure:

                     •   All services listed are required on the system and approved by local site policy.
                     •   Both the port and interface the service is listening on are approved by local site
                         policy.
                     •   If a listed service is not required:
                             o Remove the package containing the service
                             o - IF - the service's package is required for a dependency, stop and mask
                                  the service and/or socket

                Remediation:
                Run the following commands to stop the service and remove the package containing
                the service:
                # systemctl stop <service_name>.socket <service_name>.service
                # apt purge <package_name>
                - OR - If required packages have a dependency:
                Run the following commands to stop and mask the service and socket:
                # systemctl stop <service_name>.socket <service_name>.service
                # systemctl mask <service_name>.socket <service_name>.service
                Note: replace <service_name> with the appropriate service name.

                References:

                     1. NIST SP 800-53 Rev. 5: CM-7

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.

                2.2 Configure Client Services

                A number of insecure services exist. While disabling the servers prevents a local attack
                against these services, it is advised to remove their clients unless they are required.
                Note: This should not be considered a comprehensive list of insecure service clients.
                You may wish to consider additions to those listed here for your environment.
  '2.1.22': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '2.1.23': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '2.2.1': !unsafe |-
    Uninstall nis:
                # apt purge nis

                References:

                     1. NIST SP 800-53 Rev. 5: CM-7, CM-11

                CIS Controls:

                   Controls
                                                                Control                                         IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,         ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              2.6 Address unapproved software
                        v7       Ensure that unauthorized software is either removed or the inventory is         ●    ●    ●
                              updated in a timely manner
  '2.2.2': !unsafe |-
    Uninstall rsh:
                # apt purge rsh-client

                References:

                     1. NIST SP 800-53 Rev. 5: CM-7

                CIS Controls:

                   Controls
                                                                  Control                                         IG 1 IG 2 IG 3
                   Version
                                 4.8 Uninstall or Disable Unnecessary Services on
                        v8       Enterprise Assets and Software                                                         ●    ●
                                   Uninstall or disable unnecessary services on enterprise assets and software,

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '2.2.3': !unsafe |-
    Uninstall talk:
                # apt purge talk

                References:

                     1. NIST SP 800-53 Rev. 5: CM-7

                CIS Controls:

                   Controls
                                                                 Control                                         IG 1 IG 2 IG 3
                   Version
                               4.8 Uninstall or Disable Unnecessary Services on
                               Enterprise Assets and Software
                        v8        Uninstall or disable unnecessary services on enterprise assets and software,         ●    ●
                               such as an unused file sharing service, web application module, or service
                               function.

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '2.2.4': !unsafe |-
    Run the following commands to uninstall telnet & inetutils-telnet:
                # apt purge telnet
                # apt purge inetutils-telnet

                References:

                     1. NIST SP 800-53 Rev. 5: CM-7, CM-11

                CIS Controls:

                   Controls
                                                        Control                            IG 1 IG 2 IG 3
                   Version
                               4.8 Uninstall or Disable Unnecessary Services on
                        v8
                               Enterprise Assets and Software
                                                                                                  ●        ●

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                                 Uninstall or disable unnecessary services on enterprise assets and software,
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '2.2.5': !unsafe |-
    Uninstall ldap-utils:
                # apt purge ldap-utils

                References:

                     1. NIST SP 800-53 Rev. 5: CM-7

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '2.2.6': !unsafe |-
    Run the following commands to uninstall the tnftp and ftp packages:
                # apt purge ftp
                # apt purge tnftp

                References:

                     1. NIST SP 800-53 Rev. 5: CM-7, CM-11

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.

                2.3 Configure Time Synchronization

                It is recommended that systems be configured to synchronize their time using a service
                such as systemd-timesyncd, or chrony.
                Virtual systems may be configured to receive their time synchronization from their host
                system.
                The host system must be configured to synchronize its time from an authoritative source
                to be considered compliant with this section.
                Any "physical" clock present on a system should be synchronized from an authoritative
                time source.
                Only one time synchronization method should be in use on the system
                Notes: Only the section related to the time synchronization method in use on the
                system should be followed, all other time synchronization recommendations should be
                skipped

                2.3.1 Ensure time synchronization is in use

                It is recommended that physical systems and virtual guests lacking direct access to the
                physical host's clock be configured to synchronize their time using a service such as
                systemd-timesyncd, or chrony.
  '2.3.1.1': !unsafe |-
    On physical systems, and virtual systems where host based time synchronization is not
                available.
                Select one of the two time synchronization daemons; chrony (1) or systemd-
                timesyncd (2) and following the remediation procedure for the selected daemon.
                Note: enabling more than one synchronization daemon could lead to unexpected or
                unreliable results:

                     1. chrony

                Run the following command to install chrony:
                # apt install chrony
                Run the following commands to stop and mask the systemd-timesyncd daemon:
                # systemctl stop systemd-timesyncd.service

                # systemctl mask systemd-timesyncd.service
                Note:

                     •    Subsection: Configure chrony should be followed
                     •    Subsection: Configure systemd-timesyncd should be skipped

                     2. systemd-timesyncd

                Run the following command to remove the chrony package:
                # apt purge chrony
                # apt autoremove chrony
                Note:

                     •    Subsection: Configure systemd-timesyncd should be followed
                     •    Subsection: Configure chrony should be skipped

                References:

                     1. NIST SP 800-53 Rev. 5: AU-3, AU-12

                CIS Controls:

                   Controls
                                                                  Control                                       IG 1 IG 2 IG 3
                   Version
                                8.4 Standardize Time Synchronization
                         v8        Standardize time synchronization. Configure at least two synchronized time         ●    ●
                                sources across enterprise assets, where supported.

                         v7     6.1 Utilize Three Synchronized Time Sources                                           ●    ●
                                   Use at least three synchronized time sources from which all servers and

                   Controls
                                                                 Control                                        IG 1 IG 2 IG 3
                   Version
                              network devices retrieve time information on a regular basis so that timestamps
                              in logs are consistent.

                2.3.2 Configure systemd-timesyncd

                systemd-timesyncd is a daemon that has been added for synchronizing the system
                clock across the network. It implements an SNTP client. In contrast to NTP
                implementations such as chrony or the NTP reference server this only implements a
                client side, and does not bother with the full NTP complexity, focusing only on querying
                time from one remote server and synchronizing the local clock to it. The daemon runs
                with minimal privileges, and has been hooked up with networkd to only operate when
                network connectivity is available. The daemon saves the current clock to disk every time
                a new NTP sync has been acquired, and uses this to possibly correct the system clock
                early at bootup, in order to accommodate for systems that lack an RTC such as the
                Raspberry Pi and embedded devices, and make sure that time monotonically
                progresses on these systems, even if it is not always correct. To make use of this
                daemon a new system user and group "systemd-timesync" needs to be created on
                installation of systemd.
                The default configuration is set during compilation, so configuration is only needed
                when it is necessary to deviate from those defaults. Initially, the main configuration file
                in /etc/systemd/ contains commented out entries showing the defaults as a guide to the
                administrator. Local overrides can be created by editing this file or by creating drop-ins,
                as described below. Using drop-ins for local configuration is recommended over
                modifications to the main configuration file.
                In addition to the "main" configuration file, drop-in configuration snippets are read from
                /usr/lib/systemd/*.conf.d/, /usr/local/lib/systemd/*.conf.d/, and
                /etc/systemd/*.conf.d/. Those drop-ins have higher precedence and override the
                main configuration file. Files in the *.conf.d/ configuration subdirectories are sorted by
                their filename in lexicographic order, regardless of in which of the subdirectories they
                reside. When multiple files specify the same option, for options which accept just a
                single value, the entry in the file sorted last takes precedence, and for options which
                accept a list of values, entries are collected as they occur in the sorted files.
                When packages need to customize the configuration, they can install drop-ins under
                /usr/. Files in /etc/ are reserved for the local administrator, who may use this logic to
                override the configuration files installed by vendor packages. Drop-ins have to be used
                to override package drop-ins, since the main configuration file has lower precedence. It
                is recommended to prefix all filenames in those subdirectories with a two-digit number
                and a dash, to simplify the ordering of the files.
                To disable a configuration file supplied by the vendor, the recommended way is to place
                a symlink to /dev/null in the configuration directory in /etc/, with the same filename as
                the vendor configuration file.
                Note:

                     •   The recommendations in this section only apply if timesyncd is in use on the
                         system
                     •   The systemd-timesyncd service specifically implements only SNTP.
                            o This minimalistic service will set the system clock for large offsets or
                               slowly adjust it for smaller deltas
                            o More complex use cases are not covered by systemd-timesyncd

                     •   If chrony is used, systemd-timesyncd should be stopped and masked, and
                         this section skipped
                     •   One, and only one, time synchronization method should be in use on the
                         system

                2.3.2.1 Ensure systemd-timesyncd configured with authorized
                timeserver (Automated)
                Profile Applicability:

                • Level 1 - Server
                • Level 1 - Workstation

                Description:
                NTP=

                     •   A space-separated list of NTP server host names or IP addresses. During
                         runtime this list is combined with any per-interface NTP servers acquired from
                         systemd-networkd.service(8). systemd-timesyncd will contact all configured
                         system or per-interface servers in turn, until one responds. When the empty
                         string is assigned, the list of NTP servers is reset, and all prior assignments will
                         have no effect. This setting defaults to an empty list.

                FallbackNTP=

                     •   A space-separated list of NTP server host names or IP addresses to be used as
                         the fallback NTP servers. Any per-interface NTP servers obtained from systemd-
                         networkd.service(8) take precedence over this setting, as do any servers set via
                         NTP= above. This setting is hence only relevant if no other NTP server
                         information is known. When the empty string is assigned, the list of NTP servers
                         is reset, and all prior assignments will have no effect. If this option is not given, a
                         compiled-in list of NTP servers is used.

                Rationale:
                Time synchronization is important to support time sensitive security mechanisms and to
                ensure log files have consistent time records across the enterprise to aid in forensic
                investigations
                Audit:
                Run the following command to verify the NTP and/or FallbackNTP option is set to local
                site approved authoritative time server(s):

                #!/usr/bin/env bash

                {
                   a_output=() a_output2=() a_output3=() a_out=() a_out2=() a_parlist=("NTP=[^#\n\r]+"
                "FallbackNTP=[^#\n\r]+")
                   l_analyze_cmd="$(readlink -f /bin/systemd-analyze)"
                l_systemd_config_file="/etc/systemd/timesyncd.conf"
                   f_config_file_parameter_chk()
                   {
                      l_used_parameter_setting=""
                      while IFS= read -r l_file; do
                         l_file="$(tr -d '# ' <<< "$l_file")"
                         l_used_parameter_setting="$(grep -PHs -- '^\h*'"$l_parameter_name"'\b' "$l_file" |
                tail -n 1)"
                         [ -n "$l_used_parameter_setting" ] && break
                      done < <($l_analyze_cmd cat-config "$l_systemd_config_file" | tac | grep -Pio
                '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                      if [ -n "$l_used_parameter_setting" ]; then
                         while IFS=: read -r l_file_name l_file_parameter; do
                            while IFS="=" read -r l_file_parameter_name l_file_parameter_value; do
                               if grep -Pq -- "$l_parameter_value" <<< "$l_file_parameter_value"; then
                                  a_out+=(" - Parameter: \"${l_file_parameter_name// /}\"" \
                                  "    correctly set to: \"${l_file_parameter_value// /}\"" \
                                  "    in the file: \"$l_file_name\"")
                               else
                                  a_out2+=(" - Parameter: \"${l_file_parameter_name// /}\"" \
                                  "    incorrectly set to: \"${l_file_parameter_value// /}\"" \
                                  "    in the file: \"$l_file_name\"" \
                                  "    Should be set to: \"$l_value_out\"")
                               fi
                            done <<< "$l_file_parameter"
                         done <<< "$l_used_parameter_setting"
                      else
                         a_out2+=(" - Parameter: \"$l_parameter_name\" is not set in an included file" \
                         "   *** Note: \"$l_parameter_name\" May be set in a file that's ignored by load
                procedure ***")
                      fi
                   }
                   while IFS="=" read -r l_parameter_name l_parameter_value; do # Assess and check
                parameters
                      l_parameter_name="${l_parameter_name// /}"; l_parameter_value="${l_parameter_value//
                /}"
                      l_value_out="${l_parameter_value//-/ through }"; l_value_out="${l_value_out//|/ or }"
                      l_value_out="$(tr -d '(){}' <<< "$l_value_out")"
                      f_config_file_parameter_chk
                   done < <(printf '%s\n' "${a_parlist[@]}")
                   if [ "${#a_out[@]}" -gt 0 ]; then
                      a_output+=("${a_out[@]}"); [ "${#a_out2[@]}" -gt 0 ] && a_output3+=(" ** INFO: **"
                "${a_out2[@]}")
                   else
                      a_output2+=("${a_out2[@]}")
                   fi
                   if [ "${#a_output2[@]}" -le 0 ]; then
                      printf '%s\n' "" "- Audit Result:" " ** PASS **" "${a_output[@]}" ""
                      [ "${#a_output3[@]}" -gt 0 ] && printf '%s\n' "${a_output3[@]}"
                   else
                      printf '%s\n' "" "- Audit Result:" " ** FAIL **" " - Reason(s) for audit failure:"
                "${a_output2[@]}"
                      [ "${#a_output[@]}" -gt 0 ] && printf '%s\n' "" "- Correctly set:" "${a_output[@]}"
                ""
                   fi
                }
                Note: Please ensure the output for NTP and/or FallbackNTP is in accordance with local
                site policy. The timeservers in the example output are provided as an example of
                possible timeservers and they may not follow local site policy.

                Remediation:
                Set NTP and/or FallbackNTP parameters to local site approved authoritative time
                server(s) in /etc/systemd/timesyncd.conf or a file in
                /etc/systemd/timesyncd.conf.d/ ending in .conf in the [Time] section:
                Example file:
                [Time]
                NTP=time.nist.gov # Uses the generic name for NIST's time servers
                FallbackNTP=time-a-g.nist.gov time-b-g.nist.gov time-c-g.nist.gov # Space
                separated list of NIST time servers
                Example script to create systemd drop-in configuration file:
                #!/usr/bin/env bash

                {
                   a_settings=("NTP=time.nist.gov" "FallbackNTP=time-a-g.nist.gov time-b-
                g.nist.gov time-c-g.nist.gov")
                   [ ! -d /etc/systemd/timesyncd.conf.d/ ] && mkdir
                /etc/systemd/timesyncd.conf.d/
                   if grep -Psq -- '^\h*\[Time\]' /etc/systemd/timesyncd.conf.d/60-
                timesyncd.conf; then
                      printf '%s\n' "" "${a_settings[@]}" >>
                /etc/systemd/timesyncd.conf.d/60-timesyncd.conf
                   else
                      printf '%s\n' "" "[Time]" "${a_settings[@]}" >>
                /etc/systemd/timesyncd.conf.d/60-timesyncd.conf
                   fi
                }
                Note: If this setting appears in a canonically later file, or later in the same file, the
                setting will be overwritten
                Run to following command to update the parameters in the service:
                # systemctl reload-or-restart systemd-timesyncd

                Default Value:
                #NTP=
                #FallbackNTP=
                References:

                     1. https://www.freedesktop.org/software/systemd/man/timesyncd.conf.html
                     2. https://tf.nist.gov/tf-cgi/servers.cgi
                     3. NIST SP 800-53 Rev. 5: AU-7, AU-8

                CIS Controls:

                    Controls
                                                                 Control                                       IG 1 IG 2 IG 3
                    Version
                               8.4 Standardize Time Synchronization
                        v8        Standardize time synchronization. Configure at least two synchronized time         ●    ●
                               sources across enterprise assets, where supported.

                   Controls
                                                                 Control                                        IG 1 IG 2 IG 3
                   Version
                              6.1 Utilize Three Synchronized Time Sources
                        v7        Use at least three synchronized time sources from which all servers and
                              network devices retrieve time information on a regular basis so that timestamps
                                                                                                                      ●    ●
                              in logs are consistent.
  '2.3.2.1': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '2.3.2.2': !unsafe |-
    - IF - systemd-timesyncd is in use on the system, run the following commands:
                Run the following command to unmask systemd-timesyncd.service:
                # systemctl unmask systemd-timesyncd.service
                Run the following command to enable and start systemd-timesyncd.service:
                # systemctl --now enable systemd-timesyncd.service
                - OR -
                If another time synchronization service is in use on the system, run the following
                command to stop and mask systemd-timesyncd:
                # systemctl --now mask systemd-timesyncd.service

                References:

                     1. NIST SP 800-53 Rev. 5: AU-7, AU-8

                CIS Controls:

                   Controls
                                                                 Control                                        IG 1 IG 2 IG 3
                   Version
                              8.4 Standardize Time Synchronization
                        v8       Standardize time synchronization. Configure at least two synchronized time           ●    ●
                              sources across enterprise assets, where supported.
                              6.1 Utilize Three Synchronized Time Sources
                        v7        Use at least three synchronized time sources from which all servers and
                              network devices retrieve time information on a regular basis so that timestamps
                                                                                                                      ●    ●
                              in logs are consistent.

                2.3.3 Configure chrony

                chrony is a daemon which implements the Network Time Protocol (NTP) and is
                designed to synchronize system clocks across a variety of systems and use a source
                that is highly accurate.
                chrony can be configured to be a client and/or a server.
                More information on chrony can be found at: http://chrony.tuxfamily.org/.
                Note:

                     •   If systemd-timesyncd is being used, chrony should be removed and this
                         section skipped
                     •   Only one time synchronization method should be in use on the system
  '2.3.3.1': !unsafe |-
    Edit /etc/chrony/chrony.conf or a file ending in .sources in
                /etc/chrony/sources.d/ and add or edit server or pool lines as appropriate
                according to local site policy:
                Edit the Chrony configuration and add or edit the server and/or pool lines returned by
                the Audit Procedure as appropriate according to local site policy
                <[server|pool]> <[remote-server|remote-pool]>
                Example script to add a drop-in configuration for the pool directive:
                #!/usr/bin/env bash

                {
                     [ ! -d "/etc/chrony/sources.d/" ] && mkdir /etc/chrony/sources.d/
                     printf '%s\n' "" "#The maxsources option is unique to the pool directive"
                \
                   "pool time.nist.gov iburst maxsources 4" >> /etc/chrony/sources.d/60-
                sources.sources
                   chronyc reload sources &>/dev/null
                }
                Example script to add a drop-in configuration for the server directive:
                #!/usr/bin/env bash

                {
                   [ ! -d "/etc/chrony/sources.d/" ] && mkdir /etc/chrony/sources.d/
                   printf '%s\n' "" "server time-a-g.nist.gov iburst" "server 132.163.97.3
                iburst" \
                   "server time-d-b.nist.gov iburst" >> /etc/chrony/sources.d/60-
                sources.sources
                   chronyc reload sources &>/dev/null
                }
                Run the following command to reload the chronyd config:
                # systemctl reload-or-restart chronyd

                References:

                     1. chrony.conf(5) Manual Page
                     2. https://tf.nist.gov/tf-cgi/servers.cgi
                     3. NIST SP 800-53 Rev. 5: AU-3, AU-12

                Additional Information:
                If pool and/or server directive(s) are set in a sources file in /etc/chrony/sources.d,
                the line:
                sourcedir /etc/chrony/sources.d
                must be present in /etc/chrony/chrony.conf

                CIS Controls:

                   Controls
                                                                 Control                                        IG 1 IG 2 IG 3
                   Version
                              8.4 Standardize Time Synchronization
                        v8       Standardize time synchronization. Configure at least two synchronized time           ●    ●
                              sources across enterprise assets, where supported.
                              6.1 Utilize Three Synchronized Time Sources
                        v7        Use at least three synchronized time sources from which all servers and
                              network devices retrieve time information on a regular basis so that timestamps
                                                                                                                      ●    ●
                              in logs are consistent.
  '2.3.3.2': !unsafe |-
    Add or edit the user line to /etc/chrony/chrony.conf or a file ending in .conf in
                /etc/chrony/conf.d/:
                user _chrony
                - OR -
                If another time synchronization service is in use on the system, run the following
                command to remove chrony from the system:
                # apt purge chrony
                # apt autoremove chrony

                Default Value:
                user _chrony
                References:

                     1. NIST SP 800-53 Rev. 5: AU-8

                CIS Controls:

                     Controls Version                  Control      IG 1 IG 2 IG 3
                              v8        0.0 Explicitly Not Mapped
                                          Explicitly Not Mapped

                              v7        0.0 Explicitly Not Mapped
                                          Explicitly Not Mapped
  '2.3.3.3': !unsafe |-
    - IF - chrony is in use on the system, run the following commands:
                Run the following command to unmask chrony.service:
                # systemctl unmask chrony.service
                Run the following command to enable and start chrony.service:
                # systemctl --now enable chrony.service
                - OR -
                If another time synchronization service is in use on the system, run the following
                command to remove chrony:
                # apt purge chrony
                # apt autoremove chrony

                References:

                     1. NIST SP 800-53 Rev. 5: AU-8

                CIS Controls:

                   Controls
                                                                 Control                                        IG 1 IG 2 IG 3
                   Version
                              8.4 Standardize Time Synchronization
                        v8       Standardize time synchronization. Configure at least two synchronized time           ●    ●
                              sources across enterprise assets, where supported.
                              6.1 Utilize Three Synchronized Time Sources
                        v7        Use at least three synchronized time sources from which all servers and
                              network devices retrieve time information on a regular basis so that timestamps
                                                                                                                      ●    ●
                              in logs are consistent.

                2.4 Job Schedulers

                A job scheduler is used to execute jobs, commands, or shell scripts, at fixed times,
                dates, or intervals

                2.4.1 Configure cron

                cron is a time based job scheduler
                - IF - cron is not installed on the system, this sub section can be skipped
                Note: Other methods such as systemd timers exist for scheduling jobs. If an alternate
                method is in use, it should be secured in accordance with local site policy
  '2.4.1.1': !unsafe |-
    - IF - cron is installed on the system:
                Run the following commands to unmask, enable, and start cron:
                # systemctl unmask "$(systemctl list-unit-files | awk
                '$1~/^crond?\.service/{print $1}')"
                # systemctl --now enable "$(systemctl list-unit-files | awk
                '$1~/^crond?\.service/{print $1}')"

                References:

                     1. NIST SP 800-53 Rev. 5: CM-1, CM-2, CM-6, CM-7, IA-5

                CIS Controls:

                     Controls Version                    Control             IG 1 IG 2 IG 3
                              v8          0.0 Explicitly Not Mapped
                                            Explicitly Not Mapped

                              v7          0.0 Explicitly Not Mapped
                                            Explicitly Not Mapped
  '2.4.1.2': !unsafe |-
    - IF - cron is installed on the system:
                Run the following commands to set ownership and permissions on /etc/crontab:
                # chown root:root /etc/crontab
                # chmod og-rwx /etc/crontab

                Default Value:
                Access: (644/-rw-r--r--) Uid: ( 0/ root) Gid: ( 0/ root)
                References:

                     1. NIST SP 800-53 Rev. 5: AC-3, MP-2

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '2.4.1.3': !unsafe |-
    - IF - cron is installed on the system:
                Run the following commands to set ownership and permissions on the
                /etc/cron.hourly directory:
                # chown root:root /etc/cron.hourly/
                # chmod og-rwx /etc/cron.hourly/

                Default Value:
                Access: (755/drwxr-xr-x) Uid: ( 0/ root) Gid: ( 0/ root)
                References:

                     1. NIST SP 800-53 Rev. 5: AC-3. MP-2

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '2.4.1.4': !unsafe |-
    - IF - cron is installed on the system:
                Run the following commands to set ownership and permissions on the
                /etc/cron.daily directory:
                # chown root:root /etc/cron.daily/
                # chmod og-rwx /etc/cron.daily/

                Default Value:
                Access: (755/drwxr-xr-x) Uid: ( 0/ root) Gid: ( 0/ root)
                References:

                     1. NIST SP 800-53 Rev. 5: AC-3. MP-2

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '2.4.1.5': !unsafe |-
    - IF - cron is installed on the system:
                Run the following commands to set ownership and permissions on the
                /etc/cron.weekly directory:
                # chown root:root /etc/cron.weekly/
                # chmod og-rwx /etc/cron.weekly/

                Default Value:
                Access: (755/drwxr-xr-x) Uid: ( 0/ root) Gid: ( 0/ root)
                References:

                     1. NIST SP 800-53 Rev. 5: AC-3. MP-2

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '2.4.1.6': !unsafe |-
    - IF - cron is installed on the system:
                Run the following commands to set ownership and permissions on the
                /etc/cron.monthly directory:
                # chown root:root /etc/cron.monthly/
                # chmod og-rwx /etc/cron.monthly/

                Default Value:
                Access: (755/drwxr-xr-x) Uid: ( 0/ root) Gid: ( 0/ root)
                References:

                     1. NIST SP 800-53 Rev. 5: AC-3. MP-2

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '2.4.1.7': !unsafe |-
    - IF - cron is installed on the system:
                Run the following commands to set ownership and permissions on the
                /etc/cron.yearly directory:
                # chown root:root /etc/cron.yearly/
                # chmod og-rwx /etc/cron.yearly/

                Default Value:
                Access: (755/drwxr-xr-x) Uid: ( 0/ root) Gid: ( 0/ root)
                References:

                     1. NIST SP 800-53 Rev. 5: AC-3. MP-2
  '2.4.1.8': !unsafe |-
    - IF - cron is installed on the system:
                Run the following commands to set ownership and permissions on the /etc/cron.d
                directory:
                # chown root:root /etc/cron.d/
                # chmod og-rwx /etc/cron.d/

                Default Value:
                Access: (755/drwxr-xr-x) Uid: ( 0/ root) Gid: ( 0/ root)
                References:

                     1. NIST SP 800-53 Rev. 5: AC-3. MP-2

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '2.4.1.9': !unsafe |-
    - IF - cron is installed on the system:
                Run the following script to:

                     •   Create /etc/cron.allow if it doesn't exist
                     •   Change owner to user root
                     •   Change group owner to group root - OR - group crontab if it exists
                     •   Change mode to 640 or more restrictive

                #!/usr/bin/env bash

                {
                     [ ! -e "/etc/cron.allow" ] && touch /etc/cron.allow
                     chmod u-x,g-wx,o-rwx /etc/cron.allow
                     if grep -Pq -- '^\h*crontab\:' /etc/group; then
                        chown root:crontab /etc/cron.allow
                     else
                        chown root:root /etc/cron.allow
                     fi
                }
                - IF - /etc/cron.deny exists, run the following script to:

                     •   Change owner to user root
                     •   Change group owner to group root - OR - group crontab if it exists
                     •   Change mode to 640 or more restrictive

                #!/usr/bin/env bash

                {
                     if [ -e "/etc/cron.deny" ]; then
                        chmod u-x,g-wx,o-rwx /etc/cron.deny
                        if grep -Pq -- '^\h*crontab\:' /etc/group; then
                           chown root:crontab /etc/cron.deny
                        else
                           chown root:root /etc/cron.deny
                        fi
                     fi
                }

                Note:

                     •    When the /etc/cron.allow file exists, a user must be listed, one user per
                          line, in the /etc/cron.allow file in order to be allowed to use the crontab
                          command.
                     •    If both /etc/cron.allow and /etc/cron.deny exist, /etc/cron.allow takes
                          precedence. Which means that /etc/cron.deny is not considered and users
                          must be listed in /etc/cron.allow in order to use the crontab.
                     •    If the /etc/cron.allow file does not exist but the /etc/cron.deny file does
                          exist, then you must not be listed in the /etc/cron.deny file in order to use the
                          crontab command.
                     •    Regardless of the existence of any of these files, the root administrative user is
                          always allowed to setup a crontab.
                     •    On systems where cron is configured to use the group crontab, if the group
                          crontab is not set as the owner of /etc/cron.allow, then cron will deny access
                          to all users but root, and you will see an error similar to:

                You (<USERNAME>) are not allowed to use this program (crontab)
                See crontab(1) for more information

                References:

                     1. NIST SP 800-53 Rev. 5: AC-3. MP-2

                CIS Controls:

                  Controls
                                                                    Control                                          IG 1 IG 2 IG 3
                  Version
                               3.3 Configure Data Access Control Lists
                         v8       Configure data access control lists based on a user’s need to know. Apply data
                               access control lists, also known as access permissions, to local and remote file
                                                                                                                      ●    ●    ●
                               systems, databases, and applications.
                               14.6 Protect Information through Access Control Lists
                                   Protect all information stored on systems with file system, network share,
                         v7    claims, application, or database specific access control lists. These controls will
                               enforce the principle that only authorized individuals should have access to the
                                                                                                                      ●    ●    ●
                               information based on their need to access the information as a part of their
                               responsibilities.

                2.4.2 Configure at

                at is a command-line utility used to schedule a job for later execution
                Note: if at is not installed on the system, this section can be skipped
  '2.4.2.1': !unsafe |-
    - IF - at is installed on the system:
                Run the following script to:

                     •    /etc/at.allow:
                              o Create the file if it doesn't exist
                              o Change owner or user root
                              o If group daemon exists, change to group daemon, else change group to
                                  root
                              o Change mode to 640 or more restrictive
                     •    - IF - /etc/at.deny exists:
                              o Change owner or user root
                              o If group daemon exists, change to group daemon, else change group to
                                  root
                              o Change mode to 640 or more restrictive

                #!/usr/bin/env bash

                {
                     grep -Pq -- '^daemon\b' /etc/group && l_group="daemon" || l_group="root"
                     [ ! -e "/etc/at.allow" ] && touch /etc/at.allow
                     chown root:"$l_group" /etc/at.allow
                     chmod u-x,g-wx,o-rwx /etc/at.allow
                     [ -e "/etc/at.deny" ] && chown root:"$l_group" /etc/at.deny
                     [ -e "/etc/at.deny" ] && chmod u-x,g-wx,o-rwx /etc/at.deny
                }

                References:

                     1. NIST SP 800-53 Rev. 5: AC-3. MP-2

                CIS Controls:

                    Controls
                                                                    Control                                          IG 1 IG 2 IG 3
                    Version
                               3.3 Configure Data Access Control Lists
                         v8       Configure data access control lists based on a user’s need to know. Apply data
                               access control lists, also known as access permissions, to local and remote file
                                                                                                                      ●    ●    ●
                               systems, databases, and applications.
                               14.6 Protect Information through Access Control Lists
                                   Protect all information stored on systems with file system, network share,
                         v7    claims, application, or database specific access control lists. These controls will
                               enforce the principle that only authorized individuals should have access to the
                                                                                                                      ●    ●    ●
                               information based on their need to access the information as a part of their
                               responsibilities.

                3 Network
                This section provides guidance on for securing the network configuration of the system

                3.1 Configure Network Devices

                To reduce the attack surface of a system, unused devices should be disabled.
                Note: This should not be considered a comprehensive list, you may wish to consider
                additions to those listed here for your environment.
  '3.1.1': !unsafe |-
    Enable or disable IPv6 in accordance with system requirements and local site policy
                Default Value:
                IPv6 is enabled
                References:

                     1. NIST SP 800-53 Rev. 5: CM-7

                Additional Information:
                Having more addresses has grown in importance with the expansion of smart devices
                and connectivity. IPv6 provides more than enough globally unique IP addresses for
                every networked device currently on the planet, helping ensure providers can keep pace
                with the expected proliferation of IP-based devices.

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '3.1.2': !unsafe |-
    Run the following command to disable any wireless interfaces:
                # find /lib/modules/`uname -r`/kernel/drivers/net/wireless -name '*.ko' -
                printf 'install %f /bin/false\nblacklist %f\n\n' | sed 's/\.ko//1' >>
                /etc/modprobe.d/blacklist-wireless.conf
                Note: the *.conf file in /etc/modprobe.d/ in the above command can renamed as
                needed.
                References:

                     1. NIST SP 800-53 Rev. 5: CM-7

                CIS Controls:

                   Controls
                                                                 Control                                        IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                       v8        Uninstall or disable unnecessary services on enterprise assets and software,         ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              15.4 Disable Wireless Access on Devices if Not Required
                       v7        Disable wireless access on devices that do not have a business purpose for                ●
                              wireless access.
                              15.5 Limit Wireless Access on Client Devices
                       v7        Configure wireless access on client machines that do have an essential
                              wireless business purpose, to allow access only to authorized wireless networks
                                                                                                                           ●
                              and to restrict access to other wireless networks.
  '3.1.3': !unsafe |-
    Run the following commands to stop bluetooth.service, and remove the bluez
                package:
                # systemctl stop bluetooth.service
                # apt purge bluez
                - OR -
                - IF - the bluez package is required as a dependency:
                Run the following commands to stop and mask bluetooth.service:
                # systemctl stop bluetooth.service
                # systemctl mask bluetooth.service
                Note: A reboot may be required
                References:

                     1. https://www.cisa.gov/tips/st05-015
                     2. NIST SP 800-53 Rev. 5: CM-7

                CIS Controls:

                   Controls
                                                                   Control                                         IG 1 IG 2 IG 3
                   Version
                                 4.8 Uninstall or Disable Unnecessary Services on
                                 Enterprise Assets and Software
                         v8         Uninstall or disable unnecessary services on enterprise assets and software,         ●    ●
                                 such as an unused file sharing service, web application module, or service
                                 function.
                                 9.2 Ensure Only Approved Ports, Protocols and Services
                         v7
                                 Are Running
                                                                                                                         ●    ●

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.

                3.2 Configure Network Kernel Modules

                The Linux kernel modules support several network protocols that are not commonly
                used. If these protocols are not needed, it is recommended that they be disabled in the
                kernel.
                Note: This should not be considered a comprehensive list of uncommon network
                protocols, you may wish to consider additions to those listed here for your environment.
  '3.2.1': !unsafe |-
    Unload and disable the atm kernel module.

                     1. Run the following commands to unload the atm kernel module:

                # modprobe -r atm 2>/dev/null
                # rmmod can 2>/dev/null

                     2. Perform the following to disable the atm kernel module:

                Create a file ending in .conf with install can /bin/false in the /etc/modprobe.d/
                directory
                Example:
                # printf '\n%s\n' "install atm /bin/false" >> /etc/modprobe.d/60-atm.conf
                Create a file ending in .conf with blacklist atm in the /etc/modprobe.d/ directory
                Example:
                # printf '\n%s\n' "blacklist atm" >> /etc/modprobe.d/60-atm.conf

                References:

                     1. CCI-000381
                     2. NIST SP 800-53 :: CM-7
                     3. NIST SP 800-53A :: CM-7.1 (ii)
                     4. STIG ID: RHEL-08-040021 | RULE ID: SV-230494r1069310 | CAT III
                     5. STIG ID: OL08-00-040021 | RULE ID: SV-248829r991589 | CAT II
                     6. STIG ID: RHEL-09-213045 | RULE ID: SV-257804r1044853 | CAT II
                     7. STIG ID: ALMA-09-029610 | RULE ID: SV-269341r1050223 | CAT II
                     8. STIG ID: OL09-00-000040 | RULE ID: SV-271443r1092463 | CAT II

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '3.2.2': !unsafe |-
    Unload and disable the can kernel module.

                     1. Run the following commands to unload the can kernel module:

                # modprobe -r can 2>/dev/null
                # rmmod can 2>/dev/null

                     2. Perform the following to disable the can kernel module:

                Create a file ending in .conf with install can /bin/false in the /etc/modprobe.d/
                directory
                Example:
                # printf '\n%s\n' "install can /bin/false" >> /etc/modprobe.d/60-can.conf
                Create a file ending in .conf with blacklist can in the /etc/modprobe.d/ directory
                Example:
                # printf '\n%s\n' "blacklist can" >> /etc/modprobe.d/60-can.conf

                References:

                     1. CCI-000381
                     2. NIST SP 800-53 :: CM-7
                     3. NIST SP 800-53A :: CM-7.1 (ii)
                     4. STIG ID: RHEL-08-040022 | RULE ID: SV-230495r1069311 | CAT III
                     5. STIG ID: OL08-00-040022 | RULE ID: SV-248830r991589 | CAT II
                     6. STIG ID: RHEL-09-213050 | RULE ID: SV-257805r1044856 | CAT II
                     7. STIG ID: ALMA-09-029830 | RULE ID: SV-269343r1050225 | CAT II
                     8. STIG ID: OL09-00-000041 | RULE ID: SV-271444r1091044 | CAT II

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '3.2.3': !unsafe |-
    Unload and disable the dccp kernel module.

                     1. Run the following commands to unload the dccp kernel module:

                # modprobe -r dccp 2>/dev/null
                # rmmod dccp 2>/dev/null

                     2. Perform the following to disable the dccp kernel module:

                Create a file ending in .conf with install dccp /bin/false in the
                /etc/modprobe.d/ directory
                Example:
                # printf '\n%s\n' "install dccp /bin/false" >> /etc/modprobe.d/60-dccp.conf
                Create a file ending in .conf with blacklist dccp in the /etc/modprobe.d/ directory
                Example:
                # printf '\n%s\n' "blacklist dccp" >> /etc/modprobe.d/60-dccp.conf

                References:

                     1. CCI-000381
                     2. NIST SP 800-53 :: CM-7
                     3. NIST SP 800-53A :: CM-7.1 (ii)

                CIS Controls:

                   Controls
                                                                 Control                                         IG 1 IG 2 IG 3
                   Version
                               4.8 Uninstall or Disable Unnecessary Services on
                               Enterprise Assets and Software
                        v8        Uninstall or disable unnecessary services on enterprise assets and software,         ●    ●
                               such as an unused file sharing service, web application module, or service
                               function.

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '3.2.4': !unsafe |-
    Unload and disable the rds kernel module.

                     1. Run the following commands to unload the rds kernel module:

                # modprobe -r rds 2>/dev/null
                # rmmod rds 2>/dev/null

                     2. Perform the following to disable the rds kernel module:

                Create a file ending in .conf with install rds /bin/false in the /etc/modprobe.d/
                directory
                Example:
                # printf '\n%s\n' "install rds /bin/false" >> /etc/modprobe.d/60-rds.conf
                Create a file ending in .conf with blacklist rds in the /etc/modprobe.d/ directory
                Example:
                # printf '\n%s\n' "blacklist rds" >> /etc/modprobe.d/60-rds.conf

                References:

                     1. CCI-000381
                     2. NIST SP 800-53 :: CM-7
                     3. NIST SP 800-53A :: CM-7.1 (ii)

                CIS Controls:

                   Controls
                                                                 Control                                         IG 1 IG 2 IG 3
                   Version
                               4.8 Uninstall or Disable Unnecessary Services on
                               Enterprise Assets and Software
                        v8        Uninstall or disable unnecessary services on enterprise assets and software,         ●    ●
                               such as an unused file sharing service, web application module, or service
                               function.
                               9.2 Ensure Only Approved Ports, Protocols and Services
                        v7
                               Are Running
                                                                                                                       ●    ●

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '3.2.5': !unsafe |-
    Unload and disable the sctp kernel module.

                     1. Run the following commands to unload the sctp kernel module:

                # modprobe -r sctp 2>/dev/null
                # rmmod sctp 2>/dev/null

                     2. Perform the following to disable the sctp kernel module:

                Create a file ending in .conf with install sctp /bin/false in the
                /etc/modprobe.d/ directory
                Example:
                # printf '\n%s\n' "install sctp /bin/false" >> /etc/modprobe.d/60-sctp.conf
                Create a file ending in .conf with blacklist sctp in the /etc/modprobe.d/ directory
                Example:
                # printf '\n%s\n' "blacklist sctp" >> /etc/modprobe.d/60-sctp.conf

                References:

                     1. CCI-000381
                     2. NIST SP 800-53 :: CM-7
                     3. NIST SP 800-53A :: CM-7.1 (ii)
                     4. STIG ID: RHEL-08-040023 | RULE ID: SV-230496r1069312 | CAT III
                     5. STIG ID: OL08-00-040023 | RULE ID: SV-248831r991589 | CAT II
                     6. STIG ID: RHEL-09-213060 | RULE ID: SV-257807r1044862 | CAT II
                     7. STIG ID: ALMA-09-030050 | RULE ID: SV-269345r1050227 | CAT II
                     8. STIG ID: OL09-00-000043 | RULE ID: SV-271446r1091050 | CAT II

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '3.2.6': !unsafe |-
    Unload and disable the tipc kernel module.

                     1. Run the following commands to unload the tipc kernel module:

                # modprobe -r tipc 2>/dev/null
                # rmmod tipc 2>/dev/null

                     2. Perform the following to disable the tipc kernel module:

                Create a file ending in .conf with install tipc /bin/false in the
                /etc/modprobe.d/ directory
                Example:
                # printf '\n%s\n' "install tipc /bin/false" >> /etc/modprobe.d/60-tipc.conf
                Create a file ending in .conf with blacklist tipc in the /etc/modprobe.d/ directory
                Example:
                # printf '\n%s\n' "blacklist tipc" >> /etc/modprobe.d/60-tipc.conf

                References:

                     1. CCI-000381
                     2. NIST SP 800-53 :: CM-7
                     3. NIST SP 800-53A :: CM-7.1 (ii)
                     4. STIG ID: RHEL-08-040024 | RULE ID: SV-230497r1069313 | CAT III
                     5. STIG ID: OL08-00-040024 | RULE ID: SV-248832r958478 | CAT III
                     6. STIG ID: RHEL-09-213065 | RULE ID: SV-257808r1044865 | CAT II
                     7. STIG ID: ALMA-09-030270 | RULE ID: SV-269347r1050229 | CAT II
                     8. STIG ID: OL09-00-000044 | RULE ID: SV-271447r1092464 | CAT II

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.

                3.3 Configure Network Kernel Parameters

                This section contains Recommendations covering kernel parameters effecting network
                stack.
                Note:

                     •   Some of the Recommendations in this section's subsections are not applicable if
                         the system is a router. These Recommendations include a note in their
                         description.
                     •   sysctl settings are defined through files in /usr/local/lib, /usr/lib/, /lib/,
                         /run/, and /etc/
                     •   Files are typically placed in the sysctl.d directory within the parent directory
                     •   The paths where sysctl preload files usually exist
                             o /run/sysctl.d/*.conf
                             o /etc/sysctl.d/*.conf
                             o /usr/local/lib/sysctl.d/*.conf
                             o /usr/lib/sysctl.d/*.conf
                             o /lib/sysctl.d/*.conf
                             o /etc/sysctl.conf
                     •   Files must have the ".conf" extension
                     •   Vendors settings usually live in /usr/lib/ or /usr/local/lib/
                     •   To override a whole file, create a new file with the same name in
                         /etc/sysctl.d/ and put new settings there.
                     •   To override only specific settings, add a file with a lexically later name in
                         /etc/sysctl.d/ and put new settings there.
                     •   The command /usr/lib/systemd/systemd-sysctl --cat-config produces
                         output containing The system's loaded kernel parameters and the files they're
                         configured in:
                             o Entries listed latter in the file take precedence over the same settings
                                listed earlier in the file
                             o Files containing kernel parameters that are over-ridden by other files with
                                the same name will not be listed
                             o On systems running UncomplicatedFirewall, the kernel parameters may
                                be set or over-written. This will not be visible in the output of the command
                     •   On systems with Uncomplicated Firewall, additional settings may be configured
                         in /etc/ufw/sysctl.conf
                             o The settings in /etc/ufw/sysctl.conf will override settings other
                                settings and will not be visible in the output of the
                                /usr/lib/systemd/systemd-sysctl --cat-config command
                             o This behavior can be changed by updating the IPT_SYSCTL parameter in
                                /etc/default/ufw
                     •   SYSTEM FILE PRECEDENCE
                             o When using the --system option, sysctl will read files from directories in
                                the following list in given order from top to bottom. Once a file of a given
                                filename is loaded, any file of the same name in subsequent directories is
                                ignored.

                                  /etc/sysctl.d/*.conf /run/sysctl.d/*.conf
                                  /usr/local/lib/sysctl.d/*.conf /usr/lib/sysctl.d/*.conf
                                  /lib/sysctl.d/*.conf /etc/sysctl.conf

                              o   All configuration files are sorted in lexicographic order, regardless of the
                                  directory they reside in. Configuration files can either be completely
                                  replaced (by having a new configuration file with the same name in a
                                  directory of higher priority) or partially replaced (by having a configuration
                                  file that is ordered later)--

                3.3.1 Configure IPv4 parameters

                net.ipv4 kernel parameters control various aspects of the IPv4 network stack. These
                parameters effect functions to include routing, TCP, and security.
  '3.3.1.1': !unsafe |-
    1. Review all files being used by systemd-sysctl and comment out or remove all
                        net.ipv4.ip_forward lines that are not net.ipv4.ip_forward=0.

                Example script:
                #!/usr/bin/env bash

                {
                     l_option="net.ipv4.ip_forward" l_value="0"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*(1[0-9]*)/s/^/# /' "$l_file"
                     done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .confand edit or
                        add the following line:

                net.ipv4.ip_forward = 0

                Example:
                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "net.ipv4.ip_forward = 0" >> /etc/sysctl.d/60-
                ipv4_sysctl.conf

                         Note: If the UFW file was the first file listed in the audit, the entry will be
                         commented out as part of the first step, however updating Uncomplicated
                         Firewall (UFW) may update this change. In this case the updated entry will
                         supersede the entry being created as part of this step.

                     3. Run the following command to load all sysctl configuration files:

                # sysctl --system

                Default Value:
                net.ipv4.ip_forward = 1
                References:

                     1. CCI-000366
                     2. NIST SP 800-53 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. STIG ID: SLES-15-040380 | RULE ID: SV-235024r991589 | CAT II

                CIS Controls:

                   Controls
                                                                  Control                                          IG 1 IG 2 IG 3
                   Version
                                4.8 Uninstall or Disable Unnecessary Services on
                                Enterprise Assets and Software
                        v8         Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                                such as an unused file sharing service, web application module, or service
                                function.
                                9.2 Ensure Only Approved Ports, Protocols and Services
                        v7      Are Running                                                                              ●    ●
                                   Ensure that only network ports, protocols, and services listening on a system
                                with validated business needs, are running on each system.
  '3.3.1.2': !unsafe |-
    1. Review all files being used by systemd-sysctl and comment out or remove all
                      net.ipv4.conf.all.forwarding lines that are not
                      net.ipv4.conf.all.forwarding=0.
                Example script:
                #!/usr/bin/env bash

                {
                     l_option="net.ipv4.conf.all.forwarding" l_value="0"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*(1[0-9]*)/s/^/# /' "$l_file"
                     done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .confand edit or
                        add the following line:

                net.ipv4.conf.all.forwarding = 0
                Example:
                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "net.ipv4.conf.all.forwarding = 0" \
                >> /etc/sysctl.d/60-ipv4_sysctl.conf

                         Note: If the UFW file was the first file listed in the audit, the entry will be
                         commented out as part of the first step, however updating Uncomplicated
                         Firewall (UFW) may update this change. In this case the updated entry will
                         supersede the entry being created as part of this step.

                     3. Run the following command to load all sysctl configuration files:

                # sysctl --system

                Default Value:
                net.ipv4.conf.all.forwarding = 1
                References:

                     1. CCI-000366
                     2. NIST SP 800-53 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. STIG ID: RHEL-08-040259 | RULE ID: SV-250317r1017358 | CAT II
                     5. STIG ID: OL08-00-040259 | RULE ID: SV-252662r991589 | CAT II
                     6. STIG ID: RHEL-09-253075 | RULE ID: SV-257970r1045011 | CAT II
                     7. STIG ID: ALMA-09-019160 | RULE ID: SV-269249r1050131 | CAT II
                     8. STIG ID: OL09-00-006028 | RULE ID: SV-271872r1092328 | CAT II

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '3.3.1.3': !unsafe |-
    1. Review all files being used by systemd-sysctl and comment out or remove all
                        net.ipv4.conf.default.forwarding lines that are not
                        net.ipv4.conf.default.forwarding=0.

                Example script:
                #!/usr/bin/env bash

                {
                     l_option="net.ipv4.conf.default.forwarding" l_value="0"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*(1[0-9]*)/s/^/# /' "$l_file"
                     done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .confand edit or
                        add the following line:

                net.ipv4.conf.default.forwarding = 0
                Example:

                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "net.ipv4.conf.default.forwarding = 0" \
                >> /etc/sysctl.d/60-ipv4_sysctl.conf

                         Note: If the UFW file was the first file listed in the audit, the entry will be
                         commented out as part of the first step, however updating Uncomplicated
                         Firewall (UFW) may update this change. In this case the updated entry will
                         supersede the entry being created as part of this step.

                     3. Run the following command to load all sysctl configuration files:

                # sysctl --system

                Default Value:
                net.ipv4.conf.default.forwarding = 1
                References:

                     1. CCI-000366
                     2. NIST SP 800-53 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. STIG ID: ALMA-09-019160 | RULE ID: SV-269249r1050131 | CAT II

                CIS Controls:

                   Controls
                                                                  Control                                          IG 1 IG 2 IG 3
                   Version
                                4.8 Uninstall or Disable Unnecessary Services on
                                Enterprise Assets and Software
                        v8         Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                                such as an unused file sharing service, web application module, or service
                                function.
                                9.2 Ensure Only Approved Ports, Protocols and Services
                        v7      Are Running                                                                              ●    ●
                                   Ensure that only network ports, protocols, and services listening on a system
                                with validated business needs, are running on each system.
  '3.3.1.4': !unsafe |-
    1. Review all files being used by systemd-sysctl and comment out or remove all
                         net.ipv4.conf.all.send_redirects lines that are not
                         net.ipv4.conf.all.send_redirects=0.

                Example script:

                #!/usr/bin/env bash

                {
                     l_option="net.ipv4.conf.all.send_redirects" l_value="0"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*(1[0-9]*)/s/^/# /' "$l_file"
                     done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .confand edit or
                        add the following line:

                net.ipv4.conf.all.send_redirects = 0
                Example:
                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "net.ipv4.conf.all.send_redirects = 0" \
                >> /etc/sysctl.d/60-ipv4_sysctl.conf

                         Note: If the UFW file was the first file listed in the audit, the entry will be
                         commented out as part of the first step, however updating Uncomplicated
                         Firewall (UFW) may update this change. In this case the updated entry will
                         supersede the entry being created as part of this step.

                     3. Run the following command to load all sysctl configuration files:

                # sysctl --system

                Default Value:
                net.ipv4.conf.all.send_redirects = 1
                References:

                     1. CCI-000366
                     2. NIST SP 800-53 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. STIG ID: RHEL-08-040220 | RULE ID: SV-230536r1017298 | CAT II
                     5. STIG ID: OL08-00-040220 | RULE ID: SV-248877r991589 | CAT II
                     6. STIG ID: RHEL-09-253065 | RULE ID: SV-257968r991589 | CAT II
                     7. STIG ID: ALMA-09-019930 | RULE ID: SV-269256r1050138 | CAT II
                     8. STIG ID: OL09-00-006032 | RULE ID: SV-271875r1092337 | CAT II
                     9. STIG ID: SLES-15-040370 | RULE ID: SV-235023r991589 | CAT II

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '3.3.1.5': !unsafe |-
    1. Review all files being used by systemd-sysctl and comment out or remove all
                         net.ipv4.conf.default.send_redirects lines that are not
                         net.ipv4.conf.default.send_redirects=0.

                Example script:

                #!/usr/bin/env bash

                {
                     l_option="net.ipv4.conf.default.send_redirects" l_value="0"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*(1[0-9]*)/s/^/# /' "$l_file"
                     done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .confand edit or
                        add the following line:

                net.ipv4.conf.default.send_redirects = 0
                Example:
                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "net.ipv4.conf.default.send_redirects = 0" \
                >> /etc/sysctl.d/60-ipv4_sysctl.conf

                         Note: If the UFW file was the first file listed in the audit, the entry will be
                         commented out as part of the first step, however updating Uncomplicated
                         Firewall (UFW) may update this change. In this case the updated entry will
                         supersede the entry being created as part of this step.

                     3. Run the following command to load all sysctl configuration files:

                # sysctl --system

                Default Value:
                net.ipv4.conf.default.send_redirects = 1
                References:

                     1. CCI-000366
                     2. NIST SP 800-53 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. STIG ID: RHEL-08-040270 | RULE ID: SV-230543r1017305 | CAT II
                     5. STIG ID: OL08-00-040270 | RULE ID: SV-248886r991589 | CAT II
                     6. STIG ID: RHEL-09-253070 | RULE ID: SV-257969r991589 | CAT II
                     7. STIG ID: ALMA-09-019930 | RULE ID: SV-269256r1050138 | CAT II
                     8. STIG ID: OL09-00-006033 | RULE ID: SV-271876r1092641 | CAT II
                     9. STIG ID: SLES-15-040360 | RULE ID: SV-235022r991589 | CAT II

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.

                3.3.1.6 Ensure net.ipv4.icmp_ignore_bogus_error_responses is
                configured (Automated)
                Profile Applicability:

                • Level 1 - Server
                • Level 1 - Workstation

                Description:
                net.ipv4.icmp_ignore_bogus_error_responses controls if the kernel logs bogus
                responses (RFC-1122 non-compliant) from broadcast reframes.
                More information about the kernel parameter configuration files, their location, and load
                preference is available in the "Configure Network Kernel Parameters" section overview.
                Rationale:
                Some routers (and some attackers) will send responses that violate RFC-1122 and
                attempt to fill up a log file system with many useless error messages.
                Audit:
                Verify net.ipv4.icmp_ignore_bogus_error_responses is set to 1.

                     1. Run the following command to verify
                        net.ipv4.icmp_ignore_bogus_error_responses is set to 1 in the running
                        configuration:

                # sysctl net.ipv4.icmp_ignore_bogus_error_responses
                Verify output is:
                net.ipv4.icmp_ignore_bogus_error_responses = 1

                     2. Run the following script to verify
                        net.ipv4.icmp_ignore_bogus_error_responses = 1 is set in a file being
                        used by systemd-sysctl to configure
                        net.ipv4.icmp_ignore_bogus_error_responses:

                #!/usr/bin/env bash

                {
                      l_parameter_name="net.ipv4.icmp_ignore_bogus_error_responses"
                      l_grep="${l_parameter_name//./(\\.|\\/)}" a_output=() a_files=()
                      l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                      || readlink -e /usr/lib/systemd/systemd-sysctl)"
                      l_ufw_file="$([ -f /etc/default/ufw ] && \
                      awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                      [ -f "$(readlink -e "$l_ufw_file")" ] && \
                      a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                      while IFS= read -r l_fname; do
                         l_file="$(readlink -e "${l_fname//# /}")"
                         [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                         <<< "${a_files[*]}" && a_files+=("$l_file")
                      done < <("$l_systemdsysctl" --cat-config | tac | \
                      grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                      for l_file in "${a_files[@]}"; do
                         l_opt="$(grep -Psio '^\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" | tail -n
                1)"
                      l_option_value="$(cut -d= -f2 <<< "$l_opt" | xargs)"
                      [ -n "$l_option_value" ] && \
                      a_output+=(" - \"$l_parameter_name = $l_option_value\" is set in:
                \"$l_file\"")
                   done
                   [ "${#a_output[@]}" -gt "0" ] && printf '%s\n' "" "${a_output[@]}" ""
                }
                Verify output includes net.ipv4.icmp_ignore_bogus_error_responses = 1.
                Example output:
                 - "net.ipv4.icmp_ignore_bogus_error_responses = 1" is set in:
                "/etc/sysctl.d/60-ipv4_sysctl.conf"

                         Note: If multiple lines with
                         net.ipv4.icmp_ignore_bogus_error_responses = are returned.
                            • The first line includes the value being used by systemd-sysctl. If
                                this is a correct value, this is considered a passing state. If the file
                                listed is not in the /etc/sysctl.d/ directory, it is highly
                                recommended to follow the remediation procedure to create a
                                .conf file in the /etc/sysctl.d/ directory with the correct setting
                                to prevent a potential change due to an update to the system.
                            • Any files in the /etc/sysctl.d/ directory that include an incorrect
                                value should be modified to comment out or change the incorrect
                                value to minimize the potential of the incorrect value being used by
                                systemd-sysctl due to system configuration changes.

                Remediation:

                      1. Review all files being used by systemd-sysctl and comment out or remove all
                         net.ipv4.icmp_ignore_bogus_error_responses lines that are not
                         net.ipv4.icmp_ignore_bogus_error_responses=1.

                Example script:

                #!/usr/bin/env bash

                {
                     l_option="net.ipv4.icmp_ignore_bogus_error_responses" l_value="1"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*(0|[2-9]|1[0-9]+)/s/^/# /' "$l_file"
                     done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .confand edit or
                        add the following line:

                net.ipv4.icmp_ignore_bogus_error_responses = 1
                Example:
                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "net.ipv4.icmp_ignore_bogus_error_responses = 1" \
                >> /etc/sysctl.d/60-ipv4_sysctl.conf

                         Note: If the UFW file was the first file listed in the audit, the entry will be
                         commented out as part of the first step, however updating Uncomplicated
                         Firewall (UFW) may update this change. In this case the updated entry will
                         supersede the entry being created as part of this step.

                     3. Run the following command to load all sysctl configuration files:

                # sysctl --system

                Default Value:
                net.ipv4.icmp_ignore_bogus_error_responses = 1
                References:

                     1. CCI-000366
                     2. NIST SP 800-53 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. STIG ID: RHEL-09-253060 | RULE ID: SV-257967r991589 | CAT II
                     5. STIG ID: ALMA-09-018940 | RULE ID: SV-269247r1050129 | CAT II
                     6. STIG ID: OL09-00-006031 | RULE ID: SV-271874r1092612 | CAT II

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.

                3.3.1.7 Ensure net.ipv4.icmp_echo_ignore_broadcasts is
                configured (Automated)
                Profile Applicability:

                • Level 1 - Server
                • Level 1 - Workstation

                Description:
                net.ipv4.icmp_echo_ignore_broadcasts controls if all ICMP echo and timestamp
                requests to broadcast and multicast addresses are ignored.
                More information about the kernel parameter configuration files, their location, and load
                preference is available in the "Configure Network Kernel Parameters" section overview.
                Rationale:
                Accepting ICMP echo and timestamp requests with broadcast or multicast destinations
                for your network could be used to trick your host into starting (or participating) in a
                Smurf attack. A Smurf attack relies on an attacker sending large amounts of ICMP
                broadcast messages with a spoofed source address. All hosts receiving this message
                and responding would send echo-reply messages back to the spoofed address, which is
                probably not routable. If many hosts respond to the packets, the amount of traffic on the
                network could be significantly multiplied.
                Audit:
                Verify net.ipv4.icmp_echo_ignore_broadcasts is set to 1.

                     1. Run the following command to verify
                        net.ipv4.icmp_echo_ignore_broadcasts is set to 1 in the running
                        configuration:

                # sysctl net.ipv4.icmp_echo_ignore_broadcasts
                Verify output is:
                net.ipv4.icmp_echo_ignore_broadcasts = 1

                     2. Run the following script to verify net.ipv4.icmp_echo_ignore_broadcasts =
                        1 is set in a file being used by systemd-sysctl to configure
                        net.ipv4.icmp_echo_ignore_broadcasts:

                #!/usr/bin/env bash

                {
                      l_parameter_name="net.ipv4.icmp_echo_ignore_broadcasts"
                      l_grep="${l_parameter_name//./(\\.|\\/)}" a_output=() a_files=()
                      l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                      || readlink -e /usr/lib/systemd/systemd-sysctl)"
                      l_ufw_file="$([ -f /etc/default/ufw ] && \
                      awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                      [ -f "$(readlink -e "$l_ufw_file")" ] && \
                      a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                      while IFS= read -r l_fname; do
                         l_file="$(readlink -e "${l_fname//# /}")"
                         [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                         <<< "${a_files[*]}" && a_files+=("$l_file")
                      done < <("$l_systemdsysctl" --cat-config | tac | \
                      grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                      for l_file in "${a_files[@]}"; do
                         l_opt="$(grep -Psio '^\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" | tail -n
                1)"
                      l_option_value="$(cut -d= -f2 <<< "$l_opt" | xargs)"
                      [ -n "$l_option_value" ] && \
                      a_output+=(" - \"$l_parameter_name = $l_option_value\" is set in:
                \"$l_file\"")
                   done
                   [ "${#a_output[@]}" -gt "0" ] && printf '%s\n' "" "${a_output[@]}" ""
                }
                Verify output includes net.ipv4.icmp_echo_ignore_broadcasts = 1.
                Example output:
                 - "net.ipv4.icmp_echo_ignore_broadcasts = 1" is set in: "/etc/sysctl.d/60-
                ipv4_sysctl.conf"

                         Note: If multiple lines with net.ipv4.icmp_echo_ignore_broadcasts =
                         are returned.
                            • The first line includes the value being used by systemd-sysctl. If
                                this is a correct value, this is considered a passing state. If the file
                                listed is not in the /etc/sysctl.d/ directory, it is highly
                                recommended to follow the remediation procedure to create a
                                .conf file in the /etc/sysctl.d/ directory with the correct setting
                                to prevent a potential change due to an update to the system.
                            • Any files in the /etc/sysctl.d/ directory that include an incorrect
                                value should be modified to comment out or change the incorrect
                                value to minimize the potential of the incorrect value being used by
                                systemd-sysctl due to system configuration changes.

                Remediation:

                      1. Review all files being used by systemd-sysctl and comment out or remove all
                         net.ipv4.icmp_echo_ignore_broadcasts lines that are not
                         net.ipv4.icmp_echo_ignore_broadcasts=1.

                Example script:

                #!/usr/bin/env bash

                {
                     l_option="net.ipv4.icmp_echo_ignore_broadcasts" l_value="1"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*(0|[2-9]|1[0-9]+)/s/^/# /' "$l_file"
                     done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .confand edit or
                        add the following line:

                net.ipv4.icmp_echo_ignore_broadcasts = 1
                Example:
                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "net.ipv4.icmp_echo_ignore_broadcasts = 1" \
                >> /etc/sysctl.d/60-ipv4_sysctl.conf

                         Note: If the UFW file was the first file listed in the audit, the entry will be
                         commented out as part of the first step, however updating Uncomplicated
                         Firewall (UFW) may update this change. In this case the updated entry will
                         supersede the entry being created as part of this step.

                     3. Run the following command to load all sysctl configuration files:

                # sysctl --system

                Default Value:
                net.ipv4.icmp_echo_ignore_broadcasts = 1
                References:

                     1. CCI-000366
                     2. NIST SP 800-53 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. STIG ID: RHEL-08-040230 | RULE ID: SV-230537r1017299 | CAT II
                     5. STIG ID: OL08-00-040230 | RULE ID: SV-248878r991589 | CAT II
                     6. STIG ID: RHEL-09-253055 | RULE ID: SV-257966r991589 | CAT II
                     7. STIG ID: ALMA-09-019050 | RULE ID: SV-269248r1050130 | CAT II
                     8. STIG ID: OL09-00-006030 | RULE ID: SV-271873r1092331 | CAT II

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '3.3.1.6': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '3.3.1.7': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '3.3.1.8': !unsafe |-
    1. Review all files being used by systemd-sysctl and comment out or remove all
                        net.ipv4.conf.all.accept_redirects lines that are not
                        net.ipv4.conf.all.accept_redirects=0.

                Example script:
                #!/usr/bin/env bash

                {
                     l_option="net.ipv4.conf.all.accept_redirects" l_value="0"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*(1[0-9]*)/s/^/# /' "$l_file"
                     done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .confand edit or
                        add the following line:

                net.ipv4.conf.all.accept_redirects = 0
                Example:
                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "net.ipv4.conf.all.accept_redirects = 0" \
                >> /etc/sysctl.d/60-ipv4_sysctl.conf

                         Note: If the UFW file was the first file listed in the audit, the entry will be
                         commented out as part of the first step, however updating Uncomplicated
                         Firewall (UFW) may update this change. In this case the updated entry will
                         supersede the entry being created as part of this step.

                     3. Run the following command to load all sysctl configuration files:

                # sysctl --system

                Default Value:
                net.ipv4.conf.all.accept_redirects = 1
                References:

                     1. CCI-000366
                     2. NIST SP 800-53 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. STIG ID: RHEL-08-040279 | RULE ID: SV-244553r1017353 | CAT II
                     5. STIG ID: OL08-00-040279 | RULE ID: SV-248887r991589 | CAT II
                     6. STIG ID: RHEL-09-253015 | RULE ID: SV-257958r991589 | CAT II
                     7. STIG ID: ALMA-09-018610 | RULE ID: SV-269244r1050126 | CAT II
                     8. STIG ID: OL09-00-006020 | RULE ID: SV-271864r1092304 | CAT II
                     9. STIG ID: SLES-15-040330 | RULE ID: SV-235018r991589 | CAT II

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.

                3.3.1.9 Ensure net.ipv4.conf.default.accept_redirects is
                configured (Automated)
                Profile Applicability:

                • Level 1 - Server
                • Level 1 - Workstation

                Description:
                ICMP redirect messages are packets that convey routing information and tell your host
                (acting as a router) to send packets via an alternate path. It is a way of allowing an
                outside routing device to update your system routing tables.
                net.ipv4.conf.default.accept_redirects controls accepting of all IPv4 ICMP
                redirected packets on a newly added network interface.
                More information about the kernel parameter configuration files, their location, and load
                preference is available in the "Configure Network Kernel Parameters" section overview.
                         Note: If this system is a router this recommendation is not
                         applicable.
                Rationale:
                ICMP redirect messages are packets that convey routing information and tell your host
                (acting as a router) to send packets via an alternate path. It is a way of allowing an
                outside routing device to update your system routing tables.
                By setting net.ipv4.conf.all.accept_redirects to 0, the system will not accept
                IPv4 ICMP redirect messages on a newly added network interface.
                Audit:
                Verify net.ipv4.conf.default.accept_redirects is set to 0.

                     1. Run the following command to verify
                        net.ipv4.conf.default.accept_redirects is set to 0 in the running
                        configuration:

                # sysctl net.ipv4.conf.default.accept_redirects
                Verify output is:
                net.ipv4.conf.default.accept_redirects = 0

                     2. Run the following script to verify net.ipv4.conf.default.accept_redirects
                        = 0 is set in a file being used by systemd-sysctl to configure
                        net.ipv4.conf.default.accept_redirects:

                #!/usr/bin/env bash

                {
                      l_parameter_name="net.ipv4.conf.default.accept_redirects"
                      l_grep="${l_parameter_name//./(\\.|\\/)}" a_output=() a_files=()
                      l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                      || readlink -e /usr/lib/systemd/systemd-sysctl)"
                      l_ufw_file="$([ -f /etc/default/ufw ] && \
                      awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                      [ -f "$(readlink -e "$l_ufw_file")" ] && \
                      a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                      while IFS= read -r l_fname; do
                         l_file="$(readlink -e "${l_fname//# /}")"
                         [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                         <<< "${a_files[*]}" && a_files+=("$l_file")
                      done < <("$l_systemdsysctl" --cat-config | tac | \
                      grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                      for l_file in "${a_files[@]}"; do
                         l_opt="$(grep -Psio '^\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" | tail -n
                1)"
                      l_option_value="$(cut -d= -f2 <<< "$l_opt" | xargs)"
                      [ -n "$l_option_value" ] && \
                      a_output+=(" - \"$l_parameter_name = $l_option_value\" is set in:
                \"$l_file\"")
                   done
                   [ "${#a_output[@]}" -gt "0" ] && printf '%s\n' "" "${a_output[@]}" ""
                }
                Verify output includes net.ipv4.conf.default.accept_redirects = 0.
                Example output:
                 - "net.ipv4.conf.default.accept_redirects = 0" is set in: "/etc/sysctl.d/60-
                ipv4_sysctl.conf"

                         Note: If multiple lines with net.ipv4.conf.default.accept_redirects
                         = are returned.

                              •   The first line includes the value being used by systemd-sysctl. If
                                  this is a correct value, this is considered a passing state. If the file
                                  listed is not in the /etc/sysctl.d/ directory, it is highly
                                  recommended to follow the remediation procedure to create a
                                  .conf file in the /etc/sysctl.d/ directory with the correct setting
                                  to prevent a potential change due to an update to the system.
                              •   Any files in the /etc/sysctl.d/ directory that include an incorrect
                                  value should be modified to comment out or change the incorrect
                                  value to minimize the potential of the incorrect value being used by
                                  systemd-sysctl due to system configuration changes.

                Remediation:

                     1. Review all files being used by systemd-sysctl and comment out or remove all
                        net.ipv4.conf.default.accept_redirects lines that are not
                        net.ipv4.conf.default.accept_redirects=0.

                Example script:
                #!/usr/bin/env bash

                {
                     l_option="net.ipv4.conf.default.accept_redirects" l_value="0"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*(1[0-9]*)/s/^/# /' "$l_file"
                     done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .confand edit or
                        add the following line:

                net.ipv4.conf.default.accept_redirects = 0
                Example:
                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "net.ipv4.conf.default.accept_redirects = 0" \
                >> /etc/sysctl.d/60-ipv4_sysctl.conf

                         Note: If the UFW file was the first file listed in the audit, the entry will be
                         commented out as part of the first step, however updating Uncomplicated
                         Firewall (UFW) may update this change. In this case the updated entry will
                         supersede the entry being created as part of this step.

                     3. Run the following command to load all sysctl configuration files:

                # sysctl --system

                Default Value:
                net.ipv4.conf.default.accept_redirects = 1
                References:

                     1. CCI-000366
                     2. NIST SP 800-53 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. STIG ID: RHEL-08-040209 | RULE ID: SV-244550r1017350 | CAT II
                     5. STIG ID: OL08-00-040209 | RULE ID: SV-248875r991589 | CAT II
                     6. STIG ID: RHEL-09-253040 | RULE ID: SV-257963r991589 | CAT II
                     7. STIG ID: ALMA-09-018610 | RULE ID: SV-269244r1050126 | CAT II
                     8. STIG ID: OL09-00-006025 | RULE ID: SV-271869r1092319 | CAT II
                     9. STIG ID: SLES-15-040340 | RULE ID: SV-235019r991589 | CAT II

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '3.3.1.9': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '3.3.1.10': !unsafe |-
    1. Review all files being used by systemd-sysctl and comment out or remove all
                      net.ipv4.conf.all.secure_redirects lines that are not
                      net.ipv4.conf.all.secure_redirects=0.
                Example script:
                #!/usr/bin/env bash

                {
                     l_option="net.ipv4.conf.all.secure_redirects" l_value="0"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*(1[0-9]*)/s/^/# /' "$l_file"
                     done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .confand edit or
                        add the following line:

                net.ipv4.conf.all.secure_redirects = 0
                Example:
                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "net.ipv4.conf.all.secure_redirects = 0" \
                >> /etc/sysctl.d/60-ipv4_sysctl.conf

                         Note: If the UFW file was the first file listed in the audit, the entry will be
                         commented out as part of the first step, however updating Uncomplicated
                         Firewall (UFW) may update this change. In this case the updated entry will
                         supersede the entry being created as part of this step.

                     3. Run the following command to load all sysctl configuration files:

                # sysctl --system

                Default Value:
                net.ipv4.conf.all.secure_redirects = 1

                References:

                     1. CCI-000366
                     2. NIST SP 800-53 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)

                CIS Controls:

                   Controls
                                                                 Control                                          IG 1 IG 2 IG 3
                   Version
                               4.8 Uninstall or Disable Unnecessary Services on
                               Enterprise Assets and Software
                        v8        Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                               such as an unused file sharing service, web application module, or service
                               function.
                               9.2 Ensure Only Approved Ports, Protocols and Services
                        v7     Are Running                                                                              ●    ●
                                  Ensure that only network ports, protocols, and services listening on a system
                               with validated business needs, are running on each system.

                3.3.1.11 Ensure net.ipv4.conf.default.secure_redirects is
                configured (Automated)
                Profile Applicability:

                • Level 1 - Server
                • Level 1 - Workstation

                Description:
                Secure ICMP redirects are the same as ICMP redirects, except they come from
                gateways listed on the default gateway list. It is assumed that these gateways are
                known to your system, and that they are likely to be secure.
                More information about the kernel parameter configuration files, their location, and load
                preference is available in the "Configure Network Kernel Parameters" section overview.
                Rationale:
                It is still possible for even known gateways to be compromised.
                Setting net.ipv4.conf.default.secure_redirects to 0 the system the system will
                not accept IPv4 routing table updates by possibly compromised known gateways on a
                newly added network interface.
                Audit:
                Verify net.ipv4.conf.default.secure_redirects is set to 0.

                     1. Run the following command to verify
                        net.ipv4.conf.default.secure_redirects is set to 0 in the running
                        configuration:

                # sysctl      net.ipv4.conf.default.secure_redirects
                Verify output is:
                net.ipv4.conf.default.secure_redirects = 0

                     2. Run the following script to verify net.ipv4.conf.default.secure_redirects
                        = 0 is set in a file being used by systemd-sysctl to configure
                        net.ipv4.conf.default.secure_redirects:

                #!/usr/bin/env bash

                {
                      l_parameter_name="net.ipv4.conf.default.secure_redirects"
                      l_grep="${l_parameter_name//./(\\.|\\/)}" a_output=() a_files=()
                      l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                      || readlink -e /usr/lib/systemd/systemd-sysctl)"
                      l_ufw_file="$([ -f /etc/default/ufw ] && \
                      awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                      [ -f "$(readlink -e "$l_ufw_file")" ] && \
                      a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                      while IFS= read -r l_fname; do
                         l_file="$(readlink -e "${l_fname//# /}")"
                         [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                         <<< "${a_files[*]}" && a_files+=("$l_file")
                      done < <("$l_systemdsysctl" --cat-config | tac | \
                      grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                      for l_file in "${a_files[@]}"; do
                         l_opt="$(grep -Psio '^\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" | tail -n
                1)"
                      l_option_value="$(cut -d= -f2 <<< "$l_opt" | xargs)"
                      [ -n "$l_option_value" ] && \
                      a_output+=(" - \"$l_parameter_name = $l_option_value\" is set in:
                \"$l_file\"")
                   done
                   [ "${#a_output[@]}" -gt "0" ] && printf '%s\n' "" "${a_output[@]}" ""
                }
                Verify output includes net.ipv4.conf.default.secure_redirects = 0.
                Example output:
                 - "net.ipv4.conf.default.secure_redirects = 0" is set in: "/etc/sysctl.d/60-
                ipv4_sysctl.conf"

                         Note: If multiple lines with net.ipv4.conf.default.secure_redirects
                         = are returned.

                              •   The first line includes the value being used by systemd-sysctl. If
                                  this is a correct value, this is considered a passing state. If the file
                                  listed is not in the /etc/sysctl.d/ directory, it is highly
                                  recommended to follow the remediation procedure to create a
                                  .conf file in the /etc/sysctl.d/ directory with the correct setting
                                  to prevent a potential change due to an update to the system.
                              •   Any files in the /etc/sysctl.d/ directory that include an incorrect
                                  value should be modified to comment out or change the incorrect
                                  value to minimize the potential of the incorrect value being used by
                                  systemd-sysctl due to system configuration changes.

                Remediation:
                   1. Review all files being used by systemd-sysctl and comment out or remove all
                      net.ipv4.conf.default.secure_redirects lines that are not
                      net.ipv4.conf.default.secure_redirects=0.
                Example script:
                #!/usr/bin/env bash

                {
                     l_option="net.ipv4.conf.default.secure_redirects" l_value="0"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*(1[0-9]*)/s/^/# /' "$l_file"
                     done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .confand edit or
                        add the following line:

                net.ipv4.conf.default.secure_redirects = 0
                Example:
                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "net.ipv4.conf.default.secure_redirects = 0" \
                >> /etc/sysctl.d/60-ipv4_sysctl.conf

                         Note: If the UFW file was the first file listed in the audit, the entry will be
                         commented out as part of the first step, however updating Uncomplicated
                         Firewall (UFW) may update this change. In this case the updated entry will
                         supersede the entry being created as part of this step.

                     3. Run the following command to load all sysctl configuration files:

                # sysctl --system

                Default Value:
                net.ipv4.conf.default.secure_redirects = 1

                References:

                     1. CCI-000366
                     2. NIST SP 800-53 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)

                CIS Controls:

                   Controls
                                                                 Control                                          IG 1 IG 2 IG 3
                   Version
                               4.8 Uninstall or Disable Unnecessary Services on
                               Enterprise Assets and Software
                        v8        Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                               such as an unused file sharing service, web application module, or service
                               function.
                               9.2 Ensure Only Approved Ports, Protocols and Services
                        v7     Are Running                                                                              ●    ●
                                  Ensure that only network ports, protocols, and services listening on a system
                               with validated business needs, are running on each system.
  '3.3.1.11': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '3.3.1.12': !unsafe |-
    1. Review all files being used by systemd-sysctl and comment out or remove all
                      net.ipv4.conf.all.rp_filter lines that are not
                      net.ipv4.conf.all.rp_filter=1.
                Example script:
                #!/usr/bin/env bash

                {
                     l_option="net.ipv4.conf.all.rp_filter" l_value="1"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*(0|[2-9]|1[0-9]+)/s/^/# /' "$l_file"
                     done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .confand edit or
                        add the following line:

                net.ipv4.conf.all.rp_filter = 1
                Example:

                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "net.ipv4.conf.all.rp_filter = 1" \
                >> /etc/sysctl.d/60-ipv4_sysctl.conf

                         Note: If the UFW file was the first file listed in the audit, the entry will be
                         commented out as part of the first step, however updating Uncomplicated
                         Firewall (UFW) may update this change. In this case the updated entry will
                         supersede the entry being created as part of this step.

                     3. Run the following command to load all sysctl configuration files:

                # sysctl --system

                Default Value:
                net.ipv4.conf.all.rp_filter = 2
                References:

                     1. CCI-000366
                     2. NIST SP 800-53 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. STIG ID: RHEL-08-040285 | RULE ID: SV-230549r1017311 | CAT II
                     5. STIG ID: OL08-00-040285 | RULE ID: SV-248893r991589 | CAT II
                     6. STIG ID: RHEL-09-253035 | RULE ID: SV-257962r991589 | CAT II
                     7. STIG ID: ALMA-09-019820 | RULE ID: SV-269255r1050137 | CAT II
                     8. STIG ID: OL09-00-006024 | RULE ID: SV-271868r1092316 | CAT II

                CIS Controls:

                   Controls
                                                                  Control                                          IG 1 IG 2 IG 3
                   Version
                                4.8 Uninstall or Disable Unnecessary Services on
                                Enterprise Assets and Software
                        v8         Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                                such as an unused file sharing service, web application module, or service
                                function.
                                9.2 Ensure Only Approved Ports, Protocols and Services
                        v7      Are Running                                                                              ●    ●
                                   Ensure that only network ports, protocols, and services listening on a system
                                with validated business needs, are running on each system.
  '3.3.1.13': !unsafe |-
    1. Review all files being used by systemd-sysctl and comment out or remove all
                      net.ipv4.conf.default.rp_filter lines that are not
                      net.ipv4.conf.default.rp_filter=1.
                Example script:
                #!/usr/bin/env bash

                {
                     l_option="net.ipv4.conf.default.rp_filter" l_value="1"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*(0|[2-9]|1[0-9]+)/s/^/# /' "$l_file"
                     done
                }
                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .confand edit or
                        add the following line:
                net.ipv4.conf.default.rp_filter = 1
                Example:
                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "net.ipv4.conf.default.rp_filter = 1" \
                >> /etc/sysctl.d/60-ipv4_sysctl.conf

                         Note: If the UFW file was the first file listed in the audit, the entry will be
                         commented out as part of the first step, however updating Uncomplicated
                         Firewall (UFW) may update this change. In this case the updated entry will
                         supersede the entry being created as part of this step.

                     3. Run the following command to load all sysctl configuration files:

                # sysctl --system

                Default Value:
                net.ipv4.conf.default.rp_filter = 1
                References:

                     1. CCI-000366
                     2. NIST SP 800-53 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. STIG ID: RHEL-09-253050 | RULE ID: SV-257965r991589 | CAT II
                     5. STIG ID: ALMA-09-019820 | RULE ID: SV-269255r1050137 | CAT II
                     6. STIG ID: OL09-00-006027 | RULE ID: SV-271871r1092325 | CAT II

                CIS Controls:

                   Controls
                                                                 Control                                          IG 1 IG 2 IG 3
                   Version
                               4.8 Uninstall or Disable Unnecessary Services on
                               Enterprise Assets and Software
                        v8        Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                               such as an unused file sharing service, web application module, or service
                               function.
                               9.2 Ensure Only Approved Ports, Protocols and Services
                        v7     Are Running                                                                              ●    ●
                                  Ensure that only network ports, protocols, and services listening on a system
                               with validated business needs, are running on each system.

                3.3.1.14 Ensure net.ipv4.conf.all.accept_source_route is
                configured (Automated)
                Profile Applicability:

                • Level 1 - Server
                • Level 1 - Workstation

                Description:
                The accept_source_route option causes network interfaces to accept packets with
                the Strict Source Route (SSR) or Loose Source Routing (LSR) option set.
                net.ipv4.conf.all.accept_source_route controls accepting of all IPv4 packets
                with the SSR or LSR option set on all interfaces.
                In networking, source routing allows a sender to partially or fully specify the route
                packets take through a network. In contrast, non-source routed packets travel a path
                determined by routers in the network. In some cases, systems may not be routable or
                reachable from some locations (e.g. private addresses vs. Internet routable), and so
                source routed packets would need to be used.
                More information about the kernel parameter configuration files, their location, and load
                preference is available in the "Configure Network Kernel Parameters" section overview.
                Rationale:
                Setting net.ipv4.conf.all.accept_source_route to 0 disables the system from
                accepting source routed packets. Assume this system was capable of routing packets to
                Internet routable addresses on one interface and private addresses on another
                interface. Assume that the private addresses were not routable to the Internet routable
                addresses and vice versa. Under normal routing circumstances, an attacker from the
                Internet routable addresses could not use the system as a way to reach the private
                address systems. If, however, source routed packets were allowed, they could be used
                to gain access to the private address systems as the route could be specified, rather
                than rely on routing protocols that did not allow this routing.
                Audit:
                Verify net.ipv4.conf.all.accept_source_route is set to 0.
                    1. Run the following command to verify
                       net.ipv4.conf.all.accept_source_route is set to 0 in the running
                       configuration:
                # sysctl net.ipv4.conf.all.accept_source_route
                Verify output is:
                net.ipv4.conf.all.accept_source_route = 0

                     2. Run the following script to verify net.ipv4.conf.all.accept_source_route =
                        0 is set in a file being used by systemd-sysctl to configure
                        net.ipv4.conf.all.accept_source_route:

                #!/usr/bin/env bash

                {
                      l_parameter_name="net.ipv4.conf.all.accept_source_route"
                      l_grep="${l_parameter_name//./(\\.|\\/)}" a_output=() a_files=()
                      l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                      || readlink -e /usr/lib/systemd/systemd-sysctl)"
                      l_ufw_file="$([ -f /etc/default/ufw ] && \
                      awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                      [ -f "$(readlink -e "$l_ufw_file")" ] && \
                      a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                      while IFS= read -r l_fname; do
                         l_file="$(readlink -e "${l_fname//# /}")"
                         [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                         <<< "${a_files[*]}" && a_files+=("$l_file")
                      done < <("$l_systemdsysctl" --cat-config | tac | \
                      grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                      for l_file in "${a_files[@]}"; do
                         l_opt="$(grep -Psio '^\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" | tail -n
                1)"
                      l_option_value="$(cut -d= -f2 <<< "$l_opt" | xargs)"
                      [ -n "$l_option_value" ] && \
                      a_output+=(" - \"$l_parameter_name = $l_option_value\" is set in:
                \"$l_file\"")
                   done
                   [ "${#a_output[@]}" -gt "0" ] && printf '%s\n' "" "${a_output[@]}" ""
                }
                Verify output includes net.ipv4.conf.all.accept_source_route = 0.
                Example output:
                 - "net.ipv4.conf.all.accept_source_route = 0" is set in: "/etc/sysctl.d/60-
                ipv4_sysctl.conf"

                         Note: If multiple lines with net.ipv4.conf.all.accept_source_route
                         = are returned.

                              •   The first line includes the value being used by systemd-sysctl. If
                                  this is a correct value, this is considered a passing state. If the file
                                  listed is not in the /etc/sysctl.d/ directory, it is highly
                                  recommended to follow the remediation procedure to create a
                                  .conf file in the /etc/sysctl.d/ directory with the correct setting
                                  to prevent a potential change due to an update to the system.
                              •   Any files in the /etc/sysctl.d/ directory that include an incorrect
                                  value should be modified to comment out or change the incorrect
                                  value to minimize the potential of the incorrect value being used by
                                  systemd-sysctl due to system configuration changes.

                Remediation:
                   1. Review all files being used by systemd-sysctl and comment out or remove all
                      net.ipv4.conf.all.accept_source_route lines that are not
                      net.ipv4.conf.all.accept_source_route=0.
                Example script:
                #!/usr/bin/env bash

                {
                     l_option="net.ipv4.conf.all.accept_source_route" l_value="0"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*(1[0-9]*)/s/^/# /' "$l_file"
                     done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .confand edit or
                        add the following line:

                net.ipv4.conf.all.accept_source_route = 0
                Example:
                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "net.ipv4.conf.all.accept_source_route = 0" \
                >> /etc/sysctl.d/60-ipv4_sysctl.conf

                         Note: If the UFW file was the first file listed in the audit, the entry will be
                         commented out as part of the first step, however updating Uncomplicated
                         Firewall (UFW) may update this change. In this case the updated entry will
                         supersede the entry being created as part of this step.

                     3. Run the following command to load all sysctl configuration files:

                # sysctl --system

                Default Value:
                net.ipv4.conf.all.accept_source_route = 0

                References:

                     1. CCI-000366
                     2. NIST SP 800-53 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. STIG ID: RHEL-08-040239 | RULE ID: SV-244551r1017351 | CAT II
                     5. STIG ID: OL08-00-040239 | RULE ID: SV-248879r991589 | CAT II
                     6. STIG ID: RHEL-09-253020 | RULE ID: SV-257959r991589 | CAT II
                     7. STIG ID: ALMA-09-020260 | RULE ID: SV-269259r1050141 | CAT II
                     8. STIG ID: OL09-00-006021 | RULE ID: SV-271865r1092307 | CAT II
                     9. STIG ID: SLES-15-040300 | RULE ID: SV-235014r991589 | CAT II

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.

                3.3.1.15 Ensure net.ipv4.conf.default.accept_source_route is
                configured (Automated)
                Profile Applicability:

                • Level 1 - Server
                • Level 1 - Workstation

                Description:
                The accept_source_route option causes network interfaces to accept packets with
                the Strict Source Route (SSR) or Loose Source Routing (LSR) option set.
                net.ipv4.conf.default.accept_source_route controls accepting of all IPv4
                packets with the SSR or LSR option set on newly added network interfaces.
                In networking, source routing allows a sender to partially or fully specify the route
                packets take through a network. In contrast, non-source routed packets travel a path
                determined by routers in the network. In some cases, systems may not be routable or
                reachable from some locations (e.g. private addresses vs. Internet routable), and so
                source routed packets would need to be used.
                More information about the kernel parameter configuration files, their location, and load
                preference is available in the "Configure Network Kernel Parameters" section overview.
                Rationale:
                Setting net.ipv4.conf.default.accept_source_route to 0 disables the system
                from accepting source routed packets. Assume this system was capable of routing
                packets to Internet routable addresses on one interface and private addresses on
                another interface. Assume that the private addresses were not routable to the Internet
                routable addresses and vice versa. Under normal routing circumstances, an attacker
                from the Internet routable addresses could not use the system as a way to reach the
                private address systems. If, however, source routed packets were allowed, they could
                be used to gain access to the private address systems as the route could be specified,
                rather than rely on routing protocols that did not allow this routing.
                Audit:
                Verify net.ipv4.conf.default.accept_source_route is set to 0.

                     1. Run the following command to verify
                        net.ipv4.conf.default.accept_source_route is set to 0 in the running
                        configuration:

                # sysctl net.ipv4.conf.default.accept_source_route
                Verify output is:
                net.ipv4.conf.default.accept_source_route = 0

                      2. Run the following script to verify
                         net.ipv4.conf.default.accept_source_route = 0 is set in a file being used
                         by systemd-sysctl to configure
                         net.ipv4.conf.default.accept_source_route:

                #!/usr/bin/env bash

                {
                      l_parameter_name="net.ipv4.conf.default.accept_source_route"
                      l_grep="${l_parameter_name//./(\\.|\\/)}" a_output=() a_files=()
                      l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                      || readlink -e /usr/lib/systemd/systemd-sysctl)"
                      l_ufw_file="$([ -f /etc/default/ufw ] && \
                      awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                      [ -f "$(readlink -e "$l_ufw_file")" ] && \
                      a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                      while IFS= read -r l_fname; do
                         l_file="$(readlink -e "${l_fname//# /}")"
                         [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                         <<< "${a_files[*]}" && a_files+=("$l_file")
                      done < <("$l_systemdsysctl" --cat-config | tac | \
                      grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                      for l_file in "${a_files[@]}"; do
                         l_opt="$(grep -Psio '^\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" | tail -n
                1)"
                      l_option_value="$(cut -d= -f2 <<< "$l_opt" | xargs)"
                      [ -n "$l_option_value" ] && \
                      a_output+=(" - \"$l_parameter_name = $l_option_value\" is set in:
                \"$l_file\"")
                   done
                   [ "${#a_output[@]}" -gt "0" ] && printf '%s\n' "" "${a_output[@]}" ""
                }
                Verify output includes net.ipv4.conf.default.accept_source_route = 0.
                Example output:
                 - "net.ipv4.conf.default.accept_source_route = 0" is set in:
                "/etc/sysctl.d/60-ipv4_sysctl.conf"

                         Note: If multiple lines with
                         net.ipv4.conf.default.accept_source_route = are returned.

                              •   The first line includes the value being used by systemd-sysctl. If
                                  this is a correct value, this is considered a passing state. If the file
                                  listed is not in the /etc/sysctl.d/ directory, it is highly
                                  recommended to follow the remediation procedure to create a
                                  .conf file in the /etc/sysctl.d/ directory with the correct setting
                                  to prevent a potential change due to an update to the system.
                              •   Any files in the /etc/sysctl.d/ directory that include an incorrect
                                  value should be modified to comment out or change the incorrect
                                  value to minimize the potential of the incorrect value being used by
                                  systemd-sysctl due to system configuration changes.

                Remediation:

                     1. Review all files being used by systemd-sysctl and comment out or remove all
                        net.ipv4.conf.default.accept_source_route lines that are not
                        net.ipv4.conf.default.accept_source_route=0.

                Example script:
                #!/usr/bin/env bash

                {
                     l_option="net.ipv4.conf.default.accept_source_route" l_value="0"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*(1[0-9]*)/s/^/# /' "$l_file"
                     done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .confand edit or
                        add the following line:

                net.ipv4.conf.default.accept_source_route = 0
                Example:
                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "net.ipv4.conf.default.accept_source_route = 0" \
                >> /etc/sysctl.d/60-ipv4_sysctl.conf

                         Note: If the UFW file was the first file listed in the audit, the entry will be
                         commented out as part of the first step, however updating Uncomplicated
                         Firewall (UFW) may update this change. In this case the updated entry will
                         supersede the entry being created as part of this step.

                     3. Run the following command to load all sysctl configuration files:

                # sysctl --system

                Default Value:
                net.ipv4.conf.default.accept_source_route = 1
                References:

                     1. CCI-000366
                     2. NIST SP 800-53 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. STIG ID: RHEL-08-040249 | RULE ID: SV-244552r1017352 | CAT II
                     5. STIG ID: OL08-00-040249 | RULE ID: SV-248881r991589 | CAT II
                     6. STIG ID: RHEL-09-253045 | RULE ID: SV-257964r991589 | CAT II
                     7. STIG ID: ALMA-09-020260 | RULE ID: SV-269259r1050141 | CAT II
                     8. STIG ID: OL09-00-006026 | RULE ID: SV-271870r1092322 | CAT II
                     9. STIG ID: SLES-15-040320 | RULE ID: SV-235016r991589 | CAT II

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '3.3.1.14': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '3.3.1.15': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '3.3.1.16': !unsafe |-
    1. Review all files being used by systemd-sysctl and comment out or remove all
                      net.ipv4.conf.all.log_martians lines that are not
                      net.ipv4.conf.all.log_martians=1.
                Example script:
                #!/usr/bin/env bash

                {
                     l_option="net.ipv4.conf.all.log_martians" l_value="1"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*(0|[2-9]|1[0-9]+)/s/^/# /' "$l_file"
                     done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .confand edit or
                        add the following line:

                net.ipv4.conf.all.log_martians = 1
                Example:
                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "net.ipv4.conf.all.log_martians = 1" \
                >> /etc/sysctl.d/60-ipv4_sysctl.conf

                         Note: If the UFW file was the first file listed in the audit, the entry will be
                         commented out as part of the first step, however updating Uncomplicated
                         Firewall (UFW) may update this change. In this case the updated entry will
                         supersede the entry being created as part of this step.

                     3. Run the following command to load all sysctl configuration files:

                # sysctl --system

                Default Value:
                net.ipv4.conf.all.log_martians = 0

                References:

                     1. CCI-000366
                     2. NIST SP 800-53 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. STIG ID: RHEL-09-253025 | RULE ID: SV-257960r991589 | CAT II
                     5. STIG ID: ALMA-09-019380 | RULE ID: SV-269251r1050133 | CAT II
                     6. STIG ID: OL09-00-006022 | RULE ID: SV-271866r1092310 | CAT II

                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              6.2 Activate audit logging
                       v7        Ensure that local logging has been enabled on all systems and networking             ●    ●    ●
                              devices.
                              6.3 Enable Detailed Logging
                       v7        Enable system logging to include detailed information such as an event source,
                              date, user, timestamp, source addresses, destination addresses, and other useful
                                                                                                                           ●    ●
                              elements.
  '3.3.1.17': !unsafe |-
    1. Review all files being used by systemd-sysctl and comment out or remove all
                      net.ipv4.conf.default.log_martians lines that are not
                      net.ipv4.conf.default.log_martians=1.
                Example script:
                #!/usr/bin/env bash

                {
                     l_option="net.ipv4.conf.default.log_martians" l_value="1"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*(0|[2-9]|1[0-9]+)/s/^/# /' "$l_file"
                     done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .confand edit or
                        add the following line:

                net.ipv4.conf.default.log_martians = 1

                Example:
                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "net.ipv4.conf.default.log_martians = 1" \
                >> /etc/sysctl.d/60-ipv4_sysctl.conf

                         Note: If the UFW file was the first file listed in the audit, the entry will be
                         commented out as part of the first step, however updating Uncomplicated
                         Firewall (UFW) may update this change. In this case the updated entry will
                         supersede the entry being created as part of this step.

                     3. Run the following command to load all sysctl configuration files:

                # sysctl --system

                Default Value:
                net.ipv4.conf.default.log_martians = 0
                References:

                     1. CCI-000366
                     2. NIST SP 800-53 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. STIG ID: RHEL-09-253030 | RULE ID: SV-257961r991589 | CAT II
                     5. STIG ID: ALMA-09-019380 | RULE ID: SV-269251r1050133 | CAT II
                     6. STIG ID: OL09-00-006023 | RULE ID: SV-271867r1092313 | CAT II

                CIS Controls:

                  Controls
                                                                   Control                                            IG 1 IG 2 IG 3
                  Version
                               8.5 Collect Detailed Audit Logs
                       v8         Configure detailed audit logging for enterprise assets containing sensitive data.
                               Include event source, date, username, timestamp, source addresses, destination
                                                                                                                            ●    ●
                               addresses, and other useful elements that could assist in a forensic investigation.
                               6.2 Activate audit logging
                       v7         Ensure that local logging has been enabled on all systems and networking             ●    ●    ●
                               devices.
                               6.3 Enable Detailed Logging
                       v7         Enable system logging to include detailed information such as an event source,
                               date, user, timestamp, source addresses, destination addresses, and other useful
                                                                                                                            ●    ●
                               elements.
  '3.3.1.18': !unsafe |-
    1. Review all files being used by systemd-sysctl and comment out or remove all
                      net.ipv4.tcp_syncookies lines that are not net.ipv4.tcp_syncookies=1.
                Example script:

                #!/usr/bin/env bash

                {
                     l_option="net.ipv4.tcp_syncookies" l_value="1"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*(0|[2-9]|1[0-9]+)/s/^/# /' "$l_file"
                     done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .confand edit or
                        add the following line:

                net.ipv4.tcp_syncookies = 1
                Example:
                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "net.ipv4.tcp_syncookies = 1" >> /etc/sysctl.d/60-
                ipv4_sysctl.conf

                         Note: If the UFW file was the first file listed in the audit, the entry will be
                         commented out as part of the first step, however updating Uncomplicated
                         Firewall (UFW) may update this change. In this case the updated entry will
                         supersede the entry being created as part of this step.

                     3. Run the following command to load all sysctl configuration files:

                # sysctl --system

                Default Value:
                net.ipv4.tcp_syncookies = 1
                References:

                     1. CCI-001095 | CCI-002385
                     2. NIST SP 800-53 :: SC-5 (2) | NIST SP 800-53 :: SC-5 a
                     3. NIST SP 800-53A :: SC-5 (2).1
                     4. STIG ID: RHEL-09-253010 | RULE ID: SV-257957r1045009 | CAT II

                     5. STIG ID: ALMA-09-042480 | RULE ID: SV-269435r1050318 | CAT II
                     6. STIG ID: OL09-00-006050 | RULE ID: SV-271884r1092364 | CAT II
                     7. STIG ID: UBTU-22-253010 | RULE ID: SV-260522r1069097 | CAT II
                     8. STIG ID: UBTU-24-600190 | RULE ID: SV-270753r1066748 | CAT II
                     9. STIG ID: SLES-15-010310 | RULE ID: SV-234829r958528 | CAT II

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.

                3.3.2 Configure IPv6 parameters

                net.ipv6 kernel parameters control various aspects of the IPv6 network stack. These
                parameters effect functions to include routing, TCP, and security.
                Note: If IPv6 has been disabled on the system, the Recommendations in this
                subsection are not applicable and should be skipped.
                The following script creates and runs a function called f_ipv6_chk that may be used to
                determine if IPv6 has been disabled on the system:
                #!/usr/bin/env bash

                f_ipv6_chk()
                {
                   a_output=()
                   ! grep -Pqs -- '^\h*0\b' /sys/module/ipv6/parameters/disable && \
                   a_output+=(" - IPv6 is disabled")
                   if sysctl net.ipv6.conf.all.disable_ipv6 | \
                   grep -Pqs -- "^\h*net\.ipv6\.conf\.all\.disable_ipv6\h*=\h*1\b" && \
                   sysctl net.ipv6.conf.default.disable_ipv6 | \
                   grep -Pqs -- "^\h*net\.ipv6\.conf\.default\.disable_ipv6\h*=\h*1\b"; then
                      [ "${#a_output[@]}" -le 0 ] && a_output+=(" - IPv6 is disabled")
                   fi
                   [ "${#a_output[@]}" -le 0 ] && a_output+=(" - IPv6 is not disabled")
                   printf '%s\n' "" "${a_output[@]}" ""
                }
                f_ipv6_chk
  '3.3.2.1': !unsafe |-
    1. Review all files being used by systemd-sysctl and comment out or remove all
                        net.ipv6.conf.all.forwarding lines that are not
                        net.ipv6.conf.all.forwarding=0.

                Example script:
                #!/usr/bin/env bash

                {
                     l_option="net.ipv6.conf.all.forwarding" l_value="0"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*(1[0-9]*)/s/^/# /' "$l_file"
                     done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .confand edit or
                        add the following line:

                net.ipv6.conf.all.forwarding = 0
                Example:
                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "net.ipv6.conf.all.forwarding = 0" >> /etc/sysctl.d/60-
                ipv6_sysctl.conf

                         Note: If the UFW file was the first file listed in the audit, the entry will be
                         commented out as part of the first step, however updating Uncomplicated
                         Firewall (UFW) may update this change. In this case the updated entry will
                         supersede the entry being created as part of this step.

                     3. Run the following command to load all sysctl configuration files:

                # sysctl --system

                Default Value:
                net.ipv6.conf.all.forwarding = 0
                References:

                     1. CI-000366
                     2. NIST SP 800-53 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. STIG ID: RHEL-08-040260 | RULE ID: SV-230540r1017302 | CAT II
                     5. STIG ID: OL08-00-040260 | RULE ID: SV-248883r991589 | CAT II
                     6. STIG ID: RHEL-09-254025 | RULE ID: SV-257974r991589 | CAT II
                     7. STIG ID: ALMA-09-019160 | RULE ID: SV-269249r1050131 | CAT II
                     8. STIG ID: OL09-00-006043 | RULE ID: SV-271880r1092352 | CAT II
                     9. STIG ID: SLES-15-040381 | RULE ID: SV-235025r991589 | CAT II

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '3.3.2.2': !unsafe |-
    1. Review all files being used by systemd-sysctl and comment out or remove all
                      net.ipv6.conf.default.forwarding lines that are not
                      net.ipv6.conf.default.forwarding=0.
                Example script:
                #!/usr/bin/env bash

                {
                     l_option="net.ipv6.conf.default.forwarding" l_value="0"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*(1[0-9]*)/s/^/# /' "$l_file"
                     done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .confand edit or
                        add the following line:

                net.ipv6.conf.default.forwarding = 0
                Example:
                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "net.ipv6.conf.default.forwarding = 0" >>
                /etc/sysctl.d/60-ipv6_sysctl.conf

                         Note: If the UFW file was the first file listed in the audit, the entry will be
                         commented out as part of the first step, however updating Uncomplicated
                         Firewall (UFW) may update this change. In this case the updated entry will
                         supersede the entry being created as part of this step.

                     3. Run the following command to load all sysctl configuration files:

                # sysctl --system

                Default Value:
                net.ipv6.conf.default.forwarding = 0

                References:

                     1. CI-000366
                     2. NIST SP 800-53 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. STIG ID: ALMA-09-019160 | RULE ID: SV-269249r1050131 | CAT II
                     5. STIG ID: SLES-15-040382 | RULE ID: SV-235026r991589 | CAT II

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '3.3.2.3': !unsafe |-
    1. Review all files being used by systemd-sysctl and comment out or remove all
                        net.ipv6.conf.all.accept_redirects lines that are not
                        net.ipv6.conf.all.accept_redirects=0.

                Example script:
                #!/usr/bin/env bash

                {
                     l_option="net.ipv6.conf.all.accept_redirects" l_value="0"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*(1[0-9]*)/s/^/# /' "$l_file"
                     done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .confand edit or
                        add the following line:

                net.ipv6.conf.all.accept_redirects = 0
                Example:
                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "net.ipv6.conf.all.accept_redirects = 0" \
                >> /etc/sysctl.d/60-ipv6_sysctl.conf

                         Note: If the UFW file was the first file listed in the audit, the entry will be
                         commented out as part of the first step, however updating Uncomplicated
                         Firewall (UFW) may update this change. In this case the updated entry will
                         supersede the entry being created as part of this step.

                     3. Run the following command to load all sysctl configuration files:

                # sysctl --system

                Default Value:
                net.ipv6.conf.all.accept_redirects = 1
                References:

                     1. CCI-000366
                     2. NIST SP 800-53 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. STIG ID: RHEL-08-040280 | RULE ID: SV-230544r1017306 | CAT II
                     5. STIG ID: OL08-00-040280 | RULE ID: SV-248888r991589 | CAT II
                     6. STIG ID: RHEL-09-254015 | RULE ID: SV-257972r991589 | CAT II
                     7. STIG ID: ALMA-09-018610 | RULE ID: SV-269244r1050126 | CAT II
                     8. STIG ID: OL09-00-006041 | RULE ID: SV-271878r1092346 | CAT II
                     9. STIG ID: SLES-15-040341 | RULE ID: SV-235020r991589 | CAT II

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.

                3.3.2.4 Ensure net.ipv6.conf.default.accept_redirects is
                configured (Automated)
                Profile Applicability:

                • Level 1 - Server
                • Level 1 - Workstation

                Description:
                ICMP redirect messages are packets that convey routing information and tell your host
                (acting as a router) to send packets via an alternate path. It is a way of allowing an
                outside routing device to update your system routing tables.
                net.ipv6.conf.default.accept_redirects controls accepting of all IPv6 ICMP
                redirected packets on a newly added network interface.
                More information about the kernel parameter configuration files, their location, and load
                preference is available in the "Configure Network Kernel Parameters" section overview.
                         Note: If IPv6 has been disabled, or this system is a router, this
                         recommendation is not applicable.
                Rationale:
                ICMP redirect messages are packets that convey routing information and tell your host
                (acting as a router) to send packets via an alternate path. It is a way of allowing an
                outside routing device to update your system routing tables.
                By setting net.ipv6.conf.default.accept_redirects to 0, the system will not
                accept IPv6 ICMP redirect messages on a newly added network interface.
                Audit:
                         - IF IPv6 is enabled on the system -
                Verify net.ipv6.conf.default.accept_redirects is set to 0.

                     1. Run the following command to verify
                        net.ipv6.conf.default.accept_redirects is set to 0 in the running
                        configuration:

                # sysctl net.ipv6.conf.default.accept_redirects
                Verify output is:
                net.ipv6.conf.default.accept_redirects = 0

                     2. Run the following script to verify net.ipv6.conf.default.accept_redirects
                        = 0 is set in a file being used by systemd-sysctl to configure
                        net.ipv6.conf.default.accept_redirects:

                #!/usr/bin/env bash

                {
                      l_parameter_name="net.ipv6.conf.default.accept_redirects"
                      l_grep="${l_parameter_name//./(\\.|\\/)}" a_output=() a_files=()
                      l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                      || readlink -e /usr/lib/systemd/systemd-sysctl)"
                      l_ufw_file="$([ -f /etc/default/ufw ] && \
                      awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                      [ -f "$(readlink -e "$l_ufw_file")" ] && \
                      a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                      while IFS= read -r l_fname; do
                         l_file="$(readlink -e "${l_fname//# /}")"
                         [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                         <<< "${a_files[*]}" && a_files+=("$l_file")
                      done < <("$l_systemdsysctl" --cat-config | tac | \
                      grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                      for l_file in "${a_files[@]}"; do
                         l_opt="$(grep -Psio '^\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" | tail -n
                1)"
                      l_option_value="$(cut -d= -f2 <<< "$l_opt" | xargs)"
                      [ -n "$l_option_value" ] && \
                      a_output+=(" - \"$l_parameter_name = $l_option_value\" is set in:
                \"$l_file\"")
                   done
                   [ "${#a_output[@]}" -gt "0" ] && printf '%s\n' "" "${a_output[@]}" ""
                }
                Verify output includes net.ipv6.conf.default.accept_redirects = 0.
                Example output:
                 - "net.ipv6.conf.default.accept_redirects = 0" is set in: "/etc/sysctl.d/60-
                ipv6_sysctl.conf"

                         Note: If multiple lines with net.ipv6.conf.default.accept_redirects
                         = are returned.
                            • The first line includes the value being used by systemd-sysctl. If
                                this is a correct value, this is considered a passing state. If the file
                                listed is not in the /etc/sysctl.d/ directory, it is highly
                                recommended to follow the remediation procedure to create a
                                .conf file in the /etc/sysctl.d/ directory with the correct setting
                                to prevent a potential change due to an update to the system.
                            • Any files in the /etc/sysctl.d/ directory that include an incorrect
                                value should be modified to comment out or change the incorrect
                                value to minimize the potential of the incorrect value being used by
                                systemd-sysctl due to system configuration changes.

                Remediation:
                   1. Review all files being used by systemd-sysctl and comment out or remove all
                      net.ipv6.conf.default.accept_redirects lines that are not
                      net.ipv6.conf.default.accept_redirects=0.
                Example script:
                #!/usr/bin/env bash

                {
                     l_option="net.ipv6.conf.default.accept_redirects" l_value="0"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*(1[0-9]*)/s/^/# /' "$l_file"
                     done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .confand edit or
                        add the following line:

                net.ipv6.conf.default.accept_redirects = 0
                Example:
                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "net.ipv6.conf.default.accept_redirects = 0" \
                >> /etc/sysctl.d/60-ipv6_sysctl.conf

                         Note: If the UFW file was the first file listed in the audit, the entry will be
                         commented out as part of the first step, however updating Uncomplicated
                         Firewall (UFW) may update this change. In this case the updated entry will
                         supersede the entry being created as part of this step.

                     3. Run the following command to load all sysctl configuration files:

                # sysctl --system

                Default Value:
                net.ipv6.conf.default.accept_redirects = 1

                References:

                     1. CCI-000366
                     2. NIST SP 800-53 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. STIG ID: RHEL-08-040210 | RULE ID: SV-230535r1017297 | CAT II
                     5. STIG ID: OL08-00-040210 | RULE ID: SV-248876r991589 | CAT II
                     6. STIG ID: RHEL-09-254035 | RULE ID: SV-257976r991589 | CAT II
                     7. STIG ID: ALMA-09-018610 | RULE ID: SV-269244r1050126 | CAT II
                     8. STIG ID: OL09-00-006045 | RULE ID: SV-271882r1092358 | CAT II
                     9. STIG ID: SLES-15-040350 | RULE ID: SV-235021r991589 | CAT II

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.

                3.3.2.5 Ensure net.ipv6.conf.all.accept_source_route is
                configured (Automated)
                Profile Applicability:

                • Level 1 - Server
                • Level 1 - Workstation

                Description:
                The accept_source_route option causes network interfaces to accept packets with
                the Strict Source Route (SSR) or Loose Source Routing (LSR) option set.
                net.ipv6.conf.all.accept_source_route controls accepting of all IPv6 packets
                with the SSR or LSR option set on all interfaces.
                In networking, source routing allows a sender to partially or fully specify the route
                packets take through a network. In contrast, non-source routed packets travel a path
                determined by routers in the network. In some cases, systems may not be routable or
                reachable from some locations (e.g. private addresses vs. Internet routable), and so
                source routed packets would need to be used.
                More information about the kernel parameter configuration files, their location, and load
                preference is available in the "Configure Network Kernel Parameters" section overview.
                         Note: If IPv6 has been disabled this recommendation is not
                         applicable.
                Rationale:
                Setting net.ipv6.conf.all.accept_source_route to 0 disables the system from
                accepting source routed packets. Assume this system was capable of routing packets to
                Internet routable addresses on one interface and private addresses on another
                interface. Assume that the private addresses were not routable to the Internet routable
                addresses and vice versa. Under normal routing circumstances, an attacker from the
                Internet routable addresses could not use the system as a way to reach the private
                address systems. If, however, source routed packets were allowed, they could be used
                to gain access to the private address systems as the route could be specified, rather
                than rely on routing protocols that did not allow this routing.

                Audit:
                       - IF IPv6 is enabled on the system -
                Verify net.ipv6.conf.all.accept_source_route is set to 0.
                   1. Run the following command to verify
                       net.ipv6.conf.all.accept_source_route is set to 0 in the running
                       configuration:
                # sysctl net.ipv6.conf.all.accept_source_route
                Verify output is:
                net.ipv6.conf.all.accept_source_route = 0

                      2. Run the following script to verify net.ipv6.conf.all.accept_source_route =
                         0 is set in a file being used by systemd-sysctl to configure
                         net.ipv6.conf.all.accept_source_route:

                #!/usr/bin/env bash

                {
                      l_parameter_name="net.ipv6.conf.all.accept_source_route"
                      l_grep="${l_parameter_name//./(\\.|\\/)}" a_output=() a_files=()
                      l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                      || readlink -e /usr/lib/systemd/systemd-sysctl)"
                      l_ufw_file="$([ -f /etc/default/ufw ] && \
                      awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                      [ -f "$(readlink -e "$l_ufw_file")" ] && \
                      a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                      while IFS= read -r l_fname; do
                         l_file="$(readlink -e "${l_fname//# /}")"
                         [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                         <<< "${a_files[*]}" && a_files+=("$l_file")
                      done < <("$l_systemdsysctl" --cat-config | tac | \
                      grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                      for l_file in "${a_files[@]}"; do
                         l_opt="$(grep -Psio '^\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" | tail -n
                1)"
                      l_option_value="$(cut -d= -f2 <<< "$l_opt" | xargs)"
                      [ -n "$l_option_value" ] && \
                      a_output+=(" - \"$l_parameter_name = $l_option_value\" is set in:
                \"$l_file\"")
                   done
                   [ "${#a_output[@]}" -gt "0" ] && printf '%s\n' "" "${a_output[@]}" ""
                }
                Verify output includes net.ipv4.conf.default.forwarding = 0.
                Example output:
                 - "net.ipv6.conf.all.accept_source_route = 0" is set in: "/etc/sysctl.d/60-
                ipv6_sysctl.conf"

                         Note: If multiple lines with net.ipv6.conf.all.accept_source_route
                         = are returned.

                              •   The first line includes the value being used by systemd-sysctl. If
                                  this is a correct value, this is considered a passing state. If the file
                                  listed is not in the /etc/sysctl.d/ directory, it is highly
                                  recommended to follow the remediation procedure to create a
                                  .conf file in the /etc/sysctl.d/ directory with the correct setting
                                  to prevent a potential change due to an update to the system.
                              •   Any files in the /etc/sysctl.d/ directory that include an incorrect
                                  value should be modified to comment out or change the incorrect
                                  value to minimize the potential of the incorrect value being used by
                                  systemd-sysctl due to system configuration changes.

                Remediation:
                   1. Review all files being used by systemd-sysctl and comment out or remove all
                      net.ipv6.conf.all.accept_source_route lines that are not
                      net.ipv6.conf.all.accept_source_route=0.
                Example script:
                #!/usr/bin/env bash

                {
                     l_option="net.ipv6.conf.all.accept_source_route" l_value="0"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*(1[0-9]*)/s/^/# /' "$l_file"
                     done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .confand edit or
                        add the following line:

                net.ipv6.conf.all.accept_source_route = 0
                Example:

                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "net.ipv6.conf.all.accept_source_route = 0" \
                >> /etc/sysctl.d/60-ipv6_sysctl.conf

                         Note: If the UFW file was the first file listed in the audit, the entry will be
                         commented out as part of the first step, however updating Uncomplicated
                         Firewall (UFW) may update this change. In this case the updated entry will
                         supersede the entry being created as part of this step.

                     3. Run the following command to load all sysctl configuration files:

                # sysctl --system

                Default Value:
                net.ipv6.conf.all.accept_source_route = 0
                References:

                     1. CCI-000366
                     2. NIST SP 800-53 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. STIG ID: RHEL-08-040240 | RULE ID: SV-230538r1017300 | CAT II
                     5. STIG ID: OL08-00-040240 | RULE ID: SV-248880r991589 | CAT II
                     6. STIG ID: RHEL-09-254020 | RULE ID: SV-257973r991589 | CAT II
                     7. STIG ID: ALMA-09-020260 | RULE ID: SV-269259r1050141 | CAT II
                     8. STIG ID: OL09-00-006042 | RULE ID: SV-271879r1092349 | CAT II
                     9. STIG ID: SLES-15-040310 | RULE ID: SV-235015r991589 | CAT II

                CIS Controls:

                   Controls
                                                                  Control                                          IG 1 IG 2 IG 3
                   Version
                                4.8 Uninstall or Disable Unnecessary Services on
                                Enterprise Assets and Software
                        v8         Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                                such as an unused file sharing service, web application module, or service
                                function.
                                9.2 Ensure Only Approved Ports, Protocols and Services
                        v7      Are Running                                                                              ●    ●
                                   Ensure that only network ports, protocols, and services listening on a system
                                with validated business needs, are running on each system.

                3.3.2.6 Ensure net.ipv6.conf.default.accept_source_route is
                configured (Automated)
                Profile Applicability:

                • Level 1 - Server
                • Level 1 - Workstation

                Description:
                The accept_source_route option causes network interfaces to accept packets with
                the Strict Source Route (SSR) or Loose Source Routing (LSR) option set.
                net.ipv6.conf.default.accept_source_route controls accepting of all IPv6
                packets with the SSR or LSR option set on newly added network interfaces.
                In networking, source routing allows a sender to partially or fully specify the route
                packets take through a network. In contrast, non-source routed packets travel a path
                determined by routers in the network. In some cases, systems may not be routable or
                reachable from some locations (e.g. private addresses vs. Internet routable), and so
                source routed packets would need to be used.
                More information about the kernel parameter configuration files, their location, and load
                preference is available in the "Configure Network Kernel Parameters" section overview.
                         Note: If IPv6 has been disabled this recommendation is not
                         applicable.
                Rationale:
                Setting net.ipv6.conf.default.accept_source_route to 0 disables the system
                from accepting source routed packets. Assume this system was capable of routing
                packets to Internet routable addresses on one interface and private addresses on
                another interface. Assume that the private addresses were not routable to the Internet
                routable addresses and vice versa. Under normal routing circumstances, an attacker
                from the Internet routable addresses could not use the system as a way to reach the
                private address systems. If, however, source routed packets were allowed, they could
                be used to gain access to the private address systems as the route could be specified,
                rather than rely on routing protocols that did not allow this routing.

                Audit:
                         - IF IPv6 is enabled on the system -
                Verify net.ipv6.conf.default.accept_source_route is set to 0.

                      1. Run the following command to verify
                         net.ipv6.conf.default.accept_source_route is set to 0 in the running
                         configuration:

                # sysctl net.ipv6.conf.default.accept_source_route
                Verify output is:
                net.ipv6.conf.default.accept_source_route = 0

                      2. Run the following script to verify
                         net.ipv6.conf.default.accept_source_route = 0 is set in a file being used
                         by systemd-sysctl to configure
                         net.ipv6.conf.default.accept_source_route:

                #!/usr/bin/env bash

                {
                      l_parameter_name="net.ipv6.conf.default.accept_source_route"
                      l_grep="${l_parameter_name//./(\\.|\\/)}" a_output=() a_files=()
                      l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                      || readlink -e /usr/lib/systemd/systemd-sysctl)"
                      l_ufw_file="$([ -f /etc/default/ufw ] && \
                      awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                      [ -f "$(readlink -e "$l_ufw_file")" ] && \
                      a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                      while IFS= read -r l_fname; do
                         l_file="$(readlink -e "${l_fname//# /}")"
                         [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                         <<< "${a_files[*]}" && a_files+=("$l_file")
                      done < <("$l_systemdsysctl" --cat-config | tac | \
                      grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                      for l_file in "${a_files[@]}"; do
                         l_opt="$(grep -Psio '^\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" | tail -n
                1)"
                      l_option_value="$(cut -d= -f2 <<< "$l_opt" | xargs)"
                      [ -n "$l_option_value" ] && \
                      a_output+=(" - \"$l_parameter_name = $l_option_value\" is set in:
                \"$l_file\"")
                   done
                   [ "${#a_output[@]}" -gt "0" ] && printf '%s\n' "" "${a_output[@]}" ""
                }
                Verify output includes net.ipv6.conf.default.accept_source_route = 0.
                Example output:

                 - "net.ipv6.conf.default.accept_source_route = 0" is set in:
                "/etc/sysctl.d/60-ipv6_sysctl.conf"

                         Note: If multiple lines with
                         net.ipv6.conf.default.accept_source_route = are returned.

                              •   The first line includes the value being used by systemd-sysctl. If
                                  this is a correct value, this is considered a passing state. If the file
                                  listed is not in the /etc/sysctl.d/ directory, it is highly
                                  recommended to follow the remediation procedure to create a
                                  .conf file in the /etc/sysctl.d/ directory with the correct setting
                                  to prevent a potential change due to an update to the system.
                              •   Any files in the /etc/sysctl.d/ directory that include an incorrect
                                  value should be modified to comment out or change the incorrect
                                  value to minimize the potential of the incorrect value being used by
                                  systemd-sysctl due to system configuration changes.

                Remediation:

                     1. Review all files being used by systemd-sysctl and comment out or remove all
                        net.ipv6.conf.default.accept_source_route lines that are not
                        net.ipv6.conf.default.accept_source_route=0.

                Example script:
                #!/usr/bin/env bash

                {
                     l_option="net.ipv6.conf.default.accept_source_route" l_value="0"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*(1[0-9]*)/s/^/# /' "$l_file"
                     done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .confand edit or
                        add the following line:

                net.ipv6.conf.default.accept_source_route = 0
                Example:
                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "net.ipv6.conf.default.accept_source_route = 0" \
                >> /etc/sysctl.d/60-ipv6_sysctl.conf

                         Note: If the UFW file was the first file listed in the audit, the entry will be
                         commented out as part of the first step, however updating Uncomplicated
                         Firewall (UFW) may update this change. In this case the updated entry will
                         supersede the entry being created as part of this step.

                     3. Run the following command to load all sysctl configuration files:

                # sysctl --system

                Default Value:
                net.ipv6.conf.default.accept_source_route = 0
                References:

                     1. CCI-000366
                     2. NIST SP 800-53 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. STIG ID: RHEL-08-040250 | RULE ID: SV-230539r1017301 | CAT II
                     5. STIG ID: OL08-00-040250 | RULE ID: SV-248882r991589 | CAT II
                     6. STIG ID: RHEL-09-254040 | RULE ID: SV-257977r991589 | CAT II
                     7. STIG ID: ALMA-09-020260 | RULE ID: SV-269259r1050141 | CAT II
                     8. STIG ID: OL09-00-006046 | RULE ID: SV-271883r1092361 | CAT II
                     9. STIG ID: SLES-15-040321 | RULE ID: SV-235017r991589 | CAT II

                CIS Controls:

                   Controls
                                                                  Control                                          IG 1 IG 2 IG 3
                   Version
                                4.8 Uninstall or Disable Unnecessary Services on
                                Enterprise Assets and Software
                        v8         Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                                such as an unused file sharing service, web application module, or service
                                function.
                                9.2 Ensure Only Approved Ports, Protocols and Services
                        v7      Are Running                                                                              ●    ●
                                   Ensure that only network ports, protocols, and services listening on a system
                                with validated business needs, are running on each system.
  '3.3.2.4': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '3.3.2.5': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '3.3.2.6': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '3.3.2.7': !unsafe |-
    1. Review all files being used by systemd-sysctl and comment out or remove all
                        net.ipv6.conf.all.accept_ra lines that are not
                        net.ipv6.conf.all.accept_ra=0.

                Example script:
                #!/usr/bin/env bash

                {
                     l_option="net.ipv6.conf.all.accept_ra" l_value="0"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*(1[0-9]*)/s/^/# /' "$l_file"
                     done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .confand edit or
                        add the following line:

                net.ipv6.conf.all.accept_ra = 0
                Example:
                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "net.ipv6.conf.all.accept_ra = 0" >> /etc/sysctl.d/60-
                ipv6_sysctl.conf

                         Note: If the UFW file was the first file listed in the audit, the entry will be
                         commented out as part of the first step, however updating Uncomplicated
                         Firewall (UFW) may update this change. In this case the updated entry will
                         supersede the entry being created as part of this step.

                     3. Run the following command to load all sysctl configuration files:

                # sysctl --system

                Default Value:
                net.ipv6.conf.all.accept_ra = 1

                References:

                     1. CCI-000366
                     2. NIST SP 800-53 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. STIG ID: RHEL-08-040261 | RULE ID: SV-230541r1017303 | CAT II
                     5. STIG ID: OL08-00-040261 | RULE ID: SV-248884r991589 | CAT II
                     6. STIG ID: RHEL-09-254010 | RULE ID: SV-257971r991589 | CAT II
                     7. STIG ID: ALMA-09-018500 | RULE ID: SV-269243r1050125 | CAT II
                     8. STIG ID: OL09-00-006040 | RULE ID: SV-271877r1092343 | CAT II

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '3.3.2.8': !unsafe |-
    1. Review all files being used by systemd-sysctl and comment out or remove all
                        net.ipv6.conf.default.accept_ra lines that are not
                        net.ipv6.conf.default.accept_ra=0.

                Example script:
                #!/usr/bin/env bash

                {
                     l_option="net.ipv6.conf.default.accept_ra" l_value="0"
                     l_grep="${l_option//./(\\.|\\/)}" a_files=()
                     l_systemdsysctl="$(readlink -e /lib/systemd/systemd-sysctl \
                     || readlink -e /usr/lib/systemd/systemd-sysctl)"
                     l_ufw_file="$([ -f /etc/default/ufw ] && \
                     awk -F= '/^\s*IPT_SYSCTL=/ {print $2}' /etc/default/ufw)"
                     [ -f "$(readlink -e "$l_ufw_file")" ] && \
                     a_files+=("$l_ufw_file"); a_files+=("/etc/sysctl.conf")
                     while IFS= read -r l_fname; do
                        l_file="$(readlink -e "${l_fname//# /}")"
                        [ -n "$l_file" ] && ! grep -Psiq -- '(^|\h+)'"$l_file"'\b' \
                        <<< "${a_files[*]}" && a_files+=("$l_file")
                     done < <("$l_systemdsysctl" --cat-config | tac | \
                     grep -Psio -- '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                     for l_file in "${a_files[@]}"; do
                        grep -Psio -- '\h*'"$l_grep"'\h*=\h*\H+\b' "$l_file" \
                        | grep -Psivq -- '^\h*'"$l_grep"'\h*=\h*'"$l_value"'\b' && \
                        sed -ri '/^\s*'"$l_grep"'\s*=\s*(1[0-9]*)/s/^/# /' "$l_file"
                     done
                }

                     2. Create or edit a file in the /etc/sysctl.d/ directory ending in .confand edit or
                        add the following line:

                net.ipv6.conf.default.accept_ra = 0
                Example:
                # [ ! -d "/etc/sysctl.d/" ] && mkdir -p /etc/sysctl.d/
                # printf '%s\n' "" "net.ipv6.conf.default.accept_ra = 0" >> /etc/sysctl.d/60-
                ipv6_sysctl.conf

                         Note: If the UFW file was the first file listed in the audit, the entry will be
                         commented out as part of the first step, however updating Uncomplicated
                         Firewall (UFW) may update this change. In this case the updated entry will
                         supersede the entry being created as part of this step.

                     3. Run the following command to load all sysctl configuration files:

                # sysctl --system

                Default Value:
                net.ipv6.conf.default.accept_ra = 1

                References:

                     1. CCI-000366
                     2. NIST SP 800-53 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. STIG ID: RHEL-08-040262 | RULE ID: SV-230542r1017304 | CAT II
                     5. STIG ID: OL08-00-040262 | RULE ID: SV-248885r991589 | CAT II
                     6. STIG ID: RHEL-09-254030 | RULE ID: SV-257975r991589 | CAT II
                     7. STIG ID: ALMA-09-018500 | RULE ID: SV-269243r1050125 | CAT II
                     8. STIG ID: OL09-00-006044 | RULE ID: SV-271881r1092355 | CAT II

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.

                4 Host Based Firewall
                A firewall is a set of rules. When a data packet moves into or out of a protected network
                space, its contents (in particular, information about its origin, target, and the protocol it
                plans to use) are tested against the firewall rules to see if it should be allowed through.
                To provide a Host Based Firewall, the Linux kernel includes support for NFTables.
                NFTables is subsystem of the Linux kernel providing filtering and classification of
                network packets/datagrams/frames. NFTables replaces parts of Netfilter, while keeping
                and reusing most of it. NFTables utilizes the building blocks of the Netfilter
                infrastructure, such as the existing hooks into the networking stack, connection tracking
                system, userspace queueing component, and logging subsystem.
                In order to configure firewall rules for NFTables, a firewall utility needs to be installed.
                Guidance has been included for Uncomplicated Firewall (UFW) which provides firewall
                features by acting as a front-end for the Linux kernel's netfilter framework via the
                NFTables backend, acting as an alternative to the nft command line program. UFW
                supports both IPv4 and IPv6 networks.
                Important: This Benchmark includes guidance for configuring NFTables though the UFW
                front end utility. If your organization is using a different Host Based Firewall, or a
                different utility to configure the Host Based Firewall, we recommend verifying that
                your site's configuration meets the Recommendations in this section, otherwise this
                section may be skipped.
                Note:

                     •   Only one method should be used to configure a firewall on the system. Use of
                         more than one method could produce unexpected results.
                     •   NFTables is the default packet filtering framework. Though IPTables is still
                         supported, this support is done indirectly through UFW's ability to translate
                         IPTables commands to NFTables.
                     •   This section is intended only to ensure the resulting firewall rules are in place.

                4.1 Configure Uncomplicated Firewall

                In order to configure durable firewall rules a firewall utility needs to be installed.
                Uncomplicated Firewall (UFW) is the recommended utility and is used in guidance
                provided in the following recommendations.
                UFW is a user-friendly frontend firewall utility. It uses configurable default policies, and
                allows administrators to create specific rules to permit or deny connections based on
                applications, ports, or IP addresses. You can enable the firewall, view its status, and
                manage rules using simple command-line tools to secure your system.
                Warning: Configuration of a live systems firewall directly over a remote connection will
                often result in being locked out. It is advised to have a known good firewall configuration
                set to run on boot and to configure an entire firewall structure in a script that is then run
                and tested before saving to boot.
  '4.1.1': !unsafe |-
    Run the following command to install Uncomplicated Firewall (UFW):
                # apt install ufw

                References:

                     1. CCI-002314
                     2. NIST SP 800-53 :: AC-17 (1)
                     3. STIG ID: UBTU-22-251010 | Rule ID: SV-260514r958672 | CAT II
                     4. STIG ID: UBTU-24-100300 | Rule ID: SV-270654r1067143 | CAT II

                CIS Controls:

                   Controls
                                                                   Control                                         IG 1 IG 2 IG 3
                   Version
                              4.4 Implement and Manage a Firewall on Servers
                       v8        Implement and manage a firewall on servers, where supported. Example
                              implementations include a virtual firewall, operating system firewall, or a third-
                                                                                                                    ●    ●    ●
                              party firewall agent.
                              4.5 Implement and Manage a Firewall on End-User
                              Devices
                       v8        Implement and manage a host-based firewall or port-filtering tool on end-user      ●    ●    ●
                              devices, with a default-deny rule that drops all traffic except those services and
                              ports that are explicitly allowed.
                              9.4 Apply Host-based Firewalls or Port Filtering
                       v7        Apply host-based firewalls or port filtering tools on end systems, with a
                              default-deny rule that drops all traffic except those services and ports that are
                                                                                                                    ●    ●    ●
                              explicitly allowed.
  '4.1.2': !unsafe |-
    Run the following command to unmask the ufw.service:
                # systemctl unmask ufw.service
                Run the following command to enable and start the ufw.service:
                # systemctl --now enable ufw.service

                active
                Run the following command to enable ufw:
                # ufw enable

                Default Value:
                Inactive
                References:

                     1. CCI-002314
                     2. NIST SP 800-53 :: AC-17 (1)
                     3. STIG ID: UBTU-20-010434 | Rule ID: SV-238355r958672 | CAT II
                     4. STIG ID: UBTU-22-251015 | Rule ID: SV-260515r958672 | CAT II
                     5. STIG ID: UBTU-24-100310 | Rule ID: SV-270655r1067145 | CAT II
                     6. http://manpages.ubuntu.com/manpages/precise/en/man8/ufw.8.html

                CIS Controls:

                   Controls
                                                                   Control                                         IG 1 IG 2 IG 3
                   Version
                              4.4 Implement and Manage a Firewall on Servers
                       v8        Implement and manage a firewall on servers, where supported. Example
                              implementations include a virtual firewall, operating system firewall, or a third-
                                                                                                                    ●    ●    ●
                              party firewall agent.
                              4.5 Implement and Manage a Firewall on End-User
                              Devices
                       v8        Implement and manage a host-based firewall or port-filtering tool on end-user      ●    ●    ●
                              devices, with a default-deny rule that drops all traffic except those services and
                              ports that are explicitly allowed.
                              9.4 Apply Host-based Firewalls or Port Filtering
                       v7        Apply host-based firewalls or port filtering tools on end systems, with a
                              default-deny rule that drops all traffic except those services and ports that are
                                                                                                                    ●    ●    ●
                              explicitly allowed.
  '4.1.3': !unsafe |-
    Run the following command to set the default for incoming to deny:
                # ufw default deny incoming
                Warning: Any port or protocol without a explicit allow before the default deny will be
                blocked.
                References:

                     1. NIST SP 800-53 Rev. 5: SC-7

                CIS Controls:

                   Controls
                                                                   Control                                         IG 1 IG 2 IG 3
                   Version
                              4.4 Implement and Manage a Firewall on Servers
                       v8        Implement and manage a firewall on servers, where supported. Example
                              implementations include a virtual firewall, operating system firewall, or a third-
                                                                                                                    ●    ●    ●
                              party firewall agent.
                              4.5 Implement and Manage a Firewall on End-User
                              Devices
                       v8        Implement and manage a host-based firewall or port-filtering tool on end-user      ●    ●    ●
                              devices, with a default-deny rule that drops all traffic except those services and
                              ports that are explicitly allowed.
                              9.4 Apply Host-based Firewalls or Port Filtering
                       v7        Apply host-based firewalls or port filtering tools on end systems, with a
                              default-deny rule that drops all traffic except those services and ports that are
                                                                                                                    ●    ●    ●
                              explicitly allowed.
  '4.1.4': !unsafe |-
    Run the following command to set the default for outgoing to deny:
                # ufw default deny outgoing
                Warning: Any port or protocol without a explicit allow before the default deny will be
                blocked.
                Default Value:
                allow (outgoing)

                References:

                     1. CCI-000382
                     2. NIST SP 800-53 :: CM-7
                     3. NIST SP 800-53A :: CM-7.1 (iii)

                CIS Controls:

                   Controls
                                                                    Control                                         IG 1 IG 2 IG 3
                   Version
                               4.4 Implement and Manage a Firewall on Servers
                       v8         Implement and manage a firewall on servers, where supported. Example
                               implementations include a virtual firewall, operating system firewall, or a third-
                                                                                                                     ●    ●    ●
                               party firewall agent.
                               4.5 Implement and Manage a Firewall on End-User
                               Devices
                       v8         Implement and manage a host-based firewall or port-filtering tool on end-user      ●    ●    ●
                               devices, with a default-deny rule that drops all traffic except those services and
                               ports that are explicitly allowed.
                               9.4 Apply Host-based Firewalls or Port Filtering
                       v7         Apply host-based firewalls or port filtering tools on end systems, with a
                               default-deny rule that drops all traffic except those services and ports that are
                                                                                                                     ●    ●    ●
                               explicitly allowed.
  '4.1.5': !unsafe |-
    Run the following command to set the defalut for routed to disabled:
                # ufw default disabled routed

                Default Value:
                disabled
                References:

                     1. CCI-000382
                     2. NIST SP 800-53 :: CM-7
                     3. NIST SP 800-53A :: CM-7.1 (iii)

                CIS Controls:

                   Controls
                                                                   Control                                         IG 1 IG 2 IG 3
                   Version
                              4.4 Implement and Manage a Firewall on Servers
                       v8        Implement and manage a firewall on servers, where supported. Example
                              implementations include a virtual firewall, operating system firewall, or a third-
                                                                                                                    ●    ●    ●
                              party firewall agent.
                              4.5 Implement and Manage a Firewall on End-User
                              Devices
                       v8        Implement and manage a host-based firewall or port-filtering tool on end-user      ●    ●    ●
                              devices, with a default-deny rule that drops all traffic except those services and
                              ports that are explicitly allowed.
                              9.4 Apply Host-based Firewalls or Port Filtering
                       v7        Apply host-based firewalls or port filtering tools on end systems, with a
                              default-deny rule that drops all traffic except those services and ports that are
                                                                                                                    ●    ●    ●
                              explicitly allowed.

                5 Access Control

                5.1 Configure SSH Server

                         Note: The recommendations in this section only apply if SSH server is
                         installed on the system. If remote access is not required the SSH server
                         can be removed and this section skipped.
                Secure Shell (SSH) is a secure, encrypted replacement for common login services such
                as telnet, ftp, rlogin, rsh, and rcp. It is strongly recommended that sites abandon
                older clear-text login protocols and use SSH to prevent session hijacking and sniffing of
                sensitive data off the network.
                sshd_config:

                     •   First occurrence of an option takes precedence.
                     •   The default Include location is the /etc/ssh/sshd_config.d directory.
                     •   The audits of the running configuration in this section are run in the context of the
                         root user, the local host name, and the local host's IP address. If a Match block
                         exists that matches one of these criteria, the output of the audit will be from the
                         match block. The respective matched criteria should be replaced with a non-
                         matching substitution.
                     •   Defaults:
                             o The file /etc/ssh/sshd_config includes the line: Include
                                 /etc/ssh/sshd_config.d/*.conf. This line must appear before any
                                 lines containing the Cipher argument
                             o This directory /etc/ssh/sshd_config.d/ includes a file
                                 /etc/ssh/sshd_config.d/50-redhat.conf
                             o The file /etc/ssh/sshd_config.d/50-redhat.conf includes the line
                                 Include /etc/crypto-policies/back-ends/opensshserver.config
                     •   Ciphers, MACs, and KexAlgorithms:
                             o Though these options may be configured through the Ciphers, MACs, and
                                 KexAlgorithms options in the /etc/ssh/sshd_config file, it is highly
                                 recommended that they are configured through system-wide-crypto-policy.
                             o If the recommendations in the subsection "Configure system wide crypto
                                 policy" have been followed, the audits should be in a passing state. Please
                                 review that section before following the Remediation Procedure for these
                                 Recommendations.
                             o By default, system-wide-crypto-policy is applied to the openSSH server. If
                                 the following defaults don't exist due to modifications or upgrade from a
                                 earlier release, the system-wide-crypto-policy may not be included by the
                                 openSSH server. It is recommended that these defaults be restored,
                                 created, or the line Include /etc/crypto-policies/back-
                                 ends/opensshserver.config be added before any lines containing the
                                 Cipher, MACs, or KexAlgorithms arguments.
                     •   Include:
                             o Include the specified configuration file(s).
                             o Multiple pathnames may be specified and each pathname may contain
                                 glob(7) wildcards that will be expanded and processed in lexical order.

                              oFiles without absolute paths are assumed to be in /etc/ssh/.
                              oAn Include directive may appear inside a Match block to perform
                               conditional inclusion.
                     •   Match:
                            o Introduces a conditional block. If all of the criteria on the Match line are
                               satisfied, the keywords on the following lines override those set in the
                               global section of the config file, until either another Match line or the end of
                               the file. If a keyword appears in multiple Match blocks that are satisfied,
                               only the first instance of the keyword is applied.
                            o The arguments to Match are one or more criteria-pattern pairs or the
                               single token All which matches all criteria. The available criteria are User,
                               Group, Host, LocalAddress, LocalPort, and Address.
                            o The match patterns may consist of single entries or comma-separated lists
                               and may use the wildcard and negation operators described in the
                               PATTERNS section of ssh_config(5).
                            o The patterns in an Address criteria may additionally contain addresses to
                               match in CIDR address/masklen format, such as 192.0.2.0/24 or
                               2001:db8::/32. Note that the mask length provided must be consistent
                               with the address - it is an error to specify a mask length that is too long for
                               the address or one with bits set in this host portion of the address. For
                               example, 192.0.2.0/33 and 192.0.2.0/8, respectively.
                            o Only a subset of keywords may be used on the lines following a Match
                               keyword. Available keywords are available in the ssh_config man page.
                     •   Once changes have been made to the sshd configuration, the sshd.service
                         must be reloaded.

                Command to re-load the SSH daemon configuration:
                # systemctl reload-or-restart sshd.service
                sshd command:

                     •   -T - Extended test mode. Check the validity of the configuration file, output the
                         effective configuration to stdout and then exit. Optionally, Match rules may be
                         applied by specifying the connection parameters using one or more -C options.
                     •   -C - connection_spec. Specify the connection parameters to use for the -T
                         extended test mode. If provided, any Match directives in the configuration file that
                         would apply are applied before the configuration is written to standard output.
                         The connection parameters are supplied as keyword=value pairs and may be
                         supplied in any order, either with multiple -C options or as a comma-separated
                         list. The keywords are addr, user, host, laddr, lport, and rdomain and
                         correspond to source address, user, resolved source host name, local address,
                         local port number and routing domain respectively.
  '5.1.1': !unsafe |-
    Run the following script to set ownership and permissions on /etc/ssh/sshd_config
                and files ending in .conf in the /etc/ssh/sshd_config.d directory:
                #!/usr/bin/env bash

                {
                     chmod u-x,og-rwx /etc/ssh/sshd_config
                     chown root:root /etc/ssh/sshd_config
                     while IFS= read -r -d $'\0' l_file; do
                        if [ -e "$l_file" ]; then
                           chmod u-x,og-rwx "$l_file"
                           chown root:root "$l_file"
                        fi
                     done < <(find /etc/ssh/sshd_config.d -type f -print0 2>/dev/null)
                }

                         Note: If other locations are listed in an Include statement, *.conf files in
                         these locations access should also be modified.
                Default Value:
                Access: (0600/-rw-------) Uid: ( 0/ root) Gid: ( 0/ root)
                References:

                     1. NIST SP 800-53 Rev. 5: AC-3. MP-2

                CIS Controls:

                    Controls
                                                                    Control                                          IG 1 IG 2 IG 3
                    Version
                               3.3 Configure Data Access Control Lists
                       v8         Configure data access control lists based on a user’s need to know. Apply data
                               access control lists, also known as access permissions, to local and remote file
                                                                                                                      ●    ●    ●
                               systems, databases, and applications.
                               14.6 Protect Information through Access Control Lists
                                   Protect all information stored on systems with file system, network share,
                       v7      claims, application, or database specific access control lists. These controls will
                               enforce the principle that only authorized individuals should have access to the
                                                                                                                      ●    ●    ●
                               information based on their need to access the information as a part of their
                               responsibilities.
  '5.1.2': !unsafe |-
    Update the access to the private keys being used by the open SSH server.

                     •   mode to 0600.
                     •   owner to the user root.
                     •   group owner to the group root.

                Run the following script to update the access on the private keys used by the open SSH
                server:
                #!/usr/bin/env bash

                {
                   l_sshd_cmd="$(readlink -e /usr/sbin/sshd || readlink -e /sbin/sshd)"
                   l_keygen="$(readlink -e /usr/bin/ssh-keygen || readlink -e /bin/ssh-
                keygen)"
                      while IFS= read -r l_file; do
                      if "$l_keygen" -lf &>/dev/null "$l_file"; then
                         chown root:root "$l_file"
                         chmod 0600 "$l_file"
                   done < <("$l_sshd_cmd" -T | awk '$1=="hostkey" {print $2".pub"}'
                2>/dev/null)
                }

                Default Value:
                Mode: "0600" Owner: "root" Group: "root"
                References:

                     1. CCI-000366
                     2. NIST SP 800-53 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. STIG ID: RHEL-08-010490 | RULE ID: SV-230287r1017098 | CAT II
                     5. STIG ID: RHEL-09-255120 | RULE ID: SV-258000r1045063 | CAT II
                     6. STIG ID: ALMA-09-020920 | RULE ID: SV-269265r1050147 | CAT II
                     7. STIG ID: SLES-15-040250 | RULE ID: SV-235009r991589 | CAT II

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '5.1.3': !unsafe |-
    Update the access to the public keys being used by the open SSH server.

                     •    mode to 0644.
                     •    owner to the user root.
                     •    group owner to the group root.

                Run the following script to update the access on the public keys used by the open SSH
                server:
                #!/usr/bin/env bash

                {
                   l_sshd_cmd="$(readlink -e /usr/sbin/sshd || readlink -e /sbin/sshd)"
                   l_keygen="$(readlink -e /usr/bin/ssh-keygen || readlink -e /bin/ssh-
                keygen)"
                      while IFS= read -r l_file; do
                      if "$l_keygen" -lf &>/dev/null "$l_file"; then
                         chown root:root "$l_file"
                         chmod 0644 "$l_file"
                   done < <("$l_sshd_cmd" -T | awk '$1=="hostkey" {print $2".pub"}'
                2>/dev/null)
                }

                Default Value:
                Mode: "0644" Owner: "root" Group: "root"
                References:

                     1. NIST SP 800-53 Rev. 5: AC-3. MP-2

                CIS Controls:

                    Controls
                                                                    Control                                          IG 1 IG 2 IG 3
                    Version
                               3.3 Configure Data Access Control Lists
                         v8       Configure data access control lists based on a user’s need to know. Apply data
                               access control lists, also known as access permissions, to local and remote file
                                                                                                                      ●    ●    ●
                               systems, databases, and applications.
                               14.6 Protect Information through Access Control Lists
                                   Protect all information stored on systems with file system, network share,
                         v7    claims, application, or database specific access control lists. These controls will
                               enforce the principle that only authorized individuals should have access to the
                                                                                                                      ●    ●    ●
                               information based on their need to access the information as a part of their
                               responsibilities.
  '5.1.4': !unsafe |-
    Edit the /etc/ssh/sshd_config file to set one or more of the parameters above any
                Include and Match set statements as follows:
                AllowUsers <userlist>
                 - AND/OR -
                AllowGroups <grouplist>

                         Note: It is easier to manage an allow list than a deny list. In a deny list,
                         you could potentially add a user or group and forget to add it to the deny
                         list.
                Default Value:
                None
                References:

                     1. SSHD_CONFIG(5)
                     2. NIST SP 800-53 Rev. 5: AC-3. MP-2
                     3. SSHD(8)
                     4. https://documentation.suse.com/en-us/sles/15-SP6/html/SLES-all/cha-ssh.html

                CIS Controls:

                  Controls
                                                                   Control                                         IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                    ●    ●    ●
                              systems, databases, and applications.
                              4.3 Ensure the Use of Dedicated Administrative Accounts
                       v7        Ensure that all users with administrative account access use a dedicated or
                              secondary account for elevated activities. This account should only be used for
                                                                                                                    ●    ●    ●
                              administrative activities and not internet browsing, email, or similar activities.
  '5.1.5': !unsafe |-
    Edit the /etc/ssh/sshd_config file to set the Banner parameter above any Include
                and Match entries as follows:
                Banner /etc/issue.net
                Edit the file being called by the Banner argument with the appropriate contents
                according to your site policy, remove any instances of \m , \r , \s , \v or references to
                the OS platform
                Example:
                # printf '%s\n' "Authorized users only. All activity may be monitored and
                reported." > "$(sshd -T | awk '$1 == "banner" {print $2}')"

                References:

                     1. NIST SP 800-53 Rev. 5: CM-1, CM-2, CM-6, CM-7, IA-5

                CIS Controls:

                     Controls Version                    Control                IG 1 IG 2 IG 3
                              v8          0.0 Explicitly Not Mapped
                                             Explicitly Not Mapped

                              v7          0.0 Explicitly Not Mapped
                                             Explicitly Not Mapped
  '5.1.6': !unsafe |-
    - IF - CVE-2023-48795 has been addressed, and it meets local site policy,
                         chacha20-poly1305 may be removed from the list of excluded ciphers.
                Create or edit a file in /etc/crypto-policies/policies/modules/ ending in .pmod
                and add or modify the the following line:
                cipher@SSH = -3DES-CBC -AES-128-CBC -AES-192-CBC -AES-256-CBC -CHACHA20-
                POLY1305
                Example:
                # printf '%s\n' "# This is a subpolicy to disable weak ciphers" "# for the
                SSH protocol (libssh and OpenSSH)" "cipher@SSH = -3DES-CBC -AES-128-CBC -AES-
                192-CBC -AES-256-CBC -CHACHA20-POLY1305" >> /etc/crypto-
                policies/policies/modules/NO-SSHWEAKCIPHERS.pmod

                         Note: Some organizations may have stricter requirements for approved
                         ciphers. Ensure that ciphers used are in compliance with site policy.
                Run the following command to update the system-wide cryptographic policy
                # update-crypto-policies --set
                <CRYPTO_POLICY>:<CRYPTO_SUBPOLICY1>:<CRYPTO_SUBPOLICY2>:<CRYPTO_SUBPOLICY3>
                Example:
                # update-crypto-policies --set DEFAULT:NO-SHA1:NO-WEAKMAC:NO-SSHCBC:NO-
                SSHCHACHA20:NO-SSHWEAKCIPHERS
                Run the following command to reload the openSSH server to make your cryptographic
                settings effective:
                # systemctl reload-or-restart sshd

                Default Value:
                Ciphers aes256-gcm@openssh.com,chacha20-poly1305@openssh.com,aes256-
                ctr,aes128-gcm@openssh.com,aes128-ctr
                References:

                     1. https://nvd.nist.gov/vuln/detail/CVE-2023-48795
                     2. https://nvd.nist.gov/vuln/detail/CVE-2019-1543
                     3. https://nvd.nist.gov/vuln/detail/CVE-2016-2183
                     4. https://nvd.nist.gov/vuln/detail/CVE-2008-5161
                     5. https://www.openssh.com/txt/cbc.adv
                     6. https://www.openssh.com/txt/cbc.adv
                     7. SSHD_CONFIG(5)
                     8. NIST SP 800-53 Rev. 5: SC-8

                CIS Controls:

                    Controls
                                                                    Control                                  IG 1 IG 2 IG 3
                    Version
                                3.10 Encrypt Sensitive Data in Transit
                        v8         Encrypt sensitive data in transit. Example implementations can include:         ●    ●
                                Transport Layer Security (TLS) and Open Secure Shell (OpenSSH).

                        v7      14.4 Encrypt All Sensitive Information in Transit                                  ●    ●
                                   Encrypt all sensitive information in transit.

                5.1.7 Ensure sshd ClientAliveInterval and ClientAliveCountMax
                are configured (Automated)
                Profile Applicability:

                • Level 1 - Server
                • Level 1 - Workstation

                Description:
                         Note: To clarify, the two settings described below are only meant for idle
                         connections from a protocol perspective and are not meant to check if the
                         user is active or not. An idle user does not mean an idle connection. SSH
                         does not and never had, intentionally, the capability to drop idle users. In
                         SSH versions before 8.2p1 there was a bug that caused these values to
                         behave in such a manner that they were abused to disconnect idle users.
                         This bug has been resolved in 8.2p1 and thus it can no longer be abused
                         disconnect idle users.
                The two options ClientAliveInterval and ClientAliveCountMax control the timeout
                of SSH sessions. Taken directly from man 5 sshd_config:

                     •   ClientAliveInterval Sets a timeout interval in seconds after which if no data
                         has been received from the client, sshd(8) will send a message through the
                         encrypted channel to request a response from the client. The default is 0,
                         indicating that these messages will not be sent to the client.
                     •   ClientAliveCountMax Sets the number of client alive messages which may be
                         sent without sshd(8) receiving any messages back from the client. If this
                         threshold is reached while client alive messages are being sent, sshd will
                         disconnect the client, terminating the session. It is important to note that the use
                         of client alive messages is very different from TCPKeepAlive. The client alive
                         messages are sent through the encrypted channel and therefore will not be
                         spoofable. The TCP keepalive option en‐abled by TCPKeepAlive is spoofable.
                         The client alive mechanism is valuable when the client or server depend on
                         knowing when a connection has become unresponsive.

                      The default value is 3. If ClientAliveInterval is set to 15, and ClientAliveCountMax
                      is left at the default, unresponsive SSH clients will be disconnected after
                      approximately 45 seconds. Setting a zero ClientAliveCountMax disables
                      connection termination.
                More information about the openSSH server configuration is available in the "Configure
                SSH Server" section overview.
                Rationale:
                In order to prevent resource exhaustion, appropriate values should be set for both
                ClientAliveInterval and ClientAliveCountMax. Specifically, looking at the source
                code, ClientAliveCountMax must be greater than zero in order to utilize the ability of

                SSH to drop idle connections. If connections are allowed to stay open indefinitely, this
                can potentially be used as a DDOS attack or simple resource exhaustion could occur
                over unreliable networks.
                The example set here is a 45 second timeout. Consult your site policy for network
                timeouts and apply as appropriate.
                Audit:
                Run the following command and verify ClientAliveInterval and
                ClientAliveCountMax are greater than zero:
                # sshd -T | grep -Pi -- '(clientaliveinterval|clientalivecountmax)'
                Example Output:
                clientaliveinterval 15
                clientalivecountmax 3
                - IF - Match set statements are used in your environment, specify the connection
                parameters to use for the -T extended test mode and run the audit to verify the setting
                is not incorrectly configured in a match block
                Example additional audit needed for a match block for the user sshuser:
                # sshd -T -C user=sshuser | grep -Pi --
                '(clientaliveinterval|clientalivecountmax)'
                Note: If provided, any Match directives in the configuration file that would apply are
                applied before the configuration is written to standard output. The connection
                parameters are supplied as keyword=value pairs and may be supplied in any order,
                either with multiple -C options or as a comma-separated list. The keywords are addr
                (source address), user (user), host (resolved source host name), laddr (local
                address), lport (local port number), and rdomain (routing domain).

                Remediation:
                Edit the /etc/ssh/sshd_config file to set the ClientAliveInterval and
                ClientAliveCountMax parameters above any Include and Match entries according to
                site policy.
                Example:
                ClientAliveInterval 15
                ClientAliveCountMax 3

                Default Value:
                ClientAliveInterval 0
                ClientAliveCountMax 3
                References:

                     1. SSHD_CONFIG(5)
                     2. SSHD(8)
                     3. NIST SP 800-53 Rev. 5: CM-1, CM-2, CM-6, CM-7, IA-5

                Additional Information:
                https://bugzilla.redhat.com/show_bug.cgi?id=1873547
                https://github.com/openssh/openssh-portable/blob/V_8_9/serverloop.c#L137
                CIS Controls:

                     Controls Version                    Control          IG 1 IG 2 IG 3
                              v8          0.0 Explicitly Not Mapped
                                            Explicitly Not Mapped

                              v7          0.0 Explicitly Not Mapped
                                            Explicitly Not Mapped
  '5.1.7': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '5.1.8': !unsafe |-
    Edit the /etc/ssh/sshd_config file to set the DisableForwarding parameter to yes
                above any Include entry as follows:
                DisableForwarding yes

                References:

                     1. sshd_config(5)
                     2. SSHD(8)
                     3. NIST SP 800-53 Rev. 5: CM-7

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '5.1.9': !unsafe |-
    Edit the /etc/ssh/sshd_config file to set the GSSAPIAuthentication parameter to
                no above any Include and Match entries as follows:
                GSSAPIAuthentication no

                Default Value:
                GSSAPIAuthentication no
                References:
                   1. SSHD_CONFIG(5)
                   2. SSHD(8)
                   3. NIST SP 800-53 Rev. 5: CM-1, CM-2, CM-6, CM-7, IA-5

                CIS Controls:

                   Controls
                                                                 Control                                            IG 1 IG 2 IG 3
                   Version
                              5.2 Use Unique Passwords
                       v8         Use unique passwords for all enterprise assets. Best practice implementation
                              includes, at a minimum, an 8-character password for accounts using MFA and a
                                                                                                                     ●    ●    ●
                              14-character password for accounts not using MFA.
                              4.4 Use Unique Passwords
                       v7        Where multi-factor authentication is not supported (such as local administrator,
                              root, or service accounts), accounts will use passwords that are unique to that
                                                                                                                          ●    ●
                              system.
  '5.1.10': !unsafe |-
    Edit the /etc/ssh/sshd_config file to set the HostbasedAuthentication parameter
                to no above any Include and Match entries as follows:
                HostbasedAuthentication no

                Default Value:
                HostbasedAuthentication no
                References:

                     1. SSHD_CONFIG(5)
                     2. SSHD(8)
                     3. NIST SP 800-53 Rev. 5: CM-1, CM-2, CM-6, CM-7, IA-5

                CIS Controls:

                   Controls
                                                                 Control                                            IG 1 IG 2 IG 3
                   Version
                              5.2 Use Unique Passwords
                       v8         Use unique passwords for all enterprise assets. Best practice implementation
                              includes, at a minimum, an 8-character password for accounts using MFA and a
                                                                                                                     ●    ●    ●
                              14-character password for accounts not using MFA.
                              4.4 Use Unique Passwords
                       v7        Where multi-factor authentication is not supported (such as local administrator,
                              root, or service accounts), accounts will use passwords that are unique to that
                                                                                                                          ●    ●
                              system.
  '5.1.11': !unsafe |-
    Edit the /etc/ssh/sshd_config file to set the IgnoreRhosts parameter to yes above
                any Include entry as follows:
                IgnoreRhosts yes

                Default Value:
                IgnoreRhosts yes
                References:

                     1. SSHD_CONFIG(5)
                     2. SSHD(8)
                     3. NIST SP 800-53 Rev. 5: CM-1,CM-2, CM-6, CM-7, IA-5

                CIS Controls:

                   Controls
                                                                 Control                                            IG 1 IG 2 IG 3
                   Version
                              5.2 Use Unique Passwords
                       v8         Use unique passwords for all enterprise assets. Best practice implementation
                              includes, at a minimum, an 8-character password for accounts using MFA and a
                                                                                                                     ●    ●    ●
                              14-character password for accounts not using MFA.
                              4.4 Use Unique Passwords
                       v7        Where multi-factor authentication is not supported (such as local administrator,
                              root, or service accounts), accounts will use passwords that are unique to that
                                                                                                                          ●    ●
                              system.
  '5.1.12': !unsafe |-
    Edit the /etc/ssh/sshd_config file and add/modify the KexAlgorithms line to contain
                a comma separated list of the site unapproved (weak) KexAlgorithms preceded with a -
                above any Include entries:
                Example:
                KexAlgorithms -diffie-hellman-group1-sha1,diffie-hellman-group14-sha1,diffie-
                hellman-group-exchange-sha1

                         Note: First occurrence of an option takes precedence. If Include locations
                         are enabled, used, and order of precedence is understood in your
                         environment, the entry may be created in a file in Include location.

                Default Value:
                Version 9.0 - 9.8: KexAlgorithms sntrup761x25519-sha512,sntrup761x25519-
                sha512@openssh.com,curve25519-sha256,curve25519-sha256@libssh.org,ecdh-
                sha2-nistp256,ecdh-sha2-nistp384,ecdh-sha2-nistp521
                Version 9.9 and greater: KexAlgorithms mlkem768x25519-sha256,sntrup761x25519-
                sha512,sntrup761x25519-sha512@openssh.com,curve25519-sha256,curve25519-
                sha256@libssh.org,ecdh-sha2-nistp256,ecdh-sha2-nistp384,ecdh-sha2-nistp521
                References:

                     1. https://ubuntu.com/server/docs/openssh-crypto-configuration
                     2. NIST SP 800-53 Rev. 5: SC-8
                     3. SSHD(8)
                     4. SSHD_CONFIG(5)

                Additional Information:
                The supported algorithms are:
                diffie-hellman-group1-sha1
                diffie-hellman-group14-sha1
                diffie-hellman-group14-sha256
                diffie-hellman-group16-sha512
                diffie-hellman-group18-sha512
                diffie-hellman-group-exchange-sha1
                diffie-hellman-group-exchange-sha256
                ecdh-sha2-nistp256
                ecdh-sha2-nistp384
                ecdh-sha2-nistp521
                curve25519-sha256
                curve25519-sha256@libssh.org
                sntrup761x25519-sha512
                sntrup761x25519-sha512@openssh.com
                mlkem768x25519-sha256 # Only supported by version 9.9 and greater

                CIS Controls:

                    Controls
                                                                    Control                                  IG 1 IG 2 IG 3
                    Version
                                3.10 Encrypt Sensitive Data in Transit
                        v8         Encrypt sensitive data in transit. Example implementations can include:         ●    ●
                                Transport Layer Security (TLS) and Open Secure Shell (OpenSSH).

                        v7      14.4 Encrypt All Sensitive Information in Transit                                  ●    ●
                                   Encrypt all sensitive information in transit.

                5.1.13 Ensure sshd post-quantum cryptography key exchange
                algorithms are configured (Automated)
                Profile Applicability:

                • Level 1 - Server
                • Level 1 - Workstation

                Description:
                OpenSSH has integrated post-quantum cryptography (PQC) to secure SSH
                connections against potential attacks from future quantum computers. This includes the
                adoption of hybrid key agreement algorithms that combine classical cryptographic
                methods with quantum-resistant ones.
                Rationale:
                PQC helps secure SSH connections against potential attacks from future quantum
                computers providing protection against both classical and quantum adversaries.
                The combination of established classical algorithms with the forward-looking security of
                PQC algorithms, offers a robust solution during the transition phase to fully quantum-
                resistant cryptography.
                Impact:
                A restrictive list of key exchange algorithms available to the SSH server may prevent
                some clients from being able to connect.
                Including a non-supported key exchange algorithm in the kexalgorithms option, may
                prevent the SSH server from starting.
                         Note: The list of supported key exchange algorithms may also be
                         obtained using the ssh -Q KexAlgorithms command.

                Audit:

                     1. Run the following command to determine the version of openSSH-server:

                # "$(readlink -e /usr/sbin/sshd || readlink -e /sbin/sshd)" -V 2>&1 | \
                grep -Psio 'openssh_\d+\.\d+' | awk -F'_' '{print $2}'

                     2. Verify post-quantum cryptography kexalgorithm(s) are available:

                Run the following command to verify the sntrup761x25519-sha512 post-quantum key
                agreement algorithm is available:
                # "$(readlink -e /usr/sbin/sshd || readlink -e /sbin/sshd)" -T | \
                awk '($1=="kexalgorithms" && $2~/sntrup761x25519-sha512/) {print $2}'
                Verify output includes sntrup761x25519-sha512
                - IF - openSSH-server version is 9.9 or greater, run the following command to verify the
                mlkem768x25519-sha256 post-quantum key agreement algorithm is also available:
                # "$(readlink -e /usr/sbin/sshd || readlink -e /sbin/sshd)" -T | \
                awk '($1=="kexalgorithms" && $2~/mlkem768x25519-sha256/) {print $2}'
                Verify output includes mlkem768x25519-sha256

                Remediation:
                Edit the /etc/ssh/sshd_config file and add/modify the KexAlgorithms line above
                any Include and Match statements.
                Example:
                KexAlgorithms -diffie-hellman-group1-sha1,diffie-hellman-group14-sha1,diffie-
                hellman-group-exchange-sha1

                         Note:

                              •   First occurrence of an option takes precedence. If Include locations
                                  are enabled, used, and order of precedence is understood in your
                                  environment, the entry may be created in a file in Include location.
                              •   If local site policy has stricter requirements, add any additional
                                  MACs that are not authorized by local site policy to the list in the
                                  example.

                Default Value:
                Version 9.0 - 9.8: kexalgorithms sntrup761x25519-sha512,sntrup761x25519-
                sha512@openssh.com,curve25519-sha256,curve25519-sha256@libssh.org,ecdh-
                sha2-nistp256,ecdh-sha2-nistp384,ecdh-sha2-nistp521
                Version 9.9 and greater: kexalgorithms mlkem768x25519-sha256,sntrup761x25519-
                sha512,sntrup761x25519-sha512@openssh.com,curve25519-sha256,curve25519-
                sha256@libssh.org,ecdh-sha2-nistp256,ecdh-sha2-nistp384,ecdh-sha2-nistp521

                Additional Information:
                OpenSSH 9.0: Introduced sntrup761x25519-sha512 as the first post-quantum key
                agreement algorithm, which is a hybrid scheme combining Streamlined NTRU Prime
                with X25519.
                OpenSSH 9.9: Added mlkem768x25519-sha256 as another hybrid post-quantum key
                agreement algorithm.
                OpenSSH 10.0: Made mlkem768x25519-sha256 the default key agreement algorithm.
                This algorithm combines ML-KEM (a NIST-standardized key encapsulation mechanism,
                also known as Kyber) with the classical X25519 elliptic curve method.
                CIS Controls:

                    Controls
                                                                    Control                                  IG 1 IG 2 IG 3
                    Version
                                3.10 Encrypt Sensitive Data in Transit
                        v8         Encrypt sensitive data in transit. Example implementations can include:         ●    ●
                                Transport Layer Security (TLS) and Open Secure Shell (OpenSSH).

                        v7      14.4 Encrypt All Sensitive Information in Transit                                  ●    ●
                                   Encrypt all sensitive information in transit.
  '5.1.13': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '5.1.14': !unsafe |-
    Edit the /etc/ssh/sshd_config file to set the LoginGraceTime parameter to 60
                seconds or less above any Include entry as follows:
                LoginGraceTime 60

                Default Value:
                LoginGraceTime 120
                References:

                     1. SSHD_CONFIG(5)
                     2. NIST SP 800-53 Rev. 5: CM-6
                     3. SSHD(8)

                CIS Controls:

                     Controls Version                  Control      IG 1 IG 2 IG 3
                              v8        0.0 Explicitly Not Mapped
                                          Explicitly Not Mapped

                              v7        0.0 Explicitly Not Mapped
                                          Explicitly Not Mapped
  '5.1.15': !unsafe |-
    Edit the /etc/ssh/sshd_config file to set the LogLevel parameter to VERBOSE or
                INFO above any Include and Match entries as follows:
                LogLevel VERBOSE
                   - OR -
                LogLevel INFO

                Default Value:
                LogLevel INFO
                References:

                     1. https://www.ssh.com/ssh/sshd_config/
                     2. NIST SP 800-53 Rev. 5: AU-3, AU-12, SI-5

                CIS Controls:

                   Controls
                                                                 Control                                    IG 1 IG 2 IG 3
                   Version
                              8.2 Collect Audit Logs
                        v8      Collect audit logs. Ensure that logging, per the enterprise’s audit log      ●    ●    ●
                              management process, has been enabled across enterprise assets.
                              6.2 Activate audit logging
                        v7       Ensure that local logging has been enabled on all systems and networking    ●    ●    ●
                              devices.
                              6.3 Enable Detailed Logging
                        v7       Enable system logging to include detailed information such as an event
                              source, date, user, timestamp, source addresses, destination addresses, and
                                                                                                                  ●    ●
                              other useful elements.
  '5.1.16': !unsafe |-
    Edit the /etc/ssh/sshd_config file and add/modify the MACs line to contain a comma
                separated list of the site unapproved (weak) MACs preceded with a - above any
                Include entries:
                Example:
                MACs -hmac-md5,hmac-md5-96,hmac-ripemd160,hmac-sha1-96,umac-
                64@openssh.com,hmac-md5-etm@openssh.com,hmac-md5-96-etm@openssh.com,hmac-
                ripemd160-etm@openssh.com,hmac-sha1-96-etm@openssh.com,umac-64-
                etm@openssh.com,umac-128-etm@openssh.com
                - IF - CVE-2023-48795 has not been reviewed and addressed, the following etm MACs
                should be added to the exclude list: hmac-sha1-etm@openssh.com,hmac-sha2-256-
                etm@openssh.com,hmac-sha2-512-etm@openssh.com
                Default Value:
                MACs umac-64-etm@openssh.com,umac-128-etm@openssh.com,hmac-sha2-256-
                etm@openssh.com,hmac-sha2-512-etm@openssh.com,hmac-sha1-
                etm@openssh.com,umac-64@openssh.com,umac-128@openssh.com,hmac-sha2-
                256,hmac-sha2-512,hmac-sha1
                References:

                     1. https://nvd.nist.gov/vuln/detail/CVE-2023-48795
                     2. More information on SSH downgrade attacks can be found here:
                        http://www.mitls.org/pages/attacks/SLOTH
                     3. SSHD_CONFIG(5)
                     4. NIST SP 800-53 Rev. 5: CM-1, CM-2, CM-6, CM-7, IA-5

                CIS Controls:

                    Controls
                                                                    Control                                  IG 1 IG 2 IG 3
                    Version
                                3.10 Encrypt Sensitive Data in Transit
                        v8         Encrypt sensitive data in transit. Example implementations can include:         ●    ●
                                Transport Layer Security (TLS) and Open Secure Shell (OpenSSH).

                        v7      14.4 Encrypt All Sensitive Information in Transit                                  ●    ●
                                   Encrypt all sensitive information in transit.
                                16.5 Encrypt Transmittal of Username and
                        v7      Authentication Credentials                                                         ●    ●
                                   Ensure that all account usernames and authentication credentials are
                                transmitted across networks using encrypted channels.
  '5.1.17': !unsafe |-
    Edit the /etc/ssh/sshd_config file to set the MaxAuthTries parameter to 4 or less
                above any Include and Match entries as follows:
                MaxAuthTries 4

                Default Value:
                MaxAuthTries 6
                References:

                     1. SSHD_CONFIG(5)
                     2. NIST SP 800-53 Rev. 5: AU-3

                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              16.13 Alert on Account Login Behavior Deviation
                       v7       Alert when users deviate from normal login behavior, such as time-of-day,                       ●
                              workstation location and duration.
  '5.1.18': !unsafe |-
    Edit the /etc/ssh/sshd_config file to set the MaxSessions parameter to 10 or less
                above any Include and Match entries as follows:
                MaxSessions 10

                Default Value:
                MaxSessions 10
                References:
                   1. SSHD_CONFIG(5)
                   2. NIST SP 800-53 Rev. 5: CM-1, CM-2, CM-6, CM-7, IA-5

                CIS Controls:

                     Controls Version                  Control      IG 1 IG 2 IG 3
                              v8        0.0 Explicitly Not Mapped
                                          Explicitly Not Mapped

                              v7        0.0 Explicitly Not Mapped
                                          Explicitly Not Mapped
  '5.1.19': !unsafe |-
    Edit the /etc/ssh/sshd_config file to set the MaxStartups parameter to 10:30:60 or
                more restrictive above any Include entries as follows:
                MaxStartups 10:30:60

                Default Value:
                MaxStartups 10:30:100
                References:

                     1. SSHD_CONFIG(5)
                     2. NIST SP 800-53 Rev. 5: CM-1, CM-2, CM-6, CM-7, IA-5

                CIS Controls:

                     Controls Version                    Control               IG 1 IG 2 IG 3
                              v8          0.0 Explicitly Not Mapped
                                             Explicitly Not Mapped

                              v7          0.0 Explicitly Not Mapped
                                             Explicitly Not Mapped
  '5.1.20': !unsafe |-
    Edit /etc/ssh/sshd_config and set the PermitEmptyPasswords parameter to no
                above any Include and Match entries as follows:
                PermitEmptyPasswords no

                Default Value:
                PermitEmptyPasswords no
                References:

                     1. SSHD_CONFIG(5)
                     2. NIST SP 800-53 Rev. 5: CM-1,CM-2, CM-6, CM-7, IA-5

                CIS Controls:

                   Controls
                                                                 Control                                            IG 1 IG 2 IG 3
                   Version
                              5.2 Use Unique Passwords
                       v8         Use unique passwords for all enterprise assets. Best practice implementation
                              includes, at a minimum, an 8-character password for accounts using MFA and a
                                                                                                                     ●    ●    ●
                              14-character password for accounts not using MFA.
                              4.4 Use Unique Passwords
                       v7        Where multi-factor authentication is not supported (such as local administrator,
                              root, or service accounts), accounts will use passwords that are unique to that
                                                                                                                          ●    ●
                              system.
  '5.1.21': !unsafe |-
    Edit the /etc/ssh/sshd_config file to set the PermitRootLogin parameter to no
                above any Include and Match entries as follows:
                PermitRootLogin no

                Default Value:
                PermitRootLogin without-password
                References:

                     1. SSHD_CONFIG(5)
                     2. NIST SP 800-53 Rev. 5:AC-6

                CIS Controls:

                  Controls
                                                                   Control                                           IG 1 IG 2 IG 3
                  Version
                              5.4 Restrict Administrator Privileges to Dedicated
                              Administrator Accounts
                       v8        Restrict administrator privileges to dedicated administrator accounts on
                              enterprise assets. Conduct general computing activities, such as internet
                                                                                                                      ●    ●    ●
                              browsing, email, and productivity suite use, from the user’s primary, non-privileged
                              account.
                              4.3 Ensure the Use of Dedicated Administrative Accounts
                       v7        Ensure that all users with administrative account access use a dedicated or
                              secondary account for elevated activities. This account should only be used for
                                                                                                                      ●    ●    ●
                              administrative activities and not internet browsing, email, or similar activities.
  '5.1.22': !unsafe |-
    Edit the /etc/ssh/sshd_config file to set the PermitUserEnvironment parameter to
                no above any Include entries as follows:
                PermitUserEnvironment no

                Default Value:
                PermitUserEnvironment no
                References:

                     1. SSHD_CONFIG(5)
                     2. NIST SP 800-53 Rev. 5: CM-1,CM-2, CM-6, CM-7, IA-5
                     3. SSHD(8)

                CIS Controls:

                     Controls Version                  Control      IG 1 IG 2 IG 3
                              v8        0.0 Explicitly Not Mapped
                                          Explicitly Not Mapped

                              v7        0.0 Explicitly Not Mapped
                                          Explicitly Not Mapped
  '5.1.23': !unsafe |-
    Edit the /etc/ssh/sshd_config file to set the UsePAM parameter to yes above any
                Include entries as follows:
                UsePAM yes

                Default Value:
                UsePAM yes
                References:

                     1. SSHD_CONFIG(5)
                     2. NIST SP 800-53 Rev. 5: CM-1, CM-2, CM-6, CM-7, IA-5
                     3. SSHD(8)

                CIS Controls:

                   Controls
                                                                 Control                                            IG 1 IG 2 IG 3
                   Version
                              5.2 Use Unique Passwords
                       v8         Use unique passwords for all enterprise assets. Best practice implementation
                              includes, at a minimum, an 8-character password for accounts using MFA and a
                                                                                                                     ●    ●    ●
                              14-character password for accounts not using MFA.
                              4.4 Use Unique Passwords
                       v7        Where multi-factor authentication is not supported (such as local administrator,
                              root, or service accounts), accounts will use passwords that are unique to that
                                                                                                                          ●    ●
                              system.

                5.2 Configure privilege escalation

                There are various tools which allows a permitted user to execute a command as the
                superuser or another user, as specified by the security policy.
                sudo
                sudo documentation
                The invoking user's real (not effective) user ID is used to determine the user name with
                which to query the security policy.
                sudo supports a plug-in architecture for security policies and input/output logging. Third
                parties can develop and distribute their own policy and I/O logging plug-ins to work
                seamlessly with the sudo front end. The default security policy is sudoers, which is
                configured via the file /etc/sudoers and any entries in /etc/sudoers.d.
                pkexec
                pkexec documentation
                pkexec allows an authorized user to execute PROGRAM as another user. If username
                is not specified, then the program will be executed as the administrative super user,
                root.
  '5.2.1': !unsafe |-
    First determine is LDAP functionality is required. If so, then install libsss-sudo and
                sssd.
                Example:
                # apt install libsss-sudo sssd
                Note: Debian 13, "trixie", will be the last version of Debian that supports sudo-ldap.
                The Debian sudo team recommends the use of libsss-sudo for new installations and
                the migration of existing installations from sudo-ldap to libsss-sudo and sssd. See
                /usr/share/doc/sudo-ldap/NEWS.Debian.gz for more explanation. This is also
                being discussed in #1033728 in the Debian BTS.
                References:

                     1. SUDO(8)
                     2. NIST SP 800-53 Rev. 5: AC-6

                CIS Controls:

                  Controls
                                                                   Control                                           IG 1 IG 2 IG 3
                  Version
                              5.4 Restrict Administrator Privileges to Dedicated
                              Administrator Accounts
                       v8        Restrict administrator privileges to dedicated administrator accounts on
                              enterprise assets. Conduct general computing activities, such as internet
                                                                                                                      ●    ●    ●
                              browsing, email, and productivity suite use, from the user’s primary, non-privileged
                              account.
                              4.3 Ensure the Use of Dedicated Administrative Accounts
                       v7        Ensure that all users with administrative account access use a dedicated or
                              secondary account for elevated activities. This account should only be used for
                                                                                                                      ●    ●    ●
                              administrative activities and not internet browsing, email, or similar activities.
  '5.2.2': !unsafe |-
    Edit the file /etc/sudoers with visudo or a file in /etc/sudoers.d/ with visudo -f
                <PATH TO FILE> and add the following line:
                Defaults use_pty
                Edit the file /etc/sudoers with visudo and any files in /etc/sudoers.d/ with visudo
                -f <PATH TO FILE> and remove any occurrence of !use_pty

                Note:
                     •    sudo will read each file in /etc/sudoers.d, skipping file names that end in ~ or
                          contain a . character to avoid causing problems with package manager or editor
                          temporary/backup files.
                     •    Files are parsed in sorted lexical order. That is, /etc/sudoers.d/01_first will
                          be parsed before /etc/sudoers.d/10_second.
                     •    Be aware that because the sorting is lexical, not numeric,
                          /etc/sudoers.d/1_whoops would be loaded after
                          /etc/sudoers.d/10_second.
                     •    Using a consistent number of leading zeroes in the file names can be used to
                          avoid such problems.
                References:

                     1. SUDO(8)
                     2. VISUDO(8)
                     3. sudoers(5)
                     4. NIST SP 800-53 Rev. 5: AC-6

                CIS Controls:

                  Controls
                                                                     Control                                           IG 1 IG 2 IG 3
                  Version
                                5.4 Restrict Administrator Privileges to Dedicated
                                Administrator Accounts
                         v8        Restrict administrator privileges to dedicated administrator accounts on
                                enterprise assets. Conduct general computing activities, such as internet
                                                                                                                        ●    ●    ●
                                browsing, email, and productivity suite use, from the user’s primary, non-privileged
                                account.
                                4.3 Ensure the Use of Dedicated Administrative Accounts
                         v7        Ensure that all users with administrative account access use a dedicated or
                                secondary account for elevated activities. This account should only be used for
                                                                                                                        ●    ●    ●
                                administrative activities and not internet browsing, email, or similar activities.
  '5.2.3': !unsafe |-
    Edit the file /etc/sudoers or a file in /etc/sudoers.d/ with visudo -f <PATH TO
                FILE> and add the following line:
                Defaults      logfile="<PATH TO CUSTOM LOG FILE>"
                Example:
                Defaults logfile="/var/log/sudo.log"
                Notes:

                     •   sudo will read each file in /etc/sudoers.d, skipping file names that end in ~ or
                         contain a . character to avoid causing problems with package manager or editor
                         temporary/backup files.
                     •   Files are parsed in sorted lexical order. That is, /etc/sudoers.d/01_first will
                         be parsed before /etc/sudoers.d/10_second.
                     •   Be aware that because the sorting is lexical, not numeric,
                         /etc/sudoers.d/1_whoops would be loaded after
                         /etc/sudoers.d/10_second.
                     •   Using a consistent number of leading zeroes in the file names can be used to
                         avoid such problems.

                References:

                     1. SUDO(8)
                     2. VISUDO(8)
                     3. sudoers(5)
                     4. NIST SP 800-53 Rev. 5: AU-3, AU-12

                Additional Information:
                visudo edits the sudoers file in a safe fashion, analogous to vipw(8). visudo locks the
                sudoers file against multiple simultaneous edits, provides basic sanity checks, and
                checks for parse errors. If the sudoers file is currently being edited you will receive a
                message to try again later.
                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              6.3 Enable Detailed Logging
                       v7        Enable system logging to include detailed information such as an event source,
                              date, user, timestamp, source addresses, destination addresses, and other useful
                                                                                                                           ●    ●
                              elements.
  '5.2.4': !unsafe |-
    Based on the outcome of the audit procedure, use visudo -f <PATH TO FILE> to edit
                the relevant sudoers file.
                Remove any line with occurrences of NOPASSWD tags in the file.

                References:

                     1. NIST SP 800-53 Rev. 5: AC-6

                CIS Controls:

                  Controls
                                                                   Control                                           IG 1 IG 2 IG 3
                  Version
                              5.4 Restrict Administrator Privileges to Dedicated
                              Administrator Accounts
                       v8        Restrict administrator privileges to dedicated administrator accounts on
                              enterprise assets. Conduct general computing activities, such as internet
                                                                                                                      ●    ●    ●
                              browsing, email, and productivity suite use, from the user’s primary, non-privileged
                              account.
                              4.3 Ensure the Use of Dedicated Administrative Accounts
                       v7        Ensure that all users with administrative account access use a dedicated or
                              secondary account for elevated activities. This account should only be used for
                                                                                                                      ●    ●    ●
                              administrative activities and not internet browsing, email, or similar activities.

                5.2.5 Ensure re-authentication for privilege escalation is not
                disabled globally (Automated)
                Profile Applicability:

                • Level 1 - Server
                • Level 1 - Workstation

                Description:
                The operating system must be configured so that users must re-authenticate for
                privilege escalation.
                Rationale:
                Without re-authentication, users may access resources or perform tasks for which they
                do not have authorization.
                When operating systems provide the capability to escalate a functional capability, it is
                critical the user re-authenticate.
                Audit:
                Verify the operating system requires users to re-authenticate for privilege escalation.
                Check the configuration of the /etc/sudoers and /etc/sudoers.d/* files with the
                following command:
                # grep -r '^[^#].*\!authenticate' /etc/sudoers*
                If any line is found with a !authenticate tag, refer to the remediation procedure below.

                Remediation:
                Configure the operating system to require users to reauthenticate for privilege
                escalation.
                Based on the outcome of the audit procedure, use visudo -f <PATH TO FILE> to edit
                the relevant sudoers file.
                Remove any occurrences of !authenticate tags in the file(s).

                References:

                     1. NIST SP 800-53 Rev. 5: AC-6

                CIS Controls:

                  Controls
                                                                   Control                                           IG 1 IG 2 IG 3
                  Version
                              5.4 Restrict Administrator Privileges to Dedicated
                              Administrator Accounts
                       v8        Restrict administrator privileges to dedicated administrator accounts on
                              enterprise assets. Conduct general computing activities, such as internet
                                                                                                                      ●    ●    ●
                              browsing, email, and productivity suite use, from the user’s primary, non-privileged
                              account.
                              4.3 Ensure the Use of Dedicated Administrative Accounts
                       v7        Ensure that all users with administrative account access use a dedicated or
                              secondary account for elevated activities. This account should only be used for
                                                                                                                      ●    ●    ●
                              administrative activities and not internet browsing, email, or similar activities.
  '5.2.5': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '5.2.6': !unsafe |-
    Edit the file listed in the audit section with visudo -f <PATH TO FILE> and modify the
                entry timestamp_timeout= to 15 minutes or less as per your site policy. The value is in
                minutes. This particular entry may appear on its own, or on the same line as
                env_reset. See the following two examples:
                Example 1:
                Defaults        env_reset, timestamp_timeout=15
                Example 2:
                Defaults        timestamp_timeout=15
                Defaults        env_reset

                Default Value:
                timestamp_timeout=15
                References:

                     1. https://www.sudo.ws/man/1.9.0/sudoers.man.html
                     2. NIST SP 800-53 Rev. 5: AC-6

                CIS Controls:

                  Controls
                                                                   Control                                           IG 1 IG 2 IG 3
                  Version
                              5.4 Restrict Administrator Privileges to Dedicated
                              Administrator Accounts
                       v8        Restrict administrator privileges to dedicated administrator accounts on
                              enterprise assets. Conduct general computing activities, such as internet
                                                                                                                      ●    ●    ●
                              browsing, email, and productivity suite use, from the user’s primary, non-privileged
                              account.
                              4.3 Ensure the Use of Dedicated Administrative Accounts
                       v7        Ensure that all users with administrative account access use a dedicated or
                              secondary account for elevated activities. This account should only be used for
                                                                                                                      ●    ●    ●
                              administrative activities and not internet browsing, email, or similar activities.
  '5.2.7': !unsafe |-
    Create an empty group that will be specified for use of the su command. The group
                should be named according to site policy.
                Example:
                # groupadd sugroup
                Add the following line to the /etc/pam.d/su file, specifying the empty group:
                auth required pam_wheel.so use_uid group=sugroup

                References:

                     1. NIST SP 800-53 Rev. 5: AC-3. MP-2

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.

                5.3 Pluggable Authentication Modules

                Pluggable Authentication Modules (PAM) is a service that implements modular
                authentication modules on Linux systems. PAM is implemented as a set of shared
                objects that are loaded and executed when a program needs to authenticate a user.
                Files for PAM are typically located in the /etc/pam.d directory. PAM must be carefully
                configured to secure system authentication. While this section covers some of PAM,
                please consult other PAM resources to fully understand the configuration capabilities.
                Note: This section includes guidance that requires the additional modules and the latest
                version of software packages as covered in the "Configure required security packages"
                subsection of the "Package Management" section

                5.3.1 Configure PAM software packages

                Updated versions of PAM include additional functionality
  '5.3.1.1': !unsafe |-
    Run the following command to install the latest version of libpam-runtime:
                # apt install libpam-runtime

                References:

                     1. PAM(7)

                CIS Controls:

                     Controls Version                    Control              IG 1 IG 2 IG 3
                              v8          0.0 Explicitly Not Mapped
                                             Explicitly Not Mapped

                              v7          0.0 Explicitly Not Mapped
                                             Explicitly Not Mapped
  '5.3.1.2': !unsafe |-
    Run the following command to install the latest version of libpam-modules:
                # apt install libpam-modules

                CIS Controls:

                     Controls Version                    Control             IG 1 IG 2 IG 3
                              v8          0.0 Explicitly Not Mapped
                                             Explicitly Not Mapped

                              v7          0.0 Explicitly Not Mapped
                                             Explicitly Not Mapped
  '5.3.1.3': !unsafe |-
    Run the following command to install the latest version of libpam-pwquality:
                # apt install libpam-pwquality

                References:

                     1. https://packages.debian.org/buster/libpam-pwquality

                CIS Controls:

                   Controls
                                                                 Control                                            IG 1 IG 2 IG 3
                   Version
                              5.2 Use Unique Passwords
                       v8         Use unique passwords for all enterprise assets. Best practice implementation
                              includes, at a minimum, an 8-character password for accounts using MFA and a
                                                                                                                     ●    ●    ●
                              14-character password for accounts not using MFA.
                              4.4 Use Unique Passwords
                       v7        Where multi-factor authentication is not supported (such as local administrator,
                              root, or service accounts), accounts will use passwords that are unique to that
                                                                                                                          ●    ●
                              system.

                5.3.2 Configure pam-auth-update profiles

                pam-auth-update is a utility that permits configuring the central authentication policy
                for the system using pre-defined profiles as supplied by PAM module packages.
                Profiles - Shipped in the /usr/share/pam-configs/ directory specify the modules,
                with options, to enable; the preferred ordering with respect to other profiles; and
                whether a profile should be enabled by default. Packages providing PAM modules
                register their profiles at install time by calling pam-auth-update --package.
                Selection of profiles is done using the standard debconf interface. The profile selection
                question will be asked at medium priority when packages are added or removed, so no
                user interaction is required by default. Users may invoke pam-auth-update directly to
                change their authentication configuration.
                The pam-auth-update script makes every effort to respect local changes to
                /etc/pam.d/common-*. Local modifications to the list of module options will be
                preserved, and additions of modules within the managed portion of the stack will cause
                pam-auth-update to treat the config files as locally modified and not make further
                changes to the config files unless given the --force option.
                If the user specifies that pam-auth-update should override local configuration changes,
                the locally-modified files will be saved in /etc/pam.d/ with a suffix of .pam-old.
  '5.3.2.1': !unsafe |-
    Run the following command to enable the pam_unix module:
                # pam-auth-update --enable unix
                Note: If a site specific custom profile is being used in your environment to configure
                PAM that includes the configuration for the pam_unix module, enable that module
                instead
                References:

                     1. NIST SP 800-53 Rev. 5: IA-5(1)

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '5.3.2.2': !unsafe |-
    Create two pam-auth-update profiles in /usr/share/pam-configs/:
                   1. Create the faillock profile in /usr/share/pam-configs/ with the following
                      lines:
                Name: Enable pam_faillock to deny access
                Default: yes
                Priority: 0
                Auth-Type: Primary
                Auth:
                        [default=die]                    pam_faillock.so authfail
                Example Script:
                #!/usr/bin/env bash

                {
                   arr=('Name: Enable pam_faillock to deny access' 'Default: yes' 'Priority:
                0' 'Auth-Type: Primary' 'Auth:' '        [default=die]
                pam_faillock.so authfail')
                   printf '%s\n' "${arr[@]}" > /usr/share/pam-configs/faillock
                }

                     2. Create the faillock_notify profile in /usr/share/pam-configs/ with the
                        following lines:

                Name: Notify of failed login attempts and reset count upon success
                Default: yes
                Priority: 1024
                Auth-Type: Primary
                Auth:
                         requisite                      pam_faillock.so preauth
                Account-Type: Primary
                Account:
                         required                       pam_faillock.so
                Example Script:
                #!/usr/bin/env bash

                {
                   arr=('Name: Notify of failed login attempts and reset count upon success'
                'Default: yes' 'Priority: 1024' 'Auth-Type: Primary' 'Auth:' '
                requisite                       pam_faillock.so preauth' 'Account-Type:
                Primary' 'Account:' '        required
                pam_faillock.so')
                   printf '%s\n' "${arr[@]}" > /usr/share/pam-configs/faillock_notify
                }
                Run the following command to update the common-auth and common-account PAM
                files with the new profiles:
                # pam-auth-update --enable <profile_filename>
                Example:
                # pam-auth-update --enable faillock
                # pam-auth-update --enable faillock_notify

                Note:

                     •    The name used for the file must be used in the pam-auth-update --enable
                          command
                     •    The Name: line should be easily recognizable and understood
                     •    The Priority: Line is important as it effects the order of the lines in the
                          /etc/pam.d/ files
                     •    If a site specific custom profile is being used in your environment to configure
                          PAM that includes the configuration for the pam_faillock module, enable that
                          module instead

                CIS Controls:

                  Controls
                                                                   Control                                           IG 1 IG 2 IG 3
                  Version
                               6.2 Establish an Access Revoking Process
                                  Establish and follow a process, preferably automated, for revoking access to
                         v8    enterprise assets, through disabling accounts immediately upon termination, rights     ●    ●    ●
                               revocation, or role change of a user. Disabling accounts, instead of deleting
                               accounts, may be necessary to preserve audit trails.
                               16.7 Establish Process for Revoking Access
                                   Establish and follow an automated process for revoking system access by
                         v7    disabling accounts immediately upon termination or change of responsibilities of an         ●    ●
                               employee or contractor . Disabling these accounts, instead of deleting accounts,
                               allows preservation of audit trails.
  '5.3.2.3': !unsafe |-
    Run the following script to verify the pam_pwquality.so line exists in a pam-auth-
                update profile:
                # grep -P -- '\bpam_pwquality\.so\b' /usr/share/pam-configs/*
                Output should be similar to:
                /usr/share/pam-configs/pwquality:              requisite
                pam_pwquality.so retry=3
                /usr/share/pam-configs/pwquality:              requisite
                pam_pwquality.so retry=3
                - IF - similar output is returned:
                Run the following command to update /etc/pam.d/common-password with the
                returned profile:
                # pam-auth-update --enable {PROFILE_NAME}
                Example:
                # pam-auth-update pwquality
                - IF - similar output is NOT returned:
                Create a pam-auth-update profile in /usr/share/pam-configs/ with the following
                lines:
                Name: Pwquality password strength checking
                Default: yes
                Priority: 1024
                Conflicts: cracklib
                Password-Type: Primary
                Password:
                        requisite                       pam_pwquality.so retry=3
                Example:
                #!/usr/bin/env bash

                {
                   arr=('Name: Pwquality password strength checking' 'Default: yes'
                'Priority: 1024' 'Conflicts: cracklib' 'Password-Type: Primary' 'Password:' '
                requisite                       pam_pwquality.so retry=3')
                   printf '%s\n' "${arr[@]}" > /usr/share/pam-configs/pwquality
                }
                Run the following command to update /etc/pam.d/common-password with the
                pwquality profile:
                # pam-auth-update --enable pwquality
                Note:

                     •   The name used for the file must be used in the pam-auth-update --enable
                         command
                     •   The Name: line should be easily recognizable and understood
                     •   The Priority: Line is important as it effects the order of the lines in the
                         /etc/pam.d/ files
                     •   If a site specific custom profile is being used in your environment to configure
                         PAM that includes the configuration for the pam_pwquality module, enable that
                         module instead

                CIS Controls:

                   Controls
                                                                 Control                                            IG 1 IG 2 IG 3
                   Version
                              5.2 Use Unique Passwords
                       v8         Use unique passwords for all enterprise assets. Best practice implementation
                              includes, at a minimum, an 8-character password for accounts using MFA and a
                                                                                                                     ●    ●    ●
                              14-character password for accounts not using MFA.
                              4.4 Use Unique Passwords
                       v7        Where multi-factor authentication is not supported (such as local administrator,
                              root, or service accounts), accounts will use passwords that are unique to that
                                                                                                                          ●    ●
                              system.
  '5.3.2.4': !unsafe |-
    Run the following script to verify the pam_pwhistory.so line exists in a pam-auth-
                update profile:
                # grep -P -- '\bpam_pwhistory\.so\b' /usr/share/pam-configs/*
                Output should be similar to:
                /usr/share/pam-configs/pwhistory:         requisite     pam_pwhistory.so remember=24
                enforce_for_root use_authtok
                - IF - similar output is returned:
                Run the following command to update /etc/pam.d/common-password with the
                returned profile:
                # pam-auth-update --enable {PROFILE_NAME}
                Example:
                # pam-auth-update pwhistory
                - IF - similar output is NOT returned:
                Create a pwhistory profile in /usr/share/pam-configs/ with the following lines:
                Name: pwhistory password history checking
                Default: yes
                Priority: 1024
                Password-Type: Primary
                Password: requisite pam_pwhistory.so remember=24 enforce_for_root use_authtok
                Example Script:
                #!/usr/bin/env bash

                {
                   arr=('Name: pwhistory password history checking' 'Default: yes' 'Priority:
                1024' 'Password-Type: Primary' 'Password:' '        requisite
                pam_pwhistory.so remember=24 enforce_for_root use_authtok')
                   printf '%s\n' "${arr[@]}" > /usr/share/pam-configs/pwhistory
                }
                Run the following command to update /etc/pam.d/common-password with the
                pwhistory profile:
                # pam-auth-update --enable pwhistory
                Note:

                     •   The name used for the file must be used in the pam-auth-update --enable
                         command
                     •   The Name: line should be easily recognizable and understood
                     •   The Priority: Line is important as it effects the order of the lines in the
                         /etc/pam.d/ files
                     •   If a site specific custom profile is being used in your environment to configure
                         PAM that includes the configuration for the pam_pwhistory module, enable that
                         module instead

                CIS Controls:

                   Controls
                                                                 Control                                            IG 1 IG 2 IG 3
                   Version
                              5.2 Use Unique Passwords
                       v8         Use unique passwords for all enterprise assets. Best practice implementation
                              includes, at a minimum, an 8-character password for accounts using MFA and a
                                                                                                                     ●    ●    ●
                              14-character password for accounts not using MFA.
                              4.4 Use Unique Passwords
                       v7        Where multi-factor authentication is not supported (such as local administrator,
                              root, or service accounts), accounts will use passwords that are unique to that
                                                                                                                          ●    ●
                              system.

                5.3.3 Configure PAM Arguments

                Pluggable Authentication Modules (PAM) uses arguments to pass information to a
                pluggable module during authentication for a particular module type. These arguments
                allow the PAM configuration files for particular programs to use a common PAM module
                but in different ways.
                Invalid arguments are ignored and do not otherwise affect the success or failure of the
                PAM module. When an invalid argument is passed, an error is usually written to
                /var/log/messages file. However, since the reporting method is controlled by the PAM
                module, the module must be written correctly to log the error to this file.
                Note: If custom PAM files are being used, for this section's remediation, the
                corresponding files in /etc/pam.d/ would need to be edited directly, and the pam-
                auth-update --enable <EDITED_PROFILE_NAME> command skipped

                5.3.3.1 Configure pam_faillock module
                pam_faillock.so provides a way to configure the default settings for locking the user
                after multiple failed authentication attempts.
                Options:

                     •   <dir=/path/to/tally-directory> - The directory where the user files with the
                         failure records are kept. The default is /var/run/faillock. Note: These files will
                         disappear after reboot on systems configured with directory /var/run/faillock
                         mounted on virtual memory.
                     •   audit - Will log the user name into the system log if the user is not found.
                     •   silent - Don't print informative messages to the user. Please note that when
                         this option is not used there will be difference in the authentication behavior for
                         users which exist on the system and non-existing users.
                     •   no_log_info - Don't log informative messages via syslog(3).
                     •   local_users_only - Only track failed user authentications attempts for local
                         users in /etc/passwd and ignore centralized (AD, IdM, LDAP, etc.) users. The
                         faillock(8) command will also no longer track user failed authentication attempts.
                         Enabling this option will prevent a double-lockout scenario where a user is locked
                         out locally and in the centralized mechanism.
                     •   nodelay - Don't enforce a delay after authentication failures.
                     •   deny=<n> - Deny access if the number of consecutive authentication failures for
                         this user during the recent interval exceeds . The default is 3.
                     •   fail_interval=n - The length of the interval during which the consecutive
                         authentication failures must happen for the user account lock out is n seconds.
                         The default is 900 (15 minutes).
                     •   unlock_time=n - The access will be re-enabled after n seconds after the lock
                         out. The value 0 has the same meaning as value never - the access will not be
                         re-enabled without resetting the faillock entries by the faillock(8) command. The
                         default is 600 (10 minutes). Note that the default directory that pam_faillock uses
                         is usually cleared on system boot so the access will be also re-enabled after
                         system reboot. If that is undesirable a different tally directory must be set with the
                         dir option. Also note that it is usually undesirable to permanently lock out users
                         as they can become easily a target of denial of service attack unless the
                         usernames are random and kept secret to potential attackers.
                     •   even_deny_root - Root account can become locked as well as regular accounts.
                     •   root_unlock_time=n - This option implies even_deny_root option. Allow access
                         after n seconds to root account after the account is locked. In case the option is
                         not specified the value is the same as of the unlock_time option.
                     •   admin_group=name - If a group name is specified with this option, members of
                         the group will be handled by this module the same as the root account (the
                         options even_deny_root and root_unlock_time will apply to them. By default the
                         option is not set.
  '5.3.3.1.1': !unsafe |-
    Create or edit the following line in /etc/security/faillock.conf setting the deny
                option to 5 or less:
                deny = 5
                Run the following command:
                # grep -Pl -- '\bpam_faillock\.so\h+([^#\n\r]+\h+)?deny\b' /usr/share/pam-
                configs/*
                Edit any returned files and remove the deny=<N> arguments from the
                pam_faillock.so line(s):

                Default Value:
                deny = 3

                Additional Information:
                If a user has been locked out because they have reached the maximum consecutive
                failure count defined by deny= in the pam_faillock.so module, the user can be
                unlocked by issuing the command faillock --user <USERNAME> --reset. This
                command sets the failed count to 0, effectively unlocking the user.
                CIS Controls:

                  Controls
                                                                  Control                                           IG 1 IG 2 IG 3
                  Version
                              6.2 Establish an Access Revoking Process
                                 Establish and follow a process, preferably automated, for revoking access to
                       v8     enterprise assets, through disabling accounts immediately upon termination, rights     ●    ●    ●
                              revocation, or role change of a user. Disabling accounts, instead of deleting
                              accounts, may be necessary to preserve audit trails.
                              16.7 Establish Process for Revoking Access
                                  Establish and follow an automated process for revoking system access by
                       v7     disabling accounts immediately upon termination or change of responsibilities of an         ●    ●
                              employee or contractor . Disabling these accounts, instead of deleting accounts,
                              allows preservation of audit trails.
  '5.3.3.1.2': !unsafe |-
    Set password unlock time to conform to site policy. unlock_time should be 0 (never),
                or 900 seconds or greater.
                Edit /etc/security/faillock.conf and update or add the following line:
                unlock_time = 900
                Run the following command: remove the unlock_time argument from the
                pam_faillock.so module in the PAM files:
                # grep -Pl -- '\bpam_faillock\.so\h+([^#\n\r]+\h+)?unlock_time\b'
                /usr/share/pam-configs/*
                Edit any returned files and remove the unlock_time=<N> argument from the
                pam_faillock.so line(s):

                Default Value:
                unlock_time = 600
                Additional Information:
                If a user has been locked out because they have reached the maximum consecutive
                failure count defined by deny= in the pam_faillock.so module, the user can be
                unlocked by issuing the command faillock --user <USERNAME> --reset. This
                command sets the failed count to 0, effectively unlocking the user.
                CIS Controls:

                  Controls
                                                                  Control                                           IG 1 IG 2 IG 3
                  Version
                              6.2 Establish an Access Revoking Process
                                 Establish and follow a process, preferably automated, for revoking access to
                       v8     enterprise assets, through disabling accounts immediately upon termination, rights     ●    ●    ●
                              revocation, or role change of a user. Disabling accounts, instead of deleting
                              accounts, may be necessary to preserve audit trails.
                              16.7 Establish Process for Revoking Access
                                  Establish and follow an automated process for revoking system access by
                       v7     disabling accounts immediately upon termination or change of responsibilities of an         ●    ●
                              employee or contractor . Disabling these accounts, instead of deleting accounts,
                              allows preservation of audit trails.

                5.3.3.1.3 Ensure password failed attempts lockout includes root
                account (Automated)
                Profile Applicability:

                • Level 2 - Server
                • Level 2 - Workstation

                Description:
                even_deny_root - Enables locking of the root account, as well as regular accounts,
                after failed attempts. By default, root is exempt to prevent denial-of-service risks.
                root_unlock_time=n - This option implies even_deny_root option. Allow access after n
                seconds to root account after the account is locked. In case the option is not specified
                the value is the same as of the unlock_time option.
                Rationale:
                Locking out user IDs after n unsuccessful consecutive login attempts mitigate brute
                force password attacks against your systems.
                Impact:
                Use of unlock_time=0 or root_unlock_time=0 may allow an attacker to cause denial
                of service to legitimate users.

                Audit:
                Run the following command to verify that even_deny_root and/or root_unlock_time
                is enabled:
                # grep -Pi -- '^\h*(even_deny_root|root_unlock_time\h*=\h*\d+)\b'
                /etc/security/faillock.conf
                Example output:
                even_deny_root

                --AND/OR--

                root_unlock_time = 60
                Run the following command to verify that - IF - root_unlock_time is set, it is set to 60
                (One minute) or more:
                # grep -Pi -- '^\h*root_unlock_time\h*=\h*([1-9]|[1-5][0-9])\b'
                /etc/security/faillock.conf
                Nothing should be returned
                Run the following command to check the pam_faillock.so module for the
                root_unlock_time argument. Verify -IF- root_unlock_time is set, it is set to 60 (One
                minute) or more:
                # grep -Pi --
                '^\h*auth\h+([^#\n\r]+\h+)pam_faillock\.so\h+([^#\n\r]+\h+)?root_unlock_time\
                h*=\h*([1-9]|[1-5][0-9])\b' /etc/pam.d/common-auth
                Nothing should be returned
                Remediation:
                Edit /etc/security/faillock.conf:

                     •   Remove or update any line containing root_unlock_time, - OR - set it to a
                         value of 60 or more
                     •   Update or add the following line

                even_deny_root
                Run the following command:
                # grep -Pl --
                '\bpam_faillock\.so\h+([^#\n\r]+\h+)?(even_deny_root|root_unlock_time)'
                /usr/share/pam-configs/*
                Edit any returned files and remove the even_deny_root and root_unlock_time
                arguments from the pam_faillock.so line(s).
                Note: It is recommended to remove any duplicate or contradictory entries of
                even_deny_root and root_unlock_time found in /usr/share/pam-configs/ files
                and only use the centralized /etc/security/fallock.conf to set even_deny_root.

                Default Value:
                disabled

                References:

                     1. https://linux.die.net/man/8/pam_faillock

                Additional Information:
                If a user has been locked out because they have reached the maximum consecutive
                failure count defined by deny= in the pam_faillock.so module, the user can be
                unlocked by issuing the command faillock --user <USERNAME> --reset. This
                command sets the failed count to 0, effectively unlocking the user.
                CIS Controls:

                  Controls
                                                                  Control                                           IG 1 IG 2 IG 3
                  Version
                              6.2 Establish an Access Revoking Process
                                 Establish and follow a process, preferably automated, for revoking access to
                       v8     enterprise assets, through disabling accounts immediately upon termination, rights     ●    ●    ●
                              revocation, or role change of a user. Disabling accounts, instead of deleting
                              accounts, may be necessary to preserve audit trails.
                              16.7 Establish Process for Revoking Access
                                  Establish and follow an automated process for revoking system access by
                       v7     disabling accounts immediately upon termination or change of responsibilities of an         ●    ●
                              employee or contractor . Disabling these accounts, instead of deleting accounts,
                              allows preservation of audit trails.

                5.3.3.2 Configure pam_pwquality module
                The pam_pwquality.so module checks the strength of passwords. It performs checks
                such as making sure a password is not a dictionary word, it is a certain length, contains
                a mix of characters (e.g. alphabet, numeric, other) and more.
                These checks are configurable by either:

                     •   use of the module arguments
                     •   modifying the /etc/security/pwquality.conf configuration file
                     •   creating a .conf file in the /etc/security/pwquality.conf.d/ directory.

                Note: The module arguments override the settings in the
                /etc/security/pwquality.conf configuration file. Settings in the
                /etc/security/pwquality.conf configuration file override settings in a .conf file in
                the /etc/security/pwquality.conf.d/ directory.
                The possible options in the file are:

                     •   difok - Number of characters in the new password that must not be present in
                         the old password. (default 1). The special value of 0 disables all checks of
                         similarity of the new password with the old password except the new password
                         being exactly the same as the old one.
                     •   minlen - Minimum acceptable size for the new password (plus one if credits are
                         not disabled which is the default). (See pam_pwquality(8).) Cannot be set to
                         lower value than 6. (default 8)
                     •   dcredit - The maximum credit for having digits in the new password. If less than
                         0 it is the minimum number of digits in the new password. (default 0)
                     •   ucredit - The maximum credit for having uppercase characters in the new
                         password. If less than 0 it is the minimum number of uppercase characters in the
                         new password. (default 0)
                     •   lcredit - The maximum credit for having lowercase characters in the new
                         password. If less than 0 it is the minimum number of lowercase characters in the
                         new password. (default 0)
                     •   ocredit - The maximum credit for having other characters in the new password.
                         If less than 0 it is the minimum number of other characters in the new password.
                         (default 0)
                     •   minclass - The minimum number of required classes of characters for the new
                         password (digits, uppercase, lowercase, others). (default 0)
                     •   maxrepeat - The maximum number of allowed same consecutive characters in
                         the new password. The check is disabled if the value is 0. (default 0)
                     •   maxsequence - The maximum length of monotonic character sequences in the
                         new password. Examples of such sequence are '12345' or 'fedcb'. Note that most
                         such passwords will not pass the simplicity check unless the sequence is only a
                         minor part of the password. The check is disabled if the value is 0. (default 0)
                     •   maxclassrepeat - The maximum number of allowed consecutive characters of
                         the same class in the new password. The check is disabled if the value is 0.
                         (default 0)

                     •   gecoscheck - If nonzero, check whether the words longer than 3 characters from
                         the GECOS field of the user's passwd(5) entry are contained in the new
                         password. The check is disabled if the value is 0. (default 0)
                     •   dictcheck - If nonzero, check whether the password (with possible
                         modifications) matches a word in a dictionary. Currently the dictionary check is
                         performed

                         using the cracklib library. (default 1)

                     •   usercheck=<N> - If nonzero, check whether the password (with possible
                         modifications) contains the user name in some form. It is not performed for user
                         names

                         shorter than 3 characters. (default 1)

                     •   usersubstr=<N> - If greater than 3 (due to the minimum length in usercheck),
                         check whether the password contains a substring of at least N length in some
                         form.

                         (default 0)

                     •   enforcing=<N> - If nonzero, reject the password if it fails the checks, otherwise
                         only print the warning. This setting applies only to the pam_pwquality module

                         and possibly other applications that explicitly change their behavior based on it. It
                         does not affect pwmake(1) and pwscore(1). (default 1)

                     •   badwords - Space separated list of words that must not be contained in the
                         password. These are additional words to the cracklib dictionary check. This
                         setting can be also used by applications to emulate the gecos check for user
                         accounts that are not created yet.
                     •   dictpath - Path to the cracklib dictionaries. Default is to use the cracklib default.
                     •   retry=<N> - Prompt user at most N times before returning with error. The default
                         is 1.
                     •   enforce_for_root - The module will return error on failed check even if the user
                         changing the password is root. This option is off by default which means that just
                         the message about the failed check is printed but root can change the password
                         anyway. Note that root is not asked for an old password so the checks that
                         compare the old and new password are not performed.
                     •   local_users_only - The module will not test the password quality for users that
                         are not present in the /etc/passwd file. The module still asks for the password so
                         the following modules in the stack can use the use_authtok option. This option is
                         off by default.

                5.3.3.2.1 Ensure password number of changed characters is
                configured (Automated)
                Profile Applicability:

                • Level 1 - Server
                • Level 1 - Workstation

                Description:
                The pwquality difok option sets the number of characters in a password that must not
                be present in the old password.
                Rationale:
                Use of a complex password helps to increase the time and resources required to
                compromise the password. Password complexity, or strength, is a measure of the
                effectiveness of a password in resisting attempts at guessing and brute-force attacks.
                Password complexity is one factor of several that determines how long it takes to crack
                a password. The more complex the password, the greater the number of possible
                combinations that need to be tested before the password is compromised.
                Audit:
                Run the following command to verify that the difok option is set to 2 or more and
                follows local site policy:
                # grep -Psi -- '^\h*difok\h*=\h*([2-9]|[1-9][0-9]+)\b'
                /etc/security/pwquality.conf /etc/security/pwquality.conf.d/*.conf
                Example output:
                /etc/security/pwquality.conf.d/50-pwdifok.conf:difok = 2
                Verify returned value(s) are 2 or more and meet local site policy
                Run the following command to verify that difok is not set, is 2 or more, and conforms to
                local site policy:
                # grep -Psi --
                '^\h*password\h+(requisite|required|sufficient)\h+pam_pwquality\.so\h+([^#\n\
                r]+\h+)?difok\h*=\h*([0-1])\b' /etc/pam.d/common-password
                Nothing should be returned

                Note:
                     •   settings should be configured in only one location for clarity
                     •   Settings observe an order of precedence:
                              o module arguments override the settings in the
                                  /etc/security/pwquality.conf configuration file
                              o settings in the /etc/security/pwquality.conf configuration file
                                  override settings in a .conf file in the
                                  /etc/security/pwquality.conf.d/ directory
                              o settings in a .conf file in the /etc/security/pwquality.conf.d/
                                  directory are read in canonical order, with last read file containing the
                                  setting taking precedence
                     •   It is recommended that settings be configured in a .conf file in the
                         /etc/security/pwquality.conf.d/ directory for clarity, convenience, and
                         durability.
                Remediation:
                Create or modify a file ending in .conf in the /etc/security/pwquality.conf.d/
                directory or the file /etc/security/pwquality.conf and add or modify the following
                line to set difok to 2 or more. Ensure setting conforms to local site policy:
                Example:
                #!/usr/bin/env bash

                {
                   sed -ri 's/^\s*difok\s*=/# &/' /etc/security/pwquality.conf
                   [ ! -d /etc/security/pwquality.conf.d/ ] && mkdir
                /etc/security/pwquality.conf.d/
                   printf '\n%s' "difok = 2" > /etc/security/pwquality.conf.d/50-pwdifok.conf
                }
                Run the following command:
                # grep -Pl -- '\bpam_pwquality\.so\h+([^#\n\r]+\h+)?difok\b' /usr/share/pam-
                configs/*
                Edit any returned files and remove the difok argument from the pam_pwquality.so
                line(s):
                Default Value:
                difok = 1
                References:

                     1. NIST SP 800-53 Rev. 5: IA-5

                CIS Controls:

                   Controls
                                                                 Control                                            IG 1 IG 2 IG 3
                   Version
                              5.2 Use Unique Passwords
                       v8         Use unique passwords for all enterprise assets. Best practice implementation
                              includes, at a minimum, an 8-character password for accounts using MFA and a
                                                                                                                     ●    ●    ●
                              14-character password for accounts not using MFA.
                              4.4 Use Unique Passwords
                       v7        Where multi-factor authentication is not supported (such as local administrator,
                              root, or service accounts), accounts will use passwords that are unique to that
                                                                                                                          ●    ●
                              system.
  '5.3.3.1.3': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '5.3.3.2.1': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '5.3.3.2.2': !unsafe |-
    Create or modify a file ending in .conf in the /etc/security/pwquality.conf.d/
                directory or the file /etc/security/pwquality.conf and add or modify the following
                line to set password length of 14 or more characters. Ensure that password length
                conforms to local site policy:
                Example:
                #!/usr/bin/env bash

                {
                   sed -ri 's/^\s*minlen\s*=/# &/' /etc/security/pwquality.conf
                   [ ! -d /etc/security/pwquality.conf.d/ ] && mkdir
                /etc/security/pwquality.conf.d/
                   printf '\n%s' "minlen = 14" > /etc/security/pwquality.conf.d/50-
                pwlength.conf
                }
                Run the following command:
                # grep -Pl -- '\bpam_pwquality\.so\h+([^#\n\r]+\h+)?minlen\b' /usr/share/pam-
                configs/*
                Edit any returned files and remove the minlen argument from the pam_pwquality.so
                line(s):
                Default Value:
                minlen = 8
                References:

                     1. pam_pwquality(8)
                     2. CIS Password Policy Guide
                     3. NIST SP 800-53 Rev. 5: IA-5(1)

                CIS Controls:

                    Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                    Version
                               5.2 Use Unique Passwords
                       v8          Use unique passwords for all enterprise assets. Best practice implementation
                               includes, at a minimum, an 8-character password for accounts using MFA and a
                                                                                                                      ●    ●    ●
                               14-character password for accounts not using MFA.
                               4.4 Use Unique Passwords
                       v7         Where multi-factor authentication is not supported (such as local administrator,
                               root, or service accounts), accounts will use passwords that are unique to that
                                                                                                                           ●    ●
                               system.
  '5.3.3.2.3': !unsafe |-
    Note: CIS password complexity requirements may differ from other frameworks
                or policies, adherence to site-specific policy is imperative.
                Run the following command:
                # grep -Pl --
                '\bpam_pwquality\.so\h+([^#\n\r]+\h+)?(minclass|[dulo]credit)\b'
                /usr/share/pam-configs/*
                Edit any returned files and remove the minclass, dcredit, ucredit, lcredit, and
                ocredit arguments from the pam_pwquality.so line(s)
                Create or modify a file ending in .conf in the /etc/security/pwquality.conf.d/
                directory or the file /etc/security/pwquality.conf and add or modify the following
                line(s) to set complexity according to local site policy:

                     •   minclass = _N_
                     •   dcredit = _N_ # Value should be either 0 or a number proceeded by a minus (-
                         ) symbol
                     •   ucredit = -1 # Value should be either 0 or a number proceeded by a minus (-)
                         symbol
                     •   ocredit = -1 # Value should be either 0 or a number proceeded by a minus (-)
                         symbol
                     •   lcredit = -1 # Value should be either 0 or a number proceeded by a minus (-)
                         symbol

                Example 1 - Set minclass = 3:
                #!/usr/bin/env bash

                {
                   sed -ri 's/^\s*minclass\s*=/# &/' /etc/security/pwquality.conf
                   sed -ri 's/^\s*[dulo]credit\s*=/# &/' /etc/security/pwquality.conf
                   [ ! -d /etc/security/pwquality.conf.d/ ] && mkdir
                /etc/security/pwquality.conf.d/
                   printf '\n%s' "minclass = 3" > /etc/security/pwquality.conf.d/50-
                pwcomplexity.conf
                }
                Example 2 - set dcredit = -1, ucredit = -1, and lcredit = -1:
                #!/usr/bin/env bash

                {
                   sed -ri 's/^\s*minclass\s*=/# &/' /etc/security/pwquality.conf
                   sed -ri 's/^\s*[dulo]credit\s*=/# &/' /etc/security/pwquality.conf
                   [ ! -d /etc/security/pwquality.conf.d/ ] && mkdir
                /etc/security/pwquality.conf.d/
                   printf '%s\n' "dcredit = -1" "ucredit = -1" "lcredit = -1" >
                /etc/security/pwquality.conf.d/50-pwcomplexity.conf
                }

                Default Value:
                minclass = 0
                dcredit = 0
                ucredit = 0
                ocredit = 0
                lcredit = 0
                References:

                     1. pam_pwquality(8)
                     2. PWQUALITY.CONF(5)
                     3. https://www.cisecurity.org/insights/white-papers/cis-password-policy-guide
                     4. NIST SP 800-53 Rev. 5: IA-5

                CIS Controls:

                   Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                   Version
                               5.2 Use Unique Passwords
                       v8          Use unique passwords for all enterprise assets. Best practice implementation
                               includes, at a minimum, an 8-character password for accounts using MFA and a
                                                                                                                      ●    ●    ●
                               14-character password for accounts not using MFA.
                               4.4 Use Unique Passwords
                       v7         Where multi-factor authentication is not supported (such as local administrator,
                               root, or service accounts), accounts will use passwords that are unique to that
                                                                                                                           ●    ●
                               system.

                5.3.3.2.4 Ensure password same consecutive characters is
                configured (Automated)
                Profile Applicability:

                • Level 1 - Server
                • Level 1 - Workstation

                Description:
                The pwquality maxrepeat option sets the maximum number of allowed same
                consecutive characters in a new password.
                Rationale:
                Use of a complex password helps to increase the time and resources required to
                compromise the password. Password complexity, or strength, is a measure of the
                effectiveness of a password in resisting attempts at guessing and brute-force attacks.
                Password complexity is one factor of several that determines how long it takes to crack
                a password. The more complex the password, the greater the number of possible
                combinations that need to be tested before the password is compromised.
                Audit:
                Run the following command to verify that the maxrepeat option is set to 3 or less, not 0,
                and follows local site policy:
                # grep -Psi -- '^\h*maxrepeat\h*=\h*[1-3]\b' /etc/security/pwquality.conf
                /etc/security/pwquality.conf.d/*.conf
                Example output:
                /etc/security/pwquality.conf.d/50-pwrepeat.conf:maxrepeat = 3
                Verify returned value(s) are 3 or less, not 0, and meet local site policy
                Run the following command to verify that maxrepeat is not set, is 3 or less, not 0, and
                conforms to local site policy:
                # grep -Psi --
                '^\h*password\h+(requisite|required|sufficient)\h+pam_pwquality\.so\h+([^#\n\
                r]+\h+)?maxrepeat\h*=\h*(0|[4-9]|[1-9][0-9]+)\b' /etc/pam.d/common-password
                Nothing should be returned

                Note:

                     •   settings should be configured in only one location for clarity
                     •   Settings observe an order of precedence:
                              o module arguments override the settings in the
                                  /etc/security/pwquality.conf configuration file
                              o settings in the /etc/security/pwquality.conf configuration file
                                  override settings in a .conf file in the
                                  /etc/security/pwquality.conf.d/ directory
                              o settings in a .conf file in the /etc/security/pwquality.conf.d/
                                  directory are read in canonical order, with last read file containing the
                                  setting taking precedence
                     •   It is recommended that settings be configured in a .conf file in the
                         /etc/security/pwquality.conf.d/ directory for clarity, convenience, and
                         durability.

                Remediation:
                Create or modify a file ending in .conf in the /etc/security/pwquality.conf.d/
                directory or the file /etc/security/pwquality.conf and add or modify the following
                line to set maxrepeat to 3 or less and not 0. Ensure setting conforms to local site policy:
                Example:
                #!/usr/bin/env bash

                {
                   sed -ri 's/^\s*maxrepeat\s*=/# &/' /etc/security/pwquality.conf
                   [ ! -d /etc/security/pwquality.conf.d/ ] && mkdir
                /etc/security/pwquality.conf.d/
                   printf '\n%s' "maxrepeat = 3" > /etc/security/pwquality.conf.d/50-
                pwrepeat.conf
                }
                Run the following command:
                # grep -Pl -- '\bpam_pwquality\.so\h+([^#\n\r]+\h+)?maxrepeat\b'
                /usr/share/pam-configs/*
                Edit any returned files and remove the maxrepeat argument from the
                pam_pwquality.so line(s):

                Default Value:
                maxrepeat = 0
                References:

                     1. NIST SP 800-53 Rev. 5: IA-5

                CIS Controls:

                   Controls
                                                                 Control                                            IG 1 IG 2 IG 3
                   Version
                              5.2 Use Unique Passwords
                       v8         Use unique passwords for all enterprise assets. Best practice implementation
                              includes, at a minimum, an 8-character password for accounts using MFA and a
                                                                                                                     ●    ●    ●
                              14-character password for accounts not using MFA.
                              4.4 Use Unique Passwords
                       v7        Where multi-factor authentication is not supported (such as local administrator,
                              root, or service accounts), accounts will use passwords that are unique to that
                                                                                                                          ●    ●
                              system.

                5.3.3.2.5 Ensure password maximum sequential characters is
                configured (Automated)
                Profile Applicability:

                • Level 1 - Server
                • Level 1 - Workstation

                Description:
                The pwquality maxsequence option sets the maximum length of monotonic character
                sequences in the new password. Examples of such sequence are 12345 or fedcb. The
                check is disabled if the value is 0.
                Note: Most such passwords will not pass the simplicity check unless the sequence is
                only a minor part of the password.
                Rationale:
                Use of a complex password helps to increase the time and resources required to
                compromise the password. Password complexity, or strength, is a measure of the
                effectiveness of a password in resisting attempts at guessing and brute-force attacks.
                Password complexity is one factor of several that determines how long it takes to crack
                a password. The more complex the password, the greater the number of possible
                combinations that need to be tested before the password is compromised.
                Audit:
                Run the following command to verify that the maxsequence option is set to 3 or less, not
                0, and follows local site policy:
                # grep -Psi -- '^\h*maxsequence\h*=\h*[1-3]\b' /etc/security/pwquality.conf
                /etc/security/pwquality.conf.d/*.conf
                Example output:
                /etc/security/pwquality.conf.d/50-pwmaxsequence.conf:maxsequence = 3
                Verify returned value(s) are 3 or less, not 0, and meet local site policy
                Run the following command to verify that maxsequence is not set, is 3 or less, not 0, and
                conforms to local site policy:
                # grep -Psi --
                '^\h*password\h+(requisite|required|sufficient)\h+pam_pwquality\.so\h+([^#\n\
                r]+\h+)?maxsequence\h*=\h*(0|[4-9]|[1-9][0-9]+)\b' /etc/pam.d/common-password
                Nothing should be returned

                Note:

                     •   settings should be configured in only one location for clarity
                     •   Settings observe an order of precedence:
                              o module arguments override the settings in the
                                  /etc/security/pwquality.conf configuration file
                              o settings in the /etc/security/pwquality.conf configuration file
                                  override settings in a .conf file in the
                                  /etc/security/pwquality.conf.d/ directory
                              o settings in a .conf file in the /etc/security/pwquality.conf.d/
                                  directory are read in canonical order, with last read file containing the
                                  setting taking precedence
                     •   It is recommended that settings be configured in a .conf file in the
                         /etc/security/pwquality.conf.d/ directory for clarity, convenience, and
                         durability.

                Remediation:
                Create or modify a file ending in .conf in the /etc/security/pwquality.conf.d/
                directory or the file /etc/security/pwquality.conf and add or modify the following
                line to set maxsequence to 3 or less and not 0. Ensure setting conforms to local site
                policy:
                Example:
                #!/usr/bin/env bash

                {
                   sed -ri 's/^\s*maxsequence\s*=/# &/' /etc/security/pwquality.conf
                   [ ! -d /etc/security/pwquality.conf.d/ ] && mkdir
                /etc/security/pwquality.conf.d/
                   printf '\n%s' "maxsequence = 3" > /etc/security/pwquality.conf.d/50-
                pwmaxsequence.conf
                }
                Run the following command:
                # grep -Pl -- '\bpam_pwquality\.so\h+([^#\n\r]+\h+)?maxsequence\b'
                /usr/share/pam-configs/*
                Edit any returned files and remove the maxsequence argument from the
                pam_pwquality.so line(s):

                Default Value:
                maxsequence = 0
                References:

                     1. NIST SP 800-53 Rev. 5: IA-5

                CIS Controls:

                   Controls
                                                                 Control                                            IG 1 IG 2 IG 3
                   Version
                              5.2 Use Unique Passwords
                       v8         Use unique passwords for all enterprise assets. Best practice implementation
                              includes, at a minimum, an 8-character password for accounts using MFA and a
                                                                                                                     ●    ●    ●
                              14-character password for accounts not using MFA.
                              4.4 Use Unique Passwords
                       v7        Where multi-factor authentication is not supported (such as local administrator,
                              root, or service accounts), accounts will use passwords that are unique to that
                                                                                                                          ●    ●
                              system.
  '5.3.3.2.4': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '5.3.3.2.5': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '5.3.3.2.6': !unsafe |-
    Edit any file ending in .conf in the /etc/security/pwquality.conf.d/ directory
                and/or the file /etc/security/pwquality.conf and comment out or remove any
                instance of dictcheck = 0:
                Example:
                # sed -ri 's/^\s*dictcheck\s*=/# &/' /etc/security/pwquality.conf
                /etc/security/pwquality.conf.d/*.conf
                Run the following command:
                # grep -Pl -- '\bpam_pwquality\.so\h+([^#\n\r]+\h+)?dictcheck\b'
                /usr/share/pam-configs/*
                Edit any returned files and remove the dictcheck argument from the
                pam_pwquality.so line(s)

                Default Value:
                dictcheck = 1
                References:

                     1. NIST SP 800-53 Rev. 5: IA-5

                CIS Controls:

                   Controls
                                                                   Control                                            IG 1 IG 2 IG 3
                   Version
                                5.2 Use Unique Passwords
                       v8           Use unique passwords for all enterprise assets. Best practice implementation
                                includes, at a minimum, an 8-character password for accounts using MFA and a
                                                                                                                       ●    ●    ●
                                14-character password for accounts not using MFA.
                                4.4 Use Unique Passwords
                       v7          Where multi-factor authentication is not supported (such as local administrator,
                                root, or service accounts), accounts will use passwords that are unique to that
                                                                                                                            ●    ●
                                system.
  '5.3.3.2.7': !unsafe |-
    Run the following command:
                # grep -Pl -- '\bpam_pwquality\.so\h+([^#\n\r]+\h+)?enforcing=0\b'
                /usr/share/pam-configs/*
                Edit any returned files and remove the enforcing=0 argument from the
                pam_pwquality.so line(s)
                Edit /etc/security/pwquality.conf and all files ending in .conf in the
                /etc/security/pwquality.conf.d/ directory and remove or comment out any line
                containing the enforcing = 0 argument:
                Example:
                # sed -ri 's/^\s*enforcing\s*=\s*0/# &/' /etc/security/pwquality.conf
                /etc/security/pwquality.conf.d/*.conf

                Default Value:
                enforcing=1
                References:

                     1. pam_pwquality(8)
                     2. PWQUALITY.CONF(5)
                     3. NIST SP 800-53 Rev. 5: IA-5

                CIS Controls:

                   Controls
                                                                 Control                                            IG 1 IG 2 IG 3
                   Version
                              5.2 Use Unique Passwords
                       v8         Use unique passwords for all enterprise assets. Best practice implementation
                              includes, at a minimum, an 8-character password for accounts using MFA and a
                                                                                                                     ●    ●    ●
                              14-character password for accounts not using MFA.
                              4.4 Use Unique Passwords
                       v7        Where multi-factor authentication is not supported (such as local administrator,
                              root, or service accounts), accounts will use passwords that are unique to that
                                                                                                                          ●    ●
                              system.
  '5.3.3.2.8': !unsafe |-
    Edit or add the following line in a *.conf file in /etc/security/pwquality.conf.d or
                in /etc/security/pwquality.conf:
                Example:
                #!/usr/bin/env bash

                {
                   [ ! -d /etc/security/pwquality.conf.d/ ] && mkdir
                /etc/security/pwquality.conf.d/
                   printf '\n%s\n' "enforce_for_root" > /etc/security/pwquality.conf.d/50-
                pwroot.conf
                }

                Default Value:
                disabled
                References:

                     1. NIST SP 800-53 Rev. 5: IA-5

                CIS Controls:

                    Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                    Version
                               5.2 Use Unique Passwords
                       v8          Use unique passwords for all enterprise assets. Best practice implementation
                               includes, at a minimum, an 8-character password for accounts using MFA and a
                                                                                                                      ●    ●    ●
                               14-character password for accounts not using MFA.
                               4.4 Use Unique Passwords
                       v7         Where multi-factor authentication is not supported (such as local administrator,
                               root, or service accounts), accounts will use passwords that are unique to that
                                                                                                                           ●    ●
                               system.

                5.3.3.3 Configure pam_pwhistory module
                pam_history module - This PAM module saves the last passwords for each user in
                order to force password change history and keep the user from alternating between the
                same password too frequently.
                This module does not work together with kerberos. In general, it does not make much
                sense to use this module in conjunction with NIS or LDAP, since the old passwords are
                stored on the local machine and are not available on another machine for password
                history checking
                pwhistory.conf provides a way to configure the default settings for saving the
                last passwords for each user. This file is read by the pam_pwhistory module and
                is the preferred method over configuring pam_pwhistory directly.
                The file has a very simple name = value format with possible comments starting with #
                character. The whitespace at the beginning of line, end of line, and around the = sign is
                ignored.
                Options:

                     •   debug - Turns on debugging via syslog(3).
                     •   use_authtok - When password changing enforce the module to use the new
                         password provided by a previously stacked password module (this is used in the
                         example of the stacking of the pam_passwdqc module documented below).
                     •   enforce_for_root - If this option is set, the check is enforced for root, too.
                     •   remember=<N> - The last <N> passwords for each user are saved. The default is
                         10. Value of 0 makes the module to keep the existing contents of the opasswd
                         file unchanged.
                     •   retry=<N> - Prompt user at most <N> times before returning with error. The
                         default is 1.
                     •   authtok_type=<STRING> - See pam_get_authtok(3) for more details.

                Examples:
                /etc/security/pwhistory.conf file example:
                debug
                remember=5
                file=/tmp/opasswd
                Note: The options specified on the pwhistory.so module command line override the
                values from the pwhistory.conf configuration file. The pwhistory.conf file is the
                preferred method over configuring pam_pwhistory directly.
  '5.3.3.3.1': !unsafe |-
    Note: The options specified on the pwhistory.so module command line
                         override the values from the pwhistory.conf configuration file. The
                         pwhistory.conf file is the preferred method over configuring
                         pam_pwhistory directly.
                Edit or add the remember option in /etc/security/pwhistory.conf setting it to 24 or
                more depending on local site policy:
                remember = 24
                - IF - The pam_pwhistory profile in /usr/share/pam-configs/ has been used to
                configure pwhistory. Run the following script to remove the remember= argument from
                the pam_pwhistory.so line in the Password section:
                #!/usr/bin/env bash

                {
                profiles=$(grep -Rl "pam_pwhistory.so" /usr/share/pam-configs/ || true)
                for profile in $profiles; do
                     if grep -Pq 'remember\s*=\s*\d+' "$profile"; then
                        sed -Ei 's/remember\s*=\s*[0-9]+//g' "$profile"
                        sed -Ei 's/[[:space:]]+/ /g' "$profile"
                        echo "Updated pam profile: $profile"
                        pam-auth-update --package < /dev/null
                     fi
                done
                }

                References:

                     1. NIST SP 800-53 Rev. 5: IA-5(1)
                     2. https://manpages.debian.org/trixie/libpam-runtime/pwhistory.conf.5.en.html
                     3. https://manpages.debian.org/trixie/libpam-runtime/pam_pwhistory.8.en.html

                CIS Controls:

                   Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                   Version
                               5.2 Use Unique Passwords
                       v8          Use unique passwords for all enterprise assets. Best practice implementation
                               includes, at a minimum, an 8-character password for accounts using MFA and a
                                                                                                                      ●    ●    ●
                               14-character password for accounts not using MFA.
                               4.4 Use Unique Passwords
                       v7         Where multi-factor authentication is not supported (such as local administrator,
                               root, or service accounts), accounts will use passwords that are unique to that
                                                                                                                           ●    ●
                               system.
  '5.3.3.3.2': !unsafe |-
    Edit or add the following line in /etc/security/pwhistory.conf:
                enforce_for_root
                - IF - The pam_pwhistory profile in /usr/share/pam-configs/ has been used to
                configure pwhistory. Run the following script to remove the enforce_for_root
                argument from the pam_pwhistory.so line in the Password section:
                #!/usr/bin/env bash

                {
                profiles=$(grep -Rl "pam_pwhistory.so" /usr/share/pam-configs/ || true)
                for profile in $profiles; do
                     if grep -Pq '\h+pam_pwhistory\.so\h+([^#\n\r]+\h+)?enforce_for_root\b'
                "$profile"; then
                        sed -Ei '/pam_pwhistory\.so/s/\enforce_for_root\b//g' "$profile"
                        sed -Ei 's/[[:space:]]+/ /g' "$profile"
                        echo "Updated pam profile: $profile"
                        pam-auth-update --package < /dev/null
                     fi
                done
                }

                         Note: The options specified on the pwhistory.so module command line
                         override the values from the pwhistory.conf configuration file. The
                         pwhistory.conf file is the preferred method over configuring
                         pam_pwhistory directly.

                Default Value:
                disabled
                References:

                     1. NIST SP 800-53 Rev. 5: IA-5

                CIS Controls:

                   Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                   Version
                               5.2 Use Unique Passwords
                       v8          Use unique passwords for all enterprise assets. Best practice implementation
                               includes, at a minimum, an 8-character password for accounts using MFA and a
                                                                                                                      ●    ●    ●
                               14-character password for accounts not using MFA.
                               4.4 Use Unique Passwords
                       v7         Where multi-factor authentication is not supported (such as local administrator,
                               root, or service accounts), accounts will use passwords that are unique to that
                                                                                                                           ●    ●
                               system.
  '5.3.3.3.3': !unsafe |-
    Edit or add the following line in /etc/security/pwhistory.conf:
                use_authtok
                - OR/IF - The pam_pwhistory profile in /usr/share/pam-configs/ has been used to
                configure pwhistory. Run the following script to remove the use_authtok argument to
                the pam_pwhistory line in the Password section:
                #!/usr/bin/env bash

                {
                profiles=$(grep -Rl "pam_pwhistory.so" /usr/share/pam-configs/ || true)
                for profile in $profiles; do
                     if grep -Pq '\h+pam_pwhistory\.so\h+([^#\n\r]+\h+)?use_authtok\b'
                "$profile"; then
                        sed -Ei '/pam_pwhistory\.so/s/\use_authtok\b//g' "$profile"
                        sed -Ei 's/[[:space:]]+/ /g' "$profile"
                        echo "Updated pam profile: $profile"
                        pam-auth-update --package < /dev/null
                     fi
                done
                }

                           Note: The options specified on the pwhistory.so module command line
                           override the values from the pwhistory.conf configuration file. The
                           pwhistory.conf file is the preferred method over configuring
                           pam_pwhistory directly.

                References:

                     1. NIST SP 800-53 Rev. 5: IA-5

                CIS Controls:

                  Controls
                                                                    Control                                           IG 1 IG 2 IG 3
                  Version
                               3.11 Encrypt Sensitive Data at Rest
                                  Encrypt sensitive data at rest on servers, applications, and databases containing
                               sensitive data. Storage-layer encryption, also known as server-side encryption,
                      v8       meets the minimum requirement of this Safeguard. Additional encryption methods               ●    ●
                               may include application-layer encryption, also known as client-side encryption,
                               where access to the data storage device(s) does not permit access to the plain-text
                               data.

                      v7       16.4 Encrypt or Hash all Authentication Credentials                                          ●    ●
                                  Encrypt or hash with a salt all authentication credentials when stored.

                5.3.3.4 Configure pam_unix module
                The pam_unix.so module is the standard Unix authentication module. It uses standard
                calls from the system's libraries to retrieve and set account information as well as
                authentication. Usually this is obtained from the /etc/passwd and the /etc/shadow file
                as well if shadow is enabled.
  '5.3.3.4.1': !unsafe |-
    Run the following command:
                # grep -PH -- '^\h*([^#\n\r]+\h+)?pam_unix\.so\h+([^#\n\r]+\h+)?nullok\b'
                /usr/share/pam-configs/*
                Edit any files returned and remove the nullok argument for the pam_unix lines
                Example File:
                Name: Unix authentication
                Default: yes
                Priority: 256
                Auth-Type: Primary
                Auth:
                         [success=end default=ignore]    pam_unix.so try_first_pass # <-
                **ensure line does not include nullok nullok**
                Auth-Initial:
                         [success=end default=ignore]    pam_unix.so # <- **ensure line does
                not include nullok nullok**
                Account-Type: Primary
                Account:
                         [success=end new_authtok_reqd=done default=ignore]      pam_unix.so
                Account-Initial:
                         [success=end new_authtok_reqd=done default=ignore]      pam_unix.so
                Session-Type: Additional
                Session:
                         required        pam_unix.so
                Session-Initial:
                         required        pam_unix.so
                Password-Type: Primary
                Password:
                         [success=end default=ignore]    pam_unix.so obscure use_authtok
                try_first_pass yescrypt
                Password-Initial:
                         [success=end default=ignore]    pam_unix.so obscure yescrypt
                Run the following command to update the files in the /etc/pam.d/ directory:
                # pam-auth-update --enable <EDITED_PROFILE_NAME>
                Example:
                # pam-auth-update --enable unix
                Note: If custom files are being used, the corresponding files in /etc/pam.d/ would
                need to be edited directly, and the pam-auth-update --enable
                <EDITED_PROFILE_NAME> command skipped

                CIS Controls:

                   Controls
                                                                 Control                                            IG 1 IG 2 IG 3
                   Version
                              5.2 Use Unique Passwords
                       v8         Use unique passwords for all enterprise assets. Best practice implementation
                              includes, at a minimum, an 8-character password for accounts using MFA and a
                                                                                                                     ●    ●    ●
                              14-character password for accounts not using MFA.
                              4.4 Use Unique Passwords
                       v7        Where multi-factor authentication is not supported (such as local administrator,
                              root, or service accounts), accounts will use passwords that are unique to that
                                                                                                                          ●    ●
                              system.
  '5.3.3.4.2': !unsafe |-
    Run the following command:
                # grep -PH -- '^\h*([^#\n\r]+\h+)?pam_unix\.so\h+([^#\n\r]+\h+)?remember\b'
                /usr/share/pam-configs/*
                Edit any files returned and remove the remember=_<N>_ argument for the pam_unix
                lines
                Example output:
                [success=end default=ignore]   pam_unix.so obscure use_authtok try_first_pass
                yescrypt remember=5 # **<- remove remember=<N>**
                [success=end default=ignore]   pam_unix.so obscure yescrypt remember=5 # **<-
                remove remember=<N>**

                Run the following command to update the files in the /etc/pam.d/ directory:
                # pam-auth-update --enable <EDITED_PROFILE_NAME>
                Example:
                # pam-auth-update --enable unix
                Note: If custom files are being used, the corresponding files in /etc/pam.d/ would
                need to be edited directly, and the pam-auth-update --enable
                <EDITED_PROFILE_NAME> command skipped

                CIS Controls:

                   Controls
                                                                 Control                                            IG 1 IG 2 IG 3
                   Version
                              5.2 Use Unique Passwords
                       v8         Use unique passwords for all enterprise assets. Best practice implementation
                              includes, at a minimum, an 8-character password for accounts using MFA and a
                                                                                                                     ●    ●    ●
                              14-character password for accounts not using MFA.
                              4.4 Use Unique Passwords
                       v7        Where multi-factor authentication is not supported (such as local administrator,
                              root, or service accounts), accounts will use passwords that are unique to that
                                                                                                                          ●    ●
                              system.

                5.3.3.4.3 Ensure pam_unix includes a strong password hashing
                algorithm (Automated)
                Profile Applicability:

                • Level 1 - Server
                • Level 1 - Workstation

                Description:
                A cryptographic hash function converts an arbitrary-length input into a fixed length
                output. Password hashing performs a one-way transformation of a password, turning
                the password into another string, called the hashed password.
                The pam_unix module can be configured to use one of the following hashing algorithms
                for user's passwords:

                     •   md5 - When a user changes their password next, encrypt it with the MD5
                         algorithm.
                     •   bigcrypt - When a user changes their password next, encrypt it with the DEC C2
                         algorithm.
                     •   sha256 - When a user changes their password next, encrypt it with the SHA256
                         algorithm. The SHA256 algorithm must be supported by the crypt(3) function.
                     •   sha512 - When a user changes their password next, encrypt it with the SHA512
                         algorithm. The SHA512 algorithm must be supported by the crypt(3) function.
                     •   blowfish - When a user changes their password next, encrypt it with the
                         blowfish algorithm. The blowfish algorithm must be supported by the crypt(3)
                         function.
                     •   gost_yescrypt - When a user changes their password next, encrypt it with the
                         gost-yescrypt algorithm. The gost-yescrypt algorithm must be supported by
                         the crypt(3) function.
                     •   yescrypt - When a user changes their password next, encrypt it with the
                         yescrypt algorithm. The yescrypt algorithm must be supported by the crypt(3)
                         function.

                Rationale:
                The SHA-512 and yescrypt algorithms provide a stronger hash than other algorithms
                used by Linux for password hash generation. A stronger hash provides additional
                protection to the system by increasing the level of effort needed for an attacker to
                successfully determine local user passwords.
                Note: These changes only apply to the local system.

                Audit:
                Run the following command to verify that a strong password hashing algorithm is set on
                the pam_unix.so module:
                # grep -PH --
                '^\h*password\h+([^#\n\r]+)\h+pam_unix\.so\h+([^#\n\r]+\h+)?(sha512|yescrypt)
                \b' /etc/pam.d/common-password
                Output should be similar to:
                /etc/pam.d/common-password:password   [success=1 default=ignore]
                pam_unix.so obscure use_authtok try_first_pass yescrypt
                Verify that the line(s) include either sha512 - OR - yescrypt

                Remediation:
                Run the following command:
                # awk '/Password-Type:/{ f = 1;next } /-Type:/{ f = 0 } f {if
                (/pam_unix\.so/) print FILENAME}' /usr/share/pam-configs/*
                Edit any returned files and edit or add a strong hashing algorithm, either sha512 or
                yescrypt, that meets local site policy to the pam_unix lines in the Password section:
                Example File:
                Name: Unix authentication
                Default: yes
                Priority: 256
                Auth-Type: Primary # <- Start of "Auth" section
                Auth:
                         [success=end default=ignore]    pam_unix.so try_first_pass
                Auth-Initial:
                         [success=end default=ignore]    pam_unix.so
                Account-Type: Primary # <- Start of "Account" section
                Account:
                         [success=end new_authtok_reqd=done default=ignore]      pam_unix.so
                Account-Initial:
                         [success=end new_authtok_reqd=done default=ignore]      pam_unix.so
                Session-Type: Additional # <- Start of "Session" section
                Session:
                         required        pam_unix.so
                Session-Initial:
                         required        pam_unix.so
                Password-Type: Primary # <- Start of "Password" section
                Password:
                         [success=end default=ignore]    pam_unix.so obscure use_authtok
                try_first_pass yescrypt # <- **ensure hashing algorithm is either sha512 or
                yescrypt**
                Password-Initial:
                         [success=end default=ignore]    pam_unix.so obscure yescrypt # <-
                **ensure hashing algorithm is either sha512 or yescrypt**
                Run the following command to update the files in the /etc/pam.d/ directory:
                # pam-auth-update --enable <MODIFIED_PROFILE_NAME>
                Example:
                # pam-auth-update --enable unix

                References:

                     1. NIST SP 800-53 Rev. 5: IA-5

                Additional Information:
                The following command may be used to expire all non-system user ID's immediately
                and force them to change their passwords on next login. Any system accounts that
                need to be expired should be carefully done separately by the system administrator to
                prevent any potential problems.
                # awk -F: '( $3<'"$(awk '/^\s*UID_MIN/{print $2}' /etc/login.defs)"' && $1 !=
                "nfsnobody" ) { print $1 }' /etc/passwd | xargs -n 1 chage -d 0

                CIS Controls:

                  Controls
                                                                   Control                                           IG 1 IG 2 IG 3
                  Version
                              3.11 Encrypt Sensitive Data at Rest
                                 Encrypt sensitive data at rest on servers, applications, and databases containing
                              sensitive data. Storage-layer encryption, also known as server-side encryption,
                      v8      meets the minimum requirement of this Safeguard. Additional encryption methods               ●    ●
                              may include application-layer encryption, also known as client-side encryption,
                              where access to the data storage device(s) does not permit access to the plain-text
                              data.

                      v7      16.4 Encrypt or Hash all Authentication Credentials                                          ●    ●
                                 Encrypt or hash with a salt all authentication credentials when stored.
  '5.3.3.4.3': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '5.3.3.4.4': !unsafe |-
    Run the following command:
                # awk '/Password-Type:/{ f = 1;next } /-Type:/{ f = 0 } f {if
                (/pam_unix\.so/) print FILENAME}' /usr/share/pam-configs/*
                Edit any returned files add use_authtok to the pam_unix line in the Password section
                under Password: subsection:
                Note: The if the file's Password section includes a Password-Initial: subsection,
                use_authtok should not be added to the pam_unix line in the Password-Initial:
                subsection
                Example File:
                Name: Unix authentication
                Default: yes
                Priority: 256
                Auth-Type: Primary # <- Start of "Auth" section
                Auth:
                         [success=end default=ignore]    pam_unix.so try_first_pass
                Auth-Initial:
                         [success=end default=ignore]    pam_unix.so
                Account-Type: Primary # <- Start of "Account" section
                Account:
                         [success=end new_authtok_reqd=done default=ignore]      pam_unix.so
                Account-Initial:
                         [success=end new_authtok_reqd=done default=ignore]      pam_unix.so
                Session-Type: Additional # <- Start of "Session" section
                Session:
                         required        pam_unix.so
                Session-Initial:
                         required        pam_unix.so
                Password-Type: Primary # <- Start of "Password" section
                Password:
                         [success=end default=ignore]    pam_unix.so obscure use_authtok
                try_first_pass yescrypt # <- **ensure line includes use_authtok**
                Password-Initial:
                         [success=end default=ignore]    pam_unix.so obscure yescrypt # <-
                **Password-Initial: subsection does not include use_authtok
                Run the following command to update the files in the /etc/pam.d/ directory:
                # pam-auth-update --enable <MODIFIED_PROFILE_NAME>
                Example:
                # pam-auth-update --enable unix

                References:

                     1. NIST SP 800-53 Rev. 5: IA-5

                CIS Controls:

                  Controls
                                                                   Control                                           IG 1 IG 2 IG 3
                  Version
                              3.11 Encrypt Sensitive Data at Rest
                                 Encrypt sensitive data at rest on servers, applications, and databases containing
                              sensitive data. Storage-layer encryption, also known as server-side encryption,
                      v8      meets the minimum requirement of this Safeguard. Additional encryption methods               ●    ●
                              may include application-layer encryption, also known as client-side encryption,
                              where access to the data storage device(s) does not permit access to the plain-text
                              data.

                      v7      16.4 Encrypt or Hash all Authentication Credentials                                          ●    ●
                                 Encrypt or hash with a salt all authentication credentials when stored.

                5.4 User Accounts and Environment

                This section provides guidance on setting up secure defaults for system and user
                accounts and their environment.

                5.4.1 Configure shadow password suite parameters

                While a majority of the password control parameters have been moved to PAM, some
                parameters are still available through the shadow password suite. Any changes made to
                /etc/login.defs will only be applied if the usermod command is used. If user IDs are
                added a different way, use the chage command to effect changes to individual user IDs.
  '5.4.1.1': !unsafe |-
    Set the PASS_MAX_DAYS parameter to conform to site policy in /etc/login.defs :
                PASS_MAX_DAYS 365
                Modify user parameters for all users with a password set to match:
                # chage --maxdays 365 <user>
                Edit /etc/login.defs and set PASS_MAX_DAYS to a value greater than 0 that follows
                local site policy:
                Example:
                PASS_MAX_DAYS 365
                Run the following command to modify user parameters for all users with a password set
                to a maximum age no greater than 365 or less than 1 that follows local site policy:
                # chage --maxdays <N> <user>
                Example:
                # awk -F: '($2~/^\$.+\$/) {if($5 > 365 || $5 < 1)system ("chage --maxdays 365
                " $1)}' /etc/shadow
                Warning: If a password has been set at system install or kickstart, the last change
                date field is not set, In this case, setting PASS_MAX_DAYS will immediately expire the
                password. One possible solution is to populate the last change date field through a
                command like: chage -d "$(date +%Y-%m-%d)" root

                Default Value:
                PASS_MAX_DAYS 99999
                References:

                     1. CIS Password Policy Guide
                     2. NIST SP 800-53 Rev. 5: CM-1, CM-2, CM-6, CM-7, IA-5

                Additional Information:
                A value of -1 will disable password expiration.
                CIS Controls:

                   Controls
                                                                 Control                                            IG 1 IG 2 IG 3
                   Version
                              5.2 Use Unique Passwords
                       v8         Use unique passwords for all enterprise assets. Best practice implementation
                              includes, at a minimum, an 8-character password for accounts using MFA and a
                                                                                                                     ●    ●    ●
                              14-character password for accounts not using MFA.
                              4.4 Use Unique Passwords
                       v7        Where multi-factor authentication is not supported (such as local administrator,
                              root, or service accounts), accounts will use passwords that are unique to that
                                                                                                                          ●    ●
                              system.
  '5.4.1.2': !unsafe |-
    Edit /etc/login.defs and set PASS_MIN_DAYS to a value greater than 0 that follows
                local site policy:
                Example:
                PASS_MIN_DAYS 1
                Run the following command to modify user parameters for all users with a password set
                to a minimum days greater than zero that follows local site policy:
                # chage --mindays <N> <user>
                Example:
                # awk -F: '($2~/^\$.+\$/) {if($4 < 1)system ("chage --mindays 1 " $1)}'
                /etc/shadow

                Default Value:
                PASS_MIN_DAYS 0
                References:

                     1. CIS Password Policy Guide
                     2. NIST SP 800-53 :: IA-5 (1) (d)
                     3. NIST SP 800-53A :: IA-5 (1).1 (v)
                     4. STIG ID: RHEL-08-020180 | Rule ID: SV-230364r627750 | CAT II
                     5. STIG ID: RHEL-08-020190 | Rule ID: SV-230365r858727 | CAT II
                     6. STIG ID: UBTU-20-010007 | Rule ID: SV-238202r1015140 | CAT III
                     7. STIG ID: UBTU-22-411025 | Rule ID: SV-260545r1015007 | CAT III

                CIS Controls:

                   Controls
                                                                 Control                                            IG 1 IG 2 IG 3
                   Version
                              5.2 Use Unique Passwords
                       v8         Use unique passwords for all enterprise assets. Best practice implementation
                              includes, at a minimum, an 8-character password for accounts using MFA and a
                                                                                                                     ●    ●    ●
                              14-character password for accounts not using MFA.
                              4.4 Use Unique Passwords
                       v7        Where multi-factor authentication is not supported (such as local administrator,
                              root, or service accounts), accounts will use passwords that are unique to that
                                                                                                                          ●    ●
                              system.
  '5.4.1.3': !unsafe |-
    Edit /etc/login.defs and set PASS_WARN_AGE to a value of 7 or more that follows
                local site policy:
                Example:
                PASS_WARN_AGE 7
                Run the following command to modify user parameters for all users with a password set
                to a minimum warning to 7 or more days that follows local site policy:
                # chage --warndays <N> <user>
                Example:
                # awk -F: '($2~/^\$.+\$/) {if($6 < 7)system ("chage --warndays 7 " $1)}'
                /etc/shadow

                Default Value:
                PASS_WARN_AGE 7
                CIS Controls:

                   Controls
                                                                 Control                                            IG 1 IG 2 IG 3
                   Version
                              5.2 Use Unique Passwords
                       v8         Use unique passwords for all enterprise assets. Best practice implementation
                              includes, at a minimum, an 8-character password for accounts using MFA and a
                                                                                                                     ●    ●    ●
                              14-character password for accounts not using MFA.
                              4.4 Use Unique Passwords
                       v7        Where multi-factor authentication is not supported (such as local administrator,
                              root, or service accounts), accounts will use passwords that are unique to that
                                                                                                                          ●    ●
                              system.
  '5.4.1.4': !unsafe |-
    Edit /etc/login.defs and set the ENCRYPT_METHOD to SHA512 or YESCRYPT:
                ENCRYPT_METHOD <HASHING_ALGORITHM>
                Example:
                ENCRYPT_METHOD YESCRYPT
                Note:

                     •        This only effects local groups' passwords created after updating the file to use
                              sha512 or yescrypt.
                     •        If it is determined that the password algorithm being used is not sha512 or
                              yescrypt, once it is changed, it is recommended that all group passwords be
                              updated to use the stronger hashing algorithm.
                     •        It is recommended that the chosen hashing algorithm is consistent across
                              /etc/login.defs and the PAM configuration

                Default Value:
                ENCRYPT_METHOD SHA512
                References:

                     1. NIST SP 800-53 Rev. 5: IA-5

                CIS Controls:

                  Controls
                                                                        Control                                           IG 1 IG 2 IG 3
                  Version
                                   3.11 Encrypt Sensitive Data at Rest
                                      Encrypt sensitive data at rest on servers, applications, and databases containing
                                   sensitive data. Storage-layer encryption, also known as server-side encryption,
                         v8        meets the minimum requirement of this Safeguard. Additional encryption methods               ●    ●
                                   may include application-layer encryption, also known as client-side encryption,
                                   where access to the data storage device(s) does not permit access to the plain-text
                                   data.

                         v7        16.4 Encrypt or Hash all Authentication Credentials                                          ●    ●
                                      Encrypt or hash with a salt all authentication credentials when stored.
  '5.4.1.5': !unsafe |-
    Run the following command to set the default password inactivity period to 45 days or
                less that meets local site policy:
                # useradd -D -f <N>
                Example:
                # useradd -D -f 45
                Run the following command to modify user parameters for all users with a password set
                to a inactive age of 45 days or less that follows local site policy:
                # chage --inactive <N> <user>
                Example:
                # awk -F: '($2~/^\$.+\$/) {if($7 > 45 || $7 < 0)system ("chage --inactive 45
                " $1)}' /etc/shadow

                Default Value:
                INACTIVE=-1
                References:

                     1. CIS Password Policy Guide

                Additional Information:
                A value of -1 would disable this setting.
                CIS Controls:

                   Controls
                                                                 Control                                            IG 1 IG 2 IG 3
                   Version
                              5.2 Use Unique Passwords
                       v8         Use unique passwords for all enterprise assets. Best practice implementation
                              includes, at a minimum, an 8-character password for accounts using MFA and a
                                                                                                                     ●    ●    ●
                              14-character password for accounts not using MFA.
                              4.4 Use Unique Passwords
                       v7        Where multi-factor authentication is not supported (such as local administrator,
                              root, or service accounts), accounts will use passwords that are unique to that
                                                                                                                          ●    ●
                              system.
  '5.4.1.6': !unsafe |-
    Investigate any users with a password change date in the future and correct them.
                Locking the account, expiring the password, or resetting the password manually may be
                appropriate.
                CIS Controls:

                    Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                    Version
                               5.2 Use Unique Passwords
                       v8          Use unique passwords for all enterprise assets. Best practice implementation
                               includes, at a minimum, an 8-character password for accounts using MFA and a
                                                                                                                      ●    ●    ●
                               14-character password for accounts not using MFA.
                               4.4 Use Unique Passwords
                       v7         Where multi-factor authentication is not supported (such as local administrator,
                               root, or service accounts), accounts will use passwords that are unique to that
                                                                                                                           ●    ●
                               system.

                5.4.2 Configure root and system accounts and environment
  '5.4.2.1': !unsafe |-
    Run the following command to change the root account UID to 0:
                # usermod -u 0 root
                Modify any users other than root with UID 0 and assign them a new UID.

                References:

                     1. NIST SP 800-53 Rev. 5: CM-1, CM-2, CM-6, CM-7, IA-5

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '5.4.2.2': !unsafe |-
    Run the following command to set the root user's GID to 0:
                # usermod -g 0 root
                Run the following command to set the root group's GID to 0:
                # groupmod -g 0 root
                Remove any users other than the root user with GID 0 or assign them a new GID if
                appropriate.
                References:

                     1. NIST SP 800-53 Rev. 5: CM-1, CM-2, CM-6, CM-7, IA-5

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '5.4.2.3': !unsafe |-
    Run the following command to set the root group's GID to 0:
                # groupmod -g 0 root
                Remove any groups other than the root group with GID 0 or assign them a new GID if
                appropriate.
                References:

                     1. NIST SP 800-53 Rev. 5: CM-1, CM-2, CM-6, CM-7, IA-5

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '5.4.2.4': !unsafe |-
    Run the following command to set a password for the root user:
                # passwd root
                - OR -
                Run the following command to lock the root user account:
                # usermod -L root

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '5.4.2.5': !unsafe |-
    Correct or justify any:

                     •   Locations that are not directories
                     •   Empty directories (::)
                     •   Trailing (:)
                     •   Current working directory (.)
                     •   Non root owned directories
                     •   Directories that less restrictive than mode 0755

                References:

                     1. NIST SP 800-53 Rev. 5: CM-1, CM-2, CM-6, CM-7, IA-5

                CIS Controls:

                     Controls Version                      Control          IG 1 IG 2 IG 3
                              v8            0.0 Explicitly Not Mapped
                                               Explicitly Not Mapped

                              v7            0.0 Explicitly Not Mapped
                                               Explicitly Not Mapped
  '5.4.2.6': !unsafe |-
    Edit /root/.profile and /root/.bashrc and either:

                     •    remove, comment out, or update any line with umask.

                - OR -

                     •    update any line that includes umask to a value of 0027 or more restrictive.

                Example:
                umask 027
                Note: the Recommendation "Ensure default user umask is configured" includes
                guidance to set the default umask
                Default Value:
                System default umask

                References:

                     1. NIST SP 800-53 Rev. 5: AC-3, MP-2

                CIS Controls:

                  Controls
                                                                    Control                                          IG 1 IG 2 IG 3
                  Version
                               3.3 Configure Data Access Control Lists
                         v8       Configure data access control lists based on a user’s need to know. Apply data
                               access control lists, also known as access permissions, to local and remote file
                                                                                                                      ●    ●    ●
                               systems, databases, and applications.
                               14.6 Protect Information through Access Control Lists
                                   Protect all information stored on systems with file system, network share,
                         v7    claims, application, or database specific access control lists. These controls will
                               enforce the principle that only authorized individuals should have access to the
                                                                                                                      ●    ●    ●
                               information based on their need to access the information as a part of their
                               responsibilities.
  '5.4.2.7': !unsafe |-
    Run the following command to set the shell for any service accounts returned by the
                audit to nologin:
                # usermod -s $(command -v nologin) <user>
                Example script:
                #!/usr/bin/env bash

                {
                   l_valid_shells="^($( awk -F\/ '$NF != "nologin" {print}' /etc/shells | sed
                -rn '/^\//{s,/,\\\\/,g;p}' | paste -s -d '|' - ))$"
                   awk -v pat="$l_valid_shells" -F:
                '($1!~/^(root|halt|sync|shutdown|nfsnobody)$/ && ($3<'"$(awk
                '/^\s*UID_MIN/{print $2}' /etc/login.defs)"' || $3 == 65534) && $(NF) ~ pat)
                {system ("usermod -s '"$(command -v nologin)"' " $1)}' /etc/passwd
                }

                References:

                     1. NIST SP 800-53 Rev. 5: AC-2(5), AC-3, AC-11, MP-2

                Additional Information:
                The root, sync, shutdown, and halt users are exempted from requiring a non-login
                shell.
                CIS Controls:

                    Controls
                                                                    Control                                          IG 1 IG 2 IG 3
                    Version
                               3.3 Configure Data Access Control Lists
                       v8         Configure data access control lists based on a user’s need to know. Apply data
                               access control lists, also known as access permissions, to local and remote file
                                                                                                                      ●    ●    ●
                               systems, databases, and applications.
                               14.6 Protect Information through Access Control Lists
                                   Protect all information stored on systems with file system, network share,
                       v7      claims, application, or database specific access control lists. These controls will
                               enforce the principle that only authorized individuals should have access to the
                                                                                                                      ●    ●    ●
                               information based on their need to access the information as a part of their
                               responsibilities.
  '5.4.2.8': !unsafe |-
    Run the following command to lock any non-root accounts without a valid login shell
                returned by the audit:
                # usermod -L <user>
                Example script::
                #!/usr/bin/env bash

                {
                   l_valid_shells="^($(awk -F\/ '$NF != "nologin" {print}' /etc/shells | sed
                -rn '/^\//{s,/,\\\\/,g;p}' | paste -s -d '|' - ))$"
                   while IFS= read -r l_user; do
                      passwd -S "$l_user" | awk '$2 !~ /^L/ {system ("usermod -L " $1)}'
                   done < <(awk -v pat="$l_valid_shells" -F: '($1 != "root" && $(NF) !~ pat)
                {print $1}' /etc/passwd)
                }

                References:

                     1. NIST SP 800-53 Rev. 5: AC-2(5), AC-3, AC-11, MP-2

                CIS Controls:

                    Controls
                                                                    Control                                          IG 1 IG 2 IG 3
                    Version
                               3.3 Configure Data Access Control Lists
                       v8         Configure data access control lists based on a user’s need to know. Apply data
                               access control lists, also known as access permissions, to local and remote file
                                                                                                                      ●    ●    ●
                               systems, databases, and applications.
                               14.6 Protect Information through Access Control Lists
                                   Protect all information stored on systems with file system, network share,
                       v7      claims, application, or database specific access control lists. These controls will
                               enforce the principle that only authorized individuals should have access to the
                                                                                                                      ●    ●    ●
                               information based on their need to access the information as a part of their
                               responsibilities.

                5.4.3 Configure user default environment
  '5.4.3.1': !unsafe |-
    Edit /etc/shells and remove any lines that include nologin

                References:

                     1. shells(5)
                     2. NIST SP 800-53 Rev. 5: CM-1, CM-2, CM-6, CM-7, IA-5

                CIS Controls:

                     Controls Version                     Control                IG 1 IG 2 IG 3
                              v8           0.0 Explicitly Not Mapped
                                             Explicitly Not Mapped

                              v7           0.0 Explicitly Not Mapped
                                             Explicitly Not Mapped
  '5.4.3.2': !unsafe |-
    Review /etc/bashrc, /etc/profile, and all files ending in *.sh in the
                /etc/profile.d/ directory and remove or edit all TMOUT=_n_ entries to follow local site
                policy. TMOUT should not exceed 900 or be equal to 0.
                Configure TMOUT in one of the following files:

                     •   A file in the /etc/profile.d/ directory ending in .sh
                     •   /etc/profile
                     •   /etc/bashrc

                Example command to set TMOUT to 900 seconds in a file in /etc/profile.d/:
                # printf '%s\n' "# Set TMOUT to 900 seconds" "typeset -xr TMOUT=900" >
                /etc/profile.d/50-tmout.sh
                TMOUT configuration examples:
                typeset -xr TMOUT=900
                Deprecated methods:

                     •   As multiple lines:

                TMOUT=900
                readonly TMOUT
                export TMOUT

                     •   As a single line:

                readonly TMOUT=900 ; export TMOUT

                Additional Information:
                The audit and remediation in this recommendation apply to bash and shell. If other
                shells are supported on the system, it is recommended that their configuration files also
                are checked. Other methods of setting a timeout exist for other shells not covered here.
                Ensure that the timeout conforms to your local policy.

                CIS Controls:

                  Controls
                                                                  Control                                           IG 1 IG 2 IG 3
                  Version
                              4.3 Configure Automatic Session Locking on Enterprise
                              Assets
                       v8         Configure automatic session locking on enterprise assets after a defined period    ●    ●    ●
                              of inactivity. For general purpose operating systems, the period must not exceed
                              15 minutes. For mobile end-user devices, the period must not exceed 2 minutes.

                       v7     16.11 Lock Workstation Sessions After Inactivity                                       ●    ●    ●
                                 Automatically lock workstation sessions after a standard period of inactivity.
  '5.4.3.3': !unsafe |-
    1. Run the following script to comment out all occurrences of umask that are less
                        restrictive than 027 in files ending in *.sh in the /etc/profile.d/ directory:

                #!/usr/bin/env bash

                {
                   while IFS= read -r -d $'\0' l_file; do
                      sed -ri '/^\s*umask\s+0?(0[01][0-7]|0[0-7][^7]|[^0][0-7][0-
                7])(\s*|\s+.*)$/s/^/# /' "$l_file"
                   done < <(find /etc/profile.d/ -type f -name '*.sh' -print0)
                }

                     2. Create or edit a file in /etc/profile.d/ ending in *.sh and add or modify the
                        following line:

                umask 0027
                Example:
                # printf '%s\n' "" "umask 027" >> /etc/profile.d/60-default_umask.sh

                     3. Edit /etc/login.defs and add or update the following line:

                UMASK 027

                Notes:

                     •   This method only applies to bash and shell. If other shells are supported on the
                         system, it is recommended that their configuration files also are checked
                     •   If the pam_umask.so module is going to be used to set umask, ensure that it's not
                         being overridden by another setting. Refer to the PAM_UMASK(8) man page for
                         more information

                Default Value:
                UMASK 022
                References:

                     1. CCI-000366
                     2. NIST SP 800-53 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. STIG ID: RHEL-08-020351 | RULE ID: SV-230383r1017192 | CAT II
                     5. STIG ID: RHEL-08-020353 | RULE ID: SV-230385r1017194 | CAT II
                     6. STIG ID: RHEL-09-412055 | RULE ID: SV-258072r1045155 | CAT II
                     7. STIG ID: RHEL-09-412065 | RULE ID: SV-258074r991590 | CAT II
                     8. STIG ID: RHEL-09-412070 | RULE ID: SV-258075r991590 | CAT II
                     9. STIG ID: ALMA-09-017620 | RULE ID: SV-269235r1050117 | CAT II
                     10. STIG ID: ALMA-09-017840 | RULE ID: SV-269237r1050119 | CAT II
                     11. STIG ID: UBTU-22-412035 | RULE ID: SV-260555r991590 | CAT II
                     12. STIG ID: UBTU-24-300030 | RULE ID: SV-270716r1066637 | CAT II
                     13. STIG ID: SLES-15-040420 | RULE ID: SV-235030r991590 | CAT II

                Additional Information:

                     •   Other methods of setting a default user umask exist
                     •   If other methods are in use in your environment they should be audited
                     •   The default user umask can be overridden with a user specific umask
                     •   The user creating the directories or files has the discretion of changing the
                         permissions:
                             o Using the chmod command
                             o Setting a different default umask by adding the umask command into a
                                 User Shell Configuration File, (.bashrc), in their home directory
                             o Manually changing the umask for the duration of a login session by
                                 running the umask command

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.

                6 Logging and Auditing
                The items in this section describe how to configure logging, log monitoring, and
                auditing, using tools included in most distributions.
                It is recommended that rsyslog be used for logging (with logwatch providing
                summarization) and auditd be used for auditing (with aureport providing
                summarization) to automatically monitor logs for intrusion attempts and other suspicious
                system behavior.
                In addition to the local log files created by the steps in this section, it is also
                recommended that sites collect copies of their system logs on a secure, centralized log
                server via an encrypted connection. Not only does centralized logging help sites
                correlate events that may be occurring on multiple systems, but having a second copy
                of the system log information may be critical after a system compromise where the
                attacker has modified the local log files on the affected system(s). If a log correlation
                system is deployed, configure it to process the logs described in this section.
                Because it is often necessary to correlate log information from many different systems
                (particularly after a security incident) it is recommended that the time be synchronized
                among systems and devices connected to the local network. The standard Internet
                protocol for time synchronization is the Network Time Protocol (NTP), which is
                supported by most network-ready devices. Reference <http://chrony.tuxfamily.org/>
                manual page for more information on configuring chrony.
                It is important that all logs described in this section be monitored on a regular basis and
                correlated to determine trends. A seemingly innocuous entry in one log could be more
                significant when compared to an entry in another log.
                Note on log file permissions: There really isn't a "one size fits all" solution to the
                permissions on log files. Many sites utilize group permissions so that administrators who
                are in a defined security group, such as "wheel" do not have to elevate privileges to root
                in order to read log files. Also, if a third party log aggregation tool is used, it may need to
                have group permissions to read the log files, which is preferable to having it run setuid
                to root. Therefore, there are two remediation and audit steps for log file permissions.
                One is for systems that do not have a secured group method implemented that only
                permits root to read the log files (root:root 600). The other is for sites that do have
                such a setup and are designated as root:securegrp 640 where securegrp is the
                defined security group (in some cases wheel).

                6.1 System Logging

                Logging services should be configured to prevent information leaks and to aggregate
                logs on a remote server so that they can be reviewed in the event of a system
                compromise. A centralized log server provides a single point of entry for further
                analysis, monitoring and filtering.
                Security principals for logging

                     •   Ensure transport layer security is implemented between the client and the log
                         server.
                     •   Ensure that logs are rotated as per the environment requirements.
                     •   Ensure all locally generated logs have the appropriate permissions.
                     •   Ensure all security logs are sent to a remote log server.
                     •   Ensure the required events are logged.

                What is covered
                This section will cover the minimum best practices for the usage of either rsyslog or
                journald. The recommendations are written such that each is wholly independent of
                each other and only one is implemented.

                     •   If your organization makes use of an enterprise wide logging system completely
                         outside of rsyslog or journald, then the following recommendations do not
                         directly apply. However, the principals of the recommendations should be
                         followed regardless of what solution is implemented. If the enterprise solution
                         incorporates either of these tools, careful consideration should be given to the
                         following recommendations to determine exactly what applies.
                     •   Should your organization make use of both rsyslog and journald, take care
                         how the recommendations may or may not apply to you.

                What is not covered

                     •   Enterprise logging systems not utilizing rsyslog or journald.

                         As logging is very situational and dependent on the local environment, not
                         everything can be covered here.

                     •   Transport layer security should be applied to all remote logging functionality.
                         Both rsyslog and journald supports secure transport and should be configured
                         as such.
                     •   The log server. There are a multitude of reasons for a centralized log server (and
                         keeping a short period of logging on the local system), but the log server is out of
                         scope for these recommendations.

                6.1.1 Configure journald

                Included in the systemd suite is a journaling service called systemd-
                journald.service for the collection and storage of logging data. It creates and
                maintains structured, indexed journals based on logging information that is received
                from a variety of sources such as:

                     •   Classic RFC3164 BSD syslog via the /dev/log socket
                     •   STDOUT/STDERR of programs via StandardOutput=journal +
                         StandardError=journal in service files (both of which are default settings)
                     •   Kernel log messages via the /dev/kmsg device node
                     •   Audit records via the kernel’s audit subsystem
                     •   Structured log messages via journald’s native protocol

                Any changes made to the systemd-journald configuration will require a re-start of
                systemd-journald

                6.1.1.1 Configure systemd-journald service
                systemd-journald is a system service that collects and stores logging data. It creates
                and maintains structured, indexed journals based on logging information that is received
                from a variety of sources:

                     •   Kernel log messages, via kmsg
                     •   Simple system log messages, via the libc syslog call
                     •   Structured system log messages via the native Journal API
                     •   Standard output and standard error of service units
                     •   Audit records, originating from the kernel audit subsystem

                The daemon will implicitly collect numerous metadata fields for each log messages in a
                secure and unfakeable way. See systemd.journal-fields man page for more information
                about the collected metadata.
                The journal service stores log data either persistently below /var/log/journal or in a
                volatile way below /run/log/journal/. By default, log data is stored persistently if
                /var/log/journal/ exists during boot, with an implicit fallback to volatile storage. Use
                Storage= in journald.conf to configure where log data is placed, independently of
                the existence of /var/log/journal/.
                On systems where /var/log/journal/ does not exist but where persistent logging is
                desired, and the default journald.conf is used, it is sufficient to create the directory
                and ensure it has the correct access modes and ownership.
  '6.1.1.1.1': !unsafe |-
    Run the following commands to unmask, enable, and start systemd-
                journald.service
                # systemctl unmask systemd-journald.service
                # systemctl --now enable systemd-journald.service

                References:

                     1. CCI-001665
                     2. NIST SP 800-53 :: SC-24
                     3. NIST SP 800-53A :: SC-24.1 (v)
                     4. STIG ID: RHEL-09-211040 | RULE ID: SV-257783r991562 | CAT II
                     5. STIG ID: ALMA-09-041490 | RULE ID: SV-269428r1050311 | CAT II
                     6. STIG ID: OL09-00-002400 | RULE ID: SV-271739r1091929 | CAT II

                CIS Controls:

                   Controls
                                                                 Control                                    IG 1 IG 2 IG 3
                   Version
                              8.2 Collect Audit Logs
                        v8      Collect audit logs. Ensure that logging, per the enterprise’s audit log      ●    ●    ●
                              management process, has been enabled across enterprise assets.
                              6.2 Activate audit logging
                        v7       Ensure that local logging has been enabled on all systems and networking    ●    ●    ●
                              devices.
                              6.3 Enable Detailed Logging
                        v7       Enable system logging to include detailed information such as an event
                              source, date, user, timestamp, source addresses, destination addresses, and
                                                                                                                  ●    ●
                              other useful elements.
  '6.1.1.1.2': !unsafe |-
    If the default configuration is not appropriate for the site specific requirements, copy
                /usr/lib/tmpfiles.d/systemd.conf to /etc/tmpfiles.d/systemd.conf and
                modify as required. Requirements is either 0640 or site policy if that is less restrictive.

                References:

                     1. NIST SP 800-53 Rev. 5: AC-3, AU-2, AU-12, MP-2, SI-5

                Additional Information:
                See man 5 tmpfiles.d for detailed information on the permission sets for the relevant
                log files. Further information with examples can be found at
                https://www.freedesktop.org/software/systemd/man/tmpfiles.d.html
                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '6.1.1.1.3': !unsafe |-
    Edit /etc/systemd/journald.conf or a file ending in .conf the
                /etc/systemd/journald.conf.d/ directory. Set the following parameters in the
                [Journal] section to ensure logs are rotated according to site policy. The settings
                should be carefully understood as there are specific edge cases and prioritization of
                parameters.
                Example Configuration
                [Journal]
                SystemMaxUse=1G
                SystemKeepFree=500M
                RuntimeMaxUse=200M
                RuntimeKeepFree=50M
                MaxFileSec=1month
                Note: If these settings appear in a canonically later file, or later in the same file, the
                setting will be overwritten

                References:

                     1. NIST SP 800-53 Rev. 5: AU-2, AU-7, AU-12
                     2. https://man7.org/linux/man-pages/man5/journald.conf.5.html

                Additional Information:
                See man 5 journald.conf for detailed information regarding the parameters in use.

                CIS Controls:

                   Controls
                                                                  Control                                    IG 1 IG 2 IG 3
                   Version
                               8.2 Collect Audit Logs
                        v8       Collect audit logs. Ensure that logging, per the enterprise’s audit log      ●    ●    ●
                               management process, has been enabled across enterprise assets.
                               6.2 Activate audit logging
                        v7        Ensure that local logging has been enabled on all systems and networking    ●    ●    ●
                               devices.
                               6.3 Enable Detailed Logging
                        v7        Enable system logging to include detailed information such as an event
                               source, date, user, timestamp, source addresses, destination addresses, and
                                                                                                                   ●    ●
                               other useful elements.
  '6.1.1.1.4': !unsafe |-
    - IF - rsyslog is the preferred method for capturing logs, this section and
                Recommendation should be skipped and the "Configure rsyslog" section followed.
                - IF - journald is the preferred method for capturing logs:
                Set the following parameter in the [Journal] section in
                /etc/systemd/journald.conf or a file in /etc/systemd/journald.conf.d/ ending
                in .conf:
                ForwardToSyslog=no
                Example:
                #!/usr/bin/env bash

                {
                   [ ! -d /etc/systemd/journald.conf.d/ ] && mkdir -p
                /etc/systemd/journald.conf.d/
                   if grep -Psq -- '^\h*\[Journal\]' /etc/systemd/journald.conf.d/60-
                journald.conf; then
                      printf '%s\n' "" "ForwardToSyslog=no" >>
                /etc/systemd/journald.conf.d/60-journald.conf
                   else
                      printf '%s\n' "" "[Journal]" "ForwardToSyslog=no" >>
                /etc/systemd/journald.conf.d/60-journald.conf
                   fi
                }
                Note: Drop-in configuration files have higher precedence and override the main
                configuration file. Files in the *.conf.d/ configuration subdirectories are sorted by their
                filename in lexicographic order, regardless of in which of the subdirectories they reside.
                When multiple files specify the same option, for options which accept just a single value,
                the entry in the file sorted last takes precedence, and for options which accept a list of
                values, entries are collected as they occur in the sorted files.
                Run to following command to update the parameters in the service:
                # systemctl reload-or-restart systemd-journald

                Default Value:
                ForwardToSyslog=no
                References:

                     1. NIST SP 800-53 Rev. 5: AU-2, AU-6, AU-7, AU-12
                     2. journald.conf(5) - Linux manual page: https://man7.org/linux/man-
                        pages/man5/journald.conf.5.html

                CIS Controls:

                   Controls
                                                                 Control                                    IG 1 IG 2 IG 3
                   Version
                              8.2 Collect Audit Logs
                        v8      Collect audit logs. Ensure that logging, per the enterprise’s audit log      ●    ●    ●
                              management process, has been enabled across enterprise assets.
                              6.3 Enable Detailed Logging
                        v7       Enable system logging to include detailed information such as an event
                              source, date, user, timestamp, source addresses, destination addresses, and
                                                                                                                  ●    ●
                              other useful elements.
  '6.1.1.1.5': !unsafe |-
    Set the following parameter in the [Journal] section in
                /etc/systemd/journald.conf or a file in /etc/systemd/journald.conf.d/ ending
                in .conf:
                Storage=persistent
                Example:
                #!/usr/bin/env bash

                {
                   [ ! -d /etc/systemd/journald.conf.d/ ] && mkdir -p
                /etc/systemd/journald.conf.d/
                   if grep -Psq -- '^\h*\[Journal\]' /etc/systemd/journald.conf.d/60-
                journald.conf; then
                      printf '%s\n' "Storage=persistent" >> /etc/systemd/journald.conf.d/60-
                journald.conf
                   else
                      printf '%s\n' "" "[Journal]" "Storage=persistent" >>
                /etc/systemd/journald.conf.d/60-journald.conf
                   fi
                }
                Note: Drop-in configuration files have higher precedence and override the main
                configuration file. Files in the *.conf.d/ configuration subdirectories are sorted by their
                filename in lexicographic order, regardless of in which of the subdirectories they reside.
                When multiple files specify the same option, for options which accept just a single value,
                the entry in the file sorted last takes precedence, and for options which accept a list of
                values, entries are collected as they occur in the sorted files.
                Run to following command to update the parameters in the service:
                # systemctl reload-or-restart systemd-journald

                References:

                     1. NIST SP 800-53 Rev. 5: AU-3, AU-12
                     2. journald.conf(5) - Linux manual page: https://man7.org/linux/man-
                        pages/man5/journald.conf.5.html

                CIS Controls:

                    Controls
                                                                  Control                                    IG 1 IG 2 IG 3
                    Version
                               8.2 Collect Audit Logs
                        v8       Collect audit logs. Ensure that logging, per the enterprise’s audit log      ●    ●    ●
                               management process, has been enabled across enterprise assets.
                               6.2 Activate audit logging
                        v7        Ensure that local logging has been enabled on all systems and networking    ●    ●    ●
                               devices.
                               6.3 Enable Detailed Logging
                        v7        Enable system logging to include detailed information such as an event
                               source, date, user, timestamp, source addresses, destination addresses, and
                                                                                                                   ●    ●
                               other useful elements.
  '6.1.1.1.6': !unsafe |-
    Set the following parameter in the [Journal] section in
                /etc/systemd/journald.conf or a file in /etc/systemd/journald.conf.d/ ending
                in .conf:
                Compress=yes
                Example:
                #!/usr/bin/env bash

                {
                   [ ! -d /etc/systemd/journald.conf.d/ ] && mkdir -p
                /etc/systemd/journald.conf.d/
                   if grep -Psq -- '^\h*\[Journal\]' /etc/systemd/journald.conf.d/60-
                journald.conf; then
                      printf '%s\n' "" "Compress=yes" >> /etc/systemd/journald.conf.d/60-
                journald.conf
                   else
                      printf '%s\n' "" "[Journal]" "Compress=yes" >>
                /etc/systemd/journald.conf.d/60-journald.conf
                   fi
                }
                Note: Drop-in configuration files have higher precedence and override the main
                configuration file. Files in the *.conf.d/ configuration subdirectories are sorted by their
                filename in lexicographic order, regardless of in which of the subdirectories they reside.
                When multiple files specify the same option, for options which accept just a single value,
                the entry in the file sorted last takes precedence, and for options which accept a list of
                values, entries are collected as they occur in the sorted files.
                Run to following command to update the parameters in the service:
                # systemctl reload-or-restart systemd-journald

                References:

                     1. NIST SP 800-53 Rev. 5: AU-4
                     2. journald.conf(5) - Linux manual page: https://man7.org/linux/man-
                        pages/man5/journald.conf.5.html

                CIS Controls:

                   Controls
                                                                 Control                                       IG 1 IG 2 IG 3
                   Version
                              8.2 Collect Audit Logs
                        v8      Collect audit logs. Ensure that logging, per the enterprise’s audit log         ●    ●    ●
                              management process, has been enabled across enterprise assets.
                              8.3 Ensure Adequate Audit Log Storage
                        v8       Ensure that logging destinations maintain adequate storage to comply with      ●    ●    ●
                              the enterprise’s audit log management process.
                              6.2 Activate audit logging
                        v7       Ensure that local logging has been enabled on all systems and networking       ●    ●    ●
                              devices.
                              6.3 Enable Detailed Logging
                        v7       Enable system logging to include detailed information such as an event
                              source, date, user, timestamp, source addresses, destination addresses, and
                                                                                                                     ●    ●
                              other useful elements.
                              6.4 Ensure adequate storage for logs
                        v7       Ensure that all systems that store logs have adequate storage space for the         ●    ●
                              logs generated.

                6.1.1.2 Configure systemd-journal-remote
                The systemd-journal-remote package includes systemd-journal-upload.
                systemd-journal-upload will upload journal entries to the URL specified with --url=.
                This program reads journal entries from one or more journal files, similarly to journalctl.
                systemd-journal-upload transfers the raw content of journal file and uses HTTP as a
                transport protocol.
                systemd-journal-upload.service is a system service that uses systemd-journal-
                upload to upload journal entries to a server. It uses the configuration in journal-
                upload.conf.
  '6.1.1.2.1': !unsafe |-
    Run the following command to install systemd-journal-remote:
                # apt install systemd-journal-remote

                References:

                     1. NIST SP 800-53 Rev. 5: AU-2, AU-7 AU-12

                CIS Controls:

                   Controls
                                                                 Control                                    IG 1 IG 2 IG 3
                   Version
                              8.2 Collect Audit Logs
                        v8      Collect audit logs. Ensure that logging, per the enterprise’s audit log      ●    ●    ●
                              management process, has been enabled across enterprise assets.
                              6.2 Activate audit logging
                        v7       Ensure that local logging has been enabled on all systems and networking    ●    ●    ●
                              devices.
                              6.3 Enable Detailed Logging
                        v7       Enable system logging to include detailed information such as an event
                              source, date, user, timestamp, source addresses, destination addresses, and
                                                                                                                  ●    ●
                              other useful elements.

                6.1.1.2.2 Ensure systemd-journal-upload authentication is
                configured (Manual)
                Profile Applicability:

                • Level 1 - Server
                • Level 1 - Workstation

                Description:
                Journald systemd-journal-upload supports the ability to send log events it gathers to
                a remote log host.
                Rationale:
                Storing log data on a remote host protects log integrity from local attacks. If an attacker
                gains root access on the local system, they could tamper with or remove log data that is
                stored on the local system.
                Note: This recommendation only applies if journald is the chosen method for
                client side logging. Do not apply this recommendation if rsyslog is used.

                Audit:
                Run the following script to verify systemd-journal-upload authentication is
                configured:
                #!/usr/bin/env bash

                {
                     l_analyze_cmd="$(readlink -e /bin/systemd-analyze || \
                     readlink -e /usr/bin/systemd-analyze)"
                     l_conf_file="systemd/journal-upload.conf" l_block="Upload"
                     while IFS= read -r l_file; do
                        l_file="${l_file//# /}"
                        gawk '/\['"$l_block"'\]/{a=1;next}/\[/{a=0}a' "$l_file" \
                        2>/dev/null | grep -Poi -- \

                '^\h*(URL|ServerKeyFile|ServerCertificateFile|TrustedCertificateFile)\h*=\h*\
                H+\b'
                   done < <("$l_analyze_cmd" cat-config "$l_conf_file" | tac | \
                   grep -Pio '^\h*#\h*\/[^#\n\r\h]+\.conf\b')
                }
                Verify the output matches per your environments certificate locations and the URL of
                the log server:
                Example output:
                URL=192.168.50.42
                ServerKeyFile=/etc/ssl/private/journal-upload.pem
                ServerCertificateFile=/etc/ssl/certs/journal-upload.pem
                TrustedCertificateFile=/etc/ssl/ca/trusted.pem

                Remediation:
                Edit the /etc/systemd/journal-upload.conf file or a file in
                /etc/systemd/journal-upload.conf.d ending in .conf and ensure the following
                lines are set in the [Upload] section per your environment:
                [Upload]
                URL=192.168.50.42
                ServerKeyFile=/etc/ssl/private/journal-upload.pem
                ServerCertificateFile=/etc/ssl/certs/journal-upload.pem
                TrustedCertificateFile=/etc/ssl/ca/trusted.pem
                Example script:
                #!/usr/bin/env bash

                {
                     [ ! -d "/etc/systemd/journal-upload.conf.d" ] && \
                     mkdir -p /etc/systemd/journal-upload.conf.d
                     printf '%s\n' "" "[Upload]" "URL=192.168.50.42" \
                     "ServerKeyFile=/etc/ssl/private/journal-upload.pem" \
                     "ServerCertificateFile=/etc/ssl/certs/journal-upload.pem" \
                     "TrustedCertificateFile=/etc/ssl/ca/trusted.pem" \
                     >> /etc/systemd/journal-upload.conf.d/60-journal-upload.conf
                }
                Restart the service:
                # systemctl reload-or-restart systemd-journal-upload

                References:

                     1. NIST SP 800-53 Rev. 5: AU-2, AU-12

                CIS Controls:

                    Controls
                                                                  Control                                    IG 1 IG 2 IG 3
                    Version
                               8.2 Collect Audit Logs
                        v8       Collect audit logs. Ensure that logging, per the enterprise’s audit log      ●    ●    ●
                               management process, has been enabled across enterprise assets.
                               6.2 Activate audit logging
                        v7        Ensure that local logging has been enabled on all systems and networking    ●    ●    ●
                               devices.
                               6.3 Enable Detailed Logging
                        v7        Enable system logging to include detailed information such as an event
                               source, date, user, timestamp, source addresses, destination addresses, and
                                                                                                                   ●    ●
                               other useful elements.
  '6.1.1.2.2': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '6.1.1.2.3': !unsafe |-
    Run the following commands to unmask, enable and start systemd-journal-upload:
                # systemctl unmask systemd-journal-upload.service
                # systemctl --now enable systemd-journal-upload.service

                References:

                     1. NIST SP 800-53 Rev. 5: AU-2, AU-12

                CIS Controls:

                   Controls
                                                                 Control                                    IG 1 IG 2 IG 3
                   Version
                              8.2 Collect Audit Logs
                        v8      Collect audit logs. Ensure that logging, per the enterprise’s audit log      ●    ●    ●
                              management process, has been enabled across enterprise assets.
                              6.2 Activate audit logging
                        v7       Ensure that local logging has been enabled on all systems and networking    ●    ●    ●
                              devices.
                              6.3 Enable Detailed Logging
                        v7       Enable system logging to include detailed information such as an event
                              source, date, user, timestamp, source addresses, destination addresses, and
                                                                                                                  ●    ●
                              other useful elements.
  '6.1.1.2.4': !unsafe |-
    Run the following commands to stop and mask systemd-journal-remote.socket and
                systemd-journal-remote.service:
                # systemctl stop systemd-journal-remote.socket systemd-journal-remote.service
                # systemctl mask systemd-journal-remote.socket systemd-journal-remote.service

                References:

                     1. NIST SP 800-53 Rev. 5: AU-2, AU-7 AU-12

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.

                6.1.2 Configure rsyslog

                The rsyslog software package may be used instead of the default journald logging
                mechanism.
                Rsyslog has evolved over several decades. For this reason it supports three different
                configuration formats (“languages”):

                     •   basic - previously known as the sysklogd format, this is the format best used to
                         express basic things, such as where the statement fits on a single line.
                              o It stems back to the original syslog.conf format, in use now for several
                                  decades.
                              o The most common use case is matching on facility/severity and writing
                                  matching messages to a log file.
                     •   advanced - previously known as the RainerScript format, this format was first
                         available in rsyslog v6 and is the current, best and most precise format for non-
                         trivial use cases where more than one line is needed.
                              o Prior to v7, there was a performance impact when using this format that
                                  encouraged use of the basic format for best results. Current versions of
                                  rsyslog do not suffer from this (historical) performance impact.
                              o This new style format is specifically targeted towards more advanced use
                                  cases like forwarding to remote hosts that might be partially offline.
                     •   obsolete legacy - previously known simply as the legacy format, this format is
                         exactly what its name implies: it is obsolete and should not be used when writing
                         new configurations. It was created in the early days (up to rsyslog version 5)
                         where we expected that rsyslog would extend sysklogd just mildly. Consequently,
                         it was primarily aimed at small additions to the original sysklogd format.
                              o Practice has shown that it was notoriously hard to use for more advanced
                                  use cases, and thus we replaced it with the advanced format.
                              o In essence, everything that needs to be written on a single line that starts
                                  with a dollar sign is legacy format. Users of this format are encouraged to
                                  migrate to the basic or advanced formats.

                Note: This section only applies if rsyslog is the chosen method for client side logging.
                Do not apply this section if journald is used.
  '6.1.2.1': !unsafe |-
    Run the following command to install rsyslog:
                # apt install rsyslog

                Default Value:
                Installed
                References:

                     1. NIST SP 800-53 Rev. 5: AU-2, AU-3, AU-12

                CIS Controls:

                   Controls
                                                                 Control                                    IG 1 IG 2 IG 3
                   Version
                              8.2 Collect Audit Logs
                        v8      Collect audit logs. Ensure that logging, per the enterprise’s audit log      ●    ●    ●
                              management process, has been enabled across enterprise assets.
                              6.2 Activate audit logging
                        v7       Ensure that local logging has been enabled on all systems and networking    ●    ●    ●
                              devices.
                              6.3 Enable Detailed Logging
                        v7       Enable system logging to include detailed information such as an event
                              source, date, user, timestamp, source addresses, destination addresses, and
                                                                                                                  ●    ●
                              other useful elements.
  '6.1.2.2': !unsafe |-
    - IF - rsyslog is being used for logging on the system:
                Run the following commands to unmask, enable, and start rsyslog.service:
                # systemctl unmask rsyslog.service
                # systemctl enable rsyslog.service
                # systemctl start rsyslog.service

                References:

                     1. CCI-000366
                     2. NIST SP 800-53 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)
                     4. STIG ID: RHEL-08-010561 | RULE ID: SV-230298r1017108 | CAT II
                     5. STIG ID: OL08-00-010561 | RULE ID: SV-248615r991589 | CAT II
                     6. STIG ID: ALMA-09-053150 | RULE ID: SV-269518r1050401 | CAT II
                     7. STIG ID: UBTU-22-652010 | RULE ID: SV-260588r991562 | CAT II

                CIS Controls:

                   Controls
                                                                 Control                                    IG 1 IG 2 IG 3
                   Version
                              8.2 Collect Audit Logs
                        v8      Collect audit logs. Ensure that logging, per the enterprise’s audit log      ●    ●    ●
                              management process, has been enabled across enterprise assets.
                              6.2 Activate audit logging
                        v7       Ensure that local logging has been enabled on all systems and networking    ●    ●    ●
                              devices.
                              6.3 Enable Detailed Logging
                        v7       Enable system logging to include detailed information such as an event
                              source, date, user, timestamp, source addresses, destination addresses, and
                                                                                                                  ●    ●
                              other useful elements.
  '6.1.2.3': !unsafe |-
    - IF - Journald is the preferred method for capturing logs, this section and
                Recommendation should be skipped and the "Configure Journald" section followed.
                - IF - rsyslog is the preferred method for capturing logs:
                Set the following parameter in the [Journal] section in
                /etc/systemd/journald.conf or a file in /etc/systemd/journald.conf.d/ ending
                in .conf:
                ForwardToSyslog=yes
                Example:
                #!/usr/bin/env bash

                {
                   [ ! -d /etc/systemd/journald.conf.d/ ] && mkdir
                /etc/systemd/journald.conf.d/
                   if grep -Psq -- '^\h*\[Journal\] /etc/systemd/journald.conf.d/60-
                journald.conf; then
                      printf '%s\n' "ForwardToSyslog=yes" >> /etc/systemd/journald.conf.d/60-
                journald.conf
                   else
                      printf '%s\n' "[Journal]" "ForwardToSyslog=yes" >>
                /etc/systemd/journald.conf.d/60-journald.conf
                   fi
                }

                         Note: Drop-in configuration files have higher precedence and override the
                         main configuration file. Files in the *.conf.d/ configuration subdirectories
                         are sorted by their filename in lexicographic order, regardless of in which
                         of the subdirectories they reside. When multiple files specify the same
                         option, for options which accept just a single value, the entry in the file
                         sorted last takes precedence, and for options which accept a list of values,
                         entries are collected as they occur in the sorted files.
                Run to following command to update the parameters in the service:
                Restart systemd-journald.service:
                # systemctl reload-or-restart systemd-journald.service

                References:

                     1. NIST SP 800-53 Rev. 5: AC-3, AU-2, AU-4, AU-12, MP-2
                     2. SYSTEMD-JOURNALD.SERVICE(8)
                     3. JOURNALD.CONF(5)

                Additional Information:
                As noted in the systemd-journald man pages, systemd-journald logs may be
                exported to rsyslog either through the process mentioned here, or through a facility
                like systemd-journald.service. There are trade-offs involved in each
                implementation, where ForwardToSyslog will immediately capture all events (and
                forward to an external log server, if properly configured), but may not capture all boot-up
                activities. Mechanisms such as systemd-journald.service, on the other hand, will
                record bootup events, but may delay sending the information to rsyslog, leading to the
                potential for log manipulation prior to export. Be aware of the limitations of all tools
                employed to secure a system.
                CIS Controls:

                   Controls
                                                                  Control                                        IG 1 IG 2 IG 3
                   Version
                              8.2 Collect Audit Logs
                        v8      Collect audit logs. Ensure that logging, per the enterprise’s audit log           ●    ●    ●
                              management process, has been enabled across enterprise assets.
                              8.9 Centralize Audit Logs
                        v8       Centralize, to the extent possible, audit log collection and retention across         ●    ●
                              enterprise assets.
                              6.2 Activate audit logging
                        v7       Ensure that local logging has been enabled on all systems and networking         ●    ●    ●
                              devices.
                              6.3 Enable Detailed Logging
                        v7       Enable system logging to include detailed information such as an event
                              source, date, user, timestamp, source addresses, destination addresses, and
                                                                                                                       ●    ●
                              other useful elements.
                              6.5 Central Log Management
                        v7      Ensure that appropriate logs are being aggregated to a central log                     ●    ●
                              management system for analysis and review.
  '6.1.2.4': !unsafe |-
    Edit either /etc/rsyslog.conf or a dedicated .conf file in /etc/rsyslog.d/ and set
                $FileCreateMode to 0640 or more restrictive:
                $FileCreateMode 0640
                Restart the service:
                # systemctl restart rsyslog

                References:

                     1. RSYSLOG.CONF(5)
                     2. NIST SP 800-53 Rev. 5: AC-3, AC-6, MP-2
                     3. https://www.rsyslog.com/doc/

                CIS Controls:

                   Controls
                                                                  Control                                        IG 1 IG 2 IG 3
                   Version
                              3.3 Configure Data Access Control Lists
                       v8         Configure data access control lists based on a user’s need to know. Apply
                              data access control lists, also known as access permissions, to local and remote
                                                                                                                  ●    ●    ●
                              file systems, databases, and applications.
                              8.2 Collect Audit Logs
                       v8       Collect audit logs. Ensure that logging, per the enterprise’s audit log           ●    ●    ●
                              management process, has been enabled across enterprise assets.
                              5.1 Establish Secure Configurations
                       v7        Maintain documented, standard security configuration standards for all           ●    ●    ●
                              authorized operating systems and software.
                              6.2 Activate audit logging
                       v7        Ensure that local logging has been enabled on all systems and networking         ●    ●    ●
                              devices.
                              6.3 Enable Detailed Logging
                       v7        Enable system logging to include detailed information such as an event
                              source, date, user, timestamp, source addresses, destination addresses, and
                                                                                                                       ●    ●
                              other useful elements.
  '6.1.2.5': !unsafe |-
    Edit the following lines in the /etc/rsyslog.conf and /etc/rsyslog.d/*.conf files
                as appropriate for your environment.
                         Note: The below configuration is shown for example purposes only. Due
                         care should be given to how the organization wishes to store log data.
                *.emerg                                         :omusrmsg:*
                auth,authpriv.*                                 /var/log/secure
                mail.*                                         -/var/log/mail
                mail.info                                      -/var/log/mail.info
                mail.warning                                   -/var/log/mail.warn
                mail.err                                        /var/log/mail.err
                cron.*                                          /var/log/cron
                *.=warning;*.=err                              -/var/log/warn
                *.crit                                          /var/log/warn
                *.*;mail.none;news.none                        -/var/log/messages
                local0,local1.*                                -/var/log/localmessages
                local2,local3.*                                -/var/log/localmessages
                local4,local5.*                                -/var/log/localmessages
                local6,local7.*                                -/var/log/localmessages
                Run the following command to reload the rsyslogd configuration:
                # systemctl restart rsyslog
                References:
                   1. See the rsyslog.conf(5) man page for more information.
                   2. NIST SP 800-53 Rev. 5: AU-2, AU-7, AU-12

                CIS Controls:

                   Controls
                                                                 Control                                    IG 1 IG 2 IG 3
                   Version
                              8.2 Collect Audit Logs
                        v8      Collect audit logs. Ensure that logging, per the enterprise’s audit log      ●    ●    ●
                              management process, has been enabled across enterprise assets.
                              6.2 Activate audit logging
                        v7       Ensure that local logging has been enabled on all systems and networking    ●    ●    ●
                              devices.
                              6.3 Enable Detailed Logging
                        v7       Enable system logging to include detailed information such as an event
                              source, date, user, timestamp, source addresses, destination addresses, and
                                                                                                                  ●    ●
                              other useful elements.

                6.1.2.6 Ensure rsyslog is configured to send logs to a remote log
                host (Manual)
                Profile Applicability:

                • Level 1 - Server
                • Level 1 - Workstation

                Description:
                rsyslog supports the ability to send log events it gathers to a remote log host or to
                receive messages from remote hosts, thus enabling centralized log management.
                Rationale:
                Storing log data on a remote host protects log integrity from local attacks. If an attacker
                gains root access on the local system, they could tamper with or remove log data that is
                stored on the local system.
                Audit:
                Review the /etc/rsyslog.conf and /etc/rsyslog.d/*.conf files and verify that
                logs are sent to a central host:
                basic format
                # grep "^*.*[^I][^I]*@" /etc/rsyslog.conf /etc/rsyslog.d/*.conf
                Output should include @@<FQDN or IP of remote loghost>:
                Example:
                *.* @@loghost.example.com
                advanced format
                # grep -Psi -- '^\s*([^#]+\s+)?action\(([^#]+\s+)?\btarget=\"?[^#"]+\"?\b'
                /etc/rsyslog.conf /etc/rsyslog.d/*.conf
                Output should include target=<FQDN or IP of remote loghost>:
                Example:
                *.* action(type="omfwd" target="loghost.example.com" port="514"
                protocol="tcp"

                Remediation:
                Edit the /etc/rsyslog.conf and /etc/rsyslog.d/*.conf files and add the following
                line (where loghost.example.com is the name of your central log host). The target
                directive may either be a fully qualified domain name or an IP address.
                Example:
                *.* action(type="omfwd" target="loghost.example.com" port="514"
                protocol="tcp"
                           action.resumeRetryCount="100"
                           queue.type="LinkedList" queue.size="1000")
                Run the following command to reload rsyslog.service:
                # systemctl reload-or-restart rsyslog.service

                References:

                     1. See the rsyslog.conf(5) man page for more information.
                     2. NIST SP 800-53 Rev. 5: AU-6
                     3. https://www.rsyslog.com/doc/

                Additional Information:
                In addition, see the rsyslog documentation for implementation details of TLS.
                CIS Controls:

                   Controls
                                                                  Control                                    IG 1 IG 2 IG 3
                   Version
                               8.2 Collect Audit Logs
                        v8       Collect audit logs. Ensure that logging, per the enterprise’s audit log      ●    ●    ●
                               management process, has been enabled across enterprise assets.
                               6.2 Activate audit logging
                        v7        Ensure that local logging has been enabled on all systems and networking    ●    ●    ●
                               devices.
                               6.3 Enable Detailed Logging
                        v7        Enable system logging to include detailed information such as an event
                               source, date, user, timestamp, source addresses, destination addresses, and
                                                                                                                   ●    ●
                               other useful elements.

                6.1.2.7 Ensure rsyslog is not configured to receive logs from a
                remote client (Automated)
                Profile Applicability:

                • Level 1 - Server
                • Level 1 - Workstation

                Description:
                rsyslog supports the ability to receive messages from remote hosts, thus acting as a
                log server. Clients should not receive data from other hosts.
                Rationale:
                If a client is configured to also receive data, thus turning it into a server, the client
                system is acting outside its operational boundary.
                Audit:
                Review the /etc/rsyslog.conf and /etc/rsyslog.d/*.conf files and verify that the
                system is not configured to accept incoming logs.
                advanced format
                # grep -Psi -- '^\h*module\(load=\"?imtcp\"?\)' /etc/rsyslog.conf
                /etc/rsyslog.d/*.conf
                # grep -Psi -- '^\h*input\(type=\"?imtcp\"?\b' /etc/rsyslog.conf
                /etc/rsyslog.d/*.conf
                Nothing should be returned
                obsolete legacy format
                # grep -Psi -- '^\h*\$ModLoad\h+imtcp\b' /etc/rsyslog.conf
                /etc/rsyslog.d/*.conf
                # grep -Psi -- '^\h*\$InputTCPServerRun\b' /etc/rsyslog.conf
                /etc/rsyslog.d/*.conf
                Nothing should be returned
                Remediation:
                Should there be any active log server configuration found in the auditing section, modify
                those files and remove the specific lines highlighted by the audit. Verify none of the
                following entries are present in any of /etc/rsyslog.conf or
                /etc/rsyslog.d/*.conf.
                advanced format
                module(load="imtcp")
                input(type="imtcp" port="514")
                deprecated legacy format
                $ModLoad imtcp
                $InputTCPServerRun
                Restart the service:
                # systemctl restart rsyslog

                References:

                     1. NIST SP 800-53 Rev. 5: AU-2, AU-7, AU-12, CM-6
                     2. https://www.rsyslog.com/doc/index.html

                CIS Controls:

                   Controls
                                                                Control                                          IG 1 IG 2 IG 3
                   Version
                              4.8 Uninstall or Disable Unnecessary Services on
                              Enterprise Assets and Software
                        v8       Uninstall or disable unnecessary services on enterprise assets and software,          ●    ●
                              such as an unused file sharing service, web application module, or service
                              function.
                              9.2 Ensure Only Approved Ports, Protocols and Services
                        v7    Are Running                                                                              ●    ●
                                 Ensure that only network ports, protocols, and services listening on a system
                              with validated business needs, are running on each system.
  '6.1.2.6': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '6.1.2.7': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '6.1.2.8': !unsafe |-
    Edit /etc/logrotate.conf and /etc/logrotate.d/* to ensure logs are rotated
                according to site policy.
                References:

                     1. NIST SP 800-53 Rev. 5: AU-8

                Additional Information:
                If no maxage setting is set for logrotate a situation can occur where logrotate is
                interrupted and fails to delete rotated log files. It is recommended to set this to a value
                greater than the longest any log file should exist on your system to ensure that any such
                log file is removed but standard rotation settings are not overridden.

                CIS Controls:

                    Controls
                                                                 Control                                         IG 1 IG 2 IG 3
                    Version
                                8.3 Ensure Adequate Audit Log Storage
                         v8        Ensure that logging destinations maintain adequate storage to comply with      ●    ●    ●
                                the enterprise’s audit log management process.
                                6.4 Ensure adequate storage for logs
                         v7        Ensure that all systems that store logs have adequate storage space for the         ●    ●
                                logs generated.
  '6.1.2.9': !unsafe |-
    Run the following command to install rsyslog-gnutls:
                # apt install rsyslog-gnutls

                References:

                     1. https://www.rsyslog.com/doc/tutorials/tls.html

                CIS Controls:

                   Controls
                                                                 Control                                    IG 1 IG 2 IG 3
                   Version
                              8.2 Collect Audit Logs
                        v8      Collect audit logs. Ensure that logging, per the enterprise’s audit log      ●    ●    ●
                              management process, has been enabled across enterprise assets.
                              6.2 Activate audit logging
                        v7       Ensure that local logging has been enabled on all systems and networking    ●    ●    ●
                              devices.
                              6.3 Enable Detailed Logging
                        v7       Enable system logging to include detailed information such as an event
                              source, date, user, timestamp, source addresses, destination addresses, and
                                                                                                                  ●    ●
                              other useful elements.
  '6.1.2.10': !unsafe |-
    Edit rsyslog.conf or a .conf file in /etc/rsyslog.d/ to use gtls:
                Example /etc/rsyslog.d/40-forward.conf
                # certificate files - just CA for a client
                global(DefaultNetstreamDriverCAFile="/path/to/contrib/gnutls/ca.pem")

                # set up the action for all messages
                action(type="omfwd" protocol="tcp" target="s.example.net" port="6514"
                       StreamDriver="gtls" StreamDriverMode="1" StreamDriverAuthMode="anon")

                References:

                     1. https://www.rsyslog.com/doc/tutorials/tls_cert_client.html
                     2. https://www.rsyslog.com/doc/rainerscript/global.html
                     3. https://www.rsyslog.com/doc/concepts/netstrm_drvr.html
                     4. https://www.rsyslog.com/doc/concepts/ns_gtls.html

                CIS Controls:

                    Controls
                                                                    Control                                  IG 1 IG 2 IG 3
                    Version
                                3.10 Encrypt Sensitive Data in Transit
                        v8         Encrypt sensitive data in transit. Example implementations can include:         ●    ●
                                Transport Layer Security (TLS) and Open Secure Shell (OpenSSH).

                        v7      14.4 Encrypt All Sensitive Information in Transit                                  ●    ●
                                   Encrypt all sensitive information in transit.
  '6.1.2.11': !unsafe |-
    Edit rsyslog.conf or a .conf file in /etc/rsyslog.d/ to the correct path for the CA
                certificate:
                Example
                # certificate files - just CA for a client
                global(DefaultNetstreamDriverCAFile="/path/to/contrib/gnutls/ca.pem")

                         Note: Only distribute the ca.pem, NOT ca-key.pem (the private key).
                         Distributing the CA private key would totally breach security as everybody
                         could issue new certificates on the behalf of this CA.
                References:

                     1. https://www.rsyslog.com/doc/tutorials/tls.html#client-setup

                CIS Controls:

                    Controls
                                                                     Control                                  IG 1 IG 2 IG 3
                    Version
                                 3.10 Encrypt Sensitive Data in Transit
                        v8          Encrypt sensitive data in transit. Example implementations can include:         ●    ●
                                 Transport Layer Security (TLS) and Open Secure Shell (OpenSSH).

                        v7       14.4 Encrypt All Sensitive Information in Transit                                  ●    ●
                                    Encrypt all sensitive information in transit.

                6.1.3 Configure Logfiles
  '6.1.3.1': !unsafe |-
    Run the following script to update permissions and ownership on files in /var/log.
                Although the script is not destructive, ensure that the output is captured in the event that
                the remediation causes issues.

                #!/usr/bin/env bash

                {
                   a_output2=()
                   f_file_test_fix()
                   {
                      a_out2=()
                      maxperm="$( printf '%o' $(( 0777 & ~$perm_mask)) )"
                      if [ $(( $l_mode & $perm_mask )) -gt 0 ]; then
                         a_out2+=("   o Mode: \"$l_mode\" should be \"$maxperm\" or more restrictive" "     x
                Removing excess permissions")
                         chmod "$l_rperms" "$l_fname"
                      fi
                      if [[ ! "$l_user" =~ $l_auser ]]; then
                         a_out2+=("   o Owned by: \"$l_user\" and should be owned by \"${l_auser//|/ or }\"" "
                x Changing ownership to: \"$l_fix_account\"")
                         chown "$l_fix_account" "$l_fname"
                      fi
                      if [[ ! "$l_group" =~ $l_agroup ]]; then
                         a_out2+=("   o Group owned by: \"$l_group\" and should be group owned by
                \"${l_agroup//|/ or }\"" "     x Changing group ownership to: \"$l_fix_account\"")
                         chgrp "$l_fix_account" "$l_fname"
                      fi
                      [ "${#a_out2[@]}" -gt 0 ] && a_output2+=(" - File: \"$l_fname\" is:" "${a_out2[@]}")
                   }
                   l_fix_account='root'
                   while IFS= read -r -d $'\0' l_file; do
                      while IFS=: read -r l_fname l_mode l_user l_group; do
                         if grep -Pq -- '\/(apt)\h*$' <<< "$(dirname "$l_fname")"; then
                            perm_mask='0133' l_rperms="u-x,go-wx" l_auser="root" l_agroup="(root|adm)";
                f_file_test_fix
                         else
                            case "$(basename "$l_fname")" in
                               lastlog | lastlog.* | wtmp | wtmp.* | wtmp-* | btmp | btmp.* | btmp-* | README)
                                  perm_mask='0113' l_rperms="ug-x,o-wx" l_auser="root" l_agroup="(root|utmp)"
                                  f_file_test_fix ;;
                               cloud-init.log* | localmessages* | waagent.log*)
                                  perm_mask='0133' l_rperms="u-x,go-wx" l_auser="(root|syslog)"
                l_agroup="(root|adm)"
                                  file_test_fix ;;
                               secure | auth.log | syslog | messages)
                                  perm_mask='0137' l_rperms="u-x,g-wx,o-rwx" l_auser="(root|syslog)"
                l_agroup="(root|adm)"
                                  f_file_test_fix ;;
                               *.journal | *.journal~)
                                  perm_mask='0137' l_rperms="u-x,g-wx,o-rwx" l_auser="root"
                l_agroup="(root|systemd-journal)"
                                  f_file_test_fix ;;
                               *)
                                  perm_mask='0137' l_rperms="u-x,g-wx,o-rwx" l_auser="(root|syslog)"
                l_agroup="(root|adm)"
                                  if [ "$l_user" = "root" ] || ! grep -Pq -- "^\h*$(awk -F: '$1=="'"$l_user"'"
                {print $7}' /etc/passwd)\b" /etc/shells; then
                                     ! grep -Pq -- "$l_auser" <<< "$l_user" && l_auser="(root|syslog|$l_user)"
                                     ! grep -Pq -- "$l_agroup" <<< "$l_group" && l_agroup="(root|adm|$l_group)"
                                  fi
                                  f_file_test_fix ;;
                            esac
                         fi
                      done < <(stat -Lc '%n:%#a:%U:%G' "$l_file")
                   done < <(find -L /var/log -type f \( -perm /0137 -o ! -user root -o ! -group root \) -print0)
                   if [ "${#a_output2[@]}" -le 0 ]; then # If all files passed, then we report no changes
                      a_output+=(" - All files in \"/var/log/\" have appropriate permissions and ownership")
                      printf '\n%s' "- All files in \"/var/log/\" have appropriate permissions and ownership" "
                o No changes required" ""
                   else
                      printf '\n%s' "${a_output2[@]}" ""
                   fi
                }

                         Note:

                              •    You may also need to change the configuration for your logging
                                   software or services for any logs that had incorrect permissions.
                              •    If there are services that log to other locations, ensure that those
                                   log files have the appropriate access configured.

                References:

                     1. NIST SP 800-53 Rev. 5: AC-3, MP-2

                CIS Controls:

                  Controls
                                                                       Control                                          IG 1 IG 2 IG 3
                  Version
                                  3.3 Configure Data Access Control Lists
                       v8            Configure data access control lists based on a user’s need to know. Apply data
                                  access control lists, also known as access permissions, to local and remote file
                                                                                                                         ●    ●    ●
                                  systems, databases, and applications.
                                  14.6 Protect Information through Access Control Lists
                                      Protect all information stored on systems with file system, network share,
                       v7         claims, application, or database specific access control lists. These controls will
                                  enforce the principle that only authorized individuals should have access to the
                                                                                                                         ●    ●    ●
                                  information based on their need to access the information as a part of their
                                  responsibilities.

                6.2 System Auditing

                The Linux Auditing System operates on a set of rules that collects certain types of
                system activity to facilitate incident investigation, detect unauthorized access or
                modification of data. By default events will be logged to /var/log/audit/audit.log,
                which can be configured in /etc/audit/auditd.conf.
                The following types of audit rules can be specified:

                     •   Control rules: Configuration of the auditing system.
                     •   File system rules: Allow the auditing of access to a particular file or a directory.
                         Also known as file watches.
                     •   System call rules: Allow logging of system calls that any specified program
                         makes.

                Audit rules can be set:

                     •   On the command line using the auditctl utility. These rules are not persistent
                         across reboots.
                     •   In /etc/audit/audit.rules. These rules have to be merged and loaded before
                         they are active.

                Notes:

                     •   For 64 bit systems that have arch as a rule parameter, you will need two rules:
                         one for 64 bit and one for 32 bit systems calls.
                     •   If the auditing system is configured to be locked (-e 2), a system reboot will be
                         required in order to load any changes.
                     •   Key names are optional on the rules and will not be used as a compliance
                         auditing. The usage of key names is highly recommended as it facilitates
                         organization and searching, as such, all remediation steps will have key names
                         supplied.
                     •   It is best practice to store the rules, in number prepended files, in
                         /etc/audit/rules.d/. Rules must end in a .rules suffix. This then requires
                         the use of augenrules to merge all the rules into /etc/audit/audit.rules
                         based on their their alphabetical (lexical) sort order. All benchmark
                         recommendations follow this best practice for remediation, specifically using the
                         prefix of 50 which is centre weighed if all rule sets make use of the number
                         prepending naming convention.
                     •   Your system may have been customized to change the default UID_MIN. All
                         samples output uses 1000, but this value will not be used in compliance auditing.
                         To confirm the UID_MIN for your system, run the following command: awk
                         '/^\s*UID_MIN/{print $2}' /etc/login.defs

                Normalization
                The Audit system normalizes some entries, so when you look at the sample output keep
                in mind that:

                     •   With regards to users whose login UID is not set, the values -1 / unset /
                         4294967295 are equivalent and normalized to -1.
                     •   When comparing field types and both sides of the comparison is valid fields
                         types, such aseuid!=uid, then the auditing system may normalize such that the
                         output is uid!=euid.
                     •   Some parts of the rule may be rearranged whilst others are dependant on
                         previous syntax. For example, the following two statements are the same:

                -a always,exit -F arch=b64 -S execve -C uid!=euid -F auid!=-1 -F
                key=user_emulation
                and
                -a always,exit -F arch=b64 -C euid!=uid -F auid!=unset -S execve -k
                user_emulation
                Capacity planning
                The recommendations in this section implement auditing policies that not only produces
                large quantities of logged data, but may also negatively impact system performance.
                Capacity planning is critical in order not to adversely impact production environments.

                     •   Disk space. If a significantly large set of events are captured, additional on
                         system or off system storage may need to be allocated. If the logs are not sent to
                         a remote log server, ensure that log rotation is implemented else the disk will fill
                         up and the system will halt. Even when logs are sent to a log server, ensure
                         sufficient disk space to allow caching of logs in the case of temporary network
                         outages.
                     •   Disk IO. It is not just the amount of data collected that should be considered, but
                         the rate at which logs are generated.
                     •   CPU overhead. System call rules might incur considerable CPU overhead. Test
                         the systems open/close syscalls per second with and without the rules to gauge
                         the impact of the rules.

                6.2.1 Configure auditd Service

                The capturing of system events provides system administrators with information to allow
                them to determine if unauthorized access to their system is occurring.
  '6.2.1.1': !unsafe |-
    Run the following command to Install auditd and audispd-plugins
                # apt install auditd audispd-plugins

                References:

                     1. NIST SP 800-53 Rev. 5: AU-2, AU-3, AU-12, SI-5
                     2. STIG ID: UBTU-20-010182 | RULE ID: SV-238298r958506 | CAT II
                     3. STIG ID: UBTU-22-653015 | RULE ID: SV-260591r1015023 | CAT II

                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              6.2 Activate audit logging
                       v7        Ensure that local logging has been enabled on all systems and networking             ●    ●    ●
                              devices.
  '6.2.1.2': !unsafe |-
    Run the following commands to unmask, enable and start auditd:
                # systemctl unmask auditd
                # systemctl enable auditd
                # systemctl start auditd

                References:

                     1. NIST SP 800-53 Rev. 5: AU-2, AU-12, SI-5
                     2. STIG ID: UBTU-20-010182 | RULE ID: SV-238298r958506 | CAT II
                     3. STIG ID: UBTU-22-653015 | RULE ID: SV-260591r1015023 | CAT II

                CIS Controls:

                   Controls
                                                                 Control                                    IG 1 IG 2 IG 3
                   Version
                              8.2 Collect Audit Logs
                        v8      Collect audit logs. Ensure that logging, per the enterprise’s audit log      ●    ●    ●
                              management process, has been enabled across enterprise assets.
                              6.2 Activate audit logging
                        v7       Ensure that local logging has been enabled on all systems and networking    ●    ●    ●
                              devices.
                              6.3 Enable Detailed Logging
                        v7       Enable system logging to include detailed information such as an event
                              source, date, user, timestamp, source addresses, destination addresses, and
                                                                                                                  ●    ●
                              other useful elements.

                6.2.1.3 Ensure auditing for processes that start prior to auditd is
                enabled (Automated)
                Profile Applicability:

                • Level 2 - Server
                • Level 2 - Workstation

                Description:
                Configure grub2 so that processes that are capable of being audited can be audited
                even if they start up prior to auditd startup.

                Rationale:
                Audit events need to be captured on processes that start up prior to auditd , so that
                potential malicious activity cannot go undetected.
                Audit:
                Run the following command:
                # find /boot -type f -name 'grub.cfg' -exec grep -Ph -- '^\h*linux' {} + |
                grep -v 'audit=1'
                Nothing should be returned.
                Remediation:
                Edit /etc/default/grub or a file in /etc/default/grub.d/ ending in .cfg adding
                audit=1 to GRUB_CMDLINE_LINUX:
                Example:
                printf "\n%s\n" 'GRUB_CMDLINE_LINUX="audit=1"' >> /etc/default/grub.d/40-
                custom.cfg
                Run the following command to update the grub2 configuration:
                # update-grub

                References:

                     1. NIST SP 800-53 Rev. 5: AU-2, AU-3, AU-12
                     2. STIG ID: UBTU-20-010198 | RULE ID: SV-238299r991555 | CAT II
                     3. STIG ID: UBTU-22-212015 | RULE ID: SV-260471r991555 | CAT II

                Additional Information:
                This recommendation is designed around the grub2 bootloader, if another bootloader is
                in use in your environment enact equivalent settings.

                CIS Controls:

                    Controls
                                                                   Control                                    IG 1 IG 2 IG 3
                    Version
                                8.2 Collect Audit Logs
                        v8        Collect audit logs. Ensure that logging, per the enterprise’s audit log      ●    ●    ●
                                management process, has been enabled across enterprise assets.
                                6.2 Activate audit logging
                        v7         Ensure that local logging has been enabled on all systems and networking    ●    ●    ●
                                devices.
  '6.2.1.3': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '6.2.1.4': !unsafe |-
    Edit /etc/default/grub or a file in /etc/default/grub.d/ ending in .cfg adding
                audit_backlog_limit=N to GRUB_CMDLINE_LINUX. The recommended size for N is
                8192 or larger`:
                Example:
                # printf "\n%s\n" 'GRUB_CMDLINE_LINUX="audit_backlog_limit=8192"' >>
                /etc/default/grub.d/40-custom.cfg
                Run the following command to update the grub2 configuration:
                # update-grub

                Default Value:
                if audit_backlog_limit is not set, the system defaults to audit_backlog_limit=64

                References:

                     1. NIST SP 800-53 Rev. 5: AU-2, AU-3, AU-12

                CIS Controls:

                   Controls
                                                                 Control                                    IG 1 IG 2 IG 3
                   Version
                              8.2 Collect Audit Logs
                        v8      Collect audit logs. Ensure that logging, per the enterprise’s audit log      ●    ●    ●
                              management process, has been enabled across enterprise assets.
                              6.2 Activate audit logging
                        v7       Ensure that local logging has been enabled on all systems and networking    ●    ●    ●
                              devices.
                              6.3 Enable Detailed Logging
                        v7       Enable system logging to include detailed information such as an event
                              source, date, user, timestamp, source addresses, destination addresses, and
                                                                                                                  ●    ●
                              other useful elements.

                6.2.2 Configure Data Retention

                When auditing, it is important to carefully configure the storage requirements for audit
                logs. By default, auditd will max out the log files at 5MB and retain only 4 copies of
                them. Older versions will be deleted. It is possible on a system that the 20 MBs of audit
                logs may fill up the system causing loss of audit data. While the recommendations here
                provide guidance, check your site policy for audit storage requirements.
  '6.2.2.1': !unsafe |-
    Set the following parameter in /etc/audit/auditd.conf in accordance with site
                policy:
                max_log_file = <MB>

                Default Value:
                max_log_file = 8

                References:

                     1. NIST SP 800-53 Rev. 5: AU-8

                Additional Information:
                The max_log_file parameter is measured in megabytes.
                Other methods of log rotation may be appropriate based on site policy. One example is
                time-based rotation strategies which don't have native support in auditd configurations.
                Manual audit of custom configurations should be evaluated for effectiveness and
                completeness.

                CIS Controls:

                    Controls
                                                                 Control                                         IG 1 IG 2 IG 3
                    Version
                                8.3 Ensure Adequate Audit Log Storage
                         v8        Ensure that logging destinations maintain adequate storage to comply with      ●    ●    ●
                                the enterprise’s audit log management process.
                                6.4 Ensure adequate storage for logs
                         v7        Ensure that all systems that store logs have adequate storage space for the         ●    ●
                                logs generated.
  '6.2.2.2': !unsafe |-
    Set the following parameter in /etc/audit/auditd.conf:
                max_log_file_action = keep_logs

                References:

                     1. NIST SP 800-53 Rev. 5: AU-8

                CIS Controls:

                    Controls
                                                                 Control                                         IG 1 IG 2 IG 3
                    Version
                                8.3 Ensure Adequate Audit Log Storage
                         v8        Ensure that logging destinations maintain adequate storage to comply with      ●    ●    ●
                                the enterprise’s audit log management process.
                                6.4 Ensure adequate storage for logs
                         v7        Ensure that all systems that store logs have adequate storage space for the         ●    ●
                                logs generated.
  '6.2.2.3': !unsafe |-
    Set one of the following parameters in /etc/audit/auditd.conf depending on your
                local security policies.
                disk_full_action = <halt|single>
                disk_error_action = <syslog|single|halt>
                Example:
                disk_full_action = halt
                disk_error_action = halt

                References:

                     1. NIST SP 800-53 Rev. 5: AU-2, AU-8, AU-12, SI-5
                     2. AUDITD.CONF(5)

                CIS Controls:

                    Controls
                                                                   Control                                     IG 1 IG 2 IG 3
                    Version
                                8.2 Collect Audit Logs
                        v8        Collect audit logs. Ensure that logging, per the enterprise’s audit log       ●    ●    ●
                                management process, has been enabled across enterprise assets.
                                8.3 Ensure Adequate Audit Log Storage
                        v8         Ensure that logging destinations maintain adequate storage to comply with    ●    ●    ●
                                the enterprise’s audit log management process.
  '6.2.2.4': !unsafe |-
    Set the space_left_action parameter in /etc/audit/auditd.conf to email, exec,
                single, or halt:
                Example:
                space_left_action = email
                Set the admin_space_left_action parameter in /etc/audit/auditd.conf to
                single or halt:
                Example:
                admin_space_left_action = single
                Note: A Mail Transfer Agent (MTA) must be installed and configured properly to set
                space_left_action = email

                References:

                     1. NIST SP 800-53 Rev. 5: AU-2, AU-8, AU-12, SI-5
                     2. AUDITD.CONF(5)

                CIS Controls:

                    Controls
                                                                   Control                                     IG 1 IG 2 IG 3
                    Version
                                8.2 Collect Audit Logs
                        v8        Collect audit logs. Ensure that logging, per the enterprise’s audit log       ●    ●    ●
                                management process, has been enabled across enterprise assets.
                                8.3 Ensure Adequate Audit Log Storage
                        v8         Ensure that logging destinations maintain adequate storage to comply with    ●    ●    ●
                                the enterprise’s audit log management process.
                                6.2 Activate audit logging
                        v7         Ensure that local logging has been enabled on all systems and networking     ●    ●    ●
                                devices.

                6.2.3 Configure auditd Rules

                The Audit system operates on a set of rules that define what is to be captured in the log
                files. The following types of Audit rules can be specified:

                     •   Control rules: Allow the Audit system's behavior and some of its configuration to
                         be modified.
                     •   File system rules: Allow the auditing of access to a particular file or a directory
                         (Also known as file watches).

                         The syntax of these watch rules generally follow this format: -a always,exit -
                         F arch=b64 -F path=path-to-file -F perm=permissions -F
                         key=keyname

                     •   System call rules: Allow logging of system calls that any specified program
                         makes.

                       Syscall rules take the general form of: -a action,list -S syscall -F
                       field=value -k keyname
                Audit rules can be set:

                     •   On the command line using the auditctl utility, these rules are not persistent
                         across reboots.
                     •   In a file ending in .rules in the /etc/audit/rules.d/ directory.

                Note:

                     •   The rule must start with -a always,exit
                     •   The arch= field e.g. arch=b64 is optional but strongly recommended for best
                         performance. b64 is used in the example, but the arch can also be what is on
                         your system (x86_64, aarch64, ppc64le, s390x, etc).
                     •   The field -S all will be added by auditctl if it is not included in the rule.
                     •   The rule must include the path= field shown in the example output.
                     •   The rule must include the perm= field shown in the example output. The options
                         e.g. wa may include additional options e.g. rwxa
                     •   The key= field e.g. key=scope is optional but strongly recommended. Though the
                         name scope is used in the example, this can be set to any name.
                     •   The -w option e.g. -w /etc/sudoers -p wa -k scope is deprecated. While the
                         deprecated -w is in a "passing" state, it is recommended to update the rule in
                         accordance with the Remediation Procedure as the -w will soon be unsupported.

                The following script creates and runs a function called f_audit_rules_format_chk
                that may be used to determine if audit rules have deprecated -w format or missing
                arch= option:

                #!/usr/bin/env bash

                f_audit_rules_format_chk()
                {
                   rules_dir="/etc/audit/rules.d"
                   deprecated_flag="-w"
                   a_output=()
                   for file in "$rules_dir"/*rules; do
                   [[ -e "$file" ]] || continue
                   while IFS= read -r line; do
                   # Skip empty or commented lines
                   [[ "$line" =~ ^\s*# ]] || [[ -z "$line" ]] && continue
                   # Check for deprecated -w
                   if [[ "$line" == *"$deprecated_flag"* ]]; then
                   a_output+=("Deprecated '-w' found in $file
                   Rule:
                   $(wrap_text "$line")
                   Flag: $deprecated_flag")
                   fi
                   # Check for missing -F arch
                   if [[ "$line" =~ ^-a[[:space:]]+always,exit ]] && [[ "$line" != *"-F
                arch="* ]] \
                   ; then
                   a_output+=("Missing '-F arch=' in $file
                   Rule:
                   $(wrap_text "$line")
                   Flag: Missing arch")
                   fi
                   done < "$file"
                   done
                   if [ ${#a_output[@]} -eq 0 ]; then
                   echo -e "No issues found in audit rules files"
                   else
                   echo -e "\nAudit rules issue detected:"
                   for output in "${a_output[@]}"; do
                   echo -e "\n$output"
                   done
                   fi
                }
                # wrap_text function for formatting
                wrap_text() {
                    echo "$1"
                }
                f_audit_rules_format_chk
  '6.2.3.1': !unsafe |-
    Note:

                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.
                     •   For best performance, the arch field should be supplied in the rule. The
                         individual permissions will cause the selection of specific system calls that use
                         that kind of access. Not supplying the arch will cause the selection of all system
                         calls which will affect performance as all system calls will be evaluated.

                     1. Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                        extension, with the relevant rules to monitor scope changes for system
                        administrators.

                Example:
                # printf '%s\n' "
                -a always,exit -F arch=b64 -S all -F path=/etc/sudoers -F perm=wa -k scope
                -a always,exit -F arch=b64 -S all -F dir=/etc/sudoers.d -F perm=wa -k scope
                " >> /etc/audit/rules.d/50-scope.rules

                     2. Merge and load the rules into active configuration:

                # augenrules --load

                     3. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. NIST SP 800-53 :: AU-12 a
                     2. NIST SP 800-53A :: AU-12.1 (ii)
                     3. STIG ID: RHEL-08-030171 | RULE ID: SV-230409r627750 | CAT II
                     4. STIG ID: RHEL-08-030172 | RULE ID: SV-230410r627750 | CAT II
                     5. STIG ID: RHEL-09-654215 | RULE ID: SV-258217r1045436 | CAT II
                     6. STIG ID: RHEL-09-654220 | RULE ID: SV-258218r1045439 | CAT II
                     7. STIG ID: ALMA-09-004970 | RULE ID: SV-269129r1050011 | CAT II
                     8. STIG ID: ALMA-09-006070 | RULE ID: SV-269135r1050017 | CAT II
                     9. STIG ID: UBTU-22-654220 | RULE ID: SV-260646r991575 | CAT II
                     10. STIG ID: UBTU-22-654225 | RULE ID: SV-260647r991575 | CAT II
                     11. STIG ID: UBTU-24-900510 | RULE ID: SV-270807r1066910 | CAT II
                     12. STIG ID: UBTU-24-900520 | RULE ID: SV-270808r1067100 | CAT II

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.
                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.
                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              4.8 Log and Alert on Changes to Administrative Group
                       v7     Membership                                                                                   ●    ●
                                  Configure systems to issue a log entry and alert when an account is added to
                              or removed from any group assigned administrative privileges.
  '6.2.3.2': !unsafe |-
    Note:

                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.
                     •   For best performance, the arch field should be supplied in the rule. The
                         individual permissions will cause the selection of specific system calls that use
                         that kind of access. Not supplying the arch will cause the selection of all system
                         calls which will affect performance as all system calls will be evaluated

                     1. Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                        extension, with the relevant rules to monitor elevated privileges.

                Example:
                # printf "
                -a always,exit -F arch=b64 -C euid!=uid -F auid!=unset -S execve -k
                user_emulation
                " >> /etc/audit/rules.d/50-user_emulation.rules

                     2. Merge and load the rules into active configuration:

                # augenrules --load

                     3. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. NIST SP 800-53 Revision 5 :: AC-6 (8)
                     2. https://docs.redhat.com/en/documentation/red_hat_enterprise_linux/10/html/cons
                        iderations_in_adopting_rhel_10/architectures
                     3. STIG ID: RHEL-08-030000 | RULE ID: SV-230386r854037 | CAT II
                     4. STIG ID: RHEL-09-654010 | RULE ID: SV-258176r1045313 | CAT II
                     5. STIG ID: ALMA-09-007280 | RULE ID: SV-269146r1050028 | CAT II
                     6. STIG ID: UBTU-22-654230 | RULE ID: SV-260648r958730 | CAT II
                     7. STIG ID: UBTU-24-200580 | RULE ID: SV-270689r1066556 | CAT II

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.

                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.
                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              4.9 Log and Alert on Unsuccessful Administrative Account
                       v7     Login                                                                                        ●    ●
                                Configure systems to issue a log entry and alert on unsuccessful logins to an
                              administrative account.
  '6.2.3.3': !unsafe |-
    Note:

                     •   This recommendation requires that the sudo logfile is configured. For additional
                         information see guidance provided in the recommendation "Ensure sudo log file
                         exists".
                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.
                     •   For best performance, the arch field should be supplied in the rule. The
                         individual permissions will cause the selection of specific system calls that use
                         that kind of access. Not supplying the arch will cause the selection of all system
                         calls which will affect performance as all system calls will be evaluated.

                     1. Edit the file /etc/sudoers or a file in /etc/sudoers.d/ with visudo -f <PATH
                        TO FILE> and add the following line:

                Defaults logfile="<PATH TO CUSTOM LOG FILE>"
                Example:
                Defaults logfile="/var/log/sudo.log"

                     2. Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                        extension, with the relevant rules to monitor events that modify the sudo log file.

                Example:
                # printf "
                -a always,exit -F arch=b64 -S all -F path=/var/log/sudo.log -F perm=wa -k
                sudo_log_file
                " >> /etc/audit/rules.d/50-sudo.rules
                Note: path= should be set to the same sudo log file location as configured above in
                step 1.

                     3. Merge and load the rules into active configuration:

                # augenrules --load

                     4. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. STIG ID: UBTU-22-654235 | RULE ID: SV-260649r986298 | CAT II
                     2. STIG ID: UBTU-24-500010 | RULE ID: SV-270740r1066709 | CAT II

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.
                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.
                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              4.9 Log and Alert on Unsuccessful Administrative Account
                       v7     Login                                                                                        ●    ●
                                Configure systems to issue a log entry and alert on unsuccessful logins to an
                              administrative account.

                6.2.3.4 Ensure events that modify date and time information are
                collected (Automated)
                Profile Applicability:

                • Level 2 - Server
                • Level 2 - Workstation

                Description:
                Capture events where the system date and/or time has been modified. The parameters
                in this section are set to determine if the;

                     •   adjtimex - tune kernel clock
                     •   settimeofday - set time using timeval and timezone structures
                     •   stime - using seconds since 1/1/1970
                     •   clock_settime - allows for the setting of several internal clocks and timers

                system calls have been executed. Further, ensure to write an audit record to the
                configured audit log file upon exit, tagging the records with a unique identifier such as
                "time-change".
                Rationale:
                Unexpected changes in system date and/or time could be a sign of malicious activity on
                the system.

                Audit:
                Note: The -w option is deprecated and the rule should be updated in accordance with
                the Remediation Procedure.
                Run the following command to verify a rule exits in the running configuration:
                1a. Verify a rule exists for adjtimex, settimeofday, clock_settime in the running
                configuration:
                # auditctl -l | grep -Ps -- '(adjtimex|settimeofday|clock_settime)'
                Verify output of matches:
                -a always,exit -S adjtimex,settimeofday
                -a always,exit -S clock_settime -F a0=0x0
                1b. verify a rule for time-change exists in the running configuration:
                # auditctl -l | grep -Ps -- '^\h*[^#\n\r]+\h*\/etc\/localtime'
                Verify the output matches:
                -a always,exit -F path=/etc/localtime -F perm=wa
                Run the following command to verify a rule exists in a rules file:
                2a. Run the following command to verify a rule exists for adjtimex, settimeofday,
                clock_settime in a rules file:
                # grep -Ps -- '(adjtimex|settimeofday|clock_settime)'
                /etc/audit/rules.d/*.rules
                Example output:
                /etc/audit/rules.d/50-time-change.rules:-a always,exit -F arch=b64 -S
                adjtimex,settimeofday -k time-change
                /etc/audit/rules.d/50-time-change.rules:-a always,exit -F arch=b64 -S
                clock_settime -F a0=0x0 -k time-change
                2b. Run the following command to verify a rule exists for /etc/localtime in a rules file:
                # grep -Ps -- '^\h*[^#\n\r]+\h*\/etc\/localtime' /etc/audit/rules.d/*.rules
                Example output:
                /etc/audit/rules.d/50-time-change.rules:-a always,exit -F arch=b64 -S all -F
                path=/etc/localtime -F perm=wa -k localtime-change

                Remediation:
                Note:

                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.
                     •   For best performance, the arch field should be supplied in the rule. The
                         individual permissions will cause the selection of specific system calls that use
                         that kind of access. Not supplying the arch will cause the selection of all system
                         calls which will affect performance as all system calls will be evaluated.

                Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules extension,
                with the relevant rules to monitor events that modify date and time information.
                1a. Create a rule for adjtimex, settimeofday, clock_settime:
                Example
                # printf "
                -a always,exit -F arch=b64 -S adjtimex,settimeofday -k time-change
                -a always,exit -F arch=b64 -S clock_settime -F a0=0x0 -k time-change
                " >> /etc/audit/rules.d/50-time-change.rules
                1b. Create a rule for /etc/localtime:
                Example
                # printf "
                -a always,exit -F arch=b64 -S all -F path=/etc/localtime -F perm=wa -k
                localtime-change
                " >> /etc/audit/rules.d/50-local-time-change.rules

                     2. Merge and load the rules into active configuration:

                # augenrules --load

                     3. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. NIST SP 800-53 Rev. 5: AU-3, CM-6

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.

                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.
                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              5.5 Implement Automated Configuration Monitoring
                              Systems
                       v7        Utilize a Security Content Automation Protocol (SCAP) compliant configuration             ●    ●
                              monitoring system to verify all security configuration elements, catalog approved
                              exceptions, and alert when unauthorized changes occur.

                6.2.3.5 Ensure events that modify sethostname and
                setdomainname are collected (Automated)
                Profile Applicability:

                • Level 2 - Server
                • Level 2 - Workstation

                Description:
                Record changes to network environment files or system calls. The below parameters
                monitors the following system calls, and write an audit event on system call exit:

                     •   sethostname - set the systems host name
                     •   setdomainname - set the systems domain name

                Rationale:
                Monitoring sethostname and setdomainname will identify potential unauthorized
                changes to host and domain name of a system. The changing of these names could
                potentially break security parameters that are set based on those names.
                Audit:
                Note:

                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.

                     1. Run the following command to verify a rule exists in the running configuration:

                # auditctl -l | grep -Ps -- 'sethostname|setdomainname'
                Verify the output matches:
                -a always,exit -S sethostname,setdomainname

                     2. Run the following command to verify a rule exists in a rules file:

                # grep -Ps -- 'sethostname|setdomainname' /etc/audit/rules.d/*.rules
                Example output:
                /etc/audit/rules.d/50-system_locale.rules:-a always,exit -F arch=b64 -S
                sethostname,setdomainname -k system-locale

                Remediation:
                Note:

                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.
                     •   For best performance, the arch field should be supplied in the rule. The
                         individual permissions will cause the selection of specific system calls that use
                         that kind of access. Not supplying the arch will cause the selection of all system
                         calls which will affect performance as all system calls will be evaluated.

                     1. Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                        extension, with the relevant rules to monitor events that modify the system's
                        network environment.

                Example:
                # printf "
                -a always,exit -F arch=b64 -S sethostname,setdomainname -k system-locale
                " >> /etc/audit/rules.d/50-system_locale.rules

                     2. Merge and load the rules into active configuration:

                # augenrules --load

                     3. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. NIST SP 800-53 Rev. 5: AU-3, CM-6

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.
                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.

                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              5.5 Implement Automated Configuration Monitoring
                              Systems
                       v7        Utilize a Security Content Automation Protocol (SCAP) compliant configuration             ●    ●
                              monitoring system to verify all security configuration elements, catalog approved
                              exceptions, and alert when unauthorized changes occur.

                6.2.3.6 Ensure events that modify /etc/issue and /etc/issue.net are
                collected (Automated)
                Profile Applicability:

                • Level 2 - Server
                • Level 2 - Workstation

                Description:
                Record changes to network environment files or system calls. The files being monitored
                are:

                     •   /etc/issue and /etc/issue.net - messages displayed pre-login

                Rationale:
                Monitoring /etc/issue and /etc/issue.net is important, as intruders could put
                disinformation into those files and trick users into providing information to the intruder.
                Audit:
                Note: The -w option is deprecated and the rule should be updated in accordance with
                the Remediation Procedure.

                     1. Run the following command to verify a rule exists in the running configuration:

                # auditctl -l | grep -Ps -- '^\h*[^#\n\r]+\h*\/etc\/issue'
                Verify the output includes:
                -a always,exit -F path=/etc/issue -F perm=wa
                -a always,exit -F path=/etc/issue.net -F perm=wa

                     2. Run the following command to verify a rule exists in a rules file:

                # grep -Ps -- '^\h*[^#\n\r]+\h*\/etc\/issue' /etc/audit/rules.d/*.rules
                Example output:
                /etc/audit/rules.d/50-etc_issue_system_locale.rules:-a always,exit -F
                arch=b64 -S all -F path=/etc/issue -F perm=wa -k system-locale
                /etc/audit/rules.d/50-etc_issue_system_locale.rules:-a always,exit -F
                arch=b64 -S all -F path=/etc/issue.net -F perm=wa -k system-locale

                Remediation:
                Note:

                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.
                     •   For best performance, the arch field should be supplied in the rule. The
                         individual permissions will cause the selection of specific system calls that use
                         that kind of access. Not supplying the arch will cause the selection of all system
                         calls which will affect performance as all system calls will be evaluated.

                     1. Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                        extension, with the relevant rules to monitor events that modify the system's
                        network environment.

                Example:
                # printf "
                -a always,exit -F arch=b64 -S all -F path=/etc/issue -F perm=wa -k system-
                locale
                -a always,exit -F arch=b64 -S all -F path=/etc/issue.net -F perm=wa -k
                system-locale
                " >> /etc/audit/rules.d/50-etc_issue_system_locale.rules

                     2. Merge and load the rules into active configuration:

                # augenrules --load

                     3. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. NIST SP 800-53 Rev. 5: AU-3, CM-6

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.
                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.

                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              5.5 Implement Automated Configuration Monitoring
                              Systems
                       v7        Utilize a Security Content Automation Protocol (SCAP) compliant configuration             ●    ●
                              monitoring system to verify all security configuration elements, catalog approved
                              exceptions, and alert when unauthorized changes occur.

                6.2.3.7 Ensure events that modify /etc/hosts and /etc/hostname
                are collected (Automated)
                Profile Applicability:

                • Level 2 - Server
                • Level 2 - Workstation

                Description:
                Record changes to network environment files or system calls. The files being monitored
                are:

                     •   /etc/hosts - file containing host names and associated IP addresses
                     •   /etc/hostname - file contains the system's host name

                Rationale:
                The /etc/hosts file is monitored for changes that can indicate an unauthorized intruder
                is trying to change machine associations with IP addresses and trick users and
                processes into connecting to unintended machines.
                Audit:
                Note: The -w option is deprecated and the rule should be updated in accordance with
                the Remediation Procedure.

                     1. Run the following command to verify a rule exists in the running configuration:

                # auditctl -l | grep -Ps -- '^\h*[^#\n\r]+\h*\/etc\/host'
                Verify the output includes:
                -a always,exit -F path=/etc/hosts -F perm=wa
                -a always,exit -F path=/etc/hostname -F perm=wa

                     2. Run the following command to verify a rule exists in a rules file:

                # grep -Ps -- '^\h*[^#\n\r]+\h*\/etc\/host' /etc/audit/rules.d/*.rules
                Example output:
                /etc/audit/rules.d/50-etc_host_system_locale.rules:-a always,exit -F arch=b64
                -S all -F path=/etc/hosts -F perm=wa -k system-locale
                /etc/audit/rules.d/50-etc_host_system_locale.rules:-a always,exit -F arch=b64
                -S all -F path=/etc/hostname -F perm=wa -k system-locale

                Remediation:
                Note:

                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.
                     •   For best performance, the arch field should be supplied in the rule. The
                         individual permissions will cause the selection of specific system calls that use
                         that kind of access. Not supplying the arch will cause the selection of all system
                         calls which will affect performance as all system calls will be evaluated.

                     1. Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                        extension, with the relevant rules to monitor events that modify the system's
                        network environment.

                Example:
                # printf "
                -a always,exit -F arch=b64 -S all -F path=/etc/hosts -F perm=wa -k system-
                locale
                -a always,exit -F arch=b64 -S all -F path=/etc/hostname -F perm=wa -k system-
                locale
                " >> /etc/audit/rules.d/50-etc_host_system_locale.rules

                     2. Merge and load the rules into active configuration:

                # augenrules --load

                     3. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. NIST SP 800-53 Rev. 5: AU-3, CM-6

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.
                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.

                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              5.5 Implement Automated Configuration Monitoring
                              Systems
                       v7        Utilize a Security Content Automation Protocol (SCAP) compliant configuration             ●    ●
                              monitoring system to verify all security configuration elements, catalog approved
                              exceptions, and alert when unauthorized changes occur.

                6.2.3.8 Ensure events that modify the system's network
                environment are collected (Automated)
                Profile Applicability:

                • Level 2 - Server
                • Level 2 - Workstation

                Description:
                Record changes to network environment files or system calls. The files being monitored
                are:

                     •   /etc/network/interfaces - This file describes the network interfaces available
                         on your

                         system # and how to activate them.

                     •   /etc/network/interfaces.d/ - directory containing network interface scripts
                         and configurations files
                     •   /etc/netplan/ - central location for YAML networking configurations files

                Rationale:
                Monitoring the systems network environment is important as it can show if network
                interfaces or scripts are being modified in a way that can lead to the machine becoming
                unavailable or compromised. All audit records should have a relevant tag associated
                with them.

                Audit:
                Note: The -w option is deprecated and the rule should be updated in accordance with
                the Remediation Procedure.

                     1. Run the following command to verify a rule exists in the running configuration:

                # auditctl -l | grep -Ps -- '\/etc/net'
                Verify the output includes:
                -a always,exit -F path=/etc/network/interfaces -F perm=wa
                -a always,exit -F dir=/etc/network/interfaces.d -F perm=wa
                -a always,exit -F dir=/etc/netplan/ -F perm=wa

                     2. Run the following command to verify a rule exists in a rules file:

                # grep -Ps -- '\/etc/net' /etc/audit/rules.d/*.rules
                Example output:
                /etc/audit/rules.d/50-etc_sysconfig_system_locale.rules:-a always,exit -F
                arch=b64 -S all -F path=/etc/network/interfaces -F perm=wa -k system-locale
                /etc/audit/rules.d/50-etc_sysconfig_system_locale.rules:-a always,exit -F
                arch=b64 -S all -F dir=/etc/network/interfaces.d -F perm=wa -k system-locale
                /etc/audit/rules.d/50-etc_sysconfig_system_locale.rules:-a always,exit -F
                arch=b64 -S all -F dir=/etc/netplan/ -F perm=wa -k system-locale

                Remediation:
                Note:

                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.
                     •   For best performance, the arch field should be supplied in the rule. The
                         individual permissions will cause the selection of specific system calls that use
                         that kind of access. Not supplying the arch will cause the selection of all system
                         calls which will affect performance as all system calls will be evaluated.

                     1. Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                        extension, with the relevant rules to monitor events that modify the system's
                        network environment.

                Example:
                # printf "
                -a always,exit -F arch=b64 -S all -F path=/etc/network/interfaces -F perm=wa
                -k system-locale
                -a always,exit -F arch=b64 -S all -F dir=/etc/network/interfaces.d -F perm=wa
                -k system-locale
                -a always,exit -F arch=b64 -S all -F dir=/etc/netplan/ -F perm=wa -k system-
                locale
                " >> /etc/audit/rules.d/50-etc_sysconfig_system_locale.rules

                     2. Merge and load the rules into active configuration:

                # augenrules --load

                     3. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. NIST SP 800-53 Rev. 5: AU-3, CM-6

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.

                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.
                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              5.5 Implement Automated Configuration Monitoring
                              Systems
                       v7        Utilize a Security Content Automation Protocol (SCAP) compliant configuration             ●    ●
                              monitoring system to verify all security configuration elements, catalog approved
                              exceptions, and alert when unauthorized changes occur.

                6.2.3.9 Ensure events that modify /etc/NetworkManager directory
                are collected (Automated)
                Profile Applicability:

                • Level 2 - Server
                • Level 2 - Workstation

                Description:
                Record changes to network environment files or system calls. The files being monitored
                are:

                     •   /etc/NetworkManager/ - directory contains configuration files and settings used
                         by the NetworkManager

                Rationale:
                Monitoring the /etc/NetworkManager/ directory will identify potential unauthorized
                changes to network configuration files.
                Audit:
                Note: The -w option is deprecated and the rule should be updated in accordance with
                the Remediation Procedure.

                     1. Run the following command to verify a rule exists in the running configuration:

                # auditctl -l | grep -Ps -- '\/etc/NetworkManager'
                Verify the output includes:
                -a always,exit -F dir=/etc/NetworkManager/ -F perm=wa

                     2. Run the following command to verify a rule exists in a rules file:

                # grep -Ps -- '\/etc\/NetworkManager' /etc/audit/rules.d/*.rules
                Example output:
                /etc/audit/rules.d/50-etc_NetworkManager_system_locale.rules:-a always,exit -
                F arch=b64 -S all -F dir=/etc/NetworkManager/ -F perm=wa -k system-locale

                Remediation:
                Note:

                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.
                     •   For best performance, the arch field should be supplied in the rule. The
                         individual permissions will cause the selection of specific system calls that use
                         that kind of access. Not supplying the arch will cause the selection of all system
                         calls which will affect performance as all system calls will be evaluated.

                     1. Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                        extension, with the relevant rules to monitor events that modify the system's
                        network environment.

                Example:
                # printf "
                -a always,exit -F arch=b64 -S all -F dir=/etc/NetworkManager/ -F perm=wa -k
                system-locale
                " >> /etc/audit/rules.d/50-etc_NetworkManager_system_locale.rules

                     2. Merge and load the rules into active configuration:

                # augenrules --load

                     3. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. NIST SP 800-53 Rev. 5: AU-3, CM-6

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.
                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.

                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              5.5 Implement Automated Configuration Monitoring
                              Systems
                       v7        Utilize a Security Content Automation Protocol (SCAP) compliant configuration             ●    ●
                              monitoring system to verify all security configuration elements, catalog approved
                              exceptions, and alert when unauthorized changes occur.
  '6.2.3.4': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '6.2.3.5': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '6.2.3.6': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '6.2.3.8': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '6.2.3.9': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '6.2.3.10': !unsafe |-
    Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules extension,
                with the relevant rules to monitor the use of privileged commands.
                Example script:
                #!/usr/bin/env bash

                {
                  UID_MIN=$(awk '/^\s*UID_MIN/{print $2}' /etc/login.defs)
                  AUDIT_RULE_FILE="/etc/audit/rules.d/50-privileged.rules"
                  NEW_DATA=()
                  for PARTITION in $(findmnt -n -l -k -it $(awk '/nodev/ { print $2 }'
                /proc/filesystems | paste -sd,) | grep -Pv "noexec|nosuid" | awk '{print
                $1}'); do
                    readarray -t DATA < <(find "${PARTITION}" -xdev -perm /6000 -type f | awk
                -v UID_MIN=${UID_MIN} '{print "-a always,exit -F arch=b64 -S all -F path=" $1
                " -F perm=x -F auid>="UID_MIN" -F auid!=unset -k privileged" }')
                       for ENTRY in "${DATA[@]}"; do
                         NEW_DATA+=("${ENTRY}")
                       done
                  done
                  readarray &> /dev/null -t OLD_DATA < "${AUDIT_RULE_FILE}"
                  COMBINED_DATA=( "${OLD_DATA[@]}" "${NEW_DATA[@]}" )
                  printf '%s\n' "${COMBINED_DATA[@]}" | sort -u > "${AUDIT_RULE_FILE}"
                }
                Merge and load the rules into active configuration:
                # augenrules --load
                Check if reboot is required.
                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi
                Special mount points
                If there are any special mount points that are not visible by default from just scanning /,
                change the PARTITION variable to the appropriate partition and re-run the remediation.

                References:

                     1. NIST SP 800-53 Rev. 5: AU-3, AU-3(1)

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.
                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.

                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              6.2 Activate audit logging
                       v7        Ensure that local logging has been enabled on all systems and networking             ●    ●    ●
                              devices.
  '6.2.3.11': !unsafe |-
    Note: The -w option is deprecated and the rule should be updated in accordance with
                the Remediation Procedure.

                     1. Edit the /etc/login.defs file and create or add the UID_MIN setting.

                Example
                UID_MIN 1000

                     2. Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                        extension, with the relevant rules to monitor unsuccessful file access attempts.

                Example:
                # printf "
                -a always,exit -F arch=b64 -S creat,open,openat,truncate,ftruncate -F exit=-
                EACCES -F auid>=1000 -F auid!=unset -k access
                -a always,exit -F arch=b64 -S creat,open,openat,truncate,ftruncate -F exit=-
                EPERM -F auid>=1000 -F auid!=unset -k access
                " >> /etc/audit/rules.d/50-access.rules
                Note: The auid>= value should be set to the same value as UID_MIN value in
                /etc/login.defs.

                     3. Merge and load the rules into active configuration:

                # augenrules --load

                     4. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. NIST SP 800-53 :: AU-12 a
                     2. NIST SP 800-53A :: AU-12.1 (ii)
                     3. STIG ID: RHEL-08-030420 | RULE ID: SV-230449r810455 | CAT II
                     4. STIG ID: RHEL-09-654070 | RULE ID: SV-258188r1045349 | CAT II
                     5. STIG ID: ALMA-09-048090 | RULE ID: SV-269475r1050358 | CAT II
                     6. STIG ID: UBTU-22-654165 | RULE ID: SV-260635r958446 | CAT II
                     7. STIG ID: UBTU-24-900160 | RULE ID: SV-270787r1068378 | CAT II

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.

                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.
                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              14.9 Enforce Detail Logging for Access or Changes to
                              Sensitive Data
                       v7         Enforce detailed audit logging for access to sensitive data or changes to                     ●
                              sensitive data (utilizing tools such as File Integrity Monitoring or Security
                              Information and Event Monitoring).

                6.2.3.12 Ensure events that modify /etc/group information are
                collected (Automated)
                Profile Applicability:

                • Level 2 - Server
                • Level 2 - Workstation

                Description:
                Record events affecting the modification of user or group information, including that of
                passwords and old passwords if in use.

                     •   /etc/group - system groups

                Rationale:
                Unexpected changes to these files could be an indication that the system has been
                compromised and that an unauthorized user is attempting to hide their activities or
                compromise additional accounts.
                Satisfies: SRG-OS-000062-GPOS-00031, SRG-OS-000004-GPOS-00004, SRG-OS-
                000037-GPOS-00015, SRG-OS-000042-GPOS-00020, SRG-OS-000062-GPOS-00031,
                SRG-OS-000304-GPOS-00121, SRG-OS-000392-GPOS-00172, SRG-OS-000462-
                GPOS-00206, SRG-OS-000470-GPOS-00214, SRG-OS-000471-GPOS-00215, SRG-
                OS-000239-GPOS-00089, SRG-OS-000240-GPOS-00090, SRG-OS-000241-GPOS-
                00091, SRG-OS-000303-GPOS-00120, SRG-OS-000304-GPOS-00121, SRG-OS-
                000476-GPOS-00221
                Audit:
                Note: The -w option is deprecated and the rule should be updated in accordance with
                the Remediation Procedure.

                     1. Run the following command to verify a rule exists in the running configuration:

                # auditctl -l | grep -Ps -- '^\h*[^#\n\r]+\h*\/etc\/group'
                Verify the output includes:
                -a always,exit -F path=/etc/group -F perm=wa

                     2. Run the following command to verify a rule exists in a rules file:

                # grep -Ps -- '^\h*[^#\n\r]+\h*\/etc\/group' /etc/audit/rules.d/*.rules
                Example output:
                /etc/audit/rules.d/50-identity.rules:-a always,exit -F arch=b64 -S all -F
                path=/etc/group -F perm=wa -k identity

                Remediation:
                Note:

                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.
                     •   For best performance, the arch field should be supplied in the rule. The
                         individual permissions will cause the selection of specific system calls that use
                         that kind of access. Not supplying the arch will cause the selection of all system
                         calls which will affect performance as all system calls will be evaluated.

                     1. Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                        extension, with the relevant rules to monitor events that modify user/group
                        information.

                Example:
                # printf "
                -a always,exit -F arch=b64 -S all -F path=/etc/group -F perm=wa -k identity
                " >> /etc/audit/rules.d/50-identity.rules

                     2. Merge and load the rules into active configuration:

                # augenrules --load

                     3. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. https://manpages.debian.org/bookworm/manpages/nsswitch.conf.5.en.html
                     2. https://access.redhat.com/documentation/en-
                        us/red_hat_enterprise_linux/6/html/managing_smart_cards/pam_configuration_fil
                        es
                     3. NIST SP 800-53 :: AU-12 a
                     4. NIST SP 800-53A :: AU-12.1 (ii)
                     5. STIG ID: RHEL-08-030170 | RULE ID: SV-230408r1017214 | CAT II
                     6. STIG ID: RHEL-09-654225 | RULE ID: SV-258227r1014992 | CAT II
                     7. STIG ID: ALMA-09-005080 | RULE ID: SV-269129r1050011 | CAT II
                     8. STIG ID: UBTU-22-654130 | RULE ID: SV-260628r958368 | CAT II
                     9. STIG ID: UBTU-24-200290 | RULE ID: SV-270685r1066544 | CAT II

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.
                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.
                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              4.8 Log and Alert on Changes to Administrative Group
                       v7     Membership                                                                                   ●    ●
                                  Configure systems to issue a log entry and alert when an account is added to
                              or removed from any group assigned administrative privileges.

                6.2.3.13 Ensure events that modify /etc/passwd information are
                collected (Automated)
                Profile Applicability:

                • Level 2 - Server
                • Level 2 - Workstation

                Description:
                Record events affecting the modification of user or group information, including that of
                passwords and old passwords if in use.

                     •   /etc/passwd - system users

                Rationale:
                Unexpected changes to these files could be an indication that the system has been
                compromised and that an unauthorized user is attempting to hide their activities or
                compromise additional accounts.
                Satisfies: SRG-OS-000062-GPOS-00031, SRG-OS-000004-GPOS-00004, SRG-OS-
                000037-GPOS-00015, SRG-OS-000042-GPOS-00020, SRG-OS-000062-GPOS-00031,
                SRG-OS-000304-GPOS-00121, SRG-OS-000392-GPOS-00172, SRG-OS-000462-
                GPOS-00206, SRG-OS-000470-GPOS-00214, SRG-OS-000471-GPOS-00215, SRG-
                OS-000239-GPOS-00089, SRG-OS-000240-GPOS-00090, SRG-OS-000241-GPOS-
                00091, SRG-OS-000303-GPOS-00120, SRG-OS-000304-GPOS-00121, SRG-OS-
                000476-GPOS-00221
                Audit:
                Note: The -w option is deprecated and the rule should be updated in accordance with
                the Remediation Procedure.

                     1. Run the following command to verify a rule exists in the running configuration:

                # auditctl -l | grep -Ps -- '^\h*[^#\n\r]+\h*\/etc\/passwd'
                Verify the output includes:
                -a always,exit -F path=/etc/passwd -F perm=wa

                     2. Run the following command to verify a rule exists in a rules file:

                # grep -Ps -- '^\h*[^#\n\r]+\h*\/etc\/passwd' /etc/audit/rules.d/*.rules
                Example output:
                /etc/audit/rules.d/50-identity.rules:-a always,exit -F arch=b64 -S all -F
                path=/etc/passwd -F perm=wa -k identity

                Remediation:
                Note:

                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.
                     •   For best performance, the arch field should be supplied in the rule. The
                         individual permissions will cause the selection of specific system calls that use
                         that kind of access. Not supplying the arch will cause the selection of all system
                         calls which will affect performance as all system calls will be evaluated.

                     1. Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                        extension, with the relevant rules to monitor events that modify user/group
                        information.

                Example:
                # printf "
                -a always,exit -F arch=b64 -S all -F path=/etc/passwd -F perm=wa -k identity
                " >> /etc/audit/rules.d/50-identity.rules

                     2. Merge and load the rules into active configuration:

                # augenrules --load

                     3. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. https://manpages.debian.org/bookworm/manpages/nsswitch.conf.5.en.html
                     2. https://access.redhat.com/documentation/en-
                        us/red_hat_enterprise_linux/6/html/managing_smart_cards/pam_configuration_fil
                        es
                     3. NIST SP 800-53 :: AU-12 a
                     4. NIST SP 800-53A :: AU-12.1 (ii)
                     5. STIG ID: RHEL-08-030150 | RULE ID: SV-230406r1017212 | CAT II
                     6. STIG ID: RHEL-09-654240 | RULE ID: SV-258230r958408 | CAT II
                     7. STIG ID: ALMA-09-005410 | RULE ID: SV-269133r1050015 | CAT II
                     8. STIG ID: UBTU-22-654145 | RULE ID:SV-260631r958368 | CAT II
                     9. STIG ID: UBTU-24-200280| RULE ID: SV-270684r1066541 | CAT II

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.
                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.
                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              4.8 Log and Alert on Changes to Administrative Group
                       v7     Membership                                                                                   ●    ●
                                  Configure systems to issue a log entry and alert when an account is added to
                              or removed from any group assigned administrative privileges.

                6.2.3.14 Ensure events that modify /etc/shadow and /etc/gshadow
                are collected (Automated)
                Profile Applicability:

                • Level 2 - Server
                • Level 2 - Workstation

                Description:
                Record events affecting the modification of user or group information, including that of
                passwords and old passwords if in use.

                     •   /etc/gshadow - encrypted password for each group
                     •   /etc/shadow - system user passwords

                Rationale:
                Unexpected changes to these files could be an indication that the system has been
                compromised and that an unauthorized user is attempting to hide their activities or
                compromise additional accounts.
                Satisfies: SRG-OS-000062-GPOS-00031, SRG-OS-000004-GPOS-00004, SRG-OS-
                000037-GPOS-00015, SRG-OS-000042-GPOS-00020, SRG-OS-000062-GPOS-00031,
                SRG-OS-000304-GPOS-00121, SRG-OS-000392-GPOS-00172, SRG-OS-000462-
                GPOS-00206, SRG-OS-000470-GPOS-00214, SRG-OS-000471-GPOS-00215, SRG-
                OS-000239-GPOS-00089, SRG-OS-000240-GPOS-00090, SRG-OS-000241-GPOS-
                00091, SRG-OS-000303-GPOS-00120, SRG-OS-000304-GPOS-00121, SRG-OS-
                000476-GPOS-00221

                Audit:
                Note: The -w option is deprecated and the rule should be updated in accordance with
                the Remediation Procedure.

                     1. Run the following command to verify a rule exists in the running configuration:

                # auditctl -l | grep -Ps -- '^\h*[^#\n\r]+\h*shadow'
                Verify the output includes:
                -a always,exit -F path=/etc/gshadow -F perm=wa
                -a always,exit -F path=/etc/shadow -F perm=wa

                     2. Run the following command to verify a rule exists in a rules file:

                # grep -Ps -- 'shadow' /etc/audit/rules.d/*.rules
                Example output:
                /etc/audit/rules.d/50-identity.rules:-a always,exit -F arch=b64 -S all -F
                path=/etc/gshadow -F perm=wa -k identity
                /etc/audit/rules.d/50-identity.rules:-a always,exit -F arch=b64 -S all -F
                path=/etc/shadow -F perm=wa -k identity
                Remediation:
                Note:
                   • The -w option is deprecated and the rule should be updated in accordance with
                      the Remediation Procedure.
                   • For best performance, the arch field should be supplied in the rule. The
                      individual permissions will cause the selection of specific system calls that use
                      that kind of access. Not supplying the arch will cause the selection of all system
                      calls which will affect performance as all system calls will be evaluated.

                     1. Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                        extension, with the relevant rules to monitor events that modify user/group
                        information.

                Example:
                # printf "
                -a always,exit -F arch=b64 -S all -F path=/etc/gshadow -F perm=wa -k identity
                -a always,exit -F arch=b64 -S all -F path=/etc/shadow -F perm=wa -k identity
                " >> /etc/audit/rules.d/50-identity.rules

                     2. Merge and load the rules into active configuration:

                # augenrules --load

                     3. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. https://manpages.debian.org/bookworm/manpages/nsswitch.conf.5.en.html
                     2. https://access.redhat.com/documentation/en-
                         us/red_hat_enterprise_linux/6/html/managing_smart_cards/pam_configuration_fil
                         es
                     3. NIST SP 800-53 :: AU-12 a
                     4. NIST SP 800-53A :: AU-12.1 (ii)
                     5. STIG ID: RHEL-08-030130 | RULE ID: SV-230404r1017210 | CAT II
                     6. STIG ID: RHEL-09-654245 | RULE ID: SV-258231r1069375 | CAT II
                     7. STIG ID: ALMA-09-005960 | RULE ID: SV-269134r1050016 | CAT II
                     8. STIG ID: UBTU-22-654150 | RULE ID: SV-260632r958368 | CAT II
                     9. STIG ID: UBTU-24-200300 | RULE ID: SV-270686r1066547 | CAT II
                     10. STIG ID: RHEL-08-030160 | RULE ID: SV-230407r1017213 | CAT II
                     11. STIG ID: RHEL-09-654230 | RULE ID: SV-258228r991572 | CAT II
                     12. STIG ID: ALMA-09-005190 | RULE ID: SV-269131r1050013 | CAT II
                     13. STIG ID: UBTU-22-654135 | RULE ID: SV-260629r958368 | CAT II
                     14. STIG ID: UBTU-24-200310 | RULE ID: SV-270687r1066550 | CAT II

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.
                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.
                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              4.8 Log and Alert on Changes to Administrative Group
                       v7     Membership                                                                                   ●    ●
                                  Configure systems to issue a log entry and alert when an account is added to
                              or removed from any group assigned administrative privileges.

                6.2.3.15 Ensure events that modify /etc/security/opasswd are
                collected (Automated)
                Profile Applicability:

                • Level 2 - Server
                • Level 2 - Workstation

                Description:
                Record events affecting the modification of user or group information, including that of
                passwords and old passwords if in use.

                     •   /etc/security/opasswd - storage of old passwords if the relevant PAM module
                         is in use

                Rationale:
                Unexpected changes to these files could be an indication that the system has been
                compromised and that an unauthorized user is attempting to hide their activities or
                compromise additional accounts.
                Satisfies: SRG-OS-000062-GPOS-00031, SRG-OS-000004-GPOS-00004, SRG-OS-
                000037-GPOS-00015, SRG-OS-000042-GPOS-00020, SRG-OS-000062-GPOS-00031,
                SRG-OS-000304-GPOS-00121, SRG-OS-000392-GPOS-00172, SRG-OS-000462-
                GPOS-00206, SRG-OS-000470-GPOS-00214, SRG-OS-000471-GPOS-00215, SRG-
                OS-000239-GPOS-00089, SRG-OS-000240-GPOS-00090, SRG-OS-000241-GPOS-
                00091, SRG-OS-000303-GPOS-00120, SRG-OS-000304-GPOS-00121, SRG-OS-
                000476-GPOS-00221
                Audit:
                Note: The -w option is deprecated and the rule should be updated in accordance with
                the Remediation Procedure.
                   1. Run the following command to verify a rule exists in the running configuration:
                # auditctl -l | grep -Ps -- '^\h*[^#\n\r]+\h*\/etc\/security\/opasswd'
                Verify the output includes:
                -a always,exit -F path=/etc/security/opasswd -F perm=wa

                     2. Run the following command to verify a rule exists in a rules file:

                # grep -Ps -- '^\h*[^#\n\r]+\h*\/etc\/security\/opasswd'
                /etc/audit/rules.d/*.rules
                Example output:
                /etc/audit/rules.d/50-identity.rules:-a always,exit -F arch=b64 -S all -F
                path=/etc/security/opasswd -F perm=wa -k identity

                Remediation:
                Note:

                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.
                     •   For best performance, the arch field should be supplied in the rule. The
                         individual permissions will cause the selection of specific system calls that use
                         that kind of access. Not supplying the arch will cause the selection of all system
                         calls which will affect performance as all system calls will be evaluated.

                     1. Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                        extension, with the relevant rules to monitor events that modify user/group
                        information.

                Example:
                # printf "
                -a always,exit -F arch=b64 -S all -F path=/etc/security/opasswd -F perm=wa -k
                identity
                " >> /etc/audit/rules.d/50-identity.rules

                     2. Merge and load the rules into active configuration:

                # augenrules --load

                     3. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. https://manpages.debian.org/bookworm/manpages/nsswitch.conf.5.en.html
                     2. https://access.redhat.com/documentation/en-
                        us/red_hat_enterprise_linux/6/html/managing_smart_cards/pam_configuration_fil
                        es
                     3. NIST SP 800-53 :: AU-12 a
                     4. NIST SP 800-53A :: AU-12.1 (ii)
                     5. STIG ID: RHEL-08-030140| RULE ID: SV-230405r1017211 | CAT II
                     6. STIIG ID: RHEL-09-654235 | RULE ID: SV-258229r958434 | CAT II
                     7. STIG ID: ALMA-09-005300 | RULE ID: SV-269132r1050014 | CAT II
                     8. STIG ID: UBTU-22-654140 | RULE ID: SV-260630r958368 | CAT II
                     9. STIG ID: UBTU-24-200320 | RULE ID: SV-270688r1066553 | CAT II

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.
                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.
                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              4.8 Log and Alert on Changes to Administrative Group
                       v7     Membership                                                                                   ●    ●
                                  Configure systems to issue a log entry and alert when an account is added to
                              or removed from any group assigned administrative privileges.

                6.2.3.16 Ensure events that modify /etc/nsswitch.conf file are
                collected (Automated)
                Profile Applicability:

                • Level 2 - Server
                • Level 2 - Workstation

                Description:
                Record events affecting the modification of user or group information, including that of
                passwords and old passwords if in use.

                     •   /etc/nsswitch.conf - file configures how the system uses various databases
                         and name resolution mechanisms

                Rationale:
                Unexpected changes to these files could be an indication that the system has been
                compromised and that an unauthorized user is attempting to hide their activities or
                compromise additional accounts.
                Satisfies: SRG-OS-000062-GPOS-00031, SRG-OS-000004-GPOS-00004, SRG-OS-
                000037-GPOS-00015, SRG-OS-000042-GPOS-00020, SRG-OS-000062-GPOS-00031,
                SRG-OS-000304-GPOS-00121, SRG-OS-000392-GPOS-00172, SRG-OS-000462-
                GPOS-00206, SRG-OS-000470-GPOS-00214, SRG-OS-000471-GPOS-00215, SRG-
                OS-000239-GPOS-00089, SRG-OS-000240-GPOS-00090, SRG-OS-000241-GPOS-
                00091, SRG-OS-000303-GPOS-00120, SRG-OS-000304-GPOS-00121, SRG-OS-
                000476-GPOS-00221
                Audit:
                Note: The -w option is deprecated and the rule should be updated in accordance with
                the Remediation Procedure.

                     1. Run the following command to verify a rule exists in the running configuration:

                # auditctl -l | grep -Ps -- '^\h*[^#\n\r]+\h*\/etc\/nsswitch.conf'
                Verify the output includes:
                -a always,exit -F path=/etc/nsswitch.conf -F perm=wa

                     2. Run the following command to verify a rule exists in a rules file:

                # grep -Ps -- 'nsswitch.conf' /etc/audit/rules.d/*.rules
                Example output:
                /etc/audit/rules.d/50-identity.rules:-a always,exit -F arch=b64 -S all -F
                path=/etc/nsswitch.conf -F perm=wa -k identity

                Remediation:
                Note:

                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.
                     •   For best performance, the arch field should be supplied in the rule. The
                         individual permissions will cause the selection of specific system calls that use
                         that kind of access. Not supplying the arch will cause the selection of all system
                         calls which will affect performance as all system calls will be evaluated.

                     1. Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                        extension, with the relevant rules to monitor events that modify user/group
                        information.

                Example:
                # printf "
                -a always,exit -F arch=b64 -S all -F path=/etc/nsswitch.conf -F perm=wa -k
                identity
                " >> /etc/audit/rules.d/50-identity.rules

                     2. Merge and load the rules into active configuration:

                # augenrules --load

                     3. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. https://manpages.debian.org/bookworm/manpages/nsswitch.conf.5.en.html
                     2. https://access.redhat.com/documentation/en-
                        us/red_hat_enterprise_linux/6/html/managing_smart_cards/pam_configuration_fil
                        es
                     3. NIST SP 800-53 :: AU-12 a
                     4. NIST SP 800-53A :: AU-12.1 (ii)

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.

                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.
                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              4.8 Log and Alert on Changes to Administrative Group
                       v7     Membership                                                                                   ●    ●
                                  Configure systems to issue a log entry and alert when an account is added to
                              or removed from any group assigned administrative privileges.

                6.2.3.17 Ensure events that modify /etc/pam.conf and /etc/pam.d/
                information are collected (Automated)
                Profile Applicability:

                • Level 2 - Server
                • Level 2 - Workstation

                Description:
                Record events affecting the modification of user or group information, including that of
                passwords and old passwords if in use.

                     •   /etc/pam.conf - file determines the authentication services to be used, and the
                         order in which the services are used.
                     •   /etc/pam.d - directory contains the PAM configuration files for each PAM-aware
                         application.

                Rationale:
                Unexpected changes to these files could be an indication that the system has been
                compromised and that an unauthorized user is attempting to hide their activities or
                compromise additional accounts.
                Satisfies: SRG-OS-000062-GPOS-00031, SRG-OS-000004-GPOS-00004, SRG-OS-
                000037-GPOS-00015, SRG-OS-000042-GPOS-00020, SRG-OS-000062-GPOS-00031,
                SRG-OS-000304-GPOS-00121, SRG-OS-000392-GPOS-00172, SRG-OS-000462-
                GPOS-00206, SRG-OS-000470-GPOS-00214, SRG-OS-000471-GPOS-00215, SRG-
                OS-000239-GPOS-00089, SRG-OS-000240-GPOS-00090, SRG-OS-000241-GPOS-
                00091, SRG-OS-000303-GPOS-00120, SRG-OS-000304-GPOS-00121, SRG-OS-
                000476-GPOS-00221

                Audit:
                Note: The -w option is deprecated and the rule should be updated in accordance with
                the Remediation Procedure.
                   1. Run the following command to verify a rule exists in the running configuration:
                # auditctl -l | grep -Ps -- '^\h*[^#\n\r]+\h*\/etc\/pam'
                Verify the output includes:
                -a always,exit -F path=/etc/pam.conf -F perm-wa
                -a always,exit -F dir=/etc/pam.d -F perm=wa
                     2. Run the following command to verify a rule exists in a rules file:
                # grep -Ps -- '^\h*[^#\n\r]+\h*\/etc\/pam' /etc/audit/rules.d/*.rules
                Example output:
                /etc/audit/rules.d/50-identity.rules:-a always,exit -F arch=b64 -S all -F
                path=/etc/pam.conf -F perm-wa -k identity
                /etc/audit/rules.d/50-identity.rules:-a always,exit -F arch=b64 -S all -F
                dir=/etc/pam.d -F perm=wa -k identity

                Remediation:
                Note:

                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.
                     •   For best performance, the arch field should be supplied in the rule. The
                         individual permissions will cause the selection of specific system calls that use
                         that kind of access. Not supplying the arch will cause the selection of all system
                         calls which will affect performance as all system calls will be evaluated.

                     1. Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                        extension, with the relevant rules to monitor events that modify user/group
                        information.

                Example:
                # printf "
                -a always,exit -F arch=b64 -S all -F path=/etc/pam.conf -F perm=wa -k
                identity
                -a always,exit -F arch=b64 -S all -F dir=/etc/pam.d -F perm=wa -k identity
                " >> /etc/audit/rules.d/50-identity.rules

                     2. Merge and load the rules into active configuration:

                # augenrules --load

                     3. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. https://manpages.debian.org/bookworm/manpages/nsswitch.conf.5.en.html
                     2. https://access.redhat.com/documentation/en-
                        us/red_hat_enterprise_linux/6/html/managing_smart_cards/pam_configuration_fil
                        es
                     3. NIST SP 800-53 :: AU-12 a
                     4. NIST SP 800-53A :: AU-12.1 (ii)

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.

                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.
                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              4.8 Log and Alert on Changes to Administrative Group
                       v7     Membership                                                                                   ●    ●
                                  Configure systems to issue a log entry and alert when an account is added to
                              or removed from any group assigned administrative privileges.

                6.2.3.18 Ensure discretionary access control permission
                modification events chmod,fchmod,fchmodat,fchmodat2 are
                collected (Automated)
                Profile Applicability:

                • Level 2 - Server
                • Level 2 - Workstation

                Description:
                Monitor changes to file permissions, attributes, ownership and group. The parameters in
                this section track changes for system calls that affect file permissions and attributes.
                The following commands and system calls effect the permissions, ownership and
                various attributes of files.

                     •   chmod: changes the mode of the file
                     •   fchmod: changes the mode of the file referred to by the open file descriptor
                     •   fchmodat: change permissions of a file
                     •   fchmodat2: change permissions of a file relative to a directory file descriptor

                In all cases, an audit record will only be written for non-system user ids and will ignore
                Daemon events. All audit records will be tagged with the identifier "perm_mod."
                Rationale:
                Monitoring for changes in file attributes could alert a system administrator to activity that
                could indicate intruder activity or policy violation.
                Satisfies: SRG-OS-000062-GPOS-00031, SRG-OS-000037-GPOS-00015, SRG-OS-
                000042-GPOS-00020, SRG-OS-000392-GPOS-00172, SRG-OS-000462-GPOS-00206,
                SRG-OS-000471-GPOS-00215, SRG-OS-000064-GPOS-00033, SRG-OS-000466-
                GPOS-00210

                Audit:
                Note:

                     •   Output showing all audited syscalls, e.g. (-a always,exit -F arch=b64 -S
                         chmod,fchmod,fchmodat,chmod,fchmod,fchmodat,fchmodat2,setxattr,ls
                         etxattr,fsetxattr,removexattr,lremovexattr,fremovexattr -F
                         auid>=1000 -F auid!=unset -F key=perm_mod) is also acceptable. These
                         have been separated by function on the displayed output for clarity.
                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.

                     1. Run the following command to verify a rule exits in the running configuration

                # auditctl -l | grep -Ps -- 'chmod'
                Verify the output includes:
                -a always,exit -F arch=b64 -S chmod,fchmod,fchmodat,fchmodat2 -F auid>=1000 -
                F auid!=unset

                     2. Run the following command to verify a rule exists in a rules file:

                # grep -Ps -- 'chmod' /etc/audit/rules.d/*.rules
                Example output:
                /etc/audit/rules.d/50-perm_mod.rules:-a always,exit -F arch=b64 -S
                chmod,fchmod,fchmodat,fchmodat2 -F auid>=1000 -F auid!=-1 -k perm_mod

                Remediation:
                Note: The -w option is deprecated and the rule should be updated in accordance with
                the Remediation Procedure.

                     1. Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                        extension, with the relevant rules to monitor discretionary access control
                        permission modification events.

                Example:
                # printf "
                -a always,exit -F arch=b64 -S chmod,fchmod,fchmodat,fchmodat2 -F auid>=1000 -
                F auid!=unset -k perm_mod
                " >> /etc/audit/rules.d/50-perm_mod.rules
                Note: The auid>= value should be set to the same value as UID_MIN value in
                /etc/login.defs.

                     2. Merge and load the rules into active configuration:

                # augenrules --load

                     3. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. NIST SP 800-53 :: AU-12 a
                     2. NIST SP 800-53A :: AU-12.1 (ii)
                     3. STIG ID: RHEL-08-030490 | RULE ID: SV-230456r810462 | CAT II
                     4. STIG ID: RHEL-08-030200 | RULE ID: SV-230413r810463 | CAT II
                     5. STIG ID: RHEL-09-654015 | RULE ID: SV-258177r1045316 | CAT II
                     6. STIG ID: RHEL-09-654025 | RULE ID: SV-258179r1045322 | CAT II
                     7. STIG ID: ALMA-09-048640 | RULE ID: SV-269480r1050363 | CAT II
                     8. STIG ID: ALMA-09-051390 | RULE ID: SV-269505r1050388 | CAT II
                     9. STIG ID: UBTU-22-654160 | RULE ID: SV-260634r958446 | CAT II
                     10. STIG ID: UBTU-22-654180 | RULE ID: SV-260638r958446 | CAT II
                     11. STIG ID: UBTU-24-900140 | RULE ID: SV-270785r1068373 | CAT II
                     12. STIG ID: UBTU-24-900130 | RULE ID: SV-270784r1068371 | CAT II

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.

                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.
                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              5.5 Implement Automated Configuration Monitoring
                              Systems
                       v7        Utilize a Security Content Automation Protocol (SCAP) compliant configuration             ●    ●
                              monitoring system to verify all security configuration elements, catalog approved
                              exceptions, and alert when unauthorized changes occur.

                6.2.3.19 Ensure discretionary access control permission
                modification events chown,fchown,lchown,fchownat are collected
                (Automated)
                Profile Applicability:

                • Level 2 - Server
                • Level 2 - Workstation

                Description:
                Monitor changes to file permissions, attributes, ownership and group. The parameters in
                this section track changes for system calls that affect file permissions and attributes.
                The following commands and system calls effect the permissions, ownership and
                various attributes of files.

                     •   chown: changes the user and/or group ownership of each given file
                     •   fchown: changes the ownership of the file referred to by the open file descriptor
                     •   fchownat: change ownership of a file relative to a directory file descriptor
                     •   lchown: change the owner and group of a file

                In all cases, an audit record will only be written for non-system user ids and will ignore
                Daemon events. All audit records will be tagged with the identifier "perm_mod."
                Rationale:
                Monitoring for changes in file attributes could alert a system administrator to activity that
                could indicate intruder activity or policy violation.
                Satisfies: SRG-OS-000062-GPOS-00031, SRG-OS-000037-GPOS-00015, SRG-OS-
                000042-GPOS-00020, SRG-OS-000392-GPOS-00172, SRG-OS-000462-GPOS-00206,
                SRG-OS-000471-GPOS-00215, SRG-OS-000064-GPOS-00033, SRG-OS-000466-
                GPOS-00210

                Audit:
                Note:

                     •   Output showing all audited syscalls, e.g. (-a always,exit -F arch=b64 -S
                         chmod,fchmod,fchmodat,chmod,fchmod,fchmodat,setxattr,lsetxattr,fs
                         etxattr,removexattr,lremovexattr,fremovexattr -F auid>=1000 -F
                         auid!=unset -F key=perm_mod) is also acceptable. These have been
                         separated by function on the displayed output for clarity.
                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.

                     1. Run the following command to verify a rule exits in the running configuration

                # auditctl -l | grep -Ps -- 'chown'
                Verify the output includes:
                -a always,exit -F arch=b64 -S chown,fchown,lchown,fchownat -F auid>=1000 -F
                auid!=unset

                     2. Run the following command to verify a rule exists in a rules file:

                # grep -Ps -- 'chown' /etc/audit/rules.d/*.rules
                Example output:
                /etc/audit/rules.d/50-perm_mod.rules:-a always,exit -F arch=b64 -S
                chown,fchown,lchown,fchownat -F auid>=1000 -F auid!=-1 -k perm_mod

                Remediation:
                Note: The -w option is deprecated and the rule should be updated in accordance with
                the Remediation Procedure.

                     1. Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                        extension, with the relevant rules to monitor discretionary access control
                        permission modification events.

                Example:
                # printf "
                -a always,exit -F arch=b64 -S chown,fchown,lchown,fchownat -F auid>=1000 -F
                auid!=unset -k perm_mod
                " >> /etc/audit/rules.d/50-perm_mod.rules
                Note: The auid>= value should be set to the same value as UID_MIN value in
                /etc/login.defs.

                     2. Merge and load the rules into active configuration:

                # augenrules --load

                     3. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. NIST SP 800-53 :: AU-12 a
                     2. NIST SP 800-53A :: AU-12.1 (ii)
                     3. STIG ID: RHEL-08-030490 | RULE ID: SV-230456r810462 | CAT II
                     4. STIG ID: RHEL-08-030200 | RULE ID: SV-230413r810463 | CAT II
                     5. STIG ID: RHEL-09-654015 | RULE ID: SV-258177r1045316 | CAT II
                     6. STIG ID: RHEL-09-654025 | RULE ID: SV-258179r1045322 | CAT II
                     7. STIG ID: ALMA-09-048640 | RULE ID: SV-269480r1050363 | CAT II
                     8. STIG ID: ALMA-09-051390 | RULE ID: SV-269505r1050388 | CAT II
                     9. STIG ID: UBTU-22-654160 | RULE ID: SV-260634r958446 | CAT II
                     10. STIG ID: UBTU-22-654180 | RULE ID: SV-260638r958446 | CAT II
                     11. STIG ID: UBTU-24-900140 | RULE ID: SV-270785r1068373 | CAT II
                     12. STIG ID: UBTU-24-900130 | RULE ID: SV-270784r1068371 | CAT II

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.

                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.
                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              5.5 Implement Automated Configuration Monitoring
                              Systems
                       v7        Utilize a Security Content Automation Protocol (SCAP) compliant configuration             ●    ●
                              monitoring system to verify all security configuration elements, catalog approved
                              exceptions, and alert when unauthorized changes occur.

                6.2.3.20 Ensure discretionary access control permission
                modification events
                setxattr,lsetxattr,fsetxattr,removexattr,lremovexattr,fremovexattr
                collected (Automated)
                Profile Applicability:

                • Level 2 - Server
                • Level 2 - Workstation

                Description:
                Monitor changes to file permissions, attributes, ownership and group. The parameters in
                this section track changes for system calls that affect file permissions and attributes.
                The following commands and system calls effect the permissions, ownership and
                various attributes of files.

                     •   setxattr
                     •   lsetxattr
                     •   fsetxattr
                     •   removexattr
                     •   lremovexattr
                     •   fremovexattr

                In all cases, an audit record will only be written for non-system user ids and will ignore
                Daemon events. All audit records will be tagged with the identifier "perm_mod."
                Rationale:
                Monitoring for changes in file attributes could alert a system administrator to activity that
                could indicate intruder activity or policy violation.
                Satisfies: SRG-OS-000062-GPOS-00031, SRG-OS-000037-GPOS-00015, SRG-OS-
                000042-GPOS-00020, SRG-OS-000392-GPOS-00172, SRG-OS-000462-GPOS-00206,
                SRG-OS-000471-GPOS-00215, SRG-OS-000064-GPOS-00033, SRG-OS-000466-
                GPOS-00210

                Audit:
                Note:

                     •   Output showing all audited syscalls, e.g. (-a always,exit -F arch=b64 -S
                         chmod,fchmod,fchmodat,chmod,fchmod,fchmodat,setxattr,lsetxattr,fs
                         etxattr,removexattr,lremovexattr,fremovexattr -F auid>=1000 -F
                         auid!=unset -F key=perm_mod) is also acceptable. These have been
                         separated by function on the displayed output for clarity.
                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.

                     1. Run the following command to verify a rule exits in the running configuration

                # auditctl -l | grep -Ps -- 'xattr'
                Verify the output includes:
                -a always,exit -F arch=b64 -S
                setxattr,lsetxattr,fsetxattr,removexattr,lremovexattr,fremovexattr -F
                auid>=1000 -F auid!=unset

                     2. Run the following command to verify a rule exists in a rules file:

                # grep -Ps -- 'xattr' /etc/audit/rules.d/*.rules
                Example output:
                /etc/audit/rules.d/50-perm_mod.rules:-a always,exit -F arch=b64 -S
                setxattr,lsetxattr,fsetxattr,removexattr,lremovexattr,fremovexattr -F
                auid>=1000 -F auid!=-1 -k perm_mod

                Remediation:
                Note: The -w option is deprecated and the rule should be updated in accordance with
                the Remediation Procedure.

                     1. Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                        extension, with the relevant rules to monitor discretionary access control
                        permission modification events.

                Example:
                # printf "
                -a always,exit -F arch=b64 -S
                setxattr,lsetxattr,fsetxattr,removexattr,lremovexattr,fremovexattr -F
                auid>=1000 -F auid!=unset -k perm_mod
                " >> /etc/audit/rules.d/50-perm_mod.rules
                Note: The auid>= value should be set to the same value as UID_MIN value in
                /etc/login.defs.

                     2. Merge and load the rules into active configuration:

                # augenrules --load

                     3. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. NIST SP 800-53 :: AU-12 a
                     2. NIST SP 800-53A :: AU-12.1 (ii)
                     3. STIG ID: RHEL-08-030490 | RULE ID: SV-230456r810462 | CAT II
                     4. STIG ID: RHEL-08-030200 | RULE ID: SV-230413r810463 | CAT II
                     5. STIG ID: RHEL-09-654015 | RULE ID: SV-258177r1045316 | CAT II
                     6. STIG ID: RHEL-09-654025 | RULE ID: SV-258179r1045322 | CAT II
                     7. STIG ID: ALMA-09-048640 | RULE ID: SV-269480r1050363 | CAT II
                     8. STIG ID: ALMA-09-051390 | RULE ID: SV-269505r1050388 | CAT II
                     9. STIG ID: UBTU-22-654160 | RULE ID: SV-260634r958446 | CAT II
                     10. STIG ID: UBTU-22-654180 | RULE ID: SV-260638r958446 | CAT II
                     11. STIG ID: UBTU-24-900140 | RULE ID: SV-270785r1068373 | CAT II
                     12. STIG ID: UBTU-24-900130 | RULE ID: SV-270784r1068371 | CAT II

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.

                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.
                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              5.5 Implement Automated Configuration Monitoring
                              Systems
                       v7        Utilize a Security Content Automation Protocol (SCAP) compliant configuration             ●    ●
                              monitoring system to verify all security configuration elements, catalog approved
                              exceptions, and alert when unauthorized changes occur.
  '6.2.3.12': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '6.2.3.13': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '6.2.3.14': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '6.2.3.15': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '6.2.3.16': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '6.2.3.17': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '6.2.3.21': !unsafe |-
    Note:
                   • The -w option is deprecated and the rule should be updated in accordance with
                       the Remediation Procedure.
                   • For best performance, the arch field should be supplied in the rule. The
                       individual permissions will cause the selection of specific system calls that use
                       that kind of access. Not supplying the arch will cause the selection of all system
                       calls which will affect performance as all system calls will be evaluated.
                1.Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                extension, with the relevant rules to monitor successful file system mounts.
                Example:
                # printf "
                -a always,exit -F arch=b64 -S mount -F auid>=1000 -F auid!=unset -k mounts
                " >> /etc/audit/rules.d/50-mounts.rules
                Note: The auid>= value should be set to the same value as UID_MIN value in
                /etc/login.defs.

                     2. Merge and load the rules into active configuration:

                # augenrules --load

                     3. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. NIST SP 800-53 :: AU-12 a
                     2. NIST SP 800-53A :: AU-12.1 (ii)
                     3. STIG ID: RHEL-08-030302 | RULE ID: SV-230425r627750 | CAT II

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.
                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.
                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              6.3 Enable Detailed Logging
                       v7        Enable system logging to include detailed information such as an event source,
                              date, user, timestamp, source addresses, destination addresses, and other useful
                                                                                                                           ●    ●
                              elements.
  '6.2.3.22': !unsafe |-
    Note:
                   • The -w option is deprecated and the rule should be updated in accordance with
                      the Remediation Procedure.
                   • For best performance, the arch field should be supplied in the rule. The
                      individual permissions will cause the selection of specific system calls that use
                      that kind of access. Not supplying the arch will cause the selection of all system
                      calls which will affect performance as all system calls will be evaluated.

                     1. Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                        extension, with the relevant rules to monitor session initiation information.

                Example:
                # printf "
                -a always,exit -F arch=b64 -S all -F path=/var/run/utmp -F perm=wa -k session
                -a always,exit -F arch=b64 -S all -F path=/var/log/wtmp -F perm=wa -k session
                -a always,exit -F arch=b64 -S all -F path=/var/log/btmp -F perm=wa -k session
                " >> /etc/audit/rules.d/50-session.rules

                     2. Merge and load the rules into active configuration:

                # augenrules --load

                     3. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. NIST SP 800-53 Rev. 5: AU-3, AU-3
                     2. STIG ID: UBTU-24-900600 | RULE ID: SV-270811r1066922 | CAT II
                     3. STIG ID: UBTU-22-654205 | RULE ID: SV-260643r991581 | CAT II
                     4. STIG ID: UBTU-24-900590 | RULE ID: SV-270810r1066919 | CAT II
                     5. STIG ID: UBTU-22-654200 | RULE ID: SV-260642r991581 | CAT II
                     6. STIG ID: UBTU-24-900610 | RULE ID: SV-270812r1066925 | CAT II
                     7. STIG ID: UBTU-22-654195 | RULE ID: SV-260641r991581 | CAT II

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.
                System call structure

                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.
                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              4.9 Log and Alert on Unsuccessful Administrative Account
                       v7     Login                                                                                        ●    ●
                                Configure systems to issue a log entry and alert on unsuccessful logins to an
                              administrative account.
                              16.13 Alert on Account Login Behavior Deviation
                       v7       Alert when users deviate from normal login behavior, such as time-of-day,                       ●
                              workstation location and duration.
  '6.2.3.23': !unsafe |-
    Note:

                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.
                     •   For best performance, the arch field should be supplied in the rule. The
                         individual permissions will cause the selection of specific system calls that use
                         that kind of access. Not supplying the arch will cause the selection of all system
                         calls which will affect performance as all system calls will be evaluated.

                     1. Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                        extension, with the relevant rules to monitor login and logout events.

                Example:
                # printf "
                -a always,exit -F arch=b64 -S all -F path=/var/log/lastlog -F perm=wa -k
                logins
                -a always,exit -F arch=b64 -S all -F path=/var/run/faillock -F perm=wa -k
                logins
                " >> /etc/audit/rules.d/50-login.rules

                     2. Merge and load the rules into active configuration:

                # augenrules --load

                     3. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. NIST SP 800-53 :: AU-12 a
                     2. NIST SP 800-53A :: AU-12.1 (ii)
                     3. STIG ID: RHEL-08-030600 | RULE ID: SV-230467r627750 | CAT II
                     4. STIG ID: RHEL-09-654255 | RULE ID: SV-258225r1014990 | CAT II
                     5. STIG ID: ALMA-09-047540 | RULE ID: SV-269470r1050353 | CAT II
                     6. STIG ID: UBTU-24-900260 | RULE ID: SV-270797r1066880 | CAT II
                     7. STIG ID: UBTU-22-654215 | RULE ID: SV-260645r958446 | CAT II

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.

                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.
                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              4.9 Log and Alert on Unsuccessful Administrative Account
                       v7     Login                                                                                        ●    ●
                                Configure systems to issue a log entry and alert on unsuccessful logins to an
                              administrative account.

                       v7     16.11 Lock Workstation Sessions After Inactivity                                        ●    ●    ●
                                 Automatically lock workstation sessions after a standard period of inactivity.
                              16.13 Alert on Account Login Behavior Deviation
                       v7       Alert when users deviate from normal login behavior, such as time-of-day,                       ●
                              workstation location and duration.
  '6.2.3.24': !unsafe |-
    Note:

                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.
                     •   For best performance, the arch field should be supplied in the rule. The
                         individual permissions will cause the selection of specific system calls that use
                         that kind of access. Not supplying the arch will cause the selection of all system
                         calls which will affect performance as all system calls will be evaluated.

                     1. Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                        extension, with the relevant rules to monitor file deletion events by users.

                Example:
                # printf "
                -a always,exit -F arch=b64 -S unlink,unlinkat -F auid>=1000 -F auid!=unset -k
                delete
                " >> /etc/audit/rules.d/50-delete.rules
                Note: The auid>= value should be set to the same value as UID_MIN value in
                /etc/login.defs.

                     2. Merge and load the rules into active configuration:

                # augenrules --load

                     3. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. NIST SP 800-53 :: AU-12 a
                     2. NIST SP 800-53A :: AU-12.1 (ii)
                     3. STIG ID: RHEL-08-030361 | RULE ID: SV-230439r810465 | CAT II
                     4. STIG ID: RHEL-09-654065 | RULE ID: SV-258187r1045346 | CAT II
                     5. STIG ID: ALMA-09-048970 | RULE ID: SV-269483r1050366 | CAT II
                     6. STIG ID: UBTU-22-654185 | RULE ID: SV-260639r991577 | CAT II
                     7. STIG ID: UBTU-24-900540 | RULE ID: SV-270809r1068388 | CAT II

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.

                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.
                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              6.2 Activate audit logging
                       v7        Ensure that local logging has been enabled on all systems and networking             ●    ●    ●
                              devices.

                6.2.3.25 Ensure rename file deletion events by users are
                collected (Automated)
                Profile Applicability:

                • Level 2 - Server
                • Level 2 - Workstation

                Description:
                Monitor the use of system calls associated with the deletion or renaming of files and file
                attributes. This configuration statement sets up monitoring for:

                     •   rename: rename a file
                     •   renameat: rename a file attribute
                     •   renameat2: rename a file relative to directory file descriptors

                         system calls and tags them with the identifier "delete".
                Rationale:
                Monitoring these calls from non-privileged users could provide a system administrator
                with evidence that inappropriate removal of files and file attributes associated with
                protected files is occurring. While this audit option will look at all events, system
                administrators will want to look for specific privileged files that are being deleted or
                altered.
                Satisfies: SRG-OS-000062-GPOS-00031, SRG-OS-000037-GPOS-00015, SRG-OS-
                000042-GPOS-00020, SRG-OS-000392-GPOS-00172, SRG-OS-000462-GPOS-00206,
                SRG-OS-000471-GPOS-00215

                Audit:
                Note:

                     •   Output showing all audited syscalls, e.g. (-a always,exit -S
                         unlink,unlinkat,rename,renameat,renameat2 -F auid>=1000 -F
                         auid!=unset) is also acceptable. These have been separated by function on the
                         displayed output for clarity.
                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.

                     1. Run the following command to verify a rule exists in the running configuration:

                # auditctl -l | grep -Ps -- 'rename'
                Verify the output includes:
                -a always,exit -S rename,renameat,renameat2 -F auid>=1000 -F auid!=unset

                     2. Run the following command to verify a rule exists in a rules file:

                # grep -Ps -- 'rename' /etc/audit/rules.d/*.rules
                Example output:
                /etc/audit/rules.d/50-delete.rules:-a always,exit -F arch=b64 -S
                rename,renameat,renameat2 -F auid>=1000 -F auid!=-1 -k delete

                Remediation:
                Note:

                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.
                     •   For best performance, the arch field should be supplied in the rule. The
                         individual permissions will cause the selection of specific system calls that use
                         that kind of access. Not supplying the arch will cause the selection of all system
                         calls which will affect performance as all system calls will be evaluated.

                     1. Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                        extension, with the relevant rules to monitor file deletion events by users.

                Example:
                # printf "
                -a always,exit -F arch=b64 -S rename,renameat,renameat2 -F auid>=1000 -F
                auid!=unset -k delete
                " >> /etc/audit/rules.d/50-delete.rules
                Note: The auid>= value should be set to the same value as UID_MIN value in
                /etc/login.defs.

                     2. Merge and load the rules into active configuration:

                # augenrules --load

                     3. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. NIST SP 800-53 :: AU-12 a
                     2. NIST SP 800-53A :: AU-12.1 (ii)
                     3. STIG ID: RHEL-08-030361 | RULE ID: SV-230439r810465 | CAT II
                     4. STIG ID: RHEL-09-654065 | RULE ID: SV-258187r1045346 | CAT II
                     5. STIG ID: ALMA-09-048970 | RULE ID: SV-269483r1050366 | CAT II
                     6. STIG ID: UBTU-22-654185 | RULE ID: SV-260639r991577 | CAT II
                     7. STIG ID: UBTU-24-900540 | RULE ID: SV-270809r1068388 | CAT II

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.

                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.
                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              6.2 Activate audit logging
                       v7        Ensure that local logging has been enabled on all systems and networking             ●    ●    ●
                              devices.

                6.2.3.26 Ensure events that modify the system's Mandatory
                Access Controls are collected (Automated)
                Profile Applicability:

                • Level 2 - Server
                • Level 2 - Workstation

                Description:
                Monitor AppArmor, an implementation of mandatory access controls. The parameters
                below monitor any write access (potential additional, deletion or modification of files in
                the directory) or attribute changes to the /etc/apparmor/ and /etc/apparmor.d/
                directories.
                Note: If a different Mandatory Access Control method is used, changes to the
                corresponding directories should be audited.
                Rationale:
                Changes to files in the /etc/apparmor/ and /etc/apparmor.d/ directories could
                indicate that an unauthorized user is attempting to modify access controls and change
                security contexts, leading to a compromise of the system.
                Audit:
                Note: The -w option is deprecated and the rule should be updated in accordance with
                the Remediation Procedure.

                     1. Run the following command to verify a rule exists in the running configuration:

                # auditctl -l | grep -Ps -- 'apparmor'
                Verify the output includes:
                -a always,exit -F path=/etc/apparmor -F perm=wa
                -a always,exit -F dir=/etc/apparmor.d -F perm=wa

                     2. Run the following command to verify a rule exists in a rules file:

                # grep -Ps -- 'apparmor' /etc/audit/rules.d/*.rules
                Example output:
                /etc/audit/rules.d/50-MAC-policy.rules:-a always,exit -F arch=b64 -S all -F
                path=/etc/apparmor -F perm=wa -k MAC-policy
                /etc/audit/rules.d/50-MAC-policy.rules:-a always,exit -F arch=b64 -S all -F
                dir=/etc/apparmor.d -F perm=wa -k MAC-policy

                Remediation:
                Note:

                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.
                     •   For best performance, the arch field should be supplied in the rule. The
                         individual permissions will cause the selection of specific system calls that use
                         that kind of access. Not supplying the arch will cause the selection of all system
                         calls which will affect performance as all system calls will be evaluated.

                     1. Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                        extension, with the relevant rules to monitor events that modify the system's
                        Mandatory Access Controls.

                Example:
                # printf "
                -a always,exit -F arch=b64 -S all -F path=/etc/apparmor -F perm=wa -k MAC-
                policy
                -a always,exit -F arch=b64 -S all -F dir=/etc/apparmor.d -F perm=wa -k MAC-
                policy
                " >> /etc/audit/rules.d/50-MAC-policy.rules

                     2. Merge and load the rules into active configuration:

                # augenrules --load

                     3. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. NIST SP 800-53 Rev. 5: AU-3, CM-6

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.
                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.

                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              5.5 Implement Automated Configuration Monitoring
                              Systems
                       v7        Utilize a Security Content Automation Protocol (SCAP) compliant configuration             ●    ●
                              monitoring system to verify all security configuration elements, catalog approved
                              exceptions, and alert when unauthorized changes occur.

                6.2.3.27 Ensure successful and unsuccessful attempts to use the
                chcon command are collected (Automated)
                Profile Applicability:

                • Level 2 - Server
                • Level 2 - Workstation

                Description:
                The operating system must generate audit records for successful/unsuccessful uses of
                the chcon command.

                Rationale:
                The chcon command is used to change file security context. Without generating audit
                records that are specific to the security and needs of the organization, it would be
                difficult to establish, correlate, and investigate the events relating to an incident or
                identify those responsible for one.
                Audit records can be generated from various components within the information system
                (e.g., module or policy filter).
                Satisfies: SRG-OS-000062-GPOS-00031, SRG-OS-000037-GPOS-00015, SRG-OS-
                000042-GPOS-00020, SRG-OS-000062-GPOS-00031, SRG-OS-000392-GPOS-00172,
                SRG-OS-000462-GPOS-00206, SRG-OS-000468-GPOS-00212, SRG-OS-000471-
                GPOS-00215
                Audit:
                Note: The -w option is deprecated and the rule should be updated in accordance with
                the Remediation Procedure.

                     1. Run the following command to verify a rule exits in the running configuration:

                # auditctl -l | grep -Ps -- '^\h*[^#\n\r]+\h*\/usr\/bin\/chcon'
                Verify the output includes:
                -a always,exit -F path=/usr/bin/chcon -F perm=x -F auid>=1000 -F auid!=unset

                     2. Run the following command to verify a rule exists in a rules file:

                # grep -Ps -- '^\h*[^#\n\r]+\h*\/usr\/bin\/chcon' /etc/audit/rules.d/*.rules
                Example output:
                /etc/audit/rules.d/50-perm_chng.rules:-a always,exit -S all -F
                path=/usr/bin/chcon -F perm=x -F auid>=1000 -F auid!=-1 -k perm_chng

                Remediation:
                Note:

                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.
                     •   For best performance, the arch field should be supplied in the rule. The
                         individual permissions will cause the selection of specific system calls that use
                         that kind of access. Not supplying the arch will cause the selection of all system
                         calls which will affect performance as all system calls will be evaluated.

                     1. Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                        extension, with the relevant rules to monitor successful and unsuccessful
                        attempts to use the chcon command.

                Example:
                # printf "
                -a always,exit -F arch=b64 -S all -F path=/usr/bin/chcon -F perm=x -F
                auid>=1000 -F auid!=unset -k perm_chng
                " >> /etc/audit/rules.d/50-perm_chng.rules
                Note: The auid>= value should be set to the same value as UID_MIN value in
                /etc/login.defs.

                     2. Merge and load the rules into active configuration:

                # augenrules --load

                     3. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. NIST SP 800-53 :: AU-12 a
                     2. NIST SP 800-53A :: AU-12.1 (ii)
                     3. STIG ID: RHEL-08-030260 | RULE ID: SV-230419r627750 | CAT II
                     4. STIG ID: RHEL-09-654045 | RULE ID: SV-258183r1045334 | CAT II
                     5. STIG ID: ALMA-09-048420 | RULE ID: SV-269478r1050361 | CAT II
                     6. STIG ID: UBTU-22-654025 | RULE ID: SV-260607r958446 | CAT II
                     7. STIG ID: UBTU-24-900210 | RULE ID: SV-270792r1066865 | CAT II

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.
                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.
                CIS Controls:

                    Controls
                                                                   Control                                    IG 1 IG 2 IG 3
                    Version
                                8.2 Collect Audit Logs
                        v8        Collect audit logs. Ensure that logging, per the enterprise’s audit log      ●    ●    ●
                                management process, has been enabled across enterprise assets.
                                6.2 Activate audit logging
                        v7         Ensure that local logging has been enabled on all systems and networking    ●    ●    ●
                                devices.

                6.2.3.28 Ensure successful and unsuccessful attempts to use the
                setfacl command are collected (Automated)
                Profile Applicability:

                • Level 2 - Server
                • Level 2 - Workstation

                Description:
                The operating system must generate audit records for successful/unsuccessful uses of
                the setfacl command

                Rationale:
                This utility sets Access Control Lists (ACLs) of files and directories. Without generating
                audit records that are specific to the security and mission needs of the organization, it
                would be difficult to establish, correlate, and investigate the events relating to an
                incident or identify those responsible for one.
                Audit records can be generated from various components within the information system
                (e.g., module or policy filter).
                Audit:
                Note: The -w option is deprecated and the rule should be updated in accordance with
                the Remediation Procedure.

                     1. Run the following command to verify a rule exists in the running configuration:

                # auditctl -l | grep -Ps '^\h*[^#\n\r]+\h*\/usr\/bin\/setfacl'
                Verify the output includes:
                -a always,exit -F path=/usr/bin/setfacl -F perm=x -F auid>=1000 -F
                auid!=unset

                     2. Run the following command to verify a rule exists in a rules file:

                # grep -Ps '^\h*[^#\n\r]+\h*\/usr\/bin\/setfacl' /etc/audit/rules.d/*.rules
                Example output:
                /etc/audit/rules.d/50-perm_chng.rules:-a always,exit -F arch=b64 -S all -F
                path=/usr/bin/setfacl -F perm=x -F auid>=1000 -F auid!=unset -k perm_chng

                Remediation:
                Note:

                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.
                     •   For best performance, the arch field should be supplied in the rule. The
                         individual permissions will cause the selection of specific system calls that use
                         that kind of access. Not supplying the arch will cause the selection of all system
                         calls which will affect performance as all system calls will be evaluated.

                     1. Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                        extension, with the relevant rules to monitor successful and unsuccessful
                        attempts to use the setfacl command.

                Example:
                # printf "
                -a always,exit -F arch=b64 -S all -F path=/usr/bin/setfacl -F perm=x -F
                auid>=1000 -F auid!=unset -k perm_chng
                " >> /etc/audit/rules.d/50-perm_chng.rules
                Note: The auid>= value should be set to the same value as UID_MIN value in
                /etc/login.defs.

                     2. Merge and load the rules into active configuration:

                # augenrules --load

                     3. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. NIST SP 800-53 Rev. 5: AU-2, AU-12, SI-5
                     2. STIG ID: RHEL-08-030330 | RULE ID: SV-230435r627750 | CAT II
                     3. STIG ID: RHEL-09-654040 | RULE ID: SV-258182r1045331 | CAT II
                     4. STIG ID: ALMA-09-050180 | RULE ID: SV-269494r1050377 | CAT II
                     5. STIG ID: UBTU-22-654085 | RULE ID: SV-260619r958446 | CAT II
                     6. STIG ID: UBTU-24-900230 | RULE ID: SV-270794r1066871 | CAT II

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.

                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.
                CIS Controls:

                    Controls
                                                                   Control                                    IG 1 IG 2 IG 3
                    Version
                                8.2 Collect Audit Logs
                        v8        Collect audit logs. Ensure that logging, per the enterprise’s audit log      ●    ●    ●
                                management process, has been enabled across enterprise assets.
                                6.2 Activate audit logging
                        v7         Ensure that local logging has been enabled on all systems and networking    ●    ●    ●
                                devices.

                6.2.3.29 Ensure successful and unsuccessful attempts to use the
                chacl command are collected (Automated)
                Profile Applicability:

                • Level 2 - Server
                • Level 2 - Workstation

                Description:
                The operating system must generate audit records for successful/unsuccessful uses of
                the chacl command.
                chacl is an IRIX-compatibility command, and is maintained for those users who are
                familiar with its use from either XFS or IRIX.
                Rationale:
                chacl changes the ACL(s) for a file or directory. Without generating audit records that
                are specific to the security and mission needs of the organization, it would be difficult to
                establish, correlate, and investigate the events relating to an incident or identify those
                responsible for one.
                Audit records can be generated from various components within the information system
                (e.g., module or policy filter).
                Satisfies: SRG-OS-000062-GPOS-00031, SRG-OS-000037-GPOS-00015, SRG-OS-
                000042-GPOS-00020, SRG-OS-000062-GPOS-00031, SRG-OS-000392-GPOS-00172,
                SRG-OS-000462-GPOS-00206, SRG-OS-000471-GPOS-00215, SRG-OS-000466-
                GPOS-00210
                Audit:
                Note: The -w option is deprecated and the rule should be updated in accordance with
                the Remediation Procedure.

                     1. Run the following command to verify a rule exists in the running configuration:

                # auditctl -l | grep -Ps -- '^\h*[^#\n\r]+\h*\/usr\/bin\/chacl'
                Verify the output includes:
                -a always,exit -F path=/usr/bin/chacl -F perm=x -F auid>=1000 -F auid!=unset

                     2. Run the following command to verify a rule exists in a rules file:

                # grep -Ps -- '^\h*[^#\n\r]+\h*\/usr\/bin\/chacl' /etc/audit/rules.d/*.rules
                Example output:
                /etc/audit/rules.d/50-perm_chng.rules:-a always,exit -S all -F
                path=/usr/bin/chacl -F perm=x -F auid>=1000 -F auid!=-1 -k perm_chng

                Remediation:
                Note:

                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.
                     •   For best performance, the arch field should be supplied in the rule. The
                         individual permissions will cause the selection of specific system calls that use
                         that kind of access. Not supplying the arch will cause the selection of all system
                         calls which will affect performance as all system calls will be evaluated.

                     1. Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                        extension, with the relevant rules to monitor successful and unsuccessful
                        attempts to use the chacl command.

                Example:
                # printf "
                -a always,exit -F arch=b64 -S all -F path=/usr/bin/chacl -F perm=x -F
                auid>=1000 -F auid!=unset -k perm_chng
                " >> /etc/audit/rules.d/50-perm_chng.rules
                Note: The auid>= value should be set to the same value as UID_MIN value in
                /etc/login.defs.

                     2. Merge and load the rules into active configuration:

                # augenrules --load

                     3. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. NIST SP 800-53 Rev. 5: AU-2, AU-12, SI-5
                     2. NIST SP 800-53A :: AU-12.1 (ii)
                     3. STIG ID: RHEL-08-030570 | RULE ID: SV-230464r627750 | CAT II
                     4. STIG ID: RHEL-09-654035 | RULE ID: SV-258181r1045328 | CAT II
                     5. STIG ID: ALMA-09-048200 | RULE ID: SV-269476r1050359 | CAT II
                     6. STIG ID: UBTU-22-654015 | RULE ID: SV-260605r958446 | CAT II
                     7. STIG ID: UBTU-24-900240 | RULE ID: SV-270795r1066874 | CAT II

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.
                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.
                CIS Controls:

                    Controls
                                                                   Control                                    IG 1 IG 2 IG 3
                    Version
                                8.2 Collect Audit Logs
                        v8        Collect audit logs. Ensure that logging, per the enterprise’s audit log      ●    ●    ●
                                management process, has been enabled across enterprise assets.
                                6.2 Activate audit logging
                        v7         Ensure that local logging has been enabled on all systems and networking    ●    ●    ●
                                devices.

                6.2.3.30 Ensure successful and unsuccessful attempts to use the
                usermod command are collected (Automated)
                Profile Applicability:

                • Level 2 - Server
                • Level 2 - Workstation

                Description:
                The operating system must generate audit records for successful/unsuccessful uses of
                the usermod command.

                Rationale:
                The usermod command modifies the system account files to reflect the changes that are
                specified on the command line. Without generating audit records that are specific to the
                security and needs of the organization, it would be difficult to establish, correlate, and
                investigate the events relating to an incident or identify those responsible for one.
                Audit records can be generated from various components within the information system
                (e.g., module or policy filter).
                Satisfies: SRG-OS-000062-GPOS-00031, SRG-OS-000037-GPOS-00015, SRG-OS-
                000042-GPOS-00020, SRG-OS-000062-GPOS-00031, SRG-OS-000392-GPOS-00172,
                SRG-OS-000462-GPOS-00206, SRG-OS-000471-GPOS-00215, SRG-OS-000466-
                GPOS-00210
                Audit:
                Note: The -w option is deprecated and the rule should be updated in accordance with
                the Remediation Procedure.

                     1. Run the following command to verify a rule exists in the running configuration:

                # auditctl -l | grep -Ps -- '^\h*[^#\n\r]+\h*\/usr\/sbin\/usermod'
                Verify the output includes:
                -a always,exit -F path=/usr/sbin/usermod -F perm=x -F auid>=1000 -F
                auid!=unset

                     2. Run the following command to verify a rule exists in a rules file:

                # grep -Ps -- '^\h*[^#\n\r]+\h*\/usr\/sbin\/usermod'
                /etc/audit/rules.d/*.rules
                Example output:
                /etc/audit/rules.d/50-usermod.rules:-a always,exit -F arch=b64 -S all -F
                path=/usr/sbin/usermod -F perm=x -F auid>=1000 -F auid!=-1 -k usermod

                Remediation:
                Note:

                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.
                     •   For best performance, the arch field should be supplied in the rule. The
                         individual permissions will cause the selection of specific system calls that use
                         that kind of access. Not supplying the arch will cause the selection of all system
                         calls which will affect performance as all system calls will be evaluated.

                     1. Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                        extension, with the relevant rules to monitor successful and unsuccessful
                        attempts to use the usermod command.

                Example:
                # printf "
                -a always,exit -F arch=b64 -S all -F path=/usr/sbin/usermod -F perm=x -F
                auid>=1000 -F auid!=unset -k usermod
                " >> /etc/audit/rules.d/50-usermod.rules unset
                Note: The auid>= value should be set to the same value as UID_MIN value in
                /etc/login.defs.

                     2. Merge and load the rules into active configuration:

                # augenrules --load

                     3. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. NIST SP 800-53 :: AU-12 a
                     2. NIST SP 800-53A :: AU-12.1 (ii)
                     3. STIG ID: RHEL-08-030560 | RULE ID: SV-230463r627750 | CAT II
                     4. STIG ID: RHEL-09-654175 | RULE ID: SV-258209r1045412 | CAT II
                     5. STIG ID: ALMA-09-051280 | RULE ID: SV-269504r1050387 | CAT II
                     6. STIG ID: UBTU-22-654125 | RULE ID: SV-260627r958446 | CAT II
                     7. STIG ID: UBTU-24-900310 | RULE ID: SV-270802r1066895 | CAT II

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.
                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.
                CIS Controls:

                    Controls
                                                                   Control                                    IG 1 IG 2 IG 3
                    Version
                                8.2 Collect Audit Logs
                        v8        Collect audit logs. Ensure that logging, per the enterprise’s audit log      ●    ●    ●
                                management process, has been enabled across enterprise assets.
                                6.2 Activate audit logging
                        v7         Ensure that local logging has been enabled on all systems and networking    ●    ●    ●
                                devices.

                6.2.3.31 Ensure kernel module loading unloading and
                modification is collected (Automated)
                Profile Applicability:

                • Level 2 - Server
                • Level 2 - Workstation

                Description:
                Monitor the loading and unloading of kernel modules. All the loading / listing /
                dependency checking of modules is done by kmod via symbolic links.

                Rationale:
                Monitoring the use of all the various ways to manipulate kernel modules could provide
                system administrators with evidence that an unauthorized change was made to a kernel
                module, possibly compromising the security of the system.
                Satisfies: SRG-OS-000062-GPOS-00031, SRG-OS-000037-GPOS-00015, SRG-OS-
                000042-GPOS-00020, SRG-OS-000062-GPOS-00031, SRG-OS-000392-GPOS-00172,
                SRG-OS-000462-GPOS-00206, SRG-OS-000471-GPOS-00215, SRG-OS-000471-
                GPOS-00216, SRG-OS-000477-GPOS-00222

                Audit:
                Note: The -w option is deprecated and the rule should be updated in accordance with
                the Remediation Procedure.

                     1. Run the following command to verify a rule exists in the running configuration:

                # auditctl -l | grep -Ps -- '^\h*[^#\n\r]+\h*\/usr\/bin\/kmod'
                Verify the output includes:
                -a always,exit -F path=/usr/bin/kmod -F perm=x -F auid>=1000 -F auid!=unset

                     2. Run the following command to verify a rule exists in a rules file:

                # grep -Ps -- '^\h*[^#\n\r]+\h*\/usr\/bin\/kmod' /etc/audit/rules.d/*.rules
                Example output:
                /etc/audit/rules.d/50-kernel_modules.rules:-a always,exit -F arch=b64 -S all
                -F path=/usr/bin/kmod -F perm=x -F auid>=1000 -F auid!=unset -k
                kernel_modules
                Symlink audit
                Run the following script to audit if the symlinks kmod accepts are indeed pointing at it:
                #!/usr/bin/env bash

                {
                   a_files=("/usr/sbin/lsmod" "/usr/sbin/rmmod" "/usr/sbin/insmod"
                "/usr/sbin/modinfo" "/usr/sbin/modprobe" "/usr/sbin/depmod")
                   for l_file in "${a_files[@]}"; do
                      if [ "$(readlink -f "$l_file")" = "$(readlink -f /bin/kmod)" ]; then
                         printf "OK: \"$l_file\"\n"
                      else
                         printf "Issue with symlink for file: \"$l_file\"\n"
                      fi
                   done
                }
                Verify the output states OK. If there is a symlink pointing to a different location it should
                be investigated

                Remediation:
                Note:

                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.
                     •   For best performance, the arch field should be supplied in the rule. The
                         individual permissions will cause the selection of specific system calls that use
                         that kind of access. Not supplying the arch will cause the selection of all system
                         calls which will affect performance as all system calls will be evaluated.

                     1. Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                        extension, with the relevant rules to monitor kernel module modification.

                Example:
                # printf "
                -a always,exit -F arch=b64 -S all -F path=/usr/bin/kmod -F perm=x -F
                auid>=1000 -F auid!=unset -k kernel_modules
                " >> /etc/audit/rules.d/50-kernel_modules.rules

                     2. Merge and load the rules into active configuration:

                # augenrules --load

                     3. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. NIST SP 800-53 :: AU-12 a
                     2. NIST SP 800-53A :: AU-12.1 (ii)

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.
                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.

                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              6.2 Activate audit logging
                       v7        Ensure that local logging has been enabled on all systems and networking             ●    ●    ●
                              devices.

                6.2.3.32 Ensure kernel "init_module" and "finit_module" loading
                unloading and modification is collected (Automated)
                Profile Applicability:

                • Level 2 - Server
                • Level 2 - Workstation

                Description:
                The following system calls control loading and unloading of modules:

                     •   init_module - load a module
                     •   finit_module - load a module (used when the overhead of using
                         cryptographically signed modules to determine the authenticity of a module can
                         be avoided)

                Any execution of the loading and unloading module programs and system calls will
                trigger an audit record with an identifier of modules.

                Rationale:
                Monitoring the use of all the various ways to manipulate kernel modules could provide
                system administrators with evidence that an unauthorized change was made to a kernel
                module, possibly compromising the security of the system.
                Satisfies: SRG-OS-000062-GPOS-00031, SRG-OS-000037-GPOS-00015, SRG-OS-
                000042-GPOS-00020, SRG-OS-000062-GPOS-00031, SRG-OS-000392-GPOS-00172,
                SRG-OS-000462-GPOS-00206, SRG-OS-000471-GPOS-00215, SRG-OS-000471-
                GPOS-00216, SRG-OS-000477-GPOS-00222

                Audit:
                Note:

                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.
                     •   Output showing all the audited syscalls, e.g. (-a always,exit -F arch=b64 -S
                         init_module,finit_module,delete_module,create_module,query_module -F
                         auid>=1000 -F auid!=unset -k kernel_modules) is also acceptable. These have
                         been separated by function on the displayed output for clarity.

                     1. Run the following command to verify a rule exists in the running configuration:

                # auditctl -l | grep -Ps -- 'init_module'
                Verify the output includes init_module and finite_module:
                -a always,exit -S
                init_module,finit_module,delete_module,create_module,query_module -F
                auid>=1000 -F auid!=unset

                     2. Run the following command to verify a rule exists in a rules file:

                # grep -Ps -- 'init_module' /etc/audit/rules.d/*.rules
                Example output:
                /etc/audit/rules.d/50-kernel_modules.rules:-a always,exit -F arch=b64 -S
                init_module,finit_module,delete_module,create_module,query_module -F
                auid>=1000 -F auid!=unset -k kernel_modules

                Remediation:
                Note:

                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.
                     •   For best performance, the arch field should be supplied in the rule. The
                         individual permissions will cause the selection of specific system calls that use
                         that kind of access. Not supplying the arch will cause the selection of all system
                         calls which will affect performance as all system calls will be evaluated.

                     1. Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                        extension, with the relevant rules to monitor kernel module modification.

                Example:
                # printf "
                -a always,exit -F arch=b64 -S init_module,finit_module -F auid>=1000 -F
                auid!=unset -k kernel_modules
                " >> /etc/audit/rules.d/50-kernel_modules.rules
                Note: The auid>= value should be set to the same value as UID_MIN value in
                /etc/login.defs.

                     2. Merge and load the rules into active configuration:

                # augenrules --load

                     3. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. NIST SP 800-53 :: AU-12 a
                     2. NIST SP 800-53A :: AU-12.1 (ii)
                     3. STIG ID: RHEL-08-030360 | RULE ID: SV-230438r1017241 | CAT II
                     4. STIG ID: RHEL-09-654080 | RULE ID: SV-258198r1045379 | CAT II
                     5. STIG ID: ALMA-09-046660 | RULE ID: SV-269466r1050349 | CAT II
                     6. STIG ID: UBTU-22-654175 | RULE ID: SV-260637r958446 | CAT II
                     7. STIG ID: UBTU-24-900340 | RULE ID: SV-270805r1068384 | CAT II

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.

                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.
                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              6.2 Activate audit logging
                       v7        Ensure that local logging has been enabled on all systems and networking             ●    ●    ●
                              devices.

                6.2.3.33 Ensure kernel "delete_module" loading unloading and
                modification is collected (Automated)
                Profile Applicability:

                • Level 2 - Server
                • Level 2 - Workstation

                Description:
                Monitor the loading and unloading of kernel modules. All the loading / listing /
                dependency checking of modules is done by kmod via symbolic links.
                The following system calls control loading and unloading of modules:

                     •   delete_module - delete a module

                Any execution of the loading and unloading module programs and system calls will
                trigger an audit record with an identifier of modules.

                Rationale:
                Monitoring the use of all the various ways to manipulate kernel modules could provide
                system administrators with evidence that an unauthorized change was made to a kernel
                module, possibly compromising the security of the system.
                Satisfies: SRG-OS-000062-GPOS-00031, SRG-OS-000037-GPOS-00015, SRG-OS-
                000042-GPOS-00020, SRG-OS-000062-GPOS-00031, SRG-OS-000392-GPOS-00172,
                SRG-OS-000462-GPOS-00206, SRG-OS-000471-GPOS-00215, SRG-OS-000471-
                GPOS-00216, SRG-OS-000477-GPOS-00222

                Audit:
                Note:

                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.
                     •   Output showing all the audited syscalls, e.g. (-a always,exit -F arch=b64 -
                         S
                         init_module,finit_module,delete_module,create_module,query_module
                         -F auid>=1000 -F auid!=unset -k kernel_modules) is also acceptable.
                         These have been separated by function on the displayed output for clarity.

                     1. Run the following command to verify a rule exists in the running configuration:

                # auditctl -l | grep -Ps -- 'delete_module'
                Verify the output includes delete_module:
                -a always,exit -S
                init_module,finit_module,delete_module,create_module,query_module -F
                auid>=1000 -F auid!=unset

                     2. Run the following command to verify a rule exists in a rules file:

                # grep -Ps -- 'delete_module' /etc/audit/rules.d/*.rules
                Example output:
                /etc/audit/rules.d/50-kernel_modules.rules:-a always,exit -F arch=b64 -S
                delete_module -F auid>=1000 -F auid!=-1 -k kernel_modules

                Remediation:
                Note:

                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.
                     •   For best performance, the arch field should be supplied in the rule. The
                         individual permissions will cause the selection of specific system calls that use
                         that kind of access. Not supplying the arch will cause the selection of all system
                         calls which will affect performance as all system calls will be evaluated.

                     1. Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                        extension, with the relevant rules to monitor kernel module modification.

                Example:
                # printf "
                -a always,exit -F arch=b64 -S delete_module -F auid>=1000 -F auid!=unset -k
                kernel_modules
                " >> /etc/audit/rules.d/50-kernel_modules.rules
                Note: The auid>= value should be set to the same value as UID_MIN value in
                /etc/login.defs.
                2.Merge and load the rules into active configuration:
                # augenrules --load

                     3. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. NIST SP 800-53 :: AU-12 a
                     2. NIST SP 800-53A :: AU-12.1 (ii)
                     3. STIG ID: RHEL-08-030390 | RULE ID: SV-230446r627750 | CAT II
                     4. STIG ID: RHEL-09-654075 | RULE ID: SV-258189r1045352 | CAT II
                     5. STIG ID: ALMA-09-046660 | RULE ID: SV-269466r1050349 | CAT II
                     6. STIG ID: UBTU-22-654170 | RULE ID: SV-260636r958446 | CAT II
                     7. STIG ID: UBTU-24-900350 | RULE ID: SV-270806r1068386 | CAT II

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.

                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.
                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              6.2 Activate audit logging
                       v7        Ensure that local logging has been enabled on all systems and networking             ●    ●    ●
                              devices.

                6.2.3.34 Ensure kernel query_module loading unloading and
                modification is collected (Automated)
                Profile Applicability:

                • Level 2 - Server
                • Level 2 - Workstation

                Description:
                Monitor the loading and unloading of kernel modules. All the loading / listing /
                dependency checking of modules is done by kmod via symbolic links.
                The following system calls control loading and unloading of modules:

                     •   create_module - create a loadable module entry
                     •   query_module - query the kernel for various bits pertaining to modules

                Any execution of the loading and unloading module programs and system calls will
                trigger an audit record with an identifier of modules.

                Rationale:
                Monitoring the use of all the various ways to manipulate kernel modules could provide
                system administrators with evidence that an unauthorized change was made to a kernel
                module, possibly compromising the security of the system.
                Satisfies: SRG-OS-000062-GPOS-00031, SRG-OS-000037-GPOS-00015, SRG-OS-
                000042-GPOS-00020, SRG-OS-000062-GPOS-00031, SRG-OS-000392-GPOS-00172,
                SRG-OS-000462-GPOS-00206, SRG-OS-000471-GPOS-00215, SRG-OS-000471-
                GPOS-00216, SRG-OS-000477-GPOS-00222

                Audit:
                Note:

                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.
                     •   Output showing all the audited syscalls, e.g. (-a always,exit -F arch=b64 -S
                         init_module,finit_module,delete_module,create_module,query_module -F
                         auid>=1000 -F auid!=unset -k kernel_modules) is also acceptable. These have
                         been separated by function on the displayed output for clarity.

                     1. Run the following command to verify a rule exists in the running configuration:

                # auditctl -l | grep -Ps -- 'query_module'
                Verify the output includes query_module:
                -a always,exit-S init_module,finit_module,delete_module,query_module -F
                auid>=1000 -F auid!=unset

                     2. Run the following command to verify a rule exists in a rules file:

                # grep -Ps -- 'query_module' /etc/audit/rules.d/*.rules
                Example output:
                /etc/audit/rules.d/50-kernel_modules.rules :-a always,exit -F arch=b64 -S
                init_module,delete_module,query_module,finit_module -F auid>=1000 -F auid!=-1
                -k kernel_modules

                Remediation:
                Note:

                     •   The -w option is deprecated and the rule should be updated in accordance with
                         the Remediation Procedure.
                     •   For best performance, the arch field should be supplied in the rule. The
                         individual permissions will cause the selection of specific system calls that use
                         that kind of access. Not supplying the arch will cause the selection of all system
                         calls which will affect performance as all system calls will be evaluated.

                     1. Edit or create a file in the /etc/audit/rules.d/ directory, ending in .rules
                        extension, with the relevant rules to monitor kernel module modification.

                Example:
                # printf "
                -a always,exit -F arch=b64 -S query_module -F auid>=1000 -F auid!=unset -k
                kernel_modules
                " >> /etc/audit/rules.d/50-kernel_modules.rules
                Note: The auid>= value should be set to the same value as UID_MIN value in
                /etc/login.defs.

                     2. Merge and load the rules into active configuration:

                # augenrules --load

                     3. Check if reboot is required:

                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. NIST SP 800-53 :: AU-12 a
                     2. NIST SP 800-53A :: AU-12.1 (ii)

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.
                System call structure
                For performance (man 7 audit.rules) reasons it is preferable to have all the system
                calls on one line. However, your configuration may have them on one line each or some
                other combination. This is important to understand for both the auditing and remediation
                sections as the examples given are optimized for performance as per the man page.

                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              6.2 Activate audit logging
                       v7        Ensure that local logging has been enabled on all systems and networking             ●    ●    ●
                              devices.

                6.2.3.35 Ensure the audit configuration is loaded regardless of
                errors (Automated)
                Profile Applicability:

                • Level 2 - Server
                • Level 2 - Workstation

                Description:
                Set -c flag so that auditctl will continue loading rules in spite of an error. The exit
                code will not be success if any rule fails to load.
                Rationale:
                The default behaviour of auditctl is to stop loading any further rules if it encounters an
                error in the rules (for example a file watcher referencing a non-existent file). This can
                lead to auditd running without valid rules being present. It is best to have all valid rules
                loaded and active rather than a subset.
                Audit:
                Run the following command and verify output matches:
                # grep -Ph -- '^\h*-c\b' /etc/audit/rules.d/*.rules | tail -1

                -c

                Remediation:
                Edit or create the file /etc/audit/rules.d/01-initialize.rules and add the line -
                c at the end of the file:
                Example:
                # printf '%s\n' "" "-c" >> /etc/audit/rules.d/01-initialize.rules
                Load audit rules
                Merge and load the rules into active configuration:
                # augenrules --load
                Check if reboot is required.
                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.

                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              6.2 Activate audit logging
                       v7        Ensure that local logging has been enabled on all systems and networking             ●    ●    ●
                              devices.
                              6.3 Enable Detailed Logging
                       v7        Enable system logging to include detailed information such as an event source,
                              date, user, timestamp, source addresses, destination addresses, and other useful
                                                                                                                           ●    ●
                              elements.
  '6.2.3.25': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '6.2.3.26': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '6.2.3.31': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '6.2.3.33': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '6.2.3.34': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '6.2.3.35': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '6.2.3.36': !unsafe |-
    Edit or create the file /etc/audit/rules.d/99-finalize.rules and add the line -e
                2 at the end of the file:
                Example:
                # printf '\n%s' "-e 2" >> /etc/audit/rules.d/99-finalize.rules
                Load audit rules
                Merge and load the rules into active configuration:
                # augenrules --load
                Check if reboot is required.
                # if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then printf "Reboot
                required to load rules\n"; fi

                References:

                     1. NIST SP 800-53 :: AU-9
                     2. NIST SP 800-53A :: AU-9.1
                     3. STIG ID: OL08-00-030121 | RULE ID: SV-248738r958434 | CAT II
                     4. STIG ID: RHEL-08-030121 | RULE ID: SV-230402r1017208 | CAT II
                     5. STIG ID: RHEL-09-654275 | RULE ID: SV-258241r1051259 | CAT II
                     6. STIG ID: ALMA-09-057110 | RULE ID: SV-269195r1050078 | CAT II
                     7. STIG ID: OL09-00-008005 | RULE ID: SV-271886r1092370 | CAT II
                     8. STIG ID: UBTU-24-909000 | RULE ID: SV-270832r1068399 | CAT II

                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                      ●    ●    ●
                              systems, databases, and applications.
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              6.2 Activate audit logging
                       v7        Ensure that local logging has been enabled on all systems and networking             ●    ●    ●
                              devices.
                              6.3 Enable Detailed Logging
                       v7        Enable system logging to include detailed information such as an event source,
                              date, user, timestamp, source addresses, destination addresses, and other useful
                                                                                                                           ●    ●
                              elements.
  '6.2.3.37': !unsafe |-
    If the rules are not aligned across all three () areas, run the following command to
                merge and load all rules:
                # augenrules --load
                Check if reboot is required.
                if [[ $(auditctl -s | grep "enabled") =~ "2" ]]; then echo "Reboot required
                to load rules"; fi

                References:

                     1. NIST SP 800-53 Rev. 5: AU-3

                Additional Information:
                Potential reboot required
                If the auditing configuration is locked (-e 2), then augenrules will not warn in any way
                that rules could not be loaded into the running configuration. A system reboot will be
                required to load the rules into the running configuration.
                CIS Controls:

                  Controls
                                                                  Control                                            IG 1 IG 2 IG 3
                  Version
                              8.5 Collect Detailed Audit Logs
                       v8        Configure detailed audit logging for enterprise assets containing sensitive data.
                              Include event source, date, username, timestamp, source addresses, destination
                                                                                                                           ●    ●
                              addresses, and other useful elements that could assist in a forensic investigation.
                              6.3 Enable Detailed Logging
                       v7        Enable system logging to include detailed information such as an event source,
                              date, user, timestamp, source addresses, destination addresses, and other useful
                                                                                                                           ●    ●
                              elements.

                6.2.4 Configure auditd File Access

                Without the capability to restrict which roles and individuals can select which events are
                audited, unauthorized personnel may be able to prevent the auditing of critical events.
  '6.2.4.1': !unsafe |-
    Run the following command to remove more permissive mode than 0640 from audit log
                files:
                # [ -f /etc/audit/auditd.conf ] && find "$(dirname $(awk -F "="
                '/^\s*log_file/ {print $2}' /etc/audit/auditd.conf | xargs))" -type f -perm
                /0137 -exec chmod u-x,g-wx,o-rwx {} +

                References:

                     1. NIST SP 800-53 Rev. 5: AU-3

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '6.2.4.2': !unsafe |-
    Run the following command to configure the audit log files to be owned by the root
                user:
                # [ -f /etc/audit/auditd.conf ] && find "$(dirname $(awk -F "="
                '/^\s*log_file/ {print $2}' /etc/audit/auditd.conf | xargs))" -type f ! -user
                root -exec chown root {} +

                References:

                     1. NIST SP 800-53 Rev. 5: AU-3

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '6.2.4.3': !unsafe |-
    Run the following command to configure the audit log files to be group owned by adm:
                # find $(dirname $(awk -F"=" '/^\s*log_file/ {print $2}'
                /etc/audit/auditd.conf | xargs)) -type f \( ! -group adm -a ! -group root \)
                -exec chgrp adm {} +
                Run the following command to set the log_group parameter in the audit configuration
                file to log_group = adm:
                # sed -ri 's/^\s*#?\s*log_group\s*=\s*\S+(\s*#.*)?.*$/log_group = adm\1/'
                /etc/audit/auditd.conf
                Run the following command to restart the audit daemon to reload the configuration file:
                # systemctl restart auditd

                References:

                     1. NIST SP 800-53 Rev. 5: AU-3

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '6.2.4.4': !unsafe |-
    Run the following command to configure the audit log directory to have a mode of
                "0750" or less permissive:
                # chmod g-w,o-rwx "$(dirname "$(awk -F= '/^\s*log_file\s*/{print $2}'
                /etc/audit/auditd.conf | xargs)")"

                Default Value:
                750
                References:

                     1. NIST SP 800-53 Rev. 5: AU-3

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '6.2.4.5': !unsafe |-
    Run the following command to remove more permissive mode than 0640 from the audit
                configuration files:
                # find /etc/audit/ -type f \( -name '*.conf' -o -name '*.rules' \) -exec
                chmod u-x,g-wx,o-rwx {} +

                References:

                     1. NIST SP 800-53 Rev. 5: AU-3

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '6.2.4.6': !unsafe |-
    Run the following command to change ownership to root user:
                # find /etc/audit/ -type f \( -name '*.conf' -o -name '*.rules' \) ! -user
                root -exec chown root {} +

                References:

                     1. NIST SP 800-53 Rev. 5: AU-3

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '6.2.4.7': !unsafe |-
    Run the following command to change group to root:
                # find /etc/audit/ -type f \( -name '*.conf' -o -name '*.rules' \) ! -group
                root -exec chgrp root {} +

                References:

                     1. NIST SP 800-53 Rev. 5: AU-3

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '6.2.4.8': !unsafe |-
    Run the following command to remove more permissive mode from the audit tools:
                # chmod go-w /sbin/auditctl /sbin/aureport /sbin/ausearch /sbin/auditd
                /sbin/augenrules

                References:

                     1. NIST SP 800-53 Rev. 5: AU-3

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '6.2.4.9': !unsafe |-
    Run the following command to change the owner of the audit tools to the root user:
                # chown root /sbin/auditctl /sbin/aureport /sbin/ausearch /sbin/auditd
                /sbin/augenrules

                References:

                     1. NIST SP 800-53 Rev. 5: AU-3

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '6.2.4.10': !unsafe |-
    Run the following command to change group ownership to the group root:
                # chgrp root /sbin/auditctl /sbin/aureport /sbin/ausearch /sbin/auditd
                /sbin/augenrules

                References:

                     1. NIST SP 800-53 Rev. 5: AU-3

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.

                6.3 Configure Integrity Checking

                AIDE is a file integrity checking tool, similar in nature to Tripwire. While it cannot prevent
                intrusions, it can detect unauthorized changes to configuration files by alerting when the
                files are changed. When setting up AIDE, decide internally what the site policy will be
                concerning integrity checking. Review the AIDE quick start guide and AIDE
                documentation before proceeding.
  '6.3.1': !unsafe |-
    Install AIDE using the appropriate package manager or manual installation:
                # apt install aide aide-common
                Configure AIDE as appropriate for your environment. Consult the AIDE documentation
                for options.
                Run the following commands to initialize AIDE:
                # aideinit
                # mv /var/lib/aide/aide.db.new /var/lib/aide/aide.db

                References:

                     1. NIST SP 800-53 Rev. 5: AU-2

                Additional Information:
                The prelinking feature can interfere with AIDE because it alters binaries to speed up
                their start up times. Run prelink -ua to restore the binaries to their prelinked state,
                thus avoiding false positives from AIDE.

                CIS Controls:

                   Controls
                                                                 Control                                      IG 1 IG 2 IG 3
                   Version
                        v8    3.14 Log Sensitive Data Access                                                             ●
                                 Log sensitive data access, including modification and disposal.
                              14.9 Enforce Detail Logging for Access or Changes to
                              Sensitive Data
                        v7        Enforce detailed audit logging for access to sensitive data or changes to              ●
                              sensitive data (utilizing tools such as File Integrity Monitoring or Security
                              Information and Event Monitoring).
  '6.3.2': !unsafe |-
    Run the following command to unmask dailyaidecheck.timer and
                dailyaidecheck.service:
                # systemctl unmask dailyaidecheck.timer dailyaidecheck.service
                Run the following command to enable and start dailyaidecheck.timer:
                # systemctl --now enable dailyaidecheck.timer

                References:

                     1. https://github.com/konstruktoid/hardening/blob/master/config/aidecheck.service
                     2. https://github.com/konstruktoid/hardening/blob/master/config/aidecheck.timer
                     3. NIST SP 800-53 Rev. 5: AU-2

                Additional Information:
                The checking in this recommendation occurs every day at 5am. Alter the frequency and
                time of the checks in compliance with site policy
                systemd timers, timer file aidecheck.timer and service file aidecheck.service, have
                been included as an optional alternative to using cron
                Ubuntu advises using /usr/bin/aide.wrapper rather than calling /usr/bin/aide
                directly in order to protect the database and prevent conflicts
                CIS Controls:

                   Controls
                                                                 Control                                      IG 1 IG 2 IG 3
                   Version
                        v8    3.14 Log Sensitive Data Access                                                             ●
                                 Log sensitive data access, including modification and disposal.
                              14.9 Enforce Detail Logging for Access or Changes to
                              Sensitive Data
                        v7        Enforce detailed audit logging for access to sensitive data or changes to              ●
                              sensitive data (utilizing tools such as File Integrity Monitoring or Security
                              Information and Event Monitoring).

                6.3.3 Ensure cryptographic mechanisms are used to protect the
                integrity of audit tools (Automated)
                Profile Applicability:

                • Level 2 - Server
                • Level 2 - Workstation

                Description:
                Audit tools include, but are not limited to, vendor-provided and open source audit tools
                needed to successfully view and manipulate audit information system activity and
                records. Audit tools include custom queries and report generators.
                aide.conf is case-sensitive. Leading and trailing white spaces are ignored. Each
                config lines must end with new line.
                AIDE uses the backslash character \ as escape character for ' ' (space), '@' and ''
                (backslash) (e.g. '\ ' or '@'). To literally match a '' in a file path with a regular expression
                you have to escape the backslash twice (i.e. '\\').
                There are three types of lines in aide.conf:

                     •   The configuration options which are used to set configuration parameters and
                         define groups.
                     •   (restricted) rules that are used to indicate which files are added to the database.
                     •   Macro lines define or undefine variables within the config file.

                Note: Lines beginning with # are ignored as comments.
                @@include <FILE> - Include <FILE>.

                     •   The content of the file is used as if it were inserted in this part of the config file.
                     •   The maximum depth of nested includes is 16.

                `@@include <DIRECTORY> <REGEX> - [RULE_PREFIX] (added in AIDE v0.17)

                     •   Include all (regular) files found in <DIRECTORY> matching regular expression
                         <REGEX> (sub-directories are ignored).
                     •   The file are included in lexical sort order.
                     •   If RULE_PREFIX is set, all rules included by the statement are prefixed with given
                         <RULE_PREFIX> (added in AIDE v0.18). Prefixes from nested include
                         statements are concatenated.
                     •   The content of the files is used as if it were inserted in this part of the config file.

                @x_include:

                     •   is identical to @@include, except that if a config file is executable is is run and
                         the output is used as config.

                     •   If the executable file exits with status greater than zero or writes to stderr aide
                         stops with an error.
                     •   For security reasons <DIRECTORY> and each executable config file must be
                         owned by the current user or root. They must not be group- or world-writable.
                     •   @@x_include _<FILE>_ (added in AIDE v0.17):
                     •   `@@x_include <DIRECTORY> <REGEX> [RULE_PREFIX] (added in AIDE
                         v0.17)

                @@x_include_setenv <VAR> <VALUE> (added in AIDE v0.17)

                     •   Adds the variable <VAR> with the value <VALUE> to the environment used for
                         config file execution.
                     •   Environment variable names are limited to alphanumeric characters (A-Za-z0-9)
                         and the underscore '_' and must not begin with a digit.

                Rationale:
                Protecting the integrity of the tools used for auditing purposes is a critical step toward
                ensuring the integrity of audit information. Audit information includes all information
                (e.g., audit records, audit settings, and audit reports) needed to successfully audit
                information system activity.
                Attackers may replace the audit tools or inject code into the existing tools with the
                purpose of providing the capability to hide or erase system activity from the audit logs.
                Audit tools should be cryptographically signed in order to provide the capability to
                identify when the audit tools have been modified, manipulated, or replaced. An example
                is a checksum hash of the file or files.
                Audit:
                Verify that Advanced Intrusion Detection Environment (AIDE) is properly configured .
                Run the following script to verify:

                     •   AIDE is configured to use cryptographic mechanisms to protect the integrity of
                         audit tools:
                     •   The following audit tool files include the options "p, i, n, u, g, s, b, acl, xattrs and
                         sha512"
                            o auditctl
                            o auditd
                            o ausearch
                            o aureport
                            o augenrules

                #!/usr/bin/env bash

                {
                   a_output=() a_output2=() l_tool_dir="$(readlink -f /sbin)"
                   a_items=("p" "i" "n" "u" "g" "s" "b" "acl" "xattrs" "sha512")
                   l_aide_cmd="$(whereis aide | awk '{print $2}')"
                   a_audit_files=("auditctl" "auditd" "ausearch" "aureport" "augenrules")
                   if [ -f "$l_aide_cmd" ] && command -v "$l_aide_cmd" &>/dev/null; then
                      a_aide_conf_files=("$(find -L /etc -type f -name 'aide.conf')")
                      f_file_par_chk()
                      {
                         a_out2=()
                         for l_item in "${a_items[@]}"; do
                            ! grep -Psiq -- '(\h+|\+)'"$l_item"'(\h+|\+)' <<< "$l_out" && \
                            a_out2+=(" - Missing the \"$l_item\" option")
                         done
                         if [ "${#a_out2[@]}" -gt "0" ]; then
                            a_output2+=(" - Audit tool file: \"$l_file\"" "${a_out2[@]}")
                         else
                            a_output+=(" - Audit tool file: \"$l_file\" includes:" "
                \"${a_items[*]}\"")
                         fi
                      }
                      for l_file in "${a_audit_files[@]}"; do
                         if [ -f "$l_tool_dir/$l_file" ]; then
                            l_out="$("$l_aide_cmd" --config "${a_aide_conf_files[@]}" -p
                f:"$l_tool_dir/$l_file")"
                            f_file_par_chk
                         else
                            a_output+=(" - Audit tool file \"$l_file\" doesn't exist")
                         fi
                      done
                   else
                      a_output2+=(" - The command \"aide\" was not found" "      Please
                install AIDE")
                   fi
                   if [ "${#a_output2[@]}" -le 0 ]; then
                      printf '%s\n' "" "- Audit Result:" " ** PASS **" "${a_output[@]}" ""
                   else
                      printf '%s\n' "" "- Audit Result:" " ** FAIL **" " - Reason(s) for
                audit failure:" "${a_output2[@]}"
                      [ "${#a_output[@]}" -gt 0 ] && printf '%s\n' "" "- Correctly set:"
                "${a_output[@]}" ""
                   fi
                }
                Note: The script is written to read the "winning" configuration setting, to include any
                configuration settings in files included as part of the @@x_include setting.

                Remediation:
                Run the following command to determine the absolute path to the non-symlinked
                version on the audit tools:
                # readlink -f /sbin
                The output will be either /usr/sbin - OR - /sbin. Ensure the correct path is used.
                Edit /etc/aide/aide.conf and add or update the following selection lines replacing
                <PATH> with the correct path returned in the command above:
                # Audit Tools
                <PATH>/auditctl p+i+n+u+g+s+b+acl+xattrs+sha512
                <PATH>/auditd p+i+n+u+g+s+b+acl+xattrs+sha512
                <PATH>/ausearch p+i+n+u+g+s+b+acl+xattrs+sha512
                <PATH>/aureport p+i+n+u+g+s+b+acl+xattrs+sha512
                <PATH>/augenrules p+i+n+u+g+s+b+acl+xattrs+sha512
                Example
                # printf '%s\n' "" "# Audit Tools" "$(readlink -f /sbin/auditctl)
                p+i+n+u+g+s+b+acl+xattrs+sha512" "$(readlink -f /sbin/auditd)
                p+i+n+u+g+s+b+acl+xattrs+sha512" "$(readlink -f /sbin/ausearch)
                p+i+n+u+g+s+b+acl+xattrs+sha512" "$(readlink -f /sbin/aureport)
                p+i+n+u+g+s+b+acl+xattrs+sha512" "$(readlink -f /sbin/augenrules)
                p+i+n+u+g+s+b+acl+xattrs+sha512" >> /etc/aide/aide.conf
                Note: - IF - /etc/aide/aide.conf includes a @@x_include statement:

                     •   <DIRECTORY> and each executable config file must be owned by the current
                         user or root
                     •   They must not be group or world-writable

                Example:
                @@x_include /etc/aide.conf.d ^[a-zA-Z0-9_-]+$

                References:

                     1. AIDE.CONF(5)

                CIS Controls:

                     Controls Version                    Control            IG 1 IG 2 IG 3
                              v8          0.0 Explicitly Not Mapped
                                            Explicitly Not Mapped

                              v7          0.0 Explicitly Not Mapped
                                            Explicitly Not Mapped

                7 System Maintenance
                Recommendations in this section are intended as maintenance and are intended to be
                checked on a frequent basis to ensure system stability. Many recommendations do not
                have quick remediations and require investigation into the cause and best fix available
                and may indicate an attempted breach of system security.

                7.1 Configure system file and directory access

                This section provides guidance on securing aspects of system files and directories.
  '6.3.3': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '7.1.1': !unsafe |-
    Run the following commands to remove excess permissions, set owner, and set group
                on /etc/passwd:
                # chmod u-x,go-wx /etc/passwd
                # chown root:root /etc/passwd

                Default Value:
                Access: (0644/-rw-r--r--) Uid: ( 0/ root) Gid: ( 0/ root)
                References:

                     1. NIST SP 800-53 Rev. 5: AC-3, MP-2

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '7.1.2': !unsafe |-
    Run the following commands to remove excess permissions, set owner, and set group
                on /etc/passwd-:
                # chmod u-x,go-wx /etc/passwd-
                # chown root:root /etc/passwd-

                Default Value:
                Access: (0644/-rw-r--r--) Uid: ( 0/ root) Gid: { 0/ root)
                References:

                     1. NIST SP 800-53 Rev. 5: AC-3, MP-2

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '7.1.3': !unsafe |-
    Run the following commands to remove excess permissions, set owner, and set group
                on /etc/group:
                # chmod u-x,go-wx /etc/group
                # chown root:root /etc/group

                Default Value:
                Access: (0644/-rw-r--r--) Uid: ( 0/ root) Gid: ( 0/ root)
                References:

                     1. NIST SP 800-53 Rev. 5: AC-3, MP-2

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '7.1.4': !unsafe |-
    Run the following commands to remove excess permissions, set owner, and set group
                on /etc/group-:
                # chmod u-x,go-wx /etc/group-
                # chown root:root /etc/group-

                Default Value:
                Access: (0644/-rw-r--r--) Uid: ( 0/ root) Gid: ( 0/ root)
                References:

                     1. NIST SP 800-53 Rev. 5: AC-3. MP-2

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '7.1.5': !unsafe |-
    Run one of the following commands to set ownership of /etc/shadow to root and
                group to either root or shadow:
                # chown root:shadow /etc/shadow
                  -OR-
                # chown root:root /etc/shadow
                Run the following command to remove excess permissions form /etc/shadow:
                # chmod u-x,g-wx,o-rwx /etc/shadow

                Default Value:
                Access: (0640/-rw-r-----) Uid: ( 0/ root) Gid: ( 42/ shadow)
                References:

                     1. NIST SP 800-53 Rev. 5: AC-3. MP-2

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '7.1.6': !unsafe |-
    Run one of the following commands to set ownership of /etc/shadow- to root and
                group to either root or shadow:
                # chown root:shadow /etc/shadow-
                  -OR-
                # chown root:root /etc/shadow-
                Run the following command to remove excess permissions form /etc/shadow-:
                # chmod u-x,g-wx,o-rwx /etc/shadow-

                Default Value:
                Access: (0640/-rw-r-----) Uid: ( 0/ root) Gid: ( 42/ shadow)
                References:

                     1. NIST SP 800-53 Rev. 5: AC-3. MP-2

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '7.1.7': !unsafe |-
    Run one of the following commands to set ownership of /etc/gshadow to root and
                group to either root or shadow:
                # chown root:shadow /etc/gshadow
                  -OR-
                # chown root:root /etc/gshadow
                Run the following command to remove excess permissions form /etc/gshadow:
                # chmod u-x,g-wx,o-rwx /etc/gshadow

                Default Value:
                Access: (0640/-rw-r-----) Uid: ( 0/ root) Gid: ( 42/ shadow)
                References:

                     1. NIST SP 800-53 Rev. 5: AC-3. MP-2

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '7.1.8': !unsafe |-
    Run one of the following commands to set ownership of /etc/gshadow- to root and
                group to either root or shadow:
                # chown root:shadow /etc/gshadow-
                  -OR-
                # chown root:root /etc/gshadow-
                Run the following command to remove excess permissions form /etc/gshadow-:
                # chmod u-x,g-wx,o-rwx /etc/gshadow-

                Default Value:
                Access: (0640/-rw-r-----) Uid: ( 0/ root) Gid: ( 42/ shadow)
                References:

                     1. NIST SP 800-53 Rev. 5: AC-3. MP-2

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '7.1.9': !unsafe |-
    Run the following commands to remove excess permissions, set owner, and set group
                on /etc/shells:
                # chmod u-x,go-wx /etc/shells
                # chown root:root /etc/shells

                Default Value:
                Access: (0644/-rw-r--r--) Uid: ( 0/ root) Gid: ( 0/ root)
                References:

                     1. NIST SP 800-53 Rev. 5: AC-3, MP-2

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '7.1.10': !unsafe |-
    Run the following commands to remove excess permissions, set owner, and set group
                on /etc/security/opasswd and /etc/security/opasswd.old is they exist:
                # [ -e "/etc/security/opasswd" ] && chmod u-x,go-rwx /etc/security/opasswd
                # [ -e "/etc/security/opasswd" ] && chown root:root /etc/security/opasswd
                # [ -e "/etc/security/opasswd.old" ] && chmod u-x,go-rwx
                /etc/security/opasswd.old
                # [ -e "/etc/security/opasswd.old" ] && chown root:root
                /etc/security/opasswd.old

                Default Value:
                /etc/security/opasswd Access: (0600/-rw-------) Uid: ( 0/ root) Gid: ( 0/ root)
                References:

                     1. NIST SP 800-53 Rev. 5: AC-3, MP-2

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '7.1.11': !unsafe |-
    •   World Writable Files:
                           o It is recommended that write access is removed from other with the
                               command ( chmod o-w <filename> ), but always consult relevant vendor
                               documentation to avoid breaking any application dependencies on a given
                               file.
                     •   World Writable Directories:
                           o Set the sticky bit on all world writable directories with the command (
                               chmod a+t <directory_name> )

                Run the following script to:

                     •   Remove other write permission from any world writable files
                     •   Add the sticky bit to all world writable directories

                #!/usr/bin/env bash

                {
                   l_smask='01000' a_file=() a_dir=()
                   a_path=(-path "*/containers/storage/*" -o -path "*/containerd/*" -o -path
                "*/kubelet/*" -o -path "/sys/*" -o -path "/snap/*" -o -path "/boot/efi/*")
                   l_exclude="nfs|proc|cifs|smb|vfat|iso9660|efivarfs|selinuxfs|ncpfs"
                   l_exclude2="\/run|\/tmp|\/var\/tmp"
                   while IFS= read -r l_mount; do
                      while IFS= read -r -d $'\0' l_file; do
                         l_mode="$(stat -Lc '%#a' "$l_file" 2>/dev/null)"
                         if [ -f "$l_file" ]; then # Remove excess permissions from WW files
                            printf '%s\n' "" " - File: \"$l_file\" is mode: \"$l_mode\"" \
                            " - removing write permission on \"$l_file\" from \"other\""
                            chmod o-w "$l_file"
                         fi
                         if [ -d "$l_file" ]; then # Add sticky bit
                            if [ ! $(( $l_mode & $l_smask )) -gt 0 ]; then
                               printf '%s\n' "" " - Directory: \"$l_file\" is mode:
                \"$l_mode\"" \
                               "    and doesn't have the sticky bit set" \
                               " - Adding the sticky bit"
                               chmod a+t "$l_file"
                            fi
                         fi
                      done < <(find "$l_mount" -mount -xdev \( "${a_path[@]}" \) \
                      -prune -o \( -type f -o -type d \) -perm -0002 -print0 2> /dev/null)
                   done < <(findmnt -Dkerno fstype,target | \
                   awk '($1 !~ /^\s*('"$l_exclude"')/ && $2 !~
                /^('"$l_exclude2"')(\/|$)/){print $2}')
                }

                References:

                     1. NIST SP 800-53 Rev. 5: AC-3. MP-2
                     2. STIG ID: UBTU-20-010411 | Rule ID: SV-238332r958524 | CAT ||

                     3. STIG ID: UBTU-22-232145 | Rule ID: SV-260513r958524 | CAT ||

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.

                7.1.12 Ensure no files or directories without an owner and a group
                exist (Automated)
                Profile Applicability:

                • Level 1 - Server
                • Level 1 - Workstation

                Description:
                Administrators may delete users or groups from the system and neglect to remove all
                files and/or directories owned by those users or groups.
                Rationale:
                A new user or group who is assigned a deleted user's user ID or group ID may then end
                up "owning" a deleted user or group's files, and thus have more access on the system
                than was intended.
                Audit:
                Run the following script to verify no unowned or ungrouped files or directories exist:

                #!/usr/bin/env bash

                {
                   a_output=() a_output2=() a_nouser=() a_nogroup=() # Initialize arrays
                   a_path=(-path "*/containers/storage/*" -o -path "*/containerd/*" -o -path
                "*/kubelet/*" -o -path "/sys/*" -o -path "/snap/*" -o -path "/boot/efi/*")
                   while IFS= read -r l_mount; do
                      while IFS= read -r -d $'\0' l_file; do
                         if [ -e "$l_file" ]; then
                            while IFS=: read -r l_user l_group; do
                               [ "$l_user" = "UNKNOWN" ] && a_nouser+=("$l_file")
                               [ "$l_group" = "UNKNOWN" ] && a_nogroup+=("$l_file")
                            done < <(stat -Lc '%U:%G' "$l_file")
                         fi
                      done < <(find "$l_mount" -mount -xdev \( "${a_path[@]}" \) -prune -o \(
                -type f -o -type d \) \( -nouser -o -nogroup \) -print0 2> /dev/null)
                   done < <(findmnt -Dkerno fstype,target | awk '($1 !~
                /^\s*(nfs|proc|cifs|smb|vfat|iso9660|efivarfs|selinuxfs|ncpfs)/ && $2 !~
                /^(\/run|\/tmp|\/var\/tmp)(\/|$)/){print $2}')
                   if [ "${#a_nouser[@]}" -le 0 ]; then
                      a_output+=(" - No files or directories without an owner exist on the
                local filesystem.")
                   else
                      a_output2+=(" - There are \"${#a_nouser[@]}\" unowned files or
                directories on the system." \
                      "   - The following is a list of unowned files and/or directories:" \
                      "${a_nouser[@]}" "   - end of list")
                   fi
                   if [ "${#a_nogroup[@]}" -le 0 ]; then
                      a_output+=(" - No files or directories without a group owner exist on
                the local filesystem.")
                   else
                      a_output2+=(" - There are \"${#a_nogroup[@]}\" ungrouped files or
                directories on the system." \
                      "   - The following is a list of ungrouped files and/or directories:" \
                      "${a_nogroup[@]}" "   - end of list")
                   fi
                   if [ "${#a_output2[@]}" -le 0 ]; then
                      printf '%s\n' "" "- Audit Result:" " ** PASS **" "${a_output[@]}"
                   else
                      printf '%s\n' "" "- Audit Result:" " ** FAIL **" " * Reasons for
                audit failure *" "${a_output2[@]}" ""
                      [ "${#a_output[@]}" -gt 0 ] && printf '%s\n' "- Correctly set:"
                "${a_output[@]}"
                   fi
                }
                Note: On systems with a large number of files and/or directories, this audit may be a
                long running process

                Remediation:
                Remove or set ownership and group ownership of these files and/or directories to an
                active user on the system as appropriate.
                References:

                     1. NIST SP 800-53 Rev. 5: AC-3. MP-2

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '7.1.12': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '7.1.13': !unsafe |-
    Ensure that no rogue SUID or SGID programs have been introduced into the system.
                Review the files returned by the action in the Audit section and confirm the integrity of
                these binaries.
                References:

                     1. NIST SP 800-53 Rev. 5: CM-1, CM-2, CM-6, CM-7, IA-5, AC-3, MP-2

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.

                7.2 Local User and Group Settings

                This section provides guidance on securing aspects of the local users and groups.
                Note: The recommendations in this section check local users and groups. Any users or
                groups from other sources such as LDAP will not be audited. In a domain environment
                similar checks should be performed against domain users and groups.
  '7.2.1': !unsafe |-
    Run the following command to set accounts to use shadowed passwords and migrate
                passwords in /etc/passwd to /etc/shadow:
                # pwconv
                Investigate to determine if the account is logged in and what it is being used for, to
                determine if it needs to be forced off.

                References:

                     1. NIST SP 800-53 Rev. 5: IA-5
                     2. PWCONV(8)

                Additional Information:
                The pwconv command creates shadow from passwd and an optionally existing shadow.

                     •        The pwunconv command creates passwd from passwd and shadow and then
                              removes shadow.
                     •        The grpconv command creates gshadow from group and an optionally existing
                              gshadow.
                     •        The grpunconv command creates group from group and gshadow and then
                              removes gshadow.

                These four programs all operate on the normal and shadow password and group files:
                /etc/passwd, /etc/group, /etc/shadow, and /etc/gshadow.
                Each program acquires the necessary locks before conversion. pwconv and grpconv
                are similar. First, entries in the shadowed file which don't exist in the main file are
                removed. Then, shadowed entries which don't have x' as the password in the
                main file are updated. Any missing shadowed entries are added.
                Finally, passwords in the main file are replaced with x'. These programs
                can be used for initial conversion as well to update the shadowed file if the main file is
                edited by hand.
                pwconv will use the values of PASS_MIN_DAYS, PASS_MAX_DAYS, and PASS_WARN_AGE
                from /etc/login.defs when adding new entries to /etc/shadow.
                pwunconv and grpunconv are similar. Passwords in the main file are updated from the
                shadowed file. Entries which exist in the main file but not in the shadowed file are left
                alone. Finally, the shadowed file is removed. Some password aging information is lost
                by pwunconv. It will convert what it can.

                CIS Controls:

                  Controls
                                                                       Control                                           IG 1 IG 2 IG 3
                  Version
                                  3.11 Encrypt Sensitive Data at Rest
                                     Encrypt sensitive data at rest on servers, applications, and databases containing
                                  sensitive data. Storage-layer encryption, also known as server-side encryption,
                         v8       meets the minimum requirement of this Safeguard. Additional encryption methods               ●    ●
                                  may include application-layer encryption, also known as client-side encryption,
                                  where access to the data storage device(s) does not permit access to the plain-text
                                  data.

                         v7       16.4 Encrypt or Hash all Authentication Credentials                                          ●    ●
                                     Encrypt or hash with a salt all authentication credentials when stored.
  '7.2.2': !unsafe |-
    If any accounts in the /etc/shadow file do not have a password, run the following
                command to lock the account until it can be determined why it does not have a
                password:
                # passwd -l <username>
                Also, check to see if the account is logged in and investigate what it is being used for to
                determine if it needs to be forced off.
                References:

                     1. NIST SP 800-53 Rev. 5: IA-5
                     2. NIST SP 800-53 Revision 5 :: CM-6 b
                     3. NIST SP 800-53A :: CM-6.1 (iv)

                Additional Information:
                Red Hat Enterprise Linux 8 Security Technical Implementation Guide
                Version 1, Release: 14 Benchmark Date: 24 APR 2024

                Vul ID: V-251706
                Rule ID: SV-251706r809342
                STIG ID: RHEL-08-010121
                Severity: CAT II

                CIS Controls:

                   Controls
                                                                 Control                                            IG 1 IG 2 IG 3
                   Version
                              5.2 Use Unique Passwords
                       v8         Use unique passwords for all enterprise assets. Best practice implementation
                              includes, at a minimum, an 8-character password for accounts using MFA and a
                                                                                                                     ●    ●    ●
                              14-character password for accounts not using MFA.
                              4.4 Use Unique Passwords
                       v7        Where multi-factor authentication is not supported (such as local administrator,
                              root, or service accounts), accounts will use passwords that are unique to that
                                                                                                                          ●    ●
                              system.
  '7.2.3': !unsafe |-
    Analyze the output of the Audit step above and perform the appropriate action to correct
                any discrepancies found.
                References:

                     1. NIST SP 800-53 Rev. 5: CM-1, CM-2, CM-6, CM-7, IA-5

                CIS Controls:

                   Controls
                                                                 Control                                         IG 1 IG 2 IG 3
                   Version
                              3.3 Configure Data Access Control Lists
                       v8         Configure data access control lists based on a user’s need to know. Apply
                              data access control lists, also known as access permissions, to local and remote
                                                                                                                  ●    ●    ●
                              file systems, databases, and applications.
                              14.6 Train Workforce Members on Recognizing and
                       v8     Reporting Security Incidents                                                        ●    ●    ●
                                 Train workforce members to be able to recognize a potential incident and be
                              able to report such an incident.
  '7.2.4': !unsafe |-
    Run the following command to remove all users from the shadow group
                # sed -ri 's/(^shadow:[^:]*:[^:]*:)([^:]+$)/\1/' /etc/group
                Change the primary group of any users with shadow as their primary group.
                # usermod -g <primary group> <user>

                References:

                     1. NIST SP 800-53 Rev. 5: IA-5

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '7.2.5': !unsafe |-
    Based on the results of the audit script, establish unique UIDs and review all files owned
                by the shared UIDs to determine which UID they are supposed to belong to.
                References:

                     1. NIST SP 800-53 Rev. 5: CM-1, CM-2, CM-6, CM-7, IA-5
                     2. NIST SP 800-53A :: IA-2.1

                Additional Information:
                Red Hat Enterprise Linux 8 Security Technical Implementation Guide
                Version 1, Release: 14 Benchmark Date: 24 APR 2024

                Vul ID: V-230371
                Rule ID: SV-230371r627750
                STIG ID: RHEL-08-020240
                Severity: CAT II

                CIS Controls:

                     Controls Version                    Control       IG 1 IG 2 IG 3
                              v8          0.0 Explicitly Not Mapped
                                            Explicitly Not Mapped

                              v7          0.0 Explicitly Not Mapped
                                            Explicitly Not Mapped
  '7.2.6': !unsafe |-
    Based on the results of the audit script, establish unique GIDs and review all files
                owned by the shared GID to determine which group they are supposed to belong to.
                References:

                     1. NIST SP 800-53 Rev. 5: CM-1, CM-2, CM-6, CM-7, IA-5

                Additional Information:
                You can also use the grpck command to check for other inconsistencies in the
                /etc/group file.

                CIS Controls:

                     Controls Version                  Control      IG 1 IG 2 IG 3
                              v8        0.0 Explicitly Not Mapped
                                          Explicitly Not Mapped

                              v7        0.0 Explicitly Not Mapped
                                          Explicitly Not Mapped
  '7.2.7': !unsafe |-
    Based on the results of the audit script, establish unique user names for the users. File
                ownerships will automatically reflect the change as long as the users have unique UIDs.
                References:

                     1. NIST SP 800-53 Rev. 5: CM-1, CM-2, CM-6, CM-7, IA-5

                CIS Controls:

                     Controls Version                     Control                IG 1 IG 2 IG 3
                              v8           0.0 Explicitly Not Mapped
                                             Explicitly Not Mapped

                              v7           0.0 Explicitly Not Mapped
                                             Explicitly Not Mapped
  '7.2.8': !unsafe |-
    Based on the results of the audit script, establish unique names for the user groups. File
                group ownerships will automatically reflect the change as long as the groups have
                unique GIDs.
                References:

                     1. NIST SP 800-53 Rev. 5: CM-1, CM-2, CM-6, CM-7, IA-5

                CIS Controls:

                     Controls Version                     Control                IG 1 IG 2 IG 3
                              v8           0.0 Explicitly Not Mapped
                                             Explicitly Not Mapped

                              v7           0.0 Explicitly Not Mapped
                                             Explicitly Not Mapped

                7.2.9 Ensure local interactive user home directories are
                configured (Automated)
                Profile Applicability:

                • Level 1 - Server
                • Level 1 - Workstation

                Description:
                The user home directory is space defined for the particular user to set local environment
                variables and to store personal files. While the system administrator can establish
                secure permissions for users' home directories, the users can easily override these.
                Users can be defined in /etc/passwd without a home directory or with a home directory
                that does not actually exist.
                Rationale:
                Since the user is accountable for files stored in the user home directory, the user must
                be the owner of the directory. Group or world-writable user home directories may enable
                malicious users to steal or modify other users' data or to gain another user's system
                privileges. If the user's home directory does not exist or is unassigned, the user will be
                placed in "/" and will not be able to write any files or have local environment variables
                set.
                Audit:
                Run the following script to Ensure:

                     •   local interactive user home directories exist
                     •   Ensure local interactive users own their home directories
                     •   Ensure local interactive user home directories are mode 750 or more restrictive

                #!/usr/bin/env bash

                {
                   a_output=() a_output2=() a_exists2=() a_mode2=() a_owner2=()
                   l_valid_shells="^($( awk -F\/ '$NF != "nologin" {print}' /etc/shells | sed
                -rn '/^\//{s,/,\\\\/,g;p}' | paste -s -d '|' - ))$"
                   l_mask='0027'; l_max="$( printf '%o' $(( 0777 & ~$l_mask)) )"
                   l_users="$(awk -v pat="$l_valid_shells" -F: '$(NF) ~ pat { print $1 " "
                $(NF-1) }' /etc/passwd | wc -l)"
                   [ "$l_users" -gt 10000 ] && printf '%s\n' "" " ** INFO **" \
                   " $l_users Local interactive users found on the system" " This may be a
                long running check" " **********"
                   while IFS=" " read -r l_user l_home; do
                      if [ -d "$l_home" ]; then
                         while IFS=: read -r l_own l_mode; do
                            [ "$l_user" != "$l_own" ] && a_owner2+=(" - User: \"$l_user\"
                Home \"$l_home\" is owned by: \"$l_own\"")
                            [ $(( $l_mode & $l_mask )) -gt 0 ] && a_mode2+=(" - User:
                \"$l_user\" Home \"$l_home\" is mode: \"$l_mode\"" \
                            "    should be mode: \"$l_max\" or more restrictive")
                         done <<< "$(stat -Lc '%U:%#a' "$l_home")"
                      else
                         a_exists2+=(" - User: \"$l_user\" Home Directory: \"$l_home\"
                Doesn't exist")
                      fi
                   done <<< "$(awk -v pat="$l_valid_shells" -F: '$(NF) ~ pat { print $1 " "
                $(NF-1) }' /etc/passwd)"
                   [ "${#a_exists2[@]}" -gt 0 ] && a_output2+=("${a_exists2[@]}") || \
                   a_output+=(" - All interactive users home directories exist")
                   [ "${#a_mode2[@]}" -gt 0 ] && a_output2+=("${a_mode2[@]}") || \
                   a_output+=(" - All interactive users home directories are mode \"$l_max\"
                or more restrictive")
                   [ "${#a_owner2[@]}" -gt 0 ] && a_output2+=("${a_owner2[@]}") || \
                   a_output+=(" - All interactive users own their home directory")
                   if [ "${#a_output2[@]}" -le 0 ]; then
                      printf '%s\n' "" "- Audit Result:" " ** PASS **" "${a_output[@]}"
                   else
                      printf '%s\n' "" "- Audit Result:" " ** FAIL **" " - Reason(s) for
                audit failure:" "${a_output2[@]}"
                      [ "${#a_output[@]}" -gt 0 ] && printf '%s\n' "- Correctly set:"
                "${a_output[@]}"
                   fi
                }

                Remediation:
                If a local interactive users' home directory is undefined and/or doesn't exist, follow local
                site policy and perform one of the following:
                    • Lock the user account
                    • Remove the user from the system
                    • create a directory for the user. If undefined, edit /etc/passwd and add the
                        absolute path to the directory to the last field of the user.
                Run the following script to:
                    • Remove excessive permissions from local interactive users home directories
                    • Update the home directory's owner
                #!/usr/bin/env bash

                {
                   a_output=() a_output2=() a_exists2=() a_mode2=() a_owner2=()
                   l_valid_shells="^($( awk -F\/ '$NF != "nologin" {print}' /etc/shells | sed
                -rn '/^\//{s,/,\\\\/,g;p}' | paste -s -d '|' - ))$"
                   l_mask='0027'; l_max="$( printf '%o' $(( 0777 & ~$l_mask)) )"
                   l_users="$(awk -v pat="$l_valid_shells" -F: '$(NF) ~ pat { print $1 " "
                $(NF-1) }' /etc/passwd | wc -l)"
                   [ "$l_users" -gt 10000 ] && printf '%s\n' "" " ** INFO **" \
                   " $l_users Local interactive users found on the system" " This may be a
                long running process" " **********"
                   while IFS=" " read -r l_user l_home; do
                      if [ -d "$l_home" ]; then
                          while IFS=: read -r l_own l_mode; do
                             if [ "$l_user" != "$l_own" ]; then
                                a_owner2+=(" - User: \"$l_user\" Home \"$l_home\" is owned
                by: \"$l_own\"" \
                                "    changing owner to: \"$l_user\"") && chown "$l_user"
                "$l_home"
                             fi
                             if [ $(( $l_mode & $l_mask )) -gt 0 ]; then
                                a_mode2+=(" - User: \"$l_user\" Home \"$l_home\" is mode:
                \"$l_mode\"" \
                                "    changing to mode: \"$l_max\" or more restrictive")
                                chmod g-w,o-rwx "$l_home"
                             fi
                          done <<< "$(stat -Lc '%U:%#a' "$l_home")"
                      else
                          a_exists2+=(" - User: \"$l_user\" Home Directory: \"$l_home\"
                Doesn't exist")
                      fi
                   done <<< "$(awk -v pat="$l_valid_shells" -F: '$(NF) ~ pat { print $1 " "
                $(NF-1) }' /etc/passwd)"
                   [ "${#a_exists2[@]}" -gt 0 ] && a_output2+=("${a_exists2[@]}")
                   [ "${#a_mode2[@]}" -gt 0 ] && a_output2+=("${a_mode2[@]}")
                   [ "${#a_owner2[@]}" -gt 0 ] && a_output2+=("${a_owner2[@]}")
                   if [ "${#a_output2[@]}" -gt 0 ]; then
                      printf '%s\n' "" "${a_output2[@]}"
                   else
                      printf '%s\n' "" "- No changes required"
                   fi
                }

                References:

                     1. NIST SP 800-53 Revision 5 :: CM-6 b
                     2. NIST SP 800-53A :: CM-6.1 (iv)

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.
  '7.2.9': !unsafe |-
    Review the control evidence and expected state, then apply the organization-approved configuration change. Re-run the audit to verify compliance.
  '7.2.10': !unsafe |-
    Making global modifications to users' files without alerting the user community can result
                in unexpected outages and unhappy users. Therefore, it is recommended that a
                monitoring policy be established to report user dot file permissions and determine the
                action to be taken in accordance with site policy.
                The following script will:

                     •   remove excessive permissions on dot files within interactive users' home
                         directories
                     •   change ownership of dot files within interactive users' home directories to the
                         user
                     •   change group ownership of dot files within interactive users' home directories to
                         the user's primary group
                     •   list .forward and .rhost files to be investigated and manually deleted

                #!/usr/bin/env bash

                {
                   a_output2=(); a_output3=()
                   l_maxsize="1000" # Maximum number of local interactive users before
                warning (Default 1,000)
                   l_valid_shells="^($( awk -F\/ '$NF != "nologin" {print}' /etc/shells | sed
                -rn '/^\//{s,/,\\\\/,g;p}' | paste -s -d '|' - ))$"
                   a_user_and_home=() # Create array with local users and their home
                directories
                   while read -r l_local_user l_local_user_home; do # Populate array with
                users and user home location
                      [[ -n "$l_local_user" && -n "$l_local_user_home" ]] &&
                a_user_and_home+=("$l_local_user:$l_local_user_home")
                   done <<< "$(awk -v pat="$l_valid_shells" -F: '$(NF) ~ pat { print $1 " "
                $(NF-1) }' /etc/passwd)"
                   l_asize="${#a_user_and_home[@]}" # Here if we want to look at number of
                users before proceeding
                   [ "${#a_user_and_home[@]}" -gt "$l_maxsize" ] && printf '%s\n' "" " **
                INFO **" \
                   " - \"$l_asize\" Local interactive users found on the system" \
                   " - This may be a long running check" ""
                   file_access_fix()
                   {
                      a_access_out=()
                      l_max="$( printf '%o' $(( 0777 & ~$l_mask)) )"
                      if [ $(( $l_mode & $l_mask )) -gt 0 ]; then
                         printf '%s\n' "" " - File: \"$l_hdfile\" is mode: \"$l_mode\" and
                should be mode: \"$l_max\" or more restrictive" \
                         "      Updating file: \"$l_hdfile\" to be mode: \"$l_max\" or more
                restrictive"
                         chmod "$l_change" "$l_hdfile"
                      fi
                      if [[ ! "$l_owner" =~ ($l_user) ]]; then
                         printf '%s\n' "" " - File: \"$l_hdfile\" owned by: \"$l_owner\" and
                should be owned by \"${l_user//|/ or }\"" \
                         "      Updating file: \"$l_hdfile\" to be owned by \"${l_user//|/ or
                }\""
                         chown "$l_user" "$l_hdfile"
                      fi
                      if [[ ! "$l_gowner" =~ ($l_group) ]]; then
                         printf '%s\n' "" " - File: \"$l_hdfile\" group owned by:
                \"$l_gowner\" and should be group owned by \"${l_group//|/ or }\"" \
                         "      Updating file: \"$l_hdfile\" to be group owned by
                \"${l_group//|/ or }\""
                         chgrp "$l_group" "$l_hdfile"
                      fi
                   }
                   while IFS=: read -r l_user l_home; do
                      a_dot_file=(); a_netrc=(); a_netrc_warn=(); a_bhout=(); a_hdirout=()
                      if [ -d "$l_home" ]; then
                         l_group="$(id -gn "$l_user" | xargs)";l_group="${l_group// /|}"
                         while IFS= read -r -d $'\0' l_hdfile; do
                             while read -r l_mode l_owner l_gowner; do
                                case "$(basename "$l_hdfile")" in
                                   .forward | .rhost )

                                      a_dot_file+=(" - File: \"$l_hdfile\" exists" "
                Please review and manually delete this file") ;;
                                  .netrc )
                                      l_mask='0177'; l_change="u-x,go-rwx"; file_access_fix
                                      a_netrc_warn+=("   - File: \"$l_hdfile\" exists") ;;
                                  .bash_history )
                                      l_mask='0177'; l_change="u-x,go-rwx"; file_access_fix ;;
                                  * )
                                      l_mask='0133'; l_change="u-x,go-wx"; file_access_fix ;;
                               esac
                            done < <(stat -Lc '%#a %U %G' "$l_hdfile")
                         done < <(find "$l_home" -xdev -type f -name '.*' -print0)
                      fi
                      [ "${#a_dot_file[@]}" -gt 0 ] && a_output2+=(" - User: \"$l_user\" Home
                Directory: \"$l_home\"" "${a_dot_file[@]}")
                      [ "${#a_netrc_warn[@]}" -gt 0 ] && a_output3+=(" - User: \"$l_user\"
                Home Directory: \"$l_home\"" "${a_netrc_warn[@]}")
                   done <<< "$(printf '%s\n' "${a_user_and_home[@]}")"
                   [ "${#a_output3[@]}" -gt 0 ] && printf '%s\n' "" " ** WARNING **"
                "${a_output3[@]}" ""
                   [ "${#a_output2[@]}" -gt 0 ] && printf '%s\n' "" "${a_output2[@]}"
                }

                References:

                     1. NIST SP 800-53 Rev. 5: CM-1, CM-2, CM-6, CM-7, IA-5

                CIS Controls:

                  Controls
                                                                   Control                                          IG 1 IG 2 IG 3
                  Version
                              3.3 Configure Data Access Control Lists
                       v8        Configure data access control lists based on a user’s need to know. Apply data
                              access control lists, also known as access permissions, to local and remote file
                                                                                                                     ●    ●    ●
                              systems, databases, and applications.
                              14.6 Protect Information through Access Control Lists
                                  Protect all information stored on systems with file system, network share,
                       v7     claims, application, or database specific access control lists. These controls will
                              enforce the principle that only authorized individuals should have access to the
                                                                                                                     ●    ●    ●
                              information based on their need to access the information as a part of their
                              responsibilities.

                Appendix: Summary Table
                                CIS Benchmark Recommendation                                Set
                                                                                         Correctly
                                                                                         Yes No
                  1           Initial Setup
                  1.1         Filesystem
                  1.1.1       Configure Filesystem Kernel Modules
                  1.1.1.1     Ensure cramfs kernel module is not available                    
                              (Automated)
                  1.1.1.2     Ensure freevxfs kernel module is not available                  
                              (Automated)
                  1.1.1.3     Ensure hfs kernel module is not available (Automated)           
                  1.1.1.4     Ensure hfsplus kernel module is not available                   
                              (Automated)
                  1.1.1.5     Ensure jffs2 kernel module is not available (Automated)         
                  1.1.1.6     Ensure overlay kernel module is not available                   
                              (Automated)
                  1.1.1.7     Ensure squashfs kernel module is not available                  
                              (Automated)
                  1.1.1.8     Ensure udf kernel module is not available (Automated)           
                  1.1.1.9     Ensure firewire-core kernel module is not available             
                              (Automated)
                  1.1.1.10    Ensure usb-storage kernel module is not available               
                              (Automated)
                  1.1.1.11    Ensure unused filesystems kernel modules are not                
                              available (Manual)
                  1.1.2       Configure Filesystem Partitions
                  1.1.2.1     Configure /tmp
                  1.1.2.1.1   Ensure /tmp is tmpfs or a separate partition (Automated)        
                  1.1.2.1.2   Ensure nodev option set on /tmp partition (Automated)           
                  1.1.2.1.3   Ensure nosuid option set on /tmp partition (Automated)          
                  1.1.2.1.4   Ensure noexec option set on /tmp partition (Automated)          
                  1.1.2.2     Configure /dev/shm
                  1.1.2.2.1   Ensure /dev/shm is tmpfs or a separate partition                
                              (Automated)
                  1.1.2.2.2   Ensure nodev option set on /dev/shm partition                   
                              (Automated)
                  1.1.2.2.3   Ensure nosuid option set on /dev/shm partition                  
                              (Automated)
                  1.1.2.2.4   Ensure noexec option set on /dev/shm partition                  
                              (Automated)
                  1.1.2.3     Configure /home
                  1.1.2.3.1   Ensure separate partition exists for /home (Automated)          

                                CIS Benchmark Recommendation                                 Set
                                                                                          Correctly
                                                                                          Yes No
                  1.1.2.3.2   Ensure nodev option set on /home partition (Automated)          
                  1.1.2.3.3   Ensure nosuid option set on /home partition (Automated)         
                  1.1.2.4     Configure /var
                  1.1.2.4.1   Ensure separate partition exists for /var (Automated)            
                  1.1.2.4.2   Ensure nodev option set on /var partition (Automated)            
                  1.1.2.4.3   Ensure nosuid option set on /var partition (Automated)           
                  1.1.2.5     Configure /var/tmp
                  1.1.2.5.1   Ensure separate partition exists for /var/tmp (Automated)        
                  1.1.2.5.2   Ensure nodev option set on /var/tmp partition                    
                              (Automated)
                  1.1.2.5.3   Ensure nosuid option set on /var/tmp partition                   
                              (Automated)
                  1.1.2.5.4   Ensure noexec option set on /var/tmp partition                   
                              (Automated)
                  1.1.2.6     Configure /var/log
                  1.1.2.6.1   Ensure separate partition exists for /var/log (Automated)        
                  1.1.2.6.2   Ensure nodev option set on /var/log partition                    
                              (Automated)
                  1.1.2.6.3   Ensure nosuid option set on /var/log partition                   
                              (Automated)
                  1.1.2.6.4   Ensure noexec option set on /var/log partition                   
                              (Automated)
                  1.1.2.7     Configure /var/log/audit
                  1.1.2.7.1   Ensure separate partition exists for /var/log/audit              
                              (Automated)
                  1.1.2.7.2   Ensure nodev option set on /var/log/audit partition              
                              (Automated)
                  1.1.2.7.3   Ensure nosuid option set on /var/log/audit partition             
                              (Automated)
                  1.1.2.7.4   Ensure noexec option set on /var/log/audit partition             
                              (Automated)
                  1.2         Package Management
                  1.2.1       Configure Package Repositories
                  1.2.1.1     Ensure the source.list and .source files use the Signed-         
                              By option (Manual)
                  1.2.1.2     Ensure weak dependencies are configured (Automated)              
                  1.2.1.3     Ensure access to gpg key files are configured                    
                              (Automated)
                  1.2.1.4     Ensure access to /etc/apt/trusted.gpg.d directory is             
                              configured (Automated)
                  1.2.1.5     Ensure access to /etc/apt/auth.conf.d directory is               
                              configured (Automated)

                                CIS Benchmark Recommendation                               Set
                                                                                        Correctly
                                                                                        Yes No
                  1.2.1.6     Ensure access to files in the /etc/apt/auth.conf.d/           
                              directory is configured (Automated)
                  1.2.1.7     Ensure access to /usr/share/keyrings directory is              
                              configured (Automated)
                  1.2.1.8     Ensure access to /etc/apt/sources.list.d directory is          
                              configured (Automated)
                  1.2.1.9     Ensure access to files in /etc/apt/sources.list.d are          
                              configured (Automated)
                  1.2.2       Configure Package Updates
                  1.2.2.1     Ensure updates, patches, and additional security               
                              software are installed (Manual)
                  1.3         Mandatory Access Control
                  1.3.1       Configure AppArmor
                  1.3.1.1     Ensure apparmor packages are installed (Automated)             
                  1.3.1.2     Ensure AppArmor is enabled (Automated)                         
                  1.3.1.3     Ensure all AppArmor Profiles are enforcing (Automated)         
                  1.3.1.4     Ensure apparmor_restrict_unprivileged_unconfined is            
                              enabled (Automated)
                  1.4         Configure Bootloader
                  1.4.1       Ensure bootloader password is set (Automated)                  
                  1.4.2       Ensure access to bootloader config is configured               
                              (Automated)
                  1.5         Configure Additional Process Hardening
                  1.5.1       Ensure fs.protected_hardlinks is configured (Automated)        
                  1.5.2       Ensure fs.protected_symlinks is configured (Automated)         
                  1.5.3       Ensure kernel.yama.ptrace_scope is configured                  
                              (Automated)
                  1.5.4       Ensure fs.suid_dumpable is configured (Automated)              
                  1.5.5       Ensure kernel.dmesg_restrict is configured (Automated)         
                  1.5.6       Ensure prelink is not installed (Automated)                    
                  1.5.7       Ensure Automatic Error Reporting is configured                 
                              (Automated)
                  1.5.8       Ensure kernel.kptr_restrict is configured (Automated)          
                  1.5.9       Ensure kernel.randomize_va_space is configured                 
                              (Automated)
                  1.5.10      Ensure kernel.yama.ptrace_scope is configured                  
                              (Automated)
                  1.5.11      Ensure core file size is configured (Automated)                
                  1.5.12      Ensure systemd-coredump ProcessSizeMax is                      
                              configured (Automated)
                  1.5.13      Ensure systemd-coredump Storage is configured                  
                              (Automated)

                                CIS Benchmark Recommendation                               Set
                                                                                        Correctly
                                                                                        Yes No
                  1.6         Configure Command Line Warning Banners
                  1.6.1       Ensure /etc/motd is configured (Automated)                     
                  1.6.2       Ensure /etc/issue is configured (Automated)                    
                  1.6.3       Ensure /etc/issue.net is configured (Automated)                
                  1.6.4       Ensure access to /etc/motd is configured (Automated)           
                  1.6.5       Ensure access to /etc/issue is configured (Automated)          
                  1.6.6       Ensure access to /etc/issue.net is configured                  
                              (Automated)
                  1.7         Configure GNOME Display Manager
                  1.7.1       Ensure GDM is removed (Automated)                              
                  1.7.2       Ensure GDM login banner is configured (Automated)              
                  1.7.3       Ensure GDM disable-user-list option is enabled                 
                              (Automated)
                  1.7.4       Ensure GDM screen locks when the user is idle                  
                              (Automated)
                  1.7.5       Ensure GDM screen locks cannot be overridden                   
                              (Automated)
                  1.7.6       Ensure GDM automatic mounting of removable media is            
                              disabled (Automated)
                  1.7.7       Ensure GDM disabling automatic mounting of removable           
                              media is not overridden (Automated)
                  1.7.8       Ensure GDM autorun-never is enabled (Automated)                
                  1.7.9       Ensure GDM autorun-never is not overridden                     
                              (Automated)
                  1.7.10      Ensure XDMCP is not enabled (Automated)                        
                  1.7.11      Ensure Xwayland is configured (Automated)                      
                  2           Services
                  2.1         Configure Server Services
                  2.1.1       Ensure autofs services are not in use (Automated)              
                  2.1.2       Ensure avahi daemon services are not in use                    
                              (Automated)
                  2.1.3       Ensure dhcp server services are not in use (Automated)         
                  2.1.4       Ensure dns server services are not in use (Automated)          
                  2.1.5       Ensure dnsmasq services are not in use (Automated)             
                  2.1.6       Ensure ftp server services are not in use (Automated)          
                  2.1.7       Ensure ldap server services are not in use (Automated)         
                  2.1.8       Ensure message access server services are not in use           
                              (Automated)
                  2.1.9       Ensure network file system services are not in use             
                              (Automated)
                  2.1.10      Ensure nis server services are not in use (Automated)          
                  2.1.11      Ensure print server services are not in use (Automated)        

                                CIS Benchmark Recommendation                                 Set
                                                                                          Correctly
                                                                                          Yes No
                  2.1.12      Ensure rpcbind services are not in use (Automated)              
                  2.1.13      Ensure rsync services are not in use (Automated)                
                  2.1.14      Ensure samba file server services are not in use                
                              (Automated)
                  2.1.15      Ensure snmp services are not in use (Automated)                  
                  2.1.16      Ensure telnet-server services are not in use (Automated)         
                  2.1.17      Ensure tftp server services are not in use (Automated)           
                  2.1.18      Ensure web proxy server services are not in use                  
                              (Automated)
                  2.1.19      Ensure web server services are not in use (Automated)            
                  2.1.20      Ensure xinetd services are not in use (Automated)                
                  2.1.21      Ensure X window server services are not in use                   
                              (Automated)
                  2.1.22      Ensure mail transfer agents are configured for local-only        
                              mode (Automated)
                  2.1.23      Ensure only approved services are listening on a                 
                              network interface (Manual)
                  2.2         Configure Client Services
                  2.2.1       Ensure nis client is not installed (Automated)                   
                  2.2.2       Ensure rsh client is not installed (Automated)                   
                  2.2.3       Ensure talk client is not installed (Automated)                  
                  2.2.4       Ensure telnet client is not installed (Automated)                
                  2.2.5       Ensure ldap client is not installed (Automated)                  
                  2.2.6       Ensure ftp client is not installed (Automated)                   
                  2.3         Configure Time Synchronization
                  2.3.1       Ensure time synchronization is in use
                  2.3.1.1     Ensure a single time synchronization daemon is in use            
                              (Automated)
                  2.3.2       Configure systemd-timesyncd
                  2.3.2.1     Ensure systemd-timesyncd configured with authorized              
                              timeserver (Automated)
                  2.3.2.2     Ensure systemd-timesyncd is enabled and running                  
                              (Automated)
                  2.3.3       Configure chrony
                  2.3.3.1     Ensure chrony is configured with authorized timeserver           
                              (Automated)
                  2.3.3.2     Ensure chrony is running as user _chrony (Automated)             
                  2.3.3.3     Ensure chrony is enabled and running (Automated)                 
                  2.4         Job Schedulers
                  2.4.1       Configure cron
                  2.4.1.1     Ensure cron daemon is enabled and active (Automated)             
                  2.4.1.2     Ensure access to /etc/crontab is configured (Automated)          

                                CIS Benchmark Recommendation                                 Set
                                                                                          Correctly
                                                                                          Yes No
                  2.4.1.3     Ensure access to /etc/cron.hourly is configured                 
                              (Automated)
                  2.4.1.4     Ensure access to /etc/cron.daily is configured                    
                              (Automated)
                  2.4.1.5     Ensure access to /etc/cron.weekly is configured                   
                              (Automated)
                  2.4.1.6     Ensure access to /etc/cron.monthly is configured                  
                              (Automated)
                  2.4.1.7     Ensure access to /etc/cron.yearly is configured                   
                              (Automated)
                  2.4.1.8     Ensure access to /etc/cron.d is configured (Automated)            
                  2.4.1.9     Ensure access to crontab is configured (Automated)                
                  2.4.2       Configure at
                  2.4.2.1     Ensure access to at is configured (Automated)                     
                  3           Network
                  3.1         Configure Network Devices
                  3.1.1       Ensure IPv6 status is identified (Manual)                         
                  3.1.2       Ensure wireless interfaces are not available (Automated)          
                  3.1.3       Ensure bluetooth services are not in use (Automated)              
                  3.2         Configure Network Kernel Modules
                  3.2.1       Ensure atm kernel module is not available (Automated)             
                  3.2.2       Ensure can kernel module is not available (Automated)             
                  3.2.3       Ensure dccp kernel module is not available (Automated)            
                  3.2.4       Ensure rds kernel module is not available (Automated)             
                  3.2.5       Ensure sctp kernel module is not available (Automated)            
                  3.2.6       Ensure tipc kernel module is not available (Automated)            
                  3.3         Configure Network Kernel Parameters
                  3.3.1       Configure IPv4 parameters
                  3.3.1.1     Ensure net.ipv4.ip_forward is configured (Automated)              
                  3.3.1.2     Ensure net.ipv4.conf.all.forwarding is configured                 
                              (Automated)
                  3.3.1.3     Ensure net.ipv4.conf.default.forwarding is configured             
                              (Automated)
                  3.3.1.4     Ensure net.ipv4.conf.all.send_redirects is configured             
                              (Automated)
                  3.3.1.5     Ensure net.ipv4.conf.default.send_redirects is configured         
                              (Automated)
                  3.3.1.6     Ensure net.ipv4.icmp_ignore_bogus_error_responses is              
                              configured (Automated)
                  3.3.1.7     Ensure net.ipv4.icmp_echo_ignore_broadcasts is                    
                              configured (Automated)

                                CIS Benchmark Recommendation                               Set
                                                                                        Correctly
                                                                                        Yes No
                  3.3.1.8     Ensure net.ipv4.conf.all.accept_redirects is configured       
                              (Automated)
                  3.3.1.9     Ensure net.ipv4.conf.default.accept_redirects is                
                              configured (Automated)
                  3.3.1.10    Ensure net.ipv4.conf.all.secure_redirects is configured         
                              (Automated)
                  3.3.1.11    Ensure net.ipv4.conf.default.secure_redirects is                
                              configured (Automated)
                  3.3.1.12    Ensure net.ipv4.conf.all.rp_filter is configured                
                              (Automated)
                  3.3.1.13    Ensure net.ipv4.conf.default.rp_filter is configured            
                              (Automated)
                  3.3.1.14    Ensure net.ipv4.conf.all.accept_source_route is                 
                              configured (Automated)
                  3.3.1.15    Ensure net.ipv4.conf.default.accept_source_route is             
                              configured (Automated)
                  3.3.1.16    Ensure net.ipv4.conf.all.log_martians is configured             
                              (Automated)
                  3.3.1.17    Ensure net.ipv4.conf.default.log_martians is configured         
                              (Automated)
                  3.3.1.18    Ensure net.ipv4.tcp_syncookies is configured                    
                              (Automated)
                  3.3.2       Configure IPv6 parameters
                  3.3.2.1     Ensure net.ipv6.conf.all.forwarding is configured               
                              (Automated)
                  3.3.2.2     Ensure net.ipv6.conf.default.forwarding is configured           
                              (Automated)
                  3.3.2.3     Ensure net.ipv6.conf.all.accept_redirects is configured         
                              (Automated)
                  3.3.2.4     Ensure net.ipv6.conf.default.accept_redirects is                
                              configured (Automated)
                  3.3.2.5     Ensure net.ipv6.conf.all.accept_source_route is                 
                              configured (Automated)
                  3.3.2.6     Ensure net.ipv6.conf.default.accept_source_route is             
                              configured (Automated)
                  3.3.2.7     Ensure net.ipv6.conf.all.accept_ra is configured                
                              (Automated)
                  3.3.2.8     Ensure net.ipv6.conf.default.accept_ra is configured            
                              (Automated)
                  4           Host Based Firewall
                  4.1         Configure Uncomplicated Firewall
                  4.1.1       Ensure ufw is installed (Automated)                             

                                CIS Benchmark Recommendation                                Set
                                                                                         Correctly
                                                                                         Yes No
                  4.1.2       Ensure ufw service is configured (Automated)                   
                  4.1.3       Ensure ufw incoming default is configured (Automated)          
                  4.1.4       Ensure ufw outgoing default is configured (Automated)          
                  4.1.5       Ensure ufw routed default is configured (Automated)            
                  5           Access Control
                  5.1         Configure SSH Server
                  5.1.1       Ensure access to /etc/ssh/sshd_config is configured              
                              (Automated)
                  5.1.2       Ensure access to SSH private host key files is                   
                              configured (Automated)
                  5.1.3       Ensure access to SSH public host key files is configured         
                              (Automated)
                  5.1.4       Ensure sshd access is configured (Automated)                     
                  5.1.5       Ensure sshd Banner is configured (Automated)                     
                  5.1.6       Ensure sshd Ciphers are configured (Automated)                   
                  5.1.7       Ensure sshd ClientAliveInterval and                              
                              ClientAliveCountMax are configured (Automated)
                  5.1.8       Ensure sshd DisableForwarding is enabled (Automated)             
                  5.1.9       Ensure sshd GSSAPIAuthentication is disabled                     
                              (Automated)
                  5.1.10      Ensure sshd HostbasedAuthentication is disabled                  
                              (Automated)
                  5.1.11      Ensure sshd IgnoreRhosts is enabled (Automated)                  
                  5.1.12      Ensure sshd KexAlgorithms is configured (Automated)              
                  5.1.13      Ensure sshd post-quantum cryptography key exchange               
                              algorithms are configured (Automated)
                  5.1.14      Ensure sshd LoginGraceTime is configured (Automated)             
                  5.1.15      Ensure sshd LogLevel is configured (Automated)                   
                  5.1.16      Ensure sshd MACs are configured (Automated)                      
                  5.1.17      Ensure sshd MaxAuthTries is configured (Automated)               
                  5.1.18      Ensure sshd MaxSessions is configured (Automated)                
                  5.1.19      Ensure sshd MaxStartups is configured (Automated)                
                  5.1.20      Ensure sshd PermitEmptyPasswords is disabled                     
                              (Automated)
                  5.1.21      Ensure sshd PermitRootLogin is disabled (Automated)              
                  5.1.22      Ensure sshd PermitUserEnvironment is disabled                    
                              (Automated)
                  5.1.23      Ensure sshd UsePAM is enabled (Automated)                        
                  5.2         Configure privilege escalation
                  5.2.1       Ensure sudo is installed (Automated)                             
                  5.2.2       Ensure sudo commands use pty (Automated)                         
                  5.2.3       Ensure sudo log file exists (Automated)                          

                                CIS Benchmark Recommendation                                Set
                                                                                         Correctly
                                                                                         Yes No
                  5.2.4       Ensure users must provide password for escalation              
                              (Automated)
                  5.2.5       Ensure re-authentication for privilege escalation is not         
                              disabled globally (Automated)
                  5.2.6       Ensure sudo timestamp_timeout is configured                      
                              (Automated)
                  5.2.7       Ensure access to the su command is restricted                    
                              (Automated)
                  5.3         Pluggable Authentication Modules
                  5.3.1       Configure PAM software packages
                  5.3.1.1     Ensure latest version of pam is installed (Automated)            
                  5.3.1.2     Ensure latest version of libpam-modules is installed             
                              (Automated)
                  5.3.1.3     Ensure latest version of libpam-pwquality is installed           
                              (Automated)
                  5.3.2       Configure pam-auth-update profiles
                  5.3.2.1     Ensure pam_unix module is enabled (Automated)                    
                  5.3.2.2     Ensure pam_faillock module is enabled (Automated)                
                  5.3.2.3     Ensure pam_pwquality module is enabled (Automated)               
                  5.3.2.4     Ensure pam_pwhistory module is enabled (Automated)               
                  5.3.3       Configure PAM Arguments
                  5.3.3.1     Configure pam_faillock module
                  5.3.3.1.1   Ensure password failed attempts lockout is configured            
                              (Automated)
                  5.3.3.1.2   Ensure password unlock time is configured (Automated)            
                  5.3.3.1.3   Ensure password failed attempts lockout includes root            
                              account (Automated)
                  5.3.3.2     Configure pam_pwquality module
                  5.3.3.2.1   Ensure password number of changed characters is                  
                              configured (Automated)
                  5.3.3.2.2   Ensure password length is configured (Automated)                 
                  5.3.3.2.3   Ensure password complexity is configured (Manual)                
                  5.3.3.2.4   Ensure password same consecutive characters is                   
                              configured (Automated)
                  5.3.3.2.5   Ensure password maximum sequential characters is                 
                              configured (Automated)
                  5.3.3.2.6   Ensure password dictionary check is enabled                      
                              (Automated)
                  5.3.3.2.7   Ensure password quality checking is enforced                     
                              (Automated)
                  5.3.3.2.8   Ensure password quality is enforced for the root user            
                              (Automated)

                                CIS Benchmark Recommendation                              Set
                                                                                       Correctly
                                                                                       Yes No
                  5.3.3.3     Configure pam_pwhistory module
                  5.3.3.3.1   Ensure password history remember is configured                  
                              (Automated)
                  5.3.3.3.2   Ensure password history is enforced for the root user           
                              (Automated)
                  5.3.3.3.3   Ensure pam_pwhistory includes use_authtok                       
                              (Automated)
                  5.3.3.4     Configure pam_unix module
                  5.3.3.4.1   Ensure pam_unix does not include nullok (Automated)             
                  5.3.3.4.2   Ensure pam_unix does not include remember                       
                              (Automated)
                  5.3.3.4.3   Ensure pam_unix includes a strong password hashing              
                              algorithm (Automated)
                  5.3.3.4.4   Ensure pam_unix includes use_authtok (Automated)                
                  5.4         User Accounts and Environment
                  5.4.1       Configure shadow password suite parameters
                  5.4.1.1     Ensure password expiration is configured (Automated)            
                  5.4.1.2     Ensure minimum password days is configured (Manual)             
                  5.4.1.3     Ensure password expiration warning days is configured           
                              (Automated)
                  5.4.1.4     Ensure strong password hashing algorithm is configured          
                              (Automated)
                  5.4.1.5     Ensure inactive password lock is configured (Automated)         
                  5.4.1.6     Ensure all users last password change date is in the past       
                              (Automated)
                  5.4.2       Configure root and system accounts and environment
                  5.4.2.1     Ensure root is the only UID 0 account (Automated)               
                  5.4.2.2     Ensure root is the only GID 0 account (Automated)               
                  5.4.2.3     Ensure group root is the only GID 0 group (Automated)           
                  5.4.2.4     Ensure root account access is controlled (Automated)            
                  5.4.2.5     Ensure root path integrity (Automated)                          
                  5.4.2.6     Ensure root user umask is configured (Automated)                
                  5.4.2.7     Ensure system accounts do not have a valid login shell          
                              (Automated)
                  5.4.2.8     Ensure accounts without a valid login shell are locked          
                              (Automated)
                  5.4.3       Configure user default environment
                  5.4.3.1     Ensure nologin is not listed in /etc/shells (Automated)         
                  5.4.3.2     Ensure default user shell timeout is configured                 
                              (Automated)
                  5.4.3.3     Ensure default user umask is configured (Automated)             
                  6           Logging and Auditing

                                CIS Benchmark Recommendation                                 Set
                                                                                          Correctly
                                                                                          Yes No
                  6.1         System Logging
                  6.1.1       Configure journald
                  6.1.1.1     Configure systemd-journald service
                  6.1.1.1.1   Ensure journald service is active (Automated)                     
                  6.1.1.1.2   Ensure journald log file access is configured (Manual)            
                  6.1.1.1.3   Ensure journald log file rotation is configured (Manual)          
                  6.1.1.1.4   Ensure journald ForwardToSyslog is disabled                       
                              (Automated)
                  6.1.1.1.5   Ensure journald Storage is configured (Automated)                 
                  6.1.1.1.6   Ensure journald Compress is configured (Automated)                
                  6.1.1.2     Configure systemd-journal-remote
                  6.1.1.2.1   Ensure systemd-journal-remote is installed (Automated)            
                  6.1.1.2.2   Ensure systemd-journal-upload authentication is                   
                              configured (Manual)
                  6.1.1.2.3   Ensure systemd-journal-upload is enabled and active               
                              (Automated)
                  6.1.1.2.4   Ensure systemd-journal-remote service is not in use               
                              (Automated)
                  6.1.2       Configure rsyslog
                  6.1.2.1     Ensure rsyslog is installed (Automated)                           
                  6.1.2.2     Ensure rsyslog service is enabled and active                      
                              (Automated)
                  6.1.2.3     Ensure journald is configured to send logs to rsyslog             
                              (Automated)
                  6.1.2.4     Ensure rsyslog log file creation mode is configured               
                              (Automated)
                  6.1.2.5     Ensure rsyslog logging is configured (Manual)                     
                  6.1.2.6     Ensure rsyslog is configured to send logs to a remote log         
                              host (Manual)
                  6.1.2.7     Ensure rsyslog is not configured to receive logs from a           
                              remote client (Automated)
                  6.1.2.8     Ensure logrotate is configured (Manual)                           
                  6.1.2.9     Ensure rsyslog-gnutls is installed (Automated)                    
                  6.1.2.10    Ensure rsyslog forwarding uses gtls (Automated)                   
                  6.1.2.11    Ensure rsyslog CA certificates are configured (Manual)            
                  6.1.3       Configure Logfiles
                  6.1.3.1     Ensure access to all logfiles has been configured                 
                              (Automated)
                  6.2         System Auditing
                  6.2.1       Configure auditd Service
                  6.2.1.1     Ensure auditd packages are installed (Automated)                  
                  6.2.1.2     Ensure auditd service is enabled and active (Automated)           

                                CIS Benchmark Recommendation                                   Set
                                                                                            Correctly
                                                                                            Yes No
                  6.2.1.3     Ensure auditing for processes that start prior to auditd is       
                              enabled (Automated)
                  6.2.1.4     Ensure audit_backlog_limit is configured (Automated)                
                  6.2.2       Configure Data Retention
                  6.2.2.1     Ensure audit log storage size is configured (Automated)             
                  6.2.2.2     Ensure audit logs are not automatically deleted                     
                              (Automated)
                  6.2.2.3     Ensure system is disabled when audit logs are full                  
                              (Automated)
                  6.2.2.4     Ensure system warns when audit logs are low on space                
                              (Automated)
                  6.2.3       Configure auditd Rules
                  6.2.3.1     Ensure modification of the /etc/sudoers file is collected           
                              (Automated)
                  6.2.3.2     Ensure actions as another user are always logged                    
                              (Automated)
                  6.2.3.3     Ensure events that modify the sudo log file are collected           
                              (Automated)
                  6.2.3.4     Ensure events that modify date and time information are             
                              collected (Automated)
                  6.2.3.5     Ensure events that modify sethostname and                           
                              setdomainname are collected (Automated)
                  6.2.3.6     Ensure events that modify /etc/issue and /etc/issue.net             
                              are collected (Automated)
                  6.2.3.7     Ensure events that modify /etc/hosts and /etc/hostname              
                              are collected (Automated)
                  6.2.3.8     Ensure events that modify the system's network                      
                              environment are collected (Automated)
                  6.2.3.9     Ensure events that modify /etc/NetworkManager                       
                              directory are collected (Automated)
                  6.2.3.10    Ensure use of privileged commands are collected                     
                              (Automated)
                  6.2.3.11    Ensure unsuccessful file access attempts are collected              
                              (Automated)
                  6.2.3.12    Ensure events that modify /etc/group information are                
                              collected (Automated)
                  6.2.3.13    Ensure events that modify /etc/passwd information are               
                              collected (Automated)
                  6.2.3.14    Ensure events that modify /etc/shadow and                           
                              /etc/gshadow are collected (Automated)
                  6.2.3.15    Ensure events that modify /etc/security/opasswd are                 
                              collected (Automated)

                                CIS Benchmark Recommendation                                   Set
                                                                                            Correctly
                                                                                            Yes No
                  6.2.3.16    Ensure events that modify /etc/nsswitch.conf file are             
                              collected (Automated)
                  6.2.3.17    Ensure events that modify /etc/pam.conf and /etc/pam.d/             
                              information are collected (Automated)
                  6.2.3.18    Ensure discretionary access control permission                      
                              modification events chmod,fchmod,fchmodat,fchmodat2
                              are collected (Automated)
                  6.2.3.19    Ensure discretionary access control permission                      
                              modification events chown,fchown,lchown,fchownat are
                              collected (Automated)
                  6.2.3.20    Ensure discretionary access control permission                      
                              modification events
                              setxattr,lsetxattr,fsetxattr,removexattr,lremovexattr,fremo
                              vexattr collected (Automated)
                  6.2.3.21    Ensure successful file system mounts are collected                  
                              (Automated)
                  6.2.3.22    Ensure session initiation information is collected                  
                              (Automated)
                  6.2.3.23    Ensure login and logout events are collected                        
                              (Automated)
                  6.2.3.24    Ensure unlink file deletion events by users are collected           
                              (Automated)
                  6.2.3.25    Ensure rename file deletion events by users are                     
                              collected (Automated)
                  6.2.3.26    Ensure events that modify the system's Mandatory                    
                              Access Controls are collected (Automated)
                  6.2.3.27    Ensure successful and unsuccessful attempts to use the              
                              chcon command are collected (Automated)
                  6.2.3.28    Ensure successful and unsuccessful attempts to use the              
                              setfacl command are collected (Automated)
                  6.2.3.29    Ensure successful and unsuccessful attempts to use the              
                              chacl command are collected (Automated)
                  6.2.3.30    Ensure successful and unsuccessful attempts to use the              
                              usermod command are collected (Automated)
                  6.2.3.31    Ensure kernel module loading unloading and                          
                              modification is collected (Automated)
                  6.2.3.32    Ensure kernel "init_module" and "finit_module" loading              
                              unloading and modification is collected (Automated)
                  6.2.3.33    Ensure kernel "delete_module" loading unloading and                 
                              modification is collected (Automated)
                  6.2.3.34    Ensure kernel query_module loading unloading and                    
                              modification is collected (Automated)

                                CIS Benchmark Recommendation                                Set
                                                                                         Correctly
                                                                                         Yes No
                  6.2.3.35    Ensure the audit configuration is loaded regardless of         
                              errors (Automated)
                  6.2.3.36    Ensure the audit configuration is immutable (Automated)          
                  6.2.3.37    Ensure the running and on disk configuration is the same         
                              (Manual)
                  6.2.4       Configure auditd File Access
                  6.2.4.1     Ensure audit log files mode is configured (Automated)            
                  6.2.4.2     Ensure audit log files owner is configured (Automated)           
                  6.2.4.3     Ensure audit log files group owner is configured                 
                              (Automated)
                  6.2.4.4     Ensure the audit log file directory mode is configured           
                              (Automated)
                  6.2.4.5     Ensure audit configuration files mode is configured              
                              (Automated)
                  6.2.4.6     Ensure audit configuration files owner is configured             
                              (Automated)
                  6.2.4.7     Ensure audit configuration files group owner is                  
                              configured (Automated)
                  6.2.4.8     Ensure audit tools mode is configured (Automated)                
                  6.2.4.9     Ensure audit tools owner is configured (Automated)               
                  6.2.4.10    Ensure audit tools group owner is configured                     
                              (Automated)
                  6.3         Configure Integrity Checking
                  6.3.1       Ensure AIDE is installed (Automated)                             
                  6.3.2       Ensure filesystem integrity is regularly checked                 
                              (Automated)
                  6.3.3       Ensure cryptographic mechanisms are used to protect              
                              the integrity of audit tools (Automated)
                  7           System Maintenance
                  7.1         Configure system file and directory access
                  7.1.1       Ensure access to /etc/passwd is configured (Automated)           
                  7.1.2       Ensure access to /etc/passwd- is configured                      
                              (Automated)
                  7.1.3       Ensure access to /etc/group is configured (Automated)            
                  7.1.4       Ensure access to /etc/group- is configured (Automated)           
                  7.1.5       Ensure access to /etc/shadow is configured (Automated)           
                  7.1.6       Ensure access to /etc/shadow- is configured                      
                              (Automated)
                  7.1.7       Ensure access to /etc/gshadow is configured                      
                              (Automated)
                  7.1.8       Ensure access to /etc/gshadow- is configured                     
                              (Automated)

                                CIS Benchmark Recommendation                               Set
                                                                                        Correctly
                                                                                        Yes No
                  7.1.9       Ensure access to /etc/shells is configured (Automated)        
                  7.1.10      Ensure access to /etc/security/opasswd is configured          
                              (Automated)
                  7.1.11      Ensure world writable files and directories are secured         
                              (Automated)
                  7.1.12      Ensure no files or directories without an owner and a           
                              group exist (Automated)
                  7.1.13      Ensure SUID and SGID files are reviewed (Manual)                
                  7.2         Local User and Group Settings
                  7.2.1       Ensure accounts in /etc/passwd use shadowed                     
                              passwords (Automated)
                  7.2.2       Ensure /etc/shadow password fields are not empty                
                              (Automated)
                  7.2.3       Ensure all groups in /etc/passwd exist in /etc/group            
                              (Automated)
                  7.2.4       Ensure shadow group is empty (Automated)                        
                  7.2.5       Ensure no duplicate UIDs exist (Automated)                      
                  7.2.6       Ensure no duplicate GIDs exist (Automated)                      
                  7.2.7       Ensure no duplicate user names exist (Automated)                
                  7.2.8       Ensure no duplicate group names exist (Automated)               
                  7.2.9       Ensure local interactive user home directories are              
                              configured (Automated)
                  7.2.10      Ensure local interactive user dot files access is               
                              configured (Automated)

                Appendix: CIS Controls v7 IG 1 Mapped
                Recommendations
                                         Recommendation                                     Set
                                                                                         Correctly
                                                                                         Yes No
                  1.1.2.1.1   Ensure /tmp is tmpfs or a separate partition                     
                  1.1.2.1.2   Ensure nodev option set on /tmp partition                        
                  1.1.2.1.3   Ensure nosuid option set on /tmp partition                       
                  1.1.2.1.4   Ensure noexec option set on /tmp partition                       
                  1.1.2.2.1   Ensure /dev/shm is tmpfs or a separate partition                 
                  1.1.2.2.2   Ensure nodev option set on /dev/shm partition                    
                  1.1.2.2.3   Ensure nosuid option set on /dev/shm partition                   
                  1.1.2.2.4   Ensure noexec option set on /dev/shm partition                   
                  1.1.2.3.1   Ensure separate partition exists for /home                       
                  1.1.2.3.2   Ensure nodev option set on /home partition                       
                  1.1.2.3.3   Ensure nosuid option set on /home partition                      
                  1.1.2.4.1   Ensure separate partition exists for /var                        
                  1.1.2.4.2   Ensure nodev option set on /var partition                        
                  1.1.2.4.3   Ensure nosuid option set on /var partition                       
                  1.1.2.5.1   Ensure separate partition exists for /var/tmp                    
                  1.1.2.5.2   Ensure nodev option set on /var/tmp partition                    
                  1.1.2.5.3   Ensure nosuid option set on /var/tmp partition                   
                  1.1.2.5.4   Ensure noexec option set on /var/tmp partition                   
                  1.1.2.6.2   Ensure nodev option set on /var/log partition                    
                  1.1.2.6.3   Ensure nosuid option set on /var/log partition                   
                  1.1.2.6.4   Ensure noexec option set on /var/log partition                   
                  1.1.2.7.2   Ensure nodev option set on /var/log/audit partition              
                  1.1.2.7.3   Ensure nosuid option set on /var/log/audit partition             
                  1.1.2.7.4   Ensure noexec option set on /var/log/audit partition             
                  1.2.1.1     Ensure the source.list and .source files use the Signed-
                                                                                              
                              By option
                  1.2.1.6     Ensure access to files in the /etc/apt/auth.conf.d/
                                                                                              
                              directory is configured
                  1.2.2.1     Ensure updates, patches, and additional security
                                                                                              
                              software are installed
                  1.3.1.1     Ensure apparmor packages are installed                          
                  1.3.1.2     Ensure AppArmor is enabled                                      
                  1.3.1.3     Ensure all AppArmor Profiles are enforcing                      
                  1.4.1       Ensure bootloader password is set                               
                  1.4.2       Ensure access to bootloader config is configured                

                                         Recommendation                                      Set
                                                                                          Correctly
                                                                                          Yes No
                  1.5.5       Ensure kernel.dmesg_restrict is configured                        
                  1.6.4       Ensure access to /etc/motd is configured                          
                  1.6.5       Ensure access to /etc/issue is configured                         
                  1.6.6       Ensure access to /etc/issue.net is configured                     
                  1.7.4       Ensure GDM screen locks when the user is idle                     
                  1.7.5       Ensure GDM screen locks cannot be overridden                      
                  1.7.6       Ensure GDM automatic mounting of removable media is
                                                                                               
                              disabled
                  1.7.8       Ensure GDM autorun-never is enabled                              
                  1.7.9       Ensure GDM autorun-never is not overridden                       
                  2.1.1       Ensure autofs services are not in use                            
                  2.1.16      Ensure telnet-server services are not in use                     
                  2.1.21      Ensure X window server services are not in use                   
                  2.2.1       Ensure nis client is not installed                               
                  2.4.1.2     Ensure access to /etc/crontab is configured                      
                  2.4.1.3     Ensure access to /etc/cron.hourly is configured                  
                  2.4.1.4     Ensure access to /etc/cron.daily is configured                   
                  2.4.1.5     Ensure access to /etc/cron.weekly is configured                  
                  2.4.1.6     Ensure access to /etc/cron.monthly is configured                 
                  2.4.1.8     Ensure access to /etc/cron.d is configured                       
                  2.4.1.9     Ensure access to crontab is configured                           
                  2.4.2.1     Ensure access to at is configured                                
                  3.3.1.16    Ensure net.ipv4.conf.all.log_martians is configured              
                  3.3.1.17    Ensure net.ipv4.conf.default.log_martians is configured          
                  4.1.1       Ensure ufw is installed                                          
                  4.1.2       Ensure ufw service is configured                                 
                  4.1.3       Ensure ufw incoming default is configured                        
                  4.1.4       Ensure ufw outgoing default is configured                        
                  4.1.5       Ensure ufw routed default is configured                          
                  5.1.1       Ensure access to /etc/ssh/sshd_config is configured              
                  5.1.2       Ensure access to SSH private host key files is configured        
                  5.1.3       Ensure access to SSH public host key files is configured         
                  5.1.4       Ensure sshd access is configured                                 
                  5.1.15      Ensure sshd LogLevel is configured                               
                  5.1.21      Ensure sshd PermitRootLogin is disabled                          
                  5.2.1       Ensure sudo is installed                                         
                  5.2.2       Ensure sudo commands use pty                                     
                  5.2.4       Ensure users must provide password for escalation                
                  5.2.5       Ensure re-authentication for privilege escalation is not
                                                                                               
                              disabled globally

                                         Recommendation                                        Set
                                                                                            Correctly
                                                                                            Yes No
                  5.2.6       Ensure sudo timestamp_timeout is configured                         
                  5.2.7       Ensure access to the su command is restricted                       
                  5.3.2.1     Ensure pam_unix module is enabled                                   
                  5.4.2.1     Ensure root is the only UID 0 account                               
                  5.4.2.2     Ensure root is the only GID 0 account                               
                  5.4.2.3     Ensure group root is the only GID 0 group                           
                  5.4.2.4     Ensure root account access is controlled                            
                  5.4.2.6     Ensure root user umask is configured                                
                  5.4.2.7     Ensure system accounts do not have a valid login shell              
                  5.4.2.8     Ensure accounts without a valid login shell are locked              
                  5.4.3.2     Ensure default user shell timeout is configured                     
                  5.4.3.3     Ensure default user umask is configured                             
                  6.1.1.1.1   Ensure journald service is active                                   
                  6.1.1.1.2   Ensure journald log file access is configured                       
                  6.1.1.1.3   Ensure journald log file rotation is configured                     
                  6.1.1.1.5   Ensure journald Storage is configured                               
                  6.1.1.1.6   Ensure journald Compress is configured                              
                  6.1.1.2.1   Ensure systemd-journal-remote is installed                          
                  6.1.1.2.2   Ensure systemd-journal-upload authentication is
                                                                                                 
                              configured
                  6.1.1.2.3   Ensure systemd-journal-upload is enabled and active                
                  6.1.2.1     Ensure rsyslog is installed                                        
                  6.1.2.2     Ensure rsyslog service is enabled and active                       
                  6.1.2.3     Ensure journald is configured to send logs to rsyslog              
                  6.1.2.4     Ensure rsyslog log file creation mode is configured                
                  6.1.2.5     Ensure rsyslog logging is configured                               
                  6.1.2.6     Ensure rsyslog is configured to send logs to a remote log
                                                                                                 
                              host
                  6.1.2.9     Ensure rsyslog-gnutls is installed                                 
                  6.1.3.1     Ensure access to all logfiles has been configured                  
                  6.2.1.1     Ensure auditd packages are installed                               
                  6.2.1.2     Ensure auditd service is enabled and active                        
                  6.2.1.3     Ensure auditing for processes that start prior to auditd is
                                                                                                 
                              enabled
                  6.2.1.4     Ensure audit_backlog_limit is configured                           
                  6.2.2.4     Ensure system warns when audit logs are low on space               
                  6.2.3.10    Ensure use of privileged commands are collected                    
                  6.2.3.23    Ensure login and logout events are collected                       
                  6.2.3.24    Ensure unlink file deletion events by users are collected          
                  6.2.3.25    Ensure rename file deletion events by users are collected          

                                        Recommendation                                    Set
                                                                                        Correctly
                                                                                        Yes No
                  6.2.3.27    Ensure successful and unsuccessful attempts to use the
                                                                                             
                              chcon command are collected
                  6.2.3.28    Ensure successful and unsuccessful attempts to use the
                                                                                             
                              setfacl command are collected
                  6.2.3.29    Ensure successful and unsuccessful attempts to use the
                                                                                             
                              chacl command are collected
                  6.2.3.30    Ensure successful and unsuccessful attempts to use the
                                                                                             
                              usermod command are collected
                  6.2.3.31    Ensure kernel module loading unloading and modification
                                                                                             
                              is collected
                  6.2.3.32    Ensure kernel "init_module" and "finit_module" loading
                                                                                             
                              unloading and modification is collected
                  6.2.3.33    Ensure kernel "delete_module" loading unloading and
                                                                                             
                              modification is collected
                  6.2.3.34    Ensure kernel query_module loading unloading and
                                                                                             
                              modification is collected
                  6.2.3.35    Ensure the audit configuration is loaded regardless of
                                                                                             
                              errors
                  6.2.3.36    Ensure the audit configuration is immutable                    
                  6.2.4.1     Ensure audit log files mode is configured                      
                  6.2.4.2     Ensure audit log files owner is configured                     
                  6.2.4.3     Ensure audit log files group owner is configured               
                  6.2.4.4     Ensure the audit log file directory mode is configured         
                  6.2.4.5     Ensure audit configuration files mode is configured            
                  6.2.4.6     Ensure audit configuration files owner is configured           
                  6.2.4.7     Ensure audit configuration files group owner is
                                                                                             
                              configured
                  6.2.4.8     Ensure audit tools mode is configured                          
                  6.2.4.9     Ensure audit tools owner is configured                         
                  6.2.4.10    Ensure audit tools group owner is configured                   
                  7.1.1       Ensure access to /etc/passwd is configured                     
                  7.1.2       Ensure access to /etc/passwd- is configured                    
                  7.1.3       Ensure access to /etc/group is configured                      
                  7.1.4       Ensure access to /etc/group- is configured                     
                  7.1.5       Ensure access to /etc/shadow is configured                     
                  7.1.6       Ensure access to /etc/shadow- is configured                    
                  7.1.7       Ensure access to /etc/gshadow is configured                    
                  7.1.8       Ensure access to /etc/gshadow- is configured                   
                  7.1.9       Ensure access to /etc/shells is configured                     
                  7.1.10      Ensure access to /etc/security/opasswd is configured           
                  7.1.11      Ensure world writable files and directories are secured        

                                         Recommendation                                 Set
                                                                                      Correctly
                                                                                      Yes No
                  7.1.12      Ensure no files or directories without an owner and a
                                                                                           
                              group exist
                  7.1.13      Ensure SUID and SGID files are reviewed                      
                  7.2.4       Ensure shadow group is empty                                 
                  7.2.9       Ensure local interactive user home directories are
                                                                                           
                              configured
                  7.2.10      Ensure local interactive user dot files access is
                                                                                           
                              configured

                Appendix: CIS Controls v7 IG 2 Mapped
                Recommendations
                                         Recommendation                                Set
                                                                                    Correctly
                                                                                    Yes No
                  1.1.1.1     Ensure cramfs kernel module is not available                
                  1.1.1.2     Ensure freevxfs kernel module is not available              
                  1.1.1.3     Ensure hfs kernel module is not available                   
                  1.1.1.4     Ensure hfsplus kernel module is not available               
                  1.1.1.5     Ensure jffs2 kernel module is not available                 
                  1.1.1.6     Ensure overlay kernel module is not available               
                  1.1.1.7     Ensure squashfs kernel module is not available              
                  1.1.1.8     Ensure udf kernel module is not available                   
                  1.1.1.9     Ensure firewire-core kernel module is not available         
                  1.1.1.10    Ensure usb-storage kernel module is not available           
                  1.1.1.11    Ensure unused filesystems kernel modules are not
                                                                                         
                              available
                  1.1.2.1.1   Ensure /tmp is tmpfs or a separate partition               
                  1.1.2.1.2   Ensure nodev option set on /tmp partition                  
                  1.1.2.1.3   Ensure nosuid option set on /tmp partition                 
                  1.1.2.1.4   Ensure noexec option set on /tmp partition                 
                  1.1.2.2.1   Ensure /dev/shm is tmpfs or a separate partition           
                  1.1.2.2.2   Ensure nodev option set on /dev/shm partition              
                  1.1.2.2.3   Ensure nosuid option set on /dev/shm partition             
                  1.1.2.2.4   Ensure noexec option set on /dev/shm partition             
                  1.1.2.3.1   Ensure separate partition exists for /home                 
                  1.1.2.3.2   Ensure nodev option set on /home partition                 
                  1.1.2.3.3   Ensure nosuid option set on /home partition                
                  1.1.2.4.1   Ensure separate partition exists for /var                  
                  1.1.2.4.2   Ensure nodev option set on /var partition                  
                  1.1.2.4.3   Ensure nosuid option set on /var partition                 
                  1.1.2.5.1   Ensure separate partition exists for /var/tmp              
                  1.1.2.5.2   Ensure nodev option set on /var/tmp partition              
                  1.1.2.5.3   Ensure nosuid option set on /var/tmp partition             
                  1.1.2.5.4   Ensure noexec option set on /var/tmp partition             
                  1.1.2.6.1   Ensure separate partition exists for /var/log              
                  1.1.2.6.2   Ensure nodev option set on /var/log partition              
                  1.1.2.6.3   Ensure nosuid option set on /var/log partition             
                  1.1.2.6.4   Ensure noexec option set on /var/log partition             
                  1.1.2.7.1   Ensure separate partition exists for /var/log/audit        

                                         Recommendation                                     Set
                                                                                         Correctly
                                                                                         Yes No
                  1.1.2.7.2   Ensure nodev option set on /var/log/audit partition              
                  1.1.2.7.3   Ensure nosuid option set on /var/log/audit partition             
                  1.1.2.7.4   Ensure noexec option set on /var/log/audit partition             
                  1.2.1.1     Ensure the source.list and .source files use the Signed-
                                                                                              
                              By option
                  1.2.1.6     Ensure access to files in the /etc/apt/auth.conf.d/
                                                                                              
                              directory is configured
                  1.2.2.1     Ensure updates, patches, and additional security
                                                                                              
                              software are installed
                  1.3.1.1     Ensure apparmor packages are installed                          
                  1.3.1.2     Ensure AppArmor is enabled                                      
                  1.3.1.3     Ensure all AppArmor Profiles are enforcing                      
                  1.3.1.4     Ensure apparmor_restrict_unprivileged_unconfined is
                                                                                              
                              enabled
                  1.4.1       Ensure bootloader password is set                               
                  1.4.2       Ensure access to bootloader config is configured                
                  1.5.3       Ensure kernel.yama.ptrace_scope is configured                   
                  1.5.5       Ensure kernel.dmesg_restrict is configured                      
                  1.5.7       Ensure Automatic Error Reporting is configured                  
                  1.5.8       Ensure kernel.kptr_restrict is configured                       
                  1.5.9       Ensure kernel.randomize_va_space is configured                  
                  1.5.10      Ensure kernel.yama.ptrace_scope is configured                   
                  1.6.4       Ensure access to /etc/motd is configured                        
                  1.6.5       Ensure access to /etc/issue is configured                       
                  1.6.6       Ensure access to /etc/issue.net is configured                   
                  1.7.1       Ensure GDM is removed                                           
                  1.7.4       Ensure GDM screen locks when the user is idle                   
                  1.7.5       Ensure GDM screen locks cannot be overridden                    
                  1.7.6       Ensure GDM automatic mounting of removable media is
                                                                                              
                              disabled
                  1.7.8       Ensure GDM autorun-never is enabled                             
                  1.7.9       Ensure GDM autorun-never is not overridden                      
                  1.7.10      Ensure XDMCP is not enabled                                     
                  1.7.11      Ensure Xwayland is configured                                   
                  2.1.1       Ensure autofs services are not in use                           
                  2.1.2       Ensure avahi daemon services are not in use                     
                  2.1.3       Ensure dhcp server services are not in use                      
                  2.1.4       Ensure dns server services are not in use                       
                  2.1.5       Ensure dnsmasq services are not in use                          
                  2.1.6       Ensure ftp server services are not in use                       
                  2.1.7       Ensure ldap server services are not in use                      

                                         Recommendation                                      Set
                                                                                          Correctly
                                                                                          Yes No
                  2.1.8       Ensure message access server services are not in use              
                  2.1.9       Ensure network file system services are not in use                
                  2.1.10      Ensure nis server services are not in use                         
                  2.1.11      Ensure print server services are not in use                       
                  2.1.12      Ensure rpcbind services are not in use                            
                  2.1.13      Ensure rsync services are not in use                              
                  2.1.14      Ensure samba file server services are not in use                  
                  2.1.15      Ensure snmp services are not in use                               
                  2.1.16      Ensure telnet-server services are not in use                      
                  2.1.17      Ensure tftp server services are not in use                        
                  2.1.18      Ensure web proxy server services are not in use                   
                  2.1.19      Ensure web server services are not in use                         
                  2.1.20      Ensure xinetd services are not in use                             
                  2.1.21      Ensure X window server services are not in use                    
                  2.1.22      Ensure mail transfer agents are configured for local-only
                                                                                               
                              mode
                  2.1.23      Ensure only approved services are listening on a network
                                                                                               
                              interface
                  2.2.1       Ensure nis client is not installed                               
                  2.2.2       Ensure rsh client is not installed                               
                  2.2.3       Ensure talk client is not installed                              
                  2.2.4       Ensure telnet client is not installed                            
                  2.2.5       Ensure ldap client is not installed                              
                  2.2.6       Ensure ftp client is not installed                               
                  2.3.1.1     Ensure a single time synchronization daemon is in use            
                  2.3.2.1     Ensure systemd-timesyncd configured with authorized
                                                                                               
                              timeserver
                  2.3.2.2     Ensure systemd-timesyncd is enabled and running                  
                  2.3.3.1     Ensure chrony is configured with authorized timeserver           
                  2.3.3.3     Ensure chrony is enabled and running                             
                  2.4.1.2     Ensure access to /etc/crontab is configured                      
                  2.4.1.3     Ensure access to /etc/cron.hourly is configured                  
                  2.4.1.4     Ensure access to /etc/cron.daily is configured                   
                  2.4.1.5     Ensure access to /etc/cron.weekly is configured                  
                  2.4.1.6     Ensure access to /etc/cron.monthly is configured                 
                  2.4.1.8     Ensure access to /etc/cron.d is configured                       
                  2.4.1.9     Ensure access to crontab is configured                           
                  2.4.2.1     Ensure access to at is configured                                
                  3.1.1       Ensure IPv6 status is identified                                 
                  3.1.3       Ensure bluetooth services are not in use                         

                                         Recommendation                                      Set
                                                                                          Correctly
                                                                                          Yes No
                  3.2.1       Ensure atm kernel module is not available                         
                  3.2.2       Ensure can kernel module is not available                         
                  3.2.3       Ensure dccp kernel module is not available                        
                  3.2.4       Ensure rds kernel module is not available                         
                  3.2.5       Ensure sctp kernel module is not available                        
                  3.2.6       Ensure tipc kernel module is not available                        
                  3.3.1.1     Ensure net.ipv4.ip_forward is configured                          
                  3.3.1.2     Ensure net.ipv4.conf.all.forwarding is configured                 
                  3.3.1.3     Ensure net.ipv4.conf.default.forwarding is configured             
                  3.3.1.4     Ensure net.ipv4.conf.all.send_redirects is configured             
                  3.3.1.5     Ensure net.ipv4.conf.default.send_redirects is configured         
                  3.3.1.6     Ensure net.ipv4.icmp_ignore_bogus_error_responses is
                                                                                               
                              configured
                  3.3.1.7     Ensure net.ipv4.icmp_echo_ignore_broadcasts is
                                                                                               
                              configured
                  3.3.1.8     Ensure net.ipv4.conf.all.accept_redirects is configured          
                  3.3.1.9     Ensure net.ipv4.conf.default.accept_redirects is
                                                                                               
                              configured
                  3.3.1.10    Ensure net.ipv4.conf.all.secure_redirects is configured          
                  3.3.1.11    Ensure net.ipv4.conf.default.secure_redirects is
                                                                                               
                              configured
                  3.3.1.12    Ensure net.ipv4.conf.all.rp_filter is configured                 
                  3.3.1.13    Ensure net.ipv4.conf.default.rp_filter is configured             
                  3.3.1.14    Ensure net.ipv4.conf.all.accept_source_route is
                                                                                               
                              configured
                  3.3.1.15    Ensure net.ipv4.conf.default.accept_source_route is
                                                                                               
                              configured
                  3.3.1.16    Ensure net.ipv4.conf.all.log_martians is configured              
                  3.3.1.17    Ensure net.ipv4.conf.default.log_martians is configured          
                  3.3.1.18    Ensure net.ipv4.tcp_syncookies is configured                     
                  3.3.2.1     Ensure net.ipv6.conf.all.forwarding is configured                
                  3.3.2.2     Ensure net.ipv6.conf.default.forwarding is configured            
                  3.3.2.3     Ensure net.ipv6.conf.all.accept_redirects is configured          
                  3.3.2.4     Ensure net.ipv6.conf.default.accept_redirects is
                                                                                               
                              configured
                  3.3.2.5     Ensure net.ipv6.conf.all.accept_source_route is
                                                                                               
                              configured
                  3.3.2.6     Ensure net.ipv6.conf.default.accept_source_route is
                                                                                               
                              configured
                  3.3.2.7     Ensure net.ipv6.conf.all.accept_ra is configured                 
                  3.3.2.8     Ensure net.ipv6.conf.default.accept_ra is configured             

                                         Recommendation                                      Set
                                                                                          Correctly
                                                                                          Yes No
                  4.1.1       Ensure ufw is installed                                           
                  4.1.2       Ensure ufw service is configured                                  
                  4.1.3       Ensure ufw incoming default is configured                         
                  4.1.4       Ensure ufw outgoing default is configured                         
                  4.1.5       Ensure ufw routed default is configured                           
                  5.1.1       Ensure access to /etc/ssh/sshd_config is configured               
                  5.1.2       Ensure access to SSH private host key files is configured         
                  5.1.3       Ensure access to SSH public host key files is configured          
                  5.1.4       Ensure sshd access is configured                                  
                  5.1.6       Ensure sshd Ciphers are configured                                
                  5.1.8       Ensure sshd DisableForwarding is enabled                          
                  5.1.9       Ensure sshd GSSAPIAuthentication is disabled                      
                  5.1.10      Ensure sshd HostbasedAuthentication is disabled                   
                  5.1.11      Ensure sshd IgnoreRhosts is enabled                               
                  5.1.12      Ensure sshd KexAlgorithms is configured                           
                  5.1.13      Ensure sshd post-quantum cryptography key exchange
                                                                                               
                              algorithms are configured
                  5.1.15      Ensure sshd LogLevel is configured                               
                  5.1.16      Ensure sshd MACs are configured                                  
                  5.1.20      Ensure sshd PermitEmptyPasswords is disabled                     
                  5.1.21      Ensure sshd PermitRootLogin is disabled                          
                  5.1.23      Ensure sshd UsePAM is enabled                                    
                  5.2.1       Ensure sudo is installed                                         
                  5.2.2       Ensure sudo commands use pty                                     
                  5.2.3       Ensure sudo log file exists                                      
                  5.2.4       Ensure users must provide password for escalation                
                  5.2.5       Ensure re-authentication for privilege escalation is not
                                                                                               
                              disabled globally
                  5.2.6       Ensure sudo timestamp_timeout is configured                      
                  5.2.7       Ensure access to the su command is restricted                    
                  5.3.1.3     Ensure latest version of libpam-pwquality is installed           
                  5.3.2.1     Ensure pam_unix module is enabled                                
                  5.3.2.2     Ensure pam_faillock module is enabled                            
                  5.3.2.3     Ensure pam_pwquality module is enabled                           
                  5.3.2.4     Ensure pam_pwhistory module is enabled                           
                  5.3.3.1.1   Ensure password failed attempts lockout is configured            
                  5.3.3.1.2   Ensure password unlock time is configured                        
                  5.3.3.1.3   Ensure password failed attempts lockout includes root
                                                                                               
                              account
                  5.3.3.2.1   Ensure password number of changed characters is
                                                                                               
                              configured

                                         Recommendation                                      Set
                                                                                          Correctly
                                                                                          Yes No
                  5.3.3.2.2   Ensure password length is configured                              
                  5.3.3.2.3   Ensure password complexity is configured                          
                  5.3.3.2.4   Ensure password same consecutive characters is
                                                                                               
                              configured
                  5.3.3.2.5   Ensure password maximum sequential characters is
                                                                                               
                              configured
                  5.3.3.2.6   Ensure password dictionary check is enabled                      
                  5.3.3.2.7   Ensure password quality checking is enforced                     
                  5.3.3.2.8   Ensure password quality is enforced for the root user            
                  5.3.3.3.1   Ensure password history remember is configured                   
                  5.3.3.3.2   Ensure password history is enforced for the root user            
                  5.3.3.3.3   Ensure pam_pwhistory includes use_authtok                        
                  5.3.3.4.1   Ensure pam_unix does not include nullok                          
                  5.3.3.4.2   Ensure pam_unix does not include remember                        
                  5.3.3.4.3   Ensure pam_unix includes a strong password hashing
                                                                                               
                              algorithm
                  5.3.3.4.4   Ensure pam_unix includes use_authtok                             
                  5.4.1.1     Ensure password expiration is configured                         
                  5.4.1.2     Ensure minimum password days is configured                       
                  5.4.1.3     Ensure password expiration warning days is configured            
                  5.4.1.4     Ensure strong password hashing algorithm is configured           
                  5.4.1.5     Ensure inactive password lock is configured                      
                  5.4.1.6     Ensure all users last password change date is in the past        
                  5.4.2.1     Ensure root is the only UID 0 account                            
                  5.4.2.2     Ensure root is the only GID 0 account                            
                  5.4.2.3     Ensure group root is the only GID 0 group                        
                  5.4.2.4     Ensure root account access is controlled                         
                  5.4.2.6     Ensure root user umask is configured                             
                  5.4.2.7     Ensure system accounts do not have a valid login shell           
                  5.4.2.8     Ensure accounts without a valid login shell are locked           
                  5.4.3.2     Ensure default user shell timeout is configured                  
                  5.4.3.3     Ensure default user umask is configured                          
                  6.1.1.1.1   Ensure journald service is active                                
                  6.1.1.1.2   Ensure journald log file access is configured                    
                  6.1.1.1.3   Ensure journald log file rotation is configured                  
                  6.1.1.1.4   Ensure journald ForwardToSyslog is disabled                      
                  6.1.1.1.5   Ensure journald Storage is configured                            
                  6.1.1.1.6   Ensure journald Compress is configured                           
                  6.1.1.2.1   Ensure systemd-journal-remote is installed                       
                  6.1.1.2.2   Ensure systemd-journal-upload authentication is
                                                                                               
                              configured

                                         Recommendation                                        Set
                                                                                            Correctly
                                                                                            Yes No
                  6.1.1.2.3   Ensure systemd-journal-upload is enabled and active                 
                  6.1.1.2.4   Ensure systemd-journal-remote service is not in use                 
                  6.1.2.1     Ensure rsyslog is installed                                         
                  6.1.2.2     Ensure rsyslog service is enabled and active                        
                  6.1.2.3     Ensure journald is configured to send logs to rsyslog               
                  6.1.2.4     Ensure rsyslog log file creation mode is configured                 
                  6.1.2.5     Ensure rsyslog logging is configured                                
                  6.1.2.6     Ensure rsyslog is configured to send logs to a remote log
                                                                                                 
                              host
                  6.1.2.7     Ensure rsyslog is not configured to receive logs from a
                                                                                                 
                              remote client
                  6.1.2.8     Ensure logrotate is configured                                     
                  6.1.2.9     Ensure rsyslog-gnutls is installed                                 
                  6.1.2.10    Ensure rsyslog forwarding uses gtls                                
                  6.1.2.11    Ensure rsyslog CA certificates are configured                      
                  6.1.3.1     Ensure access to all logfiles has been configured                  
                  6.2.1.1     Ensure auditd packages are installed                               
                  6.2.1.2     Ensure auditd service is enabled and active                        
                  6.2.1.3     Ensure auditing for processes that start prior to auditd is
                                                                                                 
                              enabled
                  6.2.1.4     Ensure audit_backlog_limit is configured                           
                  6.2.2.1     Ensure audit log storage size is configured                        
                  6.2.2.2     Ensure audit logs are not automatically deleted                    
                  6.2.2.4     Ensure system warns when audit logs are low on space               
                  6.2.3.1     Ensure modification of the /etc/sudoers file is collected          
                  6.2.3.2     Ensure actions as another user are always logged                   
                  6.2.3.3     Ensure events that modify the sudo log file are collected          
                  6.2.3.4     Ensure events that modify date and time information are
                                                                                                 
                              collected
                  6.2.3.5     Ensure events that modify sethostname and
                                                                                                 
                              setdomainname are collected
                  6.2.3.6     Ensure events that modify /etc/issue and /etc/issue.net
                                                                                                 
                              are collected
                  6.2.3.7     Ensure events that modify /etc/hosts and /etc/hostname
                                                                                                 
                              are collected
                  6.2.3.8     Ensure events that modify the system's network
                                                                                                 
                              environment are collected
                  6.2.3.9     Ensure events that modify /etc/NetworkManager
                                                                                                 
                              directory are collected
                  6.2.3.10    Ensure use of privileged commands are collected                    

                                         Recommendation                                       Set
                                                                                            Correctly
                                                                                            Yes No
                  6.2.3.12    Ensure events that modify /etc/group information are
                                                                                                 
                              collected
                  6.2.3.13    Ensure events that modify /etc/passwd information are
                                                                                                 
                              collected
                  6.2.3.14    Ensure events that modify /etc/shadow and /etc/gshadow
                                                                                                 
                              are collected
                  6.2.3.15    Ensure events that modify /etc/security/opasswd are
                                                                                                 
                              collected
                  6.2.3.16    Ensure events that modify /etc/nsswitch.conf file are
                                                                                                 
                              collected
                  6.2.3.17    Ensure events that modify /etc/pam.conf and /etc/pam.d/
                                                                                                 
                              information are collected
                  6.2.3.18    Ensure discretionary access control permission
                              modification events chmod,fchmod,fchmodat,fchmodat2                
                              are collected
                  6.2.3.19    Ensure discretionary access control permission
                              modification events chown,fchown,lchown,fchownat are               
                              collected
                  6.2.3.20    Ensure discretionary access control permission
                              modification events
                                                                                                 
                              setxattr,lsetxattr,fsetxattr,removexattr,lremovexattr,fremo
                              vexattr collected
                  6.2.3.21    Ensure successful file system mounts are collected                 
                  6.2.3.22    Ensure session initiation information is collected                 
                  6.2.3.23    Ensure login and logout events are collected                       
                  6.2.3.24    Ensure unlink file deletion events by users are collected          
                  6.2.3.25    Ensure rename file deletion events by users are collected          
                  6.2.3.26    Ensure events that modify the system's Mandatory
                                                                                                 
                              Access Controls are collected
                  6.2.3.27    Ensure successful and unsuccessful attempts to use the
                                                                                                 
                              chcon command are collected
                  6.2.3.28    Ensure successful and unsuccessful attempts to use the
                                                                                                 
                              setfacl command are collected
                  6.2.3.29    Ensure successful and unsuccessful attempts to use the
                                                                                                 
                              chacl command are collected
                  6.2.3.30    Ensure successful and unsuccessful attempts to use the
                                                                                                 
                              usermod command are collected
                  6.2.3.31    Ensure kernel module loading unloading and modification
                                                                                                 
                              is collected
                  6.2.3.32    Ensure kernel "init_module" and "finit_module" loading
                                                                                                 
                              unloading and modification is collected

                                         Recommendation                                    Set
                                                                                         Correctly
                                                                                         Yes No
                  6.2.3.33    Ensure kernel "delete_module" loading unloading and
                                                                                              
                              modification is collected
                  6.2.3.34    Ensure kernel query_module loading unloading and
                                                                                              
                              modification is collected
                  6.2.3.35    Ensure the audit configuration is loaded regardless of
                                                                                              
                              errors
                  6.2.3.36    Ensure the audit configuration is immutable                     
                  6.2.3.37    Ensure the running and on disk configuration is the same        
                  6.2.4.1     Ensure audit log files mode is configured                       
                  6.2.4.2     Ensure audit log files owner is configured                      
                  6.2.4.3     Ensure audit log files group owner is configured                
                  6.2.4.4     Ensure the audit log file directory mode is configured          
                  6.2.4.5     Ensure audit configuration files mode is configured             
                  6.2.4.6     Ensure audit configuration files owner is configured            
                  6.2.4.7     Ensure audit configuration files group owner is
                                                                                              
                              configured
                  6.2.4.8     Ensure audit tools mode is configured                           
                  6.2.4.9     Ensure audit tools owner is configured                          
                  6.2.4.10    Ensure audit tools group owner is configured                    
                  7.1.1       Ensure access to /etc/passwd is configured                      
                  7.1.2       Ensure access to /etc/passwd- is configured                     
                  7.1.3       Ensure access to /etc/group is configured                       
                  7.1.4       Ensure access to /etc/group- is configured                      
                  7.1.5       Ensure access to /etc/shadow is configured                      
                  7.1.6       Ensure access to /etc/shadow- is configured                     
                  7.1.7       Ensure access to /etc/gshadow is configured                     
                  7.1.8       Ensure access to /etc/gshadow- is configured                    
                  7.1.9       Ensure access to /etc/shells is configured                      
                  7.1.10      Ensure access to /etc/security/opasswd is configured            
                  7.1.11      Ensure world writable files and directories are secured         
                  7.1.12      Ensure no files or directories without an owner and a
                                                                                              
                              group exist
                  7.1.13      Ensure SUID and SGID files are reviewed                         
                  7.2.1       Ensure accounts in /etc/passwd use shadowed
                                                                                              
                              passwords
                  7.2.2       Ensure /etc/shadow password fields are not empty                
                  7.2.4       Ensure shadow group is empty                                    
                  7.2.9       Ensure local interactive user home directories are
                                                                                              
                              configured
                  7.2.10      Ensure local interactive user dot files access is
                                                                                              
                              configured

                Appendix: CIS Controls v7 IG 3 Mapped
                Recommendations
                                         Recommendation                                Set
                                                                                    Correctly
                                                                                    Yes No
                  1.1.1.1     Ensure cramfs kernel module is not available                
                  1.1.1.2     Ensure freevxfs kernel module is not available              
                  1.1.1.3     Ensure hfs kernel module is not available                   
                  1.1.1.4     Ensure hfsplus kernel module is not available               
                  1.1.1.5     Ensure jffs2 kernel module is not available                 
                  1.1.1.6     Ensure overlay kernel module is not available               
                  1.1.1.7     Ensure squashfs kernel module is not available              
                  1.1.1.8     Ensure udf kernel module is not available                   
                  1.1.1.9     Ensure firewire-core kernel module is not available         
                  1.1.1.10    Ensure usb-storage kernel module is not available           
                  1.1.1.11    Ensure unused filesystems kernel modules are not
                                                                                         
                              available
                  1.1.2.1.1   Ensure /tmp is tmpfs or a separate partition               
                  1.1.2.1.2   Ensure nodev option set on /tmp partition                  
                  1.1.2.1.3   Ensure nosuid option set on /tmp partition                 
                  1.1.2.1.4   Ensure noexec option set on /tmp partition                 
                  1.1.2.2.1   Ensure /dev/shm is tmpfs or a separate partition           
                  1.1.2.2.2   Ensure nodev option set on /dev/shm partition              
                  1.1.2.2.3   Ensure nosuid option set on /dev/shm partition             
                  1.1.2.2.4   Ensure noexec option set on /dev/shm partition             
                  1.1.2.3.1   Ensure separate partition exists for /home                 
                  1.1.2.3.2   Ensure nodev option set on /home partition                 
                  1.1.2.3.3   Ensure nosuid option set on /home partition                
                  1.1.2.4.1   Ensure separate partition exists for /var                  
                  1.1.2.4.2   Ensure nodev option set on /var partition                  
                  1.1.2.4.3   Ensure nosuid option set on /var partition                 
                  1.1.2.5.1   Ensure separate partition exists for /var/tmp              
                  1.1.2.5.2   Ensure nodev option set on /var/tmp partition              
                  1.1.2.5.3   Ensure nosuid option set on /var/tmp partition             
                  1.1.2.5.4   Ensure noexec option set on /var/tmp partition             
                  1.1.2.6.1   Ensure separate partition exists for /var/log              
                  1.1.2.6.2   Ensure nodev option set on /var/log partition              
                  1.1.2.6.3   Ensure nosuid option set on /var/log partition             
                  1.1.2.6.4   Ensure noexec option set on /var/log partition             
                  1.1.2.7.1   Ensure separate partition exists for /var/log/audit        

                                         Recommendation                                     Set
                                                                                         Correctly
                                                                                         Yes No
                  1.1.2.7.2   Ensure nodev option set on /var/log/audit partition              
                  1.1.2.7.3   Ensure nosuid option set on /var/log/audit partition             
                  1.1.2.7.4   Ensure noexec option set on /var/log/audit partition             
                  1.2.1.1     Ensure the source.list and .source files use the Signed-
                                                                                              
                              By option
                  1.2.1.6     Ensure access to files in the /etc/apt/auth.conf.d/
                                                                                              
                              directory is configured
                  1.2.2.1     Ensure updates, patches, and additional security
                                                                                              
                              software are installed
                  1.3.1.1     Ensure apparmor packages are installed                          
                  1.3.1.2     Ensure AppArmor is enabled                                      
                  1.3.1.3     Ensure all AppArmor Profiles are enforcing                      
                  1.3.1.4     Ensure apparmor_restrict_unprivileged_unconfined is
                                                                                              
                              enabled
                  1.4.1       Ensure bootloader password is set                               
                  1.4.2       Ensure access to bootloader config is configured                
                  1.5.3       Ensure kernel.yama.ptrace_scope is configured                   
                  1.5.5       Ensure kernel.dmesg_restrict is configured                      
                  1.5.6       Ensure prelink is not installed                                 
                  1.5.7       Ensure Automatic Error Reporting is configured                  
                  1.5.8       Ensure kernel.kptr_restrict is configured                       
                  1.5.9       Ensure kernel.randomize_va_space is configured                  
                  1.5.10      Ensure kernel.yama.ptrace_scope is configured                   
                  1.6.4       Ensure access to /etc/motd is configured                        
                  1.6.5       Ensure access to /etc/issue is configured                       
                  1.6.6       Ensure access to /etc/issue.net is configured                   
                  1.7.1       Ensure GDM is removed                                           
                  1.7.4       Ensure GDM screen locks when the user is idle                   
                  1.7.5       Ensure GDM screen locks cannot be overridden                    
                  1.7.6       Ensure GDM automatic mounting of removable media is
                                                                                              
                              disabled
                  1.7.8       Ensure GDM autorun-never is enabled                             
                  1.7.9       Ensure GDM autorun-never is not overridden                      
                  1.7.10      Ensure XDMCP is not enabled                                     
                  1.7.11      Ensure Xwayland is configured                                   
                  2.1.1       Ensure autofs services are not in use                           
                  2.1.2       Ensure avahi daemon services are not in use                     
                  2.1.3       Ensure dhcp server services are not in use                      
                  2.1.4       Ensure dns server services are not in use                       
                  2.1.5       Ensure dnsmasq services are not in use                          
                  2.1.6       Ensure ftp server services are not in use                       

                                         Recommendation                                      Set
                                                                                          Correctly
                                                                                          Yes No
                  2.1.7       Ensure ldap server services are not in use                        
                  2.1.8       Ensure message access server services are not in use              
                  2.1.9       Ensure network file system services are not in use                
                  2.1.10      Ensure nis server services are not in use                         
                  2.1.11      Ensure print server services are not in use                       
                  2.1.12      Ensure rpcbind services are not in use                            
                  2.1.13      Ensure rsync services are not in use                              
                  2.1.14      Ensure samba file server services are not in use                  
                  2.1.15      Ensure snmp services are not in use                               
                  2.1.16      Ensure telnet-server services are not in use                      
                  2.1.17      Ensure tftp server services are not in use                        
                  2.1.18      Ensure web proxy server services are not in use                   
                  2.1.19      Ensure web server services are not in use                         
                  2.1.20      Ensure xinetd services are not in use                             
                  2.1.21      Ensure X window server services are not in use                    
                  2.1.22      Ensure mail transfer agents are configured for local-only
                                                                                               
                              mode
                  2.1.23      Ensure only approved services are listening on a network
                                                                                               
                              interface
                  2.2.1       Ensure nis client is not installed                               
                  2.2.2       Ensure rsh client is not installed                               
                  2.2.3       Ensure talk client is not installed                              
                  2.2.4       Ensure telnet client is not installed                            
                  2.2.5       Ensure ldap client is not installed                              
                  2.2.6       Ensure ftp client is not installed                               
                  2.3.1.1     Ensure a single time synchronization daemon is in use            
                  2.3.2.1     Ensure systemd-timesyncd configured with authorized
                                                                                               
                              timeserver
                  2.3.2.2     Ensure systemd-timesyncd is enabled and running                  
                  2.3.3.1     Ensure chrony is configured with authorized timeserver           
                  2.3.3.3     Ensure chrony is enabled and running                             
                  2.4.1.2     Ensure access to /etc/crontab is configured                      
                  2.4.1.3     Ensure access to /etc/cron.hourly is configured                  
                  2.4.1.4     Ensure access to /etc/cron.daily is configured                   
                  2.4.1.5     Ensure access to /etc/cron.weekly is configured                  
                  2.4.1.6     Ensure access to /etc/cron.monthly is configured                 
                  2.4.1.8     Ensure access to /etc/cron.d is configured                       
                  2.4.1.9     Ensure access to crontab is configured                           
                  2.4.2.1     Ensure access to at is configured                                
                  3.1.1       Ensure IPv6 status is identified                                 

                                         Recommendation                                      Set
                                                                                          Correctly
                                                                                          Yes No
                  3.1.2       Ensure wireless interfaces are not available                      
                  3.1.3       Ensure bluetooth services are not in use                          
                  3.2.1       Ensure atm kernel module is not available                         
                  3.2.2       Ensure can kernel module is not available                         
                  3.2.3       Ensure dccp kernel module is not available                        
                  3.2.4       Ensure rds kernel module is not available                         
                  3.2.5       Ensure sctp kernel module is not available                        
                  3.2.6       Ensure tipc kernel module is not available                        
                  3.3.1.1     Ensure net.ipv4.ip_forward is configured                          
                  3.3.1.2     Ensure net.ipv4.conf.all.forwarding is configured                 
                  3.3.1.3     Ensure net.ipv4.conf.default.forwarding is configured             
                  3.3.1.4     Ensure net.ipv4.conf.all.send_redirects is configured             
                  3.3.1.5     Ensure net.ipv4.conf.default.send_redirects is configured         
                  3.3.1.6     Ensure net.ipv4.icmp_ignore_bogus_error_responses is
                                                                                               
                              configured
                  3.3.1.7     Ensure net.ipv4.icmp_echo_ignore_broadcasts is
                                                                                               
                              configured
                  3.3.1.8     Ensure net.ipv4.conf.all.accept_redirects is configured          
                  3.3.1.9     Ensure net.ipv4.conf.default.accept_redirects is
                                                                                               
                              configured
                  3.3.1.10    Ensure net.ipv4.conf.all.secure_redirects is configured          
                  3.3.1.11    Ensure net.ipv4.conf.default.secure_redirects is
                                                                                               
                              configured
                  3.3.1.12    Ensure net.ipv4.conf.all.rp_filter is configured                 
                  3.3.1.13    Ensure net.ipv4.conf.default.rp_filter is configured             
                  3.3.1.14    Ensure net.ipv4.conf.all.accept_source_route is
                                                                                               
                              configured
                  3.3.1.15    Ensure net.ipv4.conf.default.accept_source_route is
                                                                                               
                              configured
                  3.3.1.16    Ensure net.ipv4.conf.all.log_martians is configured              
                  3.3.1.17    Ensure net.ipv4.conf.default.log_martians is configured          
                  3.3.1.18    Ensure net.ipv4.tcp_syncookies is configured                     
                  3.3.2.1     Ensure net.ipv6.conf.all.forwarding is configured                
                  3.3.2.2     Ensure net.ipv6.conf.default.forwarding is configured            
                  3.3.2.3     Ensure net.ipv6.conf.all.accept_redirects is configured          
                  3.3.2.4     Ensure net.ipv6.conf.default.accept_redirects is
                                                                                               
                              configured
                  3.3.2.5     Ensure net.ipv6.conf.all.accept_source_route is
                                                                                               
                              configured
                  3.3.2.6     Ensure net.ipv6.conf.default.accept_source_route is
                                                                                               
                              configured

                                         Recommendation                                      Set
                                                                                          Correctly
                                                                                          Yes No
                  3.3.2.7     Ensure net.ipv6.conf.all.accept_ra is configured                  
                  3.3.2.8     Ensure net.ipv6.conf.default.accept_ra is configured              
                  4.1.1       Ensure ufw is installed                                           
                  4.1.2       Ensure ufw service is configured                                  
                  4.1.3       Ensure ufw incoming default is configured                         
                  4.1.4       Ensure ufw outgoing default is configured                         
                  4.1.5       Ensure ufw routed default is configured                           
                  5.1.1       Ensure access to /etc/ssh/sshd_config is configured               
                  5.1.2       Ensure access to SSH private host key files is configured         
                  5.1.3       Ensure access to SSH public host key files is configured          
                  5.1.4       Ensure sshd access is configured                                  
                  5.1.6       Ensure sshd Ciphers are configured                                
                  5.1.8       Ensure sshd DisableForwarding is enabled                          
                  5.1.9       Ensure sshd GSSAPIAuthentication is disabled                      
                  5.1.10      Ensure sshd HostbasedAuthentication is disabled                   
                  5.1.11      Ensure sshd IgnoreRhosts is enabled                               
                  5.1.12      Ensure sshd KexAlgorithms is configured                           
                  5.1.13      Ensure sshd post-quantum cryptography key exchange
                                                                                               
                              algorithms are configured
                  5.1.15      Ensure sshd LogLevel is configured                               
                  5.1.16      Ensure sshd MACs are configured                                  
                  5.1.17      Ensure sshd MaxAuthTries is configured                           
                  5.1.20      Ensure sshd PermitEmptyPasswords is disabled                     
                  5.1.21      Ensure sshd PermitRootLogin is disabled                          
                  5.1.23      Ensure sshd UsePAM is enabled                                    
                  5.2.1       Ensure sudo is installed                                         
                  5.2.2       Ensure sudo commands use pty                                     
                  5.2.3       Ensure sudo log file exists                                      
                  5.2.4       Ensure users must provide password for escalation                
                  5.2.5       Ensure re-authentication for privilege escalation is not
                                                                                               
                              disabled globally
                  5.2.6       Ensure sudo timestamp_timeout is configured                      
                  5.2.7       Ensure access to the su command is restricted                    
                  5.3.1.3     Ensure latest version of libpam-pwquality is installed           
                  5.3.2.1     Ensure pam_unix module is enabled                                
                  5.3.2.2     Ensure pam_faillock module is enabled                            
                  5.3.2.3     Ensure pam_pwquality module is enabled                           
                  5.3.2.4     Ensure pam_pwhistory module is enabled                           
                  5.3.3.1.1   Ensure password failed attempts lockout is configured            
                  5.3.3.1.2   Ensure password unlock time is configured                        

                                         Recommendation                                     Set
                                                                                          Correctly
                                                                                          Yes No
                  5.3.3.1.3   Ensure password failed attempts lockout includes root
                                                                                               
                              account
                  5.3.3.2.1   Ensure password number of changed characters is
                                                                                               
                              configured
                  5.3.3.2.2   Ensure password length is configured                             
                  5.3.3.2.3   Ensure password complexity is configured                         
                  5.3.3.2.4   Ensure password same consecutive characters is
                                                                                               
                              configured
                  5.3.3.2.5   Ensure password maximum sequential characters is
                                                                                               
                              configured
                  5.3.3.2.6   Ensure password dictionary check is enabled                      
                  5.3.3.2.7   Ensure password quality checking is enforced                     
                  5.3.3.2.8   Ensure password quality is enforced for the root user            
                  5.3.3.3.1   Ensure password history remember is configured                   
                  5.3.3.3.2   Ensure password history is enforced for the root user            
                  5.3.3.3.3   Ensure pam_pwhistory includes use_authtok                        
                  5.3.3.4.1   Ensure pam_unix does not include nullok                          
                  5.3.3.4.2   Ensure pam_unix does not include remember                        
                  5.3.3.4.3   Ensure pam_unix includes a strong password hashing
                                                                                               
                              algorithm
                  5.3.3.4.4   Ensure pam_unix includes use_authtok                             
                  5.4.1.1     Ensure password expiration is configured                         
                  5.4.1.2     Ensure minimum password days is configured                       
                  5.4.1.3     Ensure password expiration warning days is configured            
                  5.4.1.4     Ensure strong password hashing algorithm is configured           
                  5.4.1.5     Ensure inactive password lock is configured                      
                  5.4.1.6     Ensure all users last password change date is in the past        
                  5.4.2.1     Ensure root is the only UID 0 account                            
                  5.4.2.2     Ensure root is the only GID 0 account                            
                  5.4.2.3     Ensure group root is the only GID 0 group                        
                  5.4.2.4     Ensure root account access is controlled                         
                  5.4.2.6     Ensure root user umask is configured                             
                  5.4.2.7     Ensure system accounts do not have a valid login shell           
                  5.4.2.8     Ensure accounts without a valid login shell are locked           
                  5.4.3.2     Ensure default user shell timeout is configured                  
                  5.4.3.3     Ensure default user umask is configured                          
                  6.1.1.1.1   Ensure journald service is active                                
                  6.1.1.1.2   Ensure journald log file access is configured                    
                  6.1.1.1.3   Ensure journald log file rotation is configured                  
                  6.1.1.1.4   Ensure journald ForwardToSyslog is disabled                      
                  6.1.1.1.5   Ensure journald Storage is configured                            

                                         Recommendation                                        Set
                                                                                            Correctly
                                                                                            Yes No
                  6.1.1.1.6   Ensure journald Compress is configured                              
                  6.1.1.2.1   Ensure systemd-journal-remote is installed                          
                  6.1.1.2.2   Ensure systemd-journal-upload authentication is
                                                                                                 
                              configured
                  6.1.1.2.3   Ensure systemd-journal-upload is enabled and active                
                  6.1.1.2.4   Ensure systemd-journal-remote service is not in use                
                  6.1.2.1     Ensure rsyslog is installed                                        
                  6.1.2.2     Ensure rsyslog service is enabled and active                       
                  6.1.2.3     Ensure journald is configured to send logs to rsyslog              
                  6.1.2.4     Ensure rsyslog log file creation mode is configured                
                  6.1.2.5     Ensure rsyslog logging is configured                               
                  6.1.2.6     Ensure rsyslog is configured to send logs to a remote log
                                                                                                 
                              host
                  6.1.2.7     Ensure rsyslog is not configured to receive logs from a
                                                                                                 
                              remote client
                  6.1.2.8     Ensure logrotate is configured                                     
                  6.1.2.9     Ensure rsyslog-gnutls is installed                                 
                  6.1.2.10    Ensure rsyslog forwarding uses gtls                                
                  6.1.2.11    Ensure rsyslog CA certificates are configured                      
                  6.1.3.1     Ensure access to all logfiles has been configured                  
                  6.2.1.1     Ensure auditd packages are installed                               
                  6.2.1.2     Ensure auditd service is enabled and active                        
                  6.2.1.3     Ensure auditing for processes that start prior to auditd is
                                                                                                 
                              enabled
                  6.2.1.4     Ensure audit_backlog_limit is configured                           
                  6.2.2.1     Ensure audit log storage size is configured                        
                  6.2.2.2     Ensure audit logs are not automatically deleted                    
                  6.2.2.4     Ensure system warns when audit logs are low on space               
                  6.2.3.1     Ensure modification of the /etc/sudoers file is collected          
                  6.2.3.2     Ensure actions as another user are always logged                   
                  6.2.3.3     Ensure events that modify the sudo log file are collected          
                  6.2.3.4     Ensure events that modify date and time information are
                                                                                                 
                              collected
                  6.2.3.5     Ensure events that modify sethostname and
                                                                                                 
                              setdomainname are collected
                  6.2.3.6     Ensure events that modify /etc/issue and /etc/issue.net
                                                                                                 
                              are collected
                  6.2.3.7     Ensure events that modify /etc/hosts and /etc/hostname
                                                                                                 
                              are collected
                  6.2.3.8     Ensure events that modify the system's network
                                                                                                 
                              environment are collected

                                         Recommendation                                       Set
                                                                                            Correctly
                                                                                            Yes No
                  6.2.3.9     Ensure events that modify /etc/NetworkManager
                                                                                                 
                              directory are collected
                  6.2.3.10    Ensure use of privileged commands are collected                    
                  6.2.3.11    Ensure unsuccessful file access attempts are collected             
                  6.2.3.12    Ensure events that modify /etc/group information are
                                                                                                 
                              collected
                  6.2.3.13    Ensure events that modify /etc/passwd information are
                                                                                                 
                              collected
                  6.2.3.14    Ensure events that modify /etc/shadow and /etc/gshadow
                                                                                                 
                              are collected
                  6.2.3.15    Ensure events that modify /etc/security/opasswd are
                                                                                                 
                              collected
                  6.2.3.16    Ensure events that modify /etc/nsswitch.conf file are
                                                                                                 
                              collected
                  6.2.3.17    Ensure events that modify /etc/pam.conf and /etc/pam.d/
                                                                                                 
                              information are collected
                  6.2.3.18    Ensure discretionary access control permission
                              modification events chmod,fchmod,fchmodat,fchmodat2                
                              are collected
                  6.2.3.19    Ensure discretionary access control permission
                              modification events chown,fchown,lchown,fchownat are               
                              collected
                  6.2.3.20    Ensure discretionary access control permission
                              modification events
                                                                                                 
                              setxattr,lsetxattr,fsetxattr,removexattr,lremovexattr,fremo
                              vexattr collected
                  6.2.3.21    Ensure successful file system mounts are collected                 
                  6.2.3.22    Ensure session initiation information is collected                 
                  6.2.3.23    Ensure login and logout events are collected                       
                  6.2.3.24    Ensure unlink file deletion events by users are collected          
                  6.2.3.25    Ensure rename file deletion events by users are collected          
                  6.2.3.26    Ensure events that modify the system's Mandatory
                                                                                                 
                              Access Controls are collected
                  6.2.3.27    Ensure successful and unsuccessful attempts to use the
                                                                                                 
                              chcon command are collected
                  6.2.3.28    Ensure successful and unsuccessful attempts to use the
                                                                                                 
                              setfacl command are collected
                  6.2.3.29    Ensure successful and unsuccessful attempts to use the
                                                                                                 
                              chacl command are collected
                  6.2.3.30    Ensure successful and unsuccessful attempts to use the
                                                                                                 
                              usermod command are collected

                                         Recommendation                                    Set
                                                                                         Correctly
                                                                                         Yes No
                  6.2.3.31    Ensure kernel module loading unloading and modification
                                                                                              
                              is collected
                  6.2.3.32    Ensure kernel "init_module" and "finit_module" loading
                                                                                              
                              unloading and modification is collected
                  6.2.3.33    Ensure kernel "delete_module" loading unloading and
                                                                                              
                              modification is collected
                  6.2.3.34    Ensure kernel query_module loading unloading and
                                                                                              
                              modification is collected
                  6.2.3.35    Ensure the audit configuration is loaded regardless of
                                                                                              
                              errors
                  6.2.3.36    Ensure the audit configuration is immutable                     
                  6.2.3.37    Ensure the running and on disk configuration is the same        
                  6.2.4.1     Ensure audit log files mode is configured                       
                  6.2.4.2     Ensure audit log files owner is configured                      
                  6.2.4.3     Ensure audit log files group owner is configured                
                  6.2.4.4     Ensure the audit log file directory mode is configured          
                  6.2.4.5     Ensure audit configuration files mode is configured             
                  6.2.4.6     Ensure audit configuration files owner is configured            
                  6.2.4.7     Ensure audit configuration files group owner is
                                                                                              
                              configured
                  6.2.4.8     Ensure audit tools mode is configured                           
                  6.2.4.9     Ensure audit tools owner is configured                          
                  6.2.4.10    Ensure audit tools group owner is configured                    
                  6.3.1       Ensure AIDE is installed                                        
                  6.3.2       Ensure filesystem integrity is regularly checked                
                  7.1.1       Ensure access to /etc/passwd is configured                      
                  7.1.2       Ensure access to /etc/passwd- is configured                     
                  7.1.3       Ensure access to /etc/group is configured                       
                  7.1.4       Ensure access to /etc/group- is configured                      
                  7.1.5       Ensure access to /etc/shadow is configured                      
                  7.1.6       Ensure access to /etc/shadow- is configured                     
                  7.1.7       Ensure access to /etc/gshadow is configured                     
                  7.1.8       Ensure access to /etc/gshadow- is configured                    
                  7.1.9       Ensure access to /etc/shells is configured                      
                  7.1.10      Ensure access to /etc/security/opasswd is configured            
                  7.1.11      Ensure world writable files and directories are secured         
                  7.1.12      Ensure no files or directories without an owner and a
                                                                                              
                              group exist
                  7.1.13      Ensure SUID and SGID files are reviewed                         
                  7.2.1       Ensure accounts in /etc/passwd use shadowed
                                                                                              
                              passwords

                                         Recommendation                               Set
                                                                                   Correctly
                                                                                   Yes No
                  7.2.2       Ensure /etc/shadow password fields are not empty           
                  7.2.4       Ensure shadow group is empty                               
                  7.2.9       Ensure local interactive user home directories are
                                                                                        
                              configured
                  7.2.10      Ensure local interactive user dot files access is
                                                                                        
                              configured

                Appendix: CIS Controls v7 Unmapped
                Recommendations
                                        Recommendation                                   Set
                                                                                      Correctly
                                                                                      Yes No
                  1.2.1.2     Ensure weak dependencies are configured                       
                  1.2.1.3     Ensure access to gpg key files are configured                 
                  1.2.1.4     Ensure access to /etc/apt/trusted.gpg.d directory is
                                                                                           
                              configured
                  1.2.1.5     Ensure access to /etc/apt/auth.conf.d directory is
                                                                                           
                              configured
                  1.2.1.7     Ensure access to /usr/share/keyrings directory is
                                                                                           
                              configured
                  1.2.1.8     Ensure access to /etc/apt/sources.list.d directory is
                                                                                           
                              configured
                  1.2.1.9     Ensure access to files in /etc/apt/sources.list.d are
                                                                                           
                              configured
                  1.7.2       Ensure GDM login banner is configured                        
                  1.7.3       Ensure GDM disable-user-list option is enabled               
                  1.7.7       Ensure GDM disabling automatic mounting of removable
                                                                                           
                              media is not overridden
                  2.4.1.7     Ensure access to /etc/cron.yearly is configured              
                  6.2.2.3     Ensure system is disabled when audit logs are full           
                  7.2.3       Ensure all groups in /etc/passwd exist in /etc/group         

                Appendix: CIS Controls v8 IG 1 Mapped
                Recommendations
                                         Recommendation                                     Set
                                                                                         Correctly
                                                                                         Yes No
                  1.1.1.10    Ensure usb-storage kernel module is not available                
                  1.1.2.1.1   Ensure /tmp is tmpfs or a separate partition                     
                  1.1.2.1.2   Ensure nodev option set on /tmp partition                        
                  1.1.2.1.3   Ensure nosuid option set on /tmp partition                       
                  1.1.2.1.4   Ensure noexec option set on /tmp partition                       
                  1.1.2.2.1   Ensure /dev/shm is tmpfs or a separate partition                 
                  1.1.2.2.2   Ensure nodev option set on /dev/shm partition                    
                  1.1.2.2.3   Ensure nosuid option set on /dev/shm partition                   
                  1.1.2.2.4   Ensure noexec option set on /dev/shm partition                   
                  1.1.2.3.1   Ensure separate partition exists for /home                       
                  1.1.2.3.2   Ensure nodev option set on /home partition                       
                  1.1.2.3.3   Ensure nosuid option set on /home partition                      
                  1.1.2.4.1   Ensure separate partition exists for /var                        
                  1.1.2.4.2   Ensure nodev option set on /var partition                        
                  1.1.2.4.3   Ensure nosuid option set on /var partition                       
                  1.1.2.5.1   Ensure separate partition exists for /var/tmp                    
                  1.1.2.5.2   Ensure nodev option set on /var/tmp partition                    
                  1.1.2.5.3   Ensure nosuid option set on /var/tmp partition                   
                  1.1.2.5.4   Ensure noexec option set on /var/tmp partition                   
                  1.1.2.6.1   Ensure separate partition exists for /var/log                    
                  1.1.2.6.2   Ensure nodev option set on /var/log partition                    
                  1.1.2.6.3   Ensure nosuid option set on /var/log partition                   
                  1.1.2.6.4   Ensure noexec option set on /var/log partition                   
                  1.1.2.7.1   Ensure separate partition exists for /var/log/audit              
                  1.1.2.7.2   Ensure nodev option set on /var/log/audit partition              
                  1.1.2.7.3   Ensure nosuid option set on /var/log/audit partition             
                  1.1.2.7.4   Ensure noexec option set on /var/log/audit partition             
                  1.2.1.1     Ensure the source.list and .source files use the Signed-
                                                                                              
                              By option
                  1.2.1.6     Ensure access to files in the /etc/apt/auth.conf.d/
                                                                                              
                              directory is configured
                  1.2.2.1     Ensure updates, patches, and additional security
                                                                                              
                              software are installed
                  1.3.1.1     Ensure apparmor packages are installed                          
                  1.3.1.2     Ensure AppArmor is enabled                                      

                                         Recommendation                                      Set
                                                                                          Correctly
                                                                                          Yes No
                  1.3.1.3     Ensure all AppArmor Profiles are enforcing                        
                  1.4.1       Ensure bootloader password is set                                 
                  1.4.2       Ensure access to bootloader config is configured                  
                  1.5.5       Ensure kernel.dmesg_restrict is configured                        
                  1.6.4       Ensure access to /etc/motd is configured                          
                  1.6.5       Ensure access to /etc/issue is configured                         
                  1.6.6       Ensure access to /etc/issue.net is configured                     
                  1.7.4       Ensure GDM screen locks when the user is idle                     
                  1.7.5       Ensure GDM screen locks cannot be overridden                      
                  1.7.6       Ensure GDM automatic mounting of removable media is
                                                                                               
                              disabled
                  1.7.8       Ensure GDM autorun-never is enabled                              
                  1.7.9       Ensure GDM autorun-never is not overridden                       
                  2.1.1       Ensure autofs services are not in use                            
                  2.4.1.2     Ensure access to /etc/crontab is configured                      
                  2.4.1.3     Ensure access to /etc/cron.hourly is configured                  
                  2.4.1.4     Ensure access to /etc/cron.daily is configured                   
                  2.4.1.5     Ensure access to /etc/cron.weekly is configured                  
                  2.4.1.6     Ensure access to /etc/cron.monthly is configured                 
                  2.4.1.8     Ensure access to /etc/cron.d is configured                       
                  2.4.1.9     Ensure access to crontab is configured                           
                  2.4.2.1     Ensure access to at is configured                                
                  4.1.1       Ensure ufw is installed                                          
                  4.1.2       Ensure ufw service is configured                                 
                  4.1.3       Ensure ufw incoming default is configured                        
                  4.1.4       Ensure ufw outgoing default is configured                        
                  4.1.5       Ensure ufw routed default is configured                          
                  5.1.1       Ensure access to /etc/ssh/sshd_config is configured              
                  5.1.2       Ensure access to SSH private host key files is configured        
                  5.1.3       Ensure access to SSH public host key files is configured         
                  5.1.4       Ensure sshd access is configured                                 
                  5.1.9       Ensure sshd GSSAPIAuthentication is disabled                     
                  5.1.10      Ensure sshd HostbasedAuthentication is disabled                  
                  5.1.11      Ensure sshd IgnoreRhosts is enabled                              
                  5.1.15      Ensure sshd LogLevel is configured                               
                  5.1.20      Ensure sshd PermitEmptyPasswords is disabled                     
                  5.1.21      Ensure sshd PermitRootLogin is disabled                          
                  5.1.23      Ensure sshd UsePAM is enabled                                    
                  5.2.1       Ensure sudo is installed                                         
                  5.2.2       Ensure sudo commands use pty                                     

                                         Recommendation                                      Set
                                                                                          Correctly
                                                                                          Yes No
                  5.2.4       Ensure users must provide password for escalation                 
                  5.2.5       Ensure re-authentication for privilege escalation is not
                                                                                               
                              disabled globally
                  5.2.6       Ensure sudo timestamp_timeout is configured                      
                  5.2.7       Ensure access to the su command is restricted                    
                  5.3.1.3     Ensure latest version of libpam-pwquality is installed           
                  5.3.2.1     Ensure pam_unix module is enabled                                
                  5.3.2.2     Ensure pam_faillock module is enabled                            
                  5.3.2.3     Ensure pam_pwquality module is enabled                           
                  5.3.2.4     Ensure pam_pwhistory module is enabled                           
                  5.3.3.1.1   Ensure password failed attempts lockout is configured            
                  5.3.3.1.2   Ensure password unlock time is configured                        
                  5.3.3.1.3   Ensure password failed attempts lockout includes root
                                                                                               
                              account
                  5.3.3.2.1   Ensure password number of changed characters is
                                                                                               
                              configured
                  5.3.3.2.2   Ensure password length is configured                             
                  5.3.3.2.3   Ensure password complexity is configured                         
                  5.3.3.2.4   Ensure password same consecutive characters is
                                                                                               
                              configured
                  5.3.3.2.5   Ensure password maximum sequential characters is
                                                                                               
                              configured
                  5.3.3.2.6   Ensure password dictionary check is enabled                      
                  5.3.3.2.7   Ensure password quality checking is enforced                     
                  5.3.3.2.8   Ensure password quality is enforced for the root user            
                  5.3.3.3.1   Ensure password history remember is configured                   
                  5.3.3.3.2   Ensure password history is enforced for the root user            
                  5.3.3.4.1   Ensure pam_unix does not include nullok                          
                  5.3.3.4.2   Ensure pam_unix does not include remember                        
                  5.4.1.1     Ensure password expiration is configured                         
                  5.4.1.2     Ensure minimum password days is configured                       
                  5.4.1.3     Ensure password expiration warning days is configured            
                  5.4.1.5     Ensure inactive password lock is configured                      
                  5.4.1.6     Ensure all users last password change date is in the past        
                  5.4.2.1     Ensure root is the only UID 0 account                            
                  5.4.2.2     Ensure root is the only GID 0 account                            
                  5.4.2.3     Ensure group root is the only GID 0 group                        
                  5.4.2.4     Ensure root account access is controlled                         
                  5.4.2.6     Ensure root user umask is configured                             
                  5.4.2.7     Ensure system accounts do not have a valid login shell           
                  5.4.2.8     Ensure accounts without a valid login shell are locked           

                                         Recommendation                                        Set
                                                                                            Correctly
                                                                                            Yes No
                  5.4.3.2     Ensure default user shell timeout is configured                     
                  5.4.3.3     Ensure default user umask is configured                             
                  6.1.1.1.1   Ensure journald service is active                                   
                  6.1.1.1.2   Ensure journald log file access is configured                       
                  6.1.1.1.3   Ensure journald log file rotation is configured                     
                  6.1.1.1.4   Ensure journald ForwardToSyslog is disabled                         
                  6.1.1.1.5   Ensure journald Storage is configured                               
                  6.1.1.1.6   Ensure journald Compress is configured                              
                  6.1.1.2.1   Ensure systemd-journal-remote is installed                          
                  6.1.1.2.2   Ensure systemd-journal-upload authentication is
                                                                                                 
                              configured
                  6.1.1.2.3   Ensure systemd-journal-upload is enabled and active                
                  6.1.2.1     Ensure rsyslog is installed                                        
                  6.1.2.2     Ensure rsyslog service is enabled and active                       
                  6.1.2.3     Ensure journald is configured to send logs to rsyslog              
                  6.1.2.4     Ensure rsyslog log file creation mode is configured                
                  6.1.2.5     Ensure rsyslog logging is configured                               
                  6.1.2.6     Ensure rsyslog is configured to send logs to a remote log
                                                                                                 
                              host
                  6.1.2.8     Ensure logrotate is configured                                     
                  6.1.2.9     Ensure rsyslog-gnutls is installed                                 
                  6.1.3.1     Ensure access to all logfiles has been configured                  
                  6.2.1.2     Ensure auditd service is enabled and active                        
                  6.2.1.3     Ensure auditing for processes that start prior to auditd is
                                                                                                 
                              enabled
                  6.2.1.4     Ensure audit_backlog_limit is configured                           
                  6.2.2.1     Ensure audit log storage size is configured                        
                  6.2.2.2     Ensure audit logs are not automatically deleted                    
                  6.2.2.3     Ensure system is disabled when audit logs are full                 
                  6.2.2.4     Ensure system warns when audit logs are low on space               
                  6.2.3.27    Ensure successful and unsuccessful attempts to use the
                                                                                                 
                              chcon command are collected
                  6.2.3.28    Ensure successful and unsuccessful attempts to use the
                                                                                                 
                              setfacl command are collected
                  6.2.3.29    Ensure successful and unsuccessful attempts to use the
                                                                                                 
                              chacl command are collected
                  6.2.3.30    Ensure successful and unsuccessful attempts to use the
                                                                                                 
                              usermod command are collected
                  6.2.3.36    Ensure the audit configuration is immutable                        
                  6.2.4.1     Ensure audit log files mode is configured                          
                  6.2.4.2     Ensure audit log files owner is configured                         

                                         Recommendation                                    Set
                                                                                        Correctly
                                                                                        Yes No
                  6.2.4.3     Ensure audit log files group owner is configured                
                  6.2.4.4     Ensure the audit log file directory mode is configured          
                  6.2.4.5     Ensure audit configuration files mode is configured             
                  6.2.4.6     Ensure audit configuration files owner is configured            
                  6.2.4.7     Ensure audit configuration files group owner is
                                                                                             
                              configured
                  6.2.4.8     Ensure audit tools mode is configured                          
                  6.2.4.9     Ensure audit tools owner is configured                         
                  6.2.4.10    Ensure audit tools group owner is configured                   
                  7.1.1       Ensure access to /etc/passwd is configured                     
                  7.1.2       Ensure access to /etc/passwd- is configured                    
                  7.1.3       Ensure access to /etc/group is configured                      
                  7.1.4       Ensure access to /etc/group- is configured                     
                  7.1.5       Ensure access to /etc/shadow is configured                     
                  7.1.6       Ensure access to /etc/shadow- is configured                    
                  7.1.7       Ensure access to /etc/gshadow is configured                    
                  7.1.8       Ensure access to /etc/gshadow- is configured                   
                  7.1.9       Ensure access to /etc/shells is configured                     
                  7.1.10      Ensure access to /etc/security/opasswd is configured           
                  7.1.11      Ensure world writable files and directories are secured        
                  7.1.12      Ensure no files or directories without an owner and a
                                                                                             
                              group exist
                  7.1.13      Ensure SUID and SGID files are reviewed                        
                  7.2.2       Ensure /etc/shadow password fields are not empty               
                  7.2.3       Ensure all groups in /etc/passwd exist in /etc/group           
                  7.2.4       Ensure shadow group is empty                                   
                  7.2.9       Ensure local interactive user home directories are
                                                                                             
                              configured
                  7.2.10      Ensure local interactive user dot files access is
                                                                                             
                              configured

                Appendix: CIS Controls v8 IG 2 Mapped
                Recommendations
                                         Recommendation                                Set
                                                                                    Correctly
                                                                                    Yes No
                  1.1.1.1     Ensure cramfs kernel module is not available                
                  1.1.1.2     Ensure freevxfs kernel module is not available              
                  1.1.1.3     Ensure hfs kernel module is not available                   
                  1.1.1.4     Ensure hfsplus kernel module is not available               
                  1.1.1.5     Ensure jffs2 kernel module is not available                 
                  1.1.1.6     Ensure overlay kernel module is not available               
                  1.1.1.7     Ensure squashfs kernel module is not available              
                  1.1.1.8     Ensure udf kernel module is not available                   
                  1.1.1.9     Ensure firewire-core kernel module is not available         
                  1.1.1.10    Ensure usb-storage kernel module is not available           
                  1.1.1.11    Ensure unused filesystems kernel modules are not
                                                                                         
                              available
                  1.1.2.1.1   Ensure /tmp is tmpfs or a separate partition               
                  1.1.2.1.2   Ensure nodev option set on /tmp partition                  
                  1.1.2.1.3   Ensure nosuid option set on /tmp partition                 
                  1.1.2.1.4   Ensure noexec option set on /tmp partition                 
                  1.1.2.2.1   Ensure /dev/shm is tmpfs or a separate partition           
                  1.1.2.2.2   Ensure nodev option set on /dev/shm partition              
                  1.1.2.2.3   Ensure nosuid option set on /dev/shm partition             
                  1.1.2.2.4   Ensure noexec option set on /dev/shm partition             
                  1.1.2.3.1   Ensure separate partition exists for /home                 
                  1.1.2.3.2   Ensure nodev option set on /home partition                 
                  1.1.2.3.3   Ensure nosuid option set on /home partition                
                  1.1.2.4.1   Ensure separate partition exists for /var                  
                  1.1.2.4.2   Ensure nodev option set on /var partition                  
                  1.1.2.4.3   Ensure nosuid option set on /var partition                 
                  1.1.2.5.1   Ensure separate partition exists for /var/tmp              
                  1.1.2.5.2   Ensure nodev option set on /var/tmp partition              
                  1.1.2.5.3   Ensure nosuid option set on /var/tmp partition             
                  1.1.2.5.4   Ensure noexec option set on /var/tmp partition             
                  1.1.2.6.1   Ensure separate partition exists for /var/log              
                  1.1.2.6.2   Ensure nodev option set on /var/log partition              
                  1.1.2.6.3   Ensure nosuid option set on /var/log partition             
                  1.1.2.6.4   Ensure noexec option set on /var/log partition             
                  1.1.2.7.1   Ensure separate partition exists for /var/log/audit        

                                         Recommendation                                     Set
                                                                                         Correctly
                                                                                         Yes No
                  1.1.2.7.2   Ensure nodev option set on /var/log/audit partition              
                  1.1.2.7.3   Ensure nosuid option set on /var/log/audit partition             
                  1.1.2.7.4   Ensure noexec option set on /var/log/audit partition             
                  1.2.1.1     Ensure the source.list and .source files use the Signed-
                                                                                              
                              By option
                  1.2.1.6     Ensure access to files in the /etc/apt/auth.conf.d/
                                                                                              
                              directory is configured
                  1.2.2.1     Ensure updates, patches, and additional security
                                                                                              
                              software are installed
                  1.3.1.1     Ensure apparmor packages are installed                          
                  1.3.1.2     Ensure AppArmor is enabled                                      
                  1.3.1.3     Ensure all AppArmor Profiles are enforcing                      
                  1.3.1.4     Ensure apparmor_restrict_unprivileged_unconfined is
                                                                                              
                              enabled
                  1.4.1       Ensure bootloader password is set                               
                  1.4.2       Ensure access to bootloader config is configured                
                  1.5.3       Ensure kernel.yama.ptrace_scope is configured                   
                  1.5.5       Ensure kernel.dmesg_restrict is configured                      
                  1.5.7       Ensure Automatic Error Reporting is configured                  
                  1.5.9       Ensure kernel.randomize_va_space is configured                  
                  1.5.10      Ensure kernel.yama.ptrace_scope is configured                   
                  1.6.4       Ensure access to /etc/motd is configured                        
                  1.6.5       Ensure access to /etc/issue is configured                       
                  1.6.6       Ensure access to /etc/issue.net is configured                   
                  1.7.1       Ensure GDM is removed                                           
                  1.7.4       Ensure GDM screen locks when the user is idle                   
                  1.7.5       Ensure GDM screen locks cannot be overridden                    
                  1.7.6       Ensure GDM automatic mounting of removable media is
                                                                                              
                              disabled
                  1.7.8       Ensure GDM autorun-never is enabled                             
                  1.7.9       Ensure GDM autorun-never is not overridden                      
                  1.7.10      Ensure XDMCP is not enabled                                     
                  1.7.11      Ensure Xwayland is configured                                   
                  2.1.1       Ensure autofs services are not in use                           
                  2.1.2       Ensure avahi daemon services are not in use                     
                  2.1.3       Ensure dhcp server services are not in use                      
                  2.1.4       Ensure dns server services are not in use                       
                  2.1.5       Ensure dnsmasq services are not in use                          
                  2.1.6       Ensure ftp server services are not in use                       
                  2.1.7       Ensure ldap server services are not in use                      
                  2.1.8       Ensure message access server services are not in use            

                                         Recommendation                                      Set
                                                                                          Correctly
                                                                                          Yes No
                  2.1.9       Ensure network file system services are not in use                
                  2.1.10      Ensure nis server services are not in use                         
                  2.1.11      Ensure print server services are not in use                       
                  2.1.12      Ensure rpcbind services are not in use                            
                  2.1.13      Ensure rsync services are not in use                              
                  2.1.14      Ensure samba file server services are not in use                  
                  2.1.15      Ensure snmp services are not in use                               
                  2.1.16      Ensure telnet-server services are not in use                      
                  2.1.17      Ensure tftp server services are not in use                        
                  2.1.18      Ensure web proxy server services are not in use                   
                  2.1.19      Ensure web server services are not in use                         
                  2.1.20      Ensure xinetd services are not in use                             
                  2.1.21      Ensure X window server services are not in use                    
                  2.1.22      Ensure mail transfer agents are configured for local-only
                                                                                               
                              mode
                  2.1.23      Ensure only approved services are listening on a network
                                                                                               
                              interface
                  2.2.1       Ensure nis client is not installed                               
                  2.2.2       Ensure rsh client is not installed                               
                  2.2.3       Ensure talk client is not installed                              
                  2.2.4       Ensure telnet client is not installed                            
                  2.2.5       Ensure ldap client is not installed                              
                  2.2.6       Ensure ftp client is not installed                               
                  2.3.1.1     Ensure a single time synchronization daemon is in use            
                  2.3.2.1     Ensure systemd-timesyncd configured with authorized
                                                                                               
                              timeserver
                  2.3.2.2     Ensure systemd-timesyncd is enabled and running                  
                  2.3.3.1     Ensure chrony is configured with authorized timeserver           
                  2.3.3.3     Ensure chrony is enabled and running                             
                  2.4.1.2     Ensure access to /etc/crontab is configured                      
                  2.4.1.3     Ensure access to /etc/cron.hourly is configured                  
                  2.4.1.4     Ensure access to /etc/cron.daily is configured                   
                  2.4.1.5     Ensure access to /etc/cron.weekly is configured                  
                  2.4.1.6     Ensure access to /etc/cron.monthly is configured                 
                  2.4.1.8     Ensure access to /etc/cron.d is configured                       
                  2.4.1.9     Ensure access to crontab is configured                           
                  2.4.2.1     Ensure access to at is configured                                
                  3.1.1       Ensure IPv6 status is identified                                 
                  3.1.2       Ensure wireless interfaces are not available                     
                  3.1.3       Ensure bluetooth services are not in use                         

                                         Recommendation                                      Set
                                                                                          Correctly
                                                                                          Yes No
                  3.2.1       Ensure atm kernel module is not available                         
                  3.2.2       Ensure can kernel module is not available                         
                  3.2.3       Ensure dccp kernel module is not available                        
                  3.2.4       Ensure rds kernel module is not available                         
                  3.2.5       Ensure sctp kernel module is not available                        
                  3.2.6       Ensure tipc kernel module is not available                        
                  3.3.1.1     Ensure net.ipv4.ip_forward is configured                          
                  3.3.1.2     Ensure net.ipv4.conf.all.forwarding is configured                 
                  3.3.1.3     Ensure net.ipv4.conf.default.forwarding is configured             
                  3.3.1.4     Ensure net.ipv4.conf.all.send_redirects is configured             
                  3.3.1.5     Ensure net.ipv4.conf.default.send_redirects is configured         
                  3.3.1.6     Ensure net.ipv4.icmp_ignore_bogus_error_responses is
                                                                                               
                              configured
                  3.3.1.7     Ensure net.ipv4.icmp_echo_ignore_broadcasts is
                                                                                               
                              configured
                  3.3.1.8     Ensure net.ipv4.conf.all.accept_redirects is configured          
                  3.3.1.9     Ensure net.ipv4.conf.default.accept_redirects is
                                                                                               
                              configured
                  3.3.1.10    Ensure net.ipv4.conf.all.secure_redirects is configured          
                  3.3.1.11    Ensure net.ipv4.conf.default.secure_redirects is
                                                                                               
                              configured
                  3.3.1.12    Ensure net.ipv4.conf.all.rp_filter is configured                 
                  3.3.1.13    Ensure net.ipv4.conf.default.rp_filter is configured             
                  3.3.1.14    Ensure net.ipv4.conf.all.accept_source_route is
                                                                                               
                              configured
                  3.3.1.15    Ensure net.ipv4.conf.default.accept_source_route is
                                                                                               
                              configured
                  3.3.1.16    Ensure net.ipv4.conf.all.log_martians is configured              
                  3.3.1.17    Ensure net.ipv4.conf.default.log_martians is configured          
                  3.3.1.18    Ensure net.ipv4.tcp_syncookies is configured                     
                  3.3.2.1     Ensure net.ipv6.conf.all.forwarding is configured                
                  3.3.2.2     Ensure net.ipv6.conf.default.forwarding is configured            
                  3.3.2.3     Ensure net.ipv6.conf.all.accept_redirects is configured          
                  3.3.2.4     Ensure net.ipv6.conf.default.accept_redirects is
                                                                                               
                              configured
                  3.3.2.5     Ensure net.ipv6.conf.all.accept_source_route is
                                                                                               
                              configured
                  3.3.2.6     Ensure net.ipv6.conf.default.accept_source_route is
                                                                                               
                              configured
                  3.3.2.7     Ensure net.ipv6.conf.all.accept_ra is configured                 
                  3.3.2.8     Ensure net.ipv6.conf.default.accept_ra is configured             

                                         Recommendation                                      Set
                                                                                          Correctly
                                                                                          Yes No
                  4.1.1       Ensure ufw is installed                                           
                  4.1.2       Ensure ufw service is configured                                  
                  4.1.3       Ensure ufw incoming default is configured                         
                  4.1.4       Ensure ufw outgoing default is configured                         
                  4.1.5       Ensure ufw routed default is configured                           
                  5.1.1       Ensure access to /etc/ssh/sshd_config is configured               
                  5.1.2       Ensure access to SSH private host key files is configured         
                  5.1.3       Ensure access to SSH public host key files is configured          
                  5.1.4       Ensure sshd access is configured                                  
                  5.1.6       Ensure sshd Ciphers are configured                                
                  5.1.8       Ensure sshd DisableForwarding is enabled                          
                  5.1.9       Ensure sshd GSSAPIAuthentication is disabled                      
                  5.1.10      Ensure sshd HostbasedAuthentication is disabled                   
                  5.1.11      Ensure sshd IgnoreRhosts is enabled                               
                  5.1.12      Ensure sshd KexAlgorithms is configured                           
                  5.1.13      Ensure sshd post-quantum cryptography key exchange
                                                                                               
                              algorithms are configured
                  5.1.15      Ensure sshd LogLevel is configured                               
                  5.1.16      Ensure sshd MACs are configured                                  
                  5.1.17      Ensure sshd MaxAuthTries is configured                           
                  5.1.20      Ensure sshd PermitEmptyPasswords is disabled                     
                  5.1.21      Ensure sshd PermitRootLogin is disabled                          
                  5.1.23      Ensure sshd UsePAM is enabled                                    
                  5.2.1       Ensure sudo is installed                                         
                  5.2.2       Ensure sudo commands use pty                                     
                  5.2.3       Ensure sudo log file exists                                      
                  5.2.4       Ensure users must provide password for escalation                
                  5.2.5       Ensure re-authentication for privilege escalation is not
                                                                                               
                              disabled globally
                  5.2.6       Ensure sudo timestamp_timeout is configured                      
                  5.2.7       Ensure access to the su command is restricted                    
                  5.3.1.3     Ensure latest version of libpam-pwquality is installed           
                  5.3.2.1     Ensure pam_unix module is enabled                                
                  5.3.2.2     Ensure pam_faillock module is enabled                            
                  5.3.2.3     Ensure pam_pwquality module is enabled                           
                  5.3.2.4     Ensure pam_pwhistory module is enabled                           
                  5.3.3.1.1   Ensure password failed attempts lockout is configured            
                  5.3.3.1.2   Ensure password unlock time is configured                        
                  5.3.3.1.3   Ensure password failed attempts lockout includes root
                                                                                               
                              account

                                         Recommendation                                     Set
                                                                                          Correctly
                                                                                          Yes No
                  5.3.3.2.1   Ensure password number of changed characters is
                                                                                               
                              configured
                  5.3.3.2.2   Ensure password length is configured                             
                  5.3.3.2.3   Ensure password complexity is configured                         
                  5.3.3.2.4   Ensure password same consecutive characters is
                                                                                               
                              configured
                  5.3.3.2.5   Ensure password maximum sequential characters is
                                                                                               
                              configured
                  5.3.3.2.6   Ensure password dictionary check is enabled                      
                  5.3.3.2.7   Ensure password quality checking is enforced                     
                  5.3.3.2.8   Ensure password quality is enforced for the root user            
                  5.3.3.3.1   Ensure password history remember is configured                   
                  5.3.3.3.2   Ensure password history is enforced for the root user            
                  5.3.3.3.3   Ensure pam_pwhistory includes use_authtok                        
                  5.3.3.4.1   Ensure pam_unix does not include nullok                          
                  5.3.3.4.2   Ensure pam_unix does not include remember                        
                  5.3.3.4.3   Ensure pam_unix includes a strong password hashing
                                                                                               
                              algorithm
                  5.3.3.4.4   Ensure pam_unix includes use_authtok                             
                  5.4.1.1     Ensure password expiration is configured                         
                  5.4.1.2     Ensure minimum password days is configured                       
                  5.4.1.3     Ensure password expiration warning days is configured            
                  5.4.1.4     Ensure strong password hashing algorithm is configured           
                  5.4.1.5     Ensure inactive password lock is configured                      
                  5.4.1.6     Ensure all users last password change date is in the past        
                  5.4.2.1     Ensure root is the only UID 0 account                            
                  5.4.2.2     Ensure root is the only GID 0 account                            
                  5.4.2.3     Ensure group root is the only GID 0 group                        
                  5.4.2.4     Ensure root account access is controlled                         
                  5.4.2.6     Ensure root user umask is configured                             
                  5.4.2.7     Ensure system accounts do not have a valid login shell           
                  5.4.2.8     Ensure accounts without a valid login shell are locked           
                  5.4.3.2     Ensure default user shell timeout is configured                  
                  5.4.3.3     Ensure default user umask is configured                          
                  6.1.1.1.1   Ensure journald service is active                                
                  6.1.1.1.2   Ensure journald log file access is configured                    
                  6.1.1.1.3   Ensure journald log file rotation is configured                  
                  6.1.1.1.4   Ensure journald ForwardToSyslog is disabled                      
                  6.1.1.1.5   Ensure journald Storage is configured                            
                  6.1.1.1.6   Ensure journald Compress is configured                           
                  6.1.1.2.1   Ensure systemd-journal-remote is installed                       

                                         Recommendation                                       Set
                                                                                            Correctly
                                                                                            Yes No
                  6.1.1.2.2   Ensure systemd-journal-upload authentication is
                                                                                                 
                              configured
                  6.1.1.2.3   Ensure systemd-journal-upload is enabled and active                
                  6.1.1.2.4   Ensure systemd-journal-remote service is not in use                
                  6.1.2.1     Ensure rsyslog is installed                                        
                  6.1.2.2     Ensure rsyslog service is enabled and active                       
                  6.1.2.3     Ensure journald is configured to send logs to rsyslog              
                  6.1.2.4     Ensure rsyslog log file creation mode is configured                
                  6.1.2.5     Ensure rsyslog logging is configured                               
                  6.1.2.6     Ensure rsyslog is configured to send logs to a remote log
                                                                                                 
                              host
                  6.1.2.7     Ensure rsyslog is not configured to receive logs from a
                                                                                                 
                              remote client
                  6.1.2.8     Ensure logrotate is configured                                     
                  6.1.2.9     Ensure rsyslog-gnutls is installed                                 
                  6.1.2.10    Ensure rsyslog forwarding uses gtls                                
                  6.1.2.11    Ensure rsyslog CA certificates are configured                      
                  6.1.3.1     Ensure access to all logfiles has been configured                  
                  6.2.1.1     Ensure auditd packages are installed                               
                  6.2.1.2     Ensure auditd service is enabled and active                        
                  6.2.1.3     Ensure auditing for processes that start prior to auditd is
                                                                                                 
                              enabled
                  6.2.1.4     Ensure audit_backlog_limit is configured                           
                  6.2.2.1     Ensure audit log storage size is configured                        
                  6.2.2.2     Ensure audit logs are not automatically deleted                    
                  6.2.2.3     Ensure system is disabled when audit logs are full                 
                  6.2.2.4     Ensure system warns when audit logs are low on space               
                  6.2.3.1     Ensure modification of the /etc/sudoers file is collected          
                  6.2.3.2     Ensure actions as another user are always logged                   
                  6.2.3.3     Ensure events that modify the sudo log file are collected          
                  6.2.3.4     Ensure events that modify date and time information are
                                                                                                 
                              collected
                  6.2.3.5     Ensure events that modify sethostname and
                                                                                                 
                              setdomainname are collected
                  6.2.3.6     Ensure events that modify /etc/issue and /etc/issue.net
                                                                                                 
                              are collected
                  6.2.3.7     Ensure events that modify /etc/hosts and /etc/hostname
                                                                                                 
                              are collected
                  6.2.3.8     Ensure events that modify the system's network
                                                                                                 
                              environment are collected

                                         Recommendation                                       Set
                                                                                            Correctly
                                                                                            Yes No
                  6.2.3.9     Ensure events that modify /etc/NetworkManager
                                                                                                 
                              directory are collected
                  6.2.3.10    Ensure use of privileged commands are collected                    
                  6.2.3.11    Ensure unsuccessful file access attempts are collected             
                  6.2.3.12    Ensure events that modify /etc/group information are
                                                                                                 
                              collected
                  6.2.3.13    Ensure events that modify /etc/passwd information are
                                                                                                 
                              collected
                  6.2.3.14    Ensure events that modify /etc/shadow and /etc/gshadow
                                                                                                 
                              are collected
                  6.2.3.15    Ensure events that modify /etc/security/opasswd are
                                                                                                 
                              collected
                  6.2.3.16    Ensure events that modify /etc/nsswitch.conf file are
                                                                                                 
                              collected
                  6.2.3.17    Ensure events that modify /etc/pam.conf and /etc/pam.d/
                                                                                                 
                              information are collected
                  6.2.3.18    Ensure discretionary access control permission
                              modification events chmod,fchmod,fchmodat,fchmodat2                
                              are collected
                  6.2.3.19    Ensure discretionary access control permission
                              modification events chown,fchown,lchown,fchownat are               
                              collected
                  6.2.3.20    Ensure discretionary access control permission
                              modification events
                                                                                                 
                              setxattr,lsetxattr,fsetxattr,removexattr,lremovexattr,fremo
                              vexattr collected
                  6.2.3.21    Ensure successful file system mounts are collected                 
                  6.2.3.22    Ensure session initiation information is collected                 
                  6.2.3.23    Ensure login and logout events are collected                       
                  6.2.3.24    Ensure unlink file deletion events by users are collected          
                  6.2.3.25    Ensure rename file deletion events by users are collected          
                  6.2.3.26    Ensure events that modify the system's Mandatory
                                                                                                 
                              Access Controls are collected
                  6.2.3.27    Ensure successful and unsuccessful attempts to use the
                                                                                                 
                              chcon command are collected
                  6.2.3.28    Ensure successful and unsuccessful attempts to use the
                                                                                                 
                              setfacl command are collected
                  6.2.3.29    Ensure successful and unsuccessful attempts to use the
                                                                                                 
                              chacl command are collected
                  6.2.3.30    Ensure successful and unsuccessful attempts to use the
                                                                                                 
                              usermod command are collected

                                         Recommendation                                    Set
                                                                                         Correctly
                                                                                         Yes No
                  6.2.3.31    Ensure kernel module loading unloading and modification
                                                                                              
                              is collected
                  6.2.3.32    Ensure kernel "init_module" and "finit_module" loading
                                                                                              
                              unloading and modification is collected
                  6.2.3.33    Ensure kernel "delete_module" loading unloading and
                                                                                              
                              modification is collected
                  6.2.3.34    Ensure kernel query_module loading unloading and
                                                                                              
                              modification is collected
                  6.2.3.35    Ensure the audit configuration is loaded regardless of
                                                                                              
                              errors
                  6.2.3.36    Ensure the audit configuration is immutable                     
                  6.2.3.37    Ensure the running and on disk configuration is the same        
                  6.2.4.1     Ensure audit log files mode is configured                       
                  6.2.4.2     Ensure audit log files owner is configured                      
                  6.2.4.3     Ensure audit log files group owner is configured                
                  6.2.4.4     Ensure the audit log file directory mode is configured          
                  6.2.4.5     Ensure audit configuration files mode is configured             
                  6.2.4.6     Ensure audit configuration files owner is configured            
                  6.2.4.7     Ensure audit configuration files group owner is
                                                                                              
                              configured
                  6.2.4.8     Ensure audit tools mode is configured                           
                  6.2.4.9     Ensure audit tools owner is configured                          
                  6.2.4.10    Ensure audit tools group owner is configured                    
                  7.1.1       Ensure access to /etc/passwd is configured                      
                  7.1.2       Ensure access to /etc/passwd- is configured                     
                  7.1.3       Ensure access to /etc/group is configured                       
                  7.1.4       Ensure access to /etc/group- is configured                      
                  7.1.5       Ensure access to /etc/shadow is configured                      
                  7.1.6       Ensure access to /etc/shadow- is configured                     
                  7.1.7       Ensure access to /etc/gshadow is configured                     
                  7.1.8       Ensure access to /etc/gshadow- is configured                    
                  7.1.9       Ensure access to /etc/shells is configured                      
                  7.1.10      Ensure access to /etc/security/opasswd is configured            
                  7.1.11      Ensure world writable files and directories are secured         
                  7.1.12      Ensure no files or directories without an owner and a
                                                                                              
                              group exist
                  7.1.13      Ensure SUID and SGID files are reviewed                         
                  7.2.1       Ensure accounts in /etc/passwd use shadowed
                                                                                              
                              passwords
                  7.2.2       Ensure /etc/shadow password fields are not empty                
                  7.2.3       Ensure all groups in /etc/passwd exist in /etc/group            

                                         Recommendation                               Set
                                                                                   Correctly
                                                                                   Yes No
                  7.2.4       Ensure shadow group is empty                               
                  7.2.9       Ensure local interactive user home directories are
                                                                                        
                              configured
                  7.2.10      Ensure local interactive user dot files access is
                                                                                        
                              configured

                Appendix: CIS Controls v8 IG 3 Mapped
                Recommendations
                                         Recommendation                                Set
                                                                                    Correctly
                                                                                    Yes No
                  1.1.1.1     Ensure cramfs kernel module is not available                
                  1.1.1.2     Ensure freevxfs kernel module is not available              
                  1.1.1.3     Ensure hfs kernel module is not available                   
                  1.1.1.4     Ensure hfsplus kernel module is not available               
                  1.1.1.5     Ensure jffs2 kernel module is not available                 
                  1.1.1.6     Ensure overlay kernel module is not available               
                  1.1.1.7     Ensure squashfs kernel module is not available              
                  1.1.1.8     Ensure udf kernel module is not available                   
                  1.1.1.9     Ensure firewire-core kernel module is not available         
                  1.1.1.10    Ensure usb-storage kernel module is not available           
                  1.1.1.11    Ensure unused filesystems kernel modules are not
                                                                                         
                              available
                  1.1.2.1.1   Ensure /tmp is tmpfs or a separate partition               
                  1.1.2.1.2   Ensure nodev option set on /tmp partition                  
                  1.1.2.1.3   Ensure nosuid option set on /tmp partition                 
                  1.1.2.1.4   Ensure noexec option set on /tmp partition                 
                  1.1.2.2.1   Ensure /dev/shm is tmpfs or a separate partition           
                  1.1.2.2.2   Ensure nodev option set on /dev/shm partition              
                  1.1.2.2.3   Ensure nosuid option set on /dev/shm partition             
                  1.1.2.2.4   Ensure noexec option set on /dev/shm partition             
                  1.1.2.3.1   Ensure separate partition exists for /home                 
                  1.1.2.3.2   Ensure nodev option set on /home partition                 
                  1.1.2.3.3   Ensure nosuid option set on /home partition                
                  1.1.2.4.1   Ensure separate partition exists for /var                  
                  1.1.2.4.2   Ensure nodev option set on /var partition                  
                  1.1.2.4.3   Ensure nosuid option set on /var partition                 
                  1.1.2.5.1   Ensure separate partition exists for /var/tmp              
                  1.1.2.5.2   Ensure nodev option set on /var/tmp partition              
                  1.1.2.5.3   Ensure nosuid option set on /var/tmp partition             
                  1.1.2.5.4   Ensure noexec option set on /var/tmp partition             
                  1.1.2.6.1   Ensure separate partition exists for /var/log              
                  1.1.2.6.2   Ensure nodev option set on /var/log partition              
                  1.1.2.6.3   Ensure nosuid option set on /var/log partition             
                  1.1.2.6.4   Ensure noexec option set on /var/log partition             
                  1.1.2.7.1   Ensure separate partition exists for /var/log/audit        

                                         Recommendation                                     Set
                                                                                         Correctly
                                                                                         Yes No
                  1.1.2.7.2   Ensure nodev option set on /var/log/audit partition              
                  1.1.2.7.3   Ensure nosuid option set on /var/log/audit partition             
                  1.1.2.7.4   Ensure noexec option set on /var/log/audit partition             
                  1.2.1.1     Ensure the source.list and .source files use the Signed-
                                                                                              
                              By option
                  1.2.1.6     Ensure access to files in the /etc/apt/auth.conf.d/
                                                                                              
                              directory is configured
                  1.2.2.1     Ensure updates, patches, and additional security
                                                                                              
                              software are installed
                  1.3.1.1     Ensure apparmor packages are installed                          
                  1.3.1.2     Ensure AppArmor is enabled                                      
                  1.3.1.3     Ensure all AppArmor Profiles are enforcing                      
                  1.3.1.4     Ensure apparmor_restrict_unprivileged_unconfined is
                                                                                              
                              enabled
                  1.4.1       Ensure bootloader password is set                               
                  1.4.2       Ensure access to bootloader config is configured                
                  1.5.3       Ensure kernel.yama.ptrace_scope is configured                   
                  1.5.5       Ensure kernel.dmesg_restrict is configured                      
                  1.5.6       Ensure prelink is not installed                                 
                  1.5.7       Ensure Automatic Error Reporting is configured                  
                  1.5.9       Ensure kernel.randomize_va_space is configured                  
                  1.5.10      Ensure kernel.yama.ptrace_scope is configured                   
                  1.6.4       Ensure access to /etc/motd is configured                        
                  1.6.5       Ensure access to /etc/issue is configured                       
                  1.6.6       Ensure access to /etc/issue.net is configured                   
                  1.7.1       Ensure GDM is removed                                           
                  1.7.4       Ensure GDM screen locks when the user is idle                   
                  1.7.5       Ensure GDM screen locks cannot be overridden                    
                  1.7.6       Ensure GDM automatic mounting of removable media is
                                                                                              
                              disabled
                  1.7.8       Ensure GDM autorun-never is enabled                             
                  1.7.9       Ensure GDM autorun-never is not overridden                      
                  1.7.10      Ensure XDMCP is not enabled                                     
                  1.7.11      Ensure Xwayland is configured                                   
                  2.1.1       Ensure autofs services are not in use                           
                  2.1.2       Ensure avahi daemon services are not in use                     
                  2.1.3       Ensure dhcp server services are not in use                      
                  2.1.4       Ensure dns server services are not in use                       
                  2.1.5       Ensure dnsmasq services are not in use                          
                  2.1.6       Ensure ftp server services are not in use                       
                  2.1.7       Ensure ldap server services are not in use                      

                                         Recommendation                                      Set
                                                                                          Correctly
                                                                                          Yes No
                  2.1.8       Ensure message access server services are not in use              
                  2.1.9       Ensure network file system services are not in use                
                  2.1.10      Ensure nis server services are not in use                         
                  2.1.11      Ensure print server services are not in use                       
                  2.1.12      Ensure rpcbind services are not in use                            
                  2.1.13      Ensure rsync services are not in use                              
                  2.1.14      Ensure samba file server services are not in use                  
                  2.1.15      Ensure snmp services are not in use                               
                  2.1.16      Ensure telnet-server services are not in use                      
                  2.1.17      Ensure tftp server services are not in use                        
                  2.1.18      Ensure web proxy server services are not in use                   
                  2.1.19      Ensure web server services are not in use                         
                  2.1.20      Ensure xinetd services are not in use                             
                  2.1.21      Ensure X window server services are not in use                    
                  2.1.22      Ensure mail transfer agents are configured for local-only
                                                                                               
                              mode
                  2.1.23      Ensure only approved services are listening on a network
                                                                                               
                              interface
                  2.2.1       Ensure nis client is not installed                               
                  2.2.2       Ensure rsh client is not installed                               
                  2.2.3       Ensure talk client is not installed                              
                  2.2.4       Ensure telnet client is not installed                            
                  2.2.5       Ensure ldap client is not installed                              
                  2.2.6       Ensure ftp client is not installed                               
                  2.3.1.1     Ensure a single time synchronization daemon is in use            
                  2.3.2.1     Ensure systemd-timesyncd configured with authorized
                                                                                               
                              timeserver
                  2.3.2.2     Ensure systemd-timesyncd is enabled and running                  
                  2.3.3.1     Ensure chrony is configured with authorized timeserver           
                  2.3.3.3     Ensure chrony is enabled and running                             
                  2.4.1.2     Ensure access to /etc/crontab is configured                      
                  2.4.1.3     Ensure access to /etc/cron.hourly is configured                  
                  2.4.1.4     Ensure access to /etc/cron.daily is configured                   
                  2.4.1.5     Ensure access to /etc/cron.weekly is configured                  
                  2.4.1.6     Ensure access to /etc/cron.monthly is configured                 
                  2.4.1.8     Ensure access to /etc/cron.d is configured                       
                  2.4.1.9     Ensure access to crontab is configured                           
                  2.4.2.1     Ensure access to at is configured                                
                  3.1.1       Ensure IPv6 status is identified                                 
                  3.1.2       Ensure wireless interfaces are not available                     

                                         Recommendation                                      Set
                                                                                          Correctly
                                                                                          Yes No
                  3.1.3       Ensure bluetooth services are not in use                          
                  3.2.1       Ensure atm kernel module is not available                         
                  3.2.2       Ensure can kernel module is not available                         
                  3.2.3       Ensure dccp kernel module is not available                        
                  3.2.4       Ensure rds kernel module is not available                         
                  3.2.5       Ensure sctp kernel module is not available                        
                  3.2.6       Ensure tipc kernel module is not available                        
                  3.3.1.1     Ensure net.ipv4.ip_forward is configured                          
                  3.3.1.2     Ensure net.ipv4.conf.all.forwarding is configured                 
                  3.3.1.3     Ensure net.ipv4.conf.default.forwarding is configured             
                  3.3.1.4     Ensure net.ipv4.conf.all.send_redirects is configured             
                  3.3.1.5     Ensure net.ipv4.conf.default.send_redirects is configured         
                  3.3.1.6     Ensure net.ipv4.icmp_ignore_bogus_error_responses is
                                                                                               
                              configured
                  3.3.1.7     Ensure net.ipv4.icmp_echo_ignore_broadcasts is
                                                                                               
                              configured
                  3.3.1.8     Ensure net.ipv4.conf.all.accept_redirects is configured          
                  3.3.1.9     Ensure net.ipv4.conf.default.accept_redirects is
                                                                                               
                              configured
                  3.3.1.10    Ensure net.ipv4.conf.all.secure_redirects is configured          
                  3.3.1.11    Ensure net.ipv4.conf.default.secure_redirects is
                                                                                               
                              configured
                  3.3.1.12    Ensure net.ipv4.conf.all.rp_filter is configured                 
                  3.3.1.13    Ensure net.ipv4.conf.default.rp_filter is configured             
                  3.3.1.14    Ensure net.ipv4.conf.all.accept_source_route is
                                                                                               
                              configured
                  3.3.1.15    Ensure net.ipv4.conf.default.accept_source_route is
                                                                                               
                              configured
                  3.3.1.16    Ensure net.ipv4.conf.all.log_martians is configured              
                  3.3.1.17    Ensure net.ipv4.conf.default.log_martians is configured          
                  3.3.1.18    Ensure net.ipv4.tcp_syncookies is configured                     
                  3.3.2.1     Ensure net.ipv6.conf.all.forwarding is configured                
                  3.3.2.2     Ensure net.ipv6.conf.default.forwarding is configured            
                  3.3.2.3     Ensure net.ipv6.conf.all.accept_redirects is configured          
                  3.3.2.4     Ensure net.ipv6.conf.default.accept_redirects is
                                                                                               
                              configured
                  3.3.2.5     Ensure net.ipv6.conf.all.accept_source_route is
                                                                                               
                              configured
                  3.3.2.6     Ensure net.ipv6.conf.default.accept_source_route is
                                                                                               
                              configured
                  3.3.2.7     Ensure net.ipv6.conf.all.accept_ra is configured                 

                                         Recommendation                                      Set
                                                                                          Correctly
                                                                                          Yes No
                  3.3.2.8     Ensure net.ipv6.conf.default.accept_ra is configured              
                  4.1.1       Ensure ufw is installed                                           
                  4.1.2       Ensure ufw service is configured                                  
                  4.1.3       Ensure ufw incoming default is configured                         
                  4.1.4       Ensure ufw outgoing default is configured                         
                  4.1.5       Ensure ufw routed default is configured                           
                  5.1.1       Ensure access to /etc/ssh/sshd_config is configured               
                  5.1.2       Ensure access to SSH private host key files is configured         
                  5.1.3       Ensure access to SSH public host key files is configured          
                  5.1.4       Ensure sshd access is configured                                  
                  5.1.6       Ensure sshd Ciphers are configured                                
                  5.1.8       Ensure sshd DisableForwarding is enabled                          
                  5.1.9       Ensure sshd GSSAPIAuthentication is disabled                      
                  5.1.10      Ensure sshd HostbasedAuthentication is disabled                   
                  5.1.11      Ensure sshd IgnoreRhosts is enabled                               
                  5.1.12      Ensure sshd KexAlgorithms is configured                           
                  5.1.13      Ensure sshd post-quantum cryptography key exchange
                                                                                               
                              algorithms are configured
                  5.1.15      Ensure sshd LogLevel is configured                               
                  5.1.16      Ensure sshd MACs are configured                                  
                  5.1.17      Ensure sshd MaxAuthTries is configured                           
                  5.1.20      Ensure sshd PermitEmptyPasswords is disabled                     
                  5.1.21      Ensure sshd PermitRootLogin is disabled                          
                  5.1.23      Ensure sshd UsePAM is enabled                                    
                  5.2.1       Ensure sudo is installed                                         
                  5.2.2       Ensure sudo commands use pty                                     
                  5.2.3       Ensure sudo log file exists                                      
                  5.2.4       Ensure users must provide password for escalation                
                  5.2.5       Ensure re-authentication for privilege escalation is not
                                                                                               
                              disabled globally
                  5.2.6       Ensure sudo timestamp_timeout is configured                      
                  5.2.7       Ensure access to the su command is restricted                    
                  5.3.1.3     Ensure latest version of libpam-pwquality is installed           
                  5.3.2.1     Ensure pam_unix module is enabled                                
                  5.3.2.2     Ensure pam_faillock module is enabled                            
                  5.3.2.3     Ensure pam_pwquality module is enabled                           
                  5.3.2.4     Ensure pam_pwhistory module is enabled                           
                  5.3.3.1.1   Ensure password failed attempts lockout is configured            
                  5.3.3.1.2   Ensure password unlock time is configured                        

                                         Recommendation                                     Set
                                                                                          Correctly
                                                                                          Yes No
                  5.3.3.1.3   Ensure password failed attempts lockout includes root
                                                                                               
                              account
                  5.3.3.2.1   Ensure password number of changed characters is
                                                                                               
                              configured
                  5.3.3.2.2   Ensure password length is configured                             
                  5.3.3.2.3   Ensure password complexity is configured                         
                  5.3.3.2.4   Ensure password same consecutive characters is
                                                                                               
                              configured
                  5.3.3.2.5   Ensure password maximum sequential characters is
                                                                                               
                              configured
                  5.3.3.2.6   Ensure password dictionary check is enabled                      
                  5.3.3.2.7   Ensure password quality checking is enforced                     
                  5.3.3.2.8   Ensure password quality is enforced for the root user            
                  5.3.3.3.1   Ensure password history remember is configured                   
                  5.3.3.3.2   Ensure password history is enforced for the root user            
                  5.3.3.3.3   Ensure pam_pwhistory includes use_authtok                        
                  5.3.3.4.1   Ensure pam_unix does not include nullok                          
                  5.3.3.4.2   Ensure pam_unix does not include remember                        
                  5.3.3.4.3   Ensure pam_unix includes a strong password hashing
                                                                                               
                              algorithm
                  5.3.3.4.4   Ensure pam_unix includes use_authtok                             
                  5.4.1.1     Ensure password expiration is configured                         
                  5.4.1.2     Ensure minimum password days is configured                       
                  5.4.1.3     Ensure password expiration warning days is configured            
                  5.4.1.4     Ensure strong password hashing algorithm is configured           
                  5.4.1.5     Ensure inactive password lock is configured                      
                  5.4.1.6     Ensure all users last password change date is in the past        
                  5.4.2.1     Ensure root is the only UID 0 account                            
                  5.4.2.2     Ensure root is the only GID 0 account                            
                  5.4.2.3     Ensure group root is the only GID 0 group                        
                  5.4.2.4     Ensure root account access is controlled                         
                  5.4.2.6     Ensure root user umask is configured                             
                  5.4.2.7     Ensure system accounts do not have a valid login shell           
                  5.4.2.8     Ensure accounts without a valid login shell are locked           
                  5.4.3.2     Ensure default user shell timeout is configured                  
                  5.4.3.3     Ensure default user umask is configured                          
                  6.1.1.1.1   Ensure journald service is active                                
                  6.1.1.1.2   Ensure journald log file access is configured                    
                  6.1.1.1.3   Ensure journald log file rotation is configured                  
                  6.1.1.1.4   Ensure journald ForwardToSyslog is disabled                      
                  6.1.1.1.5   Ensure journald Storage is configured                            

                                         Recommendation                                        Set
                                                                                            Correctly
                                                                                            Yes No
                  6.1.1.1.6   Ensure journald Compress is configured                              
                  6.1.1.2.1   Ensure systemd-journal-remote is installed                          
                  6.1.1.2.2   Ensure systemd-journal-upload authentication is
                                                                                                 
                              configured
                  6.1.1.2.3   Ensure systemd-journal-upload is enabled and active                
                  6.1.1.2.4   Ensure systemd-journal-remote service is not in use                
                  6.1.2.1     Ensure rsyslog is installed                                        
                  6.1.2.2     Ensure rsyslog service is enabled and active                       
                  6.1.2.3     Ensure journald is configured to send logs to rsyslog              
                  6.1.2.4     Ensure rsyslog log file creation mode is configured                
                  6.1.2.5     Ensure rsyslog logging is configured                               
                  6.1.2.6     Ensure rsyslog is configured to send logs to a remote log
                                                                                                 
                              host
                  6.1.2.7     Ensure rsyslog is not configured to receive logs from a
                                                                                                 
                              remote client
                  6.1.2.8     Ensure logrotate is configured                                     
                  6.1.2.9     Ensure rsyslog-gnutls is installed                                 
                  6.1.2.10    Ensure rsyslog forwarding uses gtls                                
                  6.1.2.11    Ensure rsyslog CA certificates are configured                      
                  6.1.3.1     Ensure access to all logfiles has been configured                  
                  6.2.1.1     Ensure auditd packages are installed                               
                  6.2.1.2     Ensure auditd service is enabled and active                        
                  6.2.1.3     Ensure auditing for processes that start prior to auditd is
                                                                                                 
                              enabled
                  6.2.1.4     Ensure audit_backlog_limit is configured                           
                  6.2.2.1     Ensure audit log storage size is configured                        
                  6.2.2.2     Ensure audit logs are not automatically deleted                    
                  6.2.2.3     Ensure system is disabled when audit logs are full                 
                  6.2.2.4     Ensure system warns when audit logs are low on space               
                  6.2.3.1     Ensure modification of the /etc/sudoers file is collected          
                  6.2.3.2     Ensure actions as another user are always logged                   
                  6.2.3.3     Ensure events that modify the sudo log file are collected          
                  6.2.3.4     Ensure events that modify date and time information are
                                                                                                 
                              collected
                  6.2.3.5     Ensure events that modify sethostname and
                                                                                                 
                              setdomainname are collected
                  6.2.3.6     Ensure events that modify /etc/issue and /etc/issue.net
                                                                                                 
                              are collected
                  6.2.3.7     Ensure events that modify /etc/hosts and /etc/hostname
                                                                                                 
                              are collected

                                         Recommendation                                       Set
                                                                                            Correctly
                                                                                            Yes No
                  6.2.3.8     Ensure events that modify the system's network
                                                                                                 
                              environment are collected
                  6.2.3.9     Ensure events that modify /etc/NetworkManager
                                                                                                 
                              directory are collected
                  6.2.3.10    Ensure use of privileged commands are collected                    
                  6.2.3.11    Ensure unsuccessful file access attempts are collected             
                  6.2.3.12    Ensure events that modify /etc/group information are
                                                                                                 
                              collected
                  6.2.3.13    Ensure events that modify /etc/passwd information are
                                                                                                 
                              collected
                  6.2.3.14    Ensure events that modify /etc/shadow and /etc/gshadow
                                                                                                 
                              are collected
                  6.2.3.15    Ensure events that modify /etc/security/opasswd are
                                                                                                 
                              collected
                  6.2.3.16    Ensure events that modify /etc/nsswitch.conf file are
                                                                                                 
                              collected
                  6.2.3.17    Ensure events that modify /etc/pam.conf and /etc/pam.d/
                                                                                                 
                              information are collected
                  6.2.3.18    Ensure discretionary access control permission
                              modification events chmod,fchmod,fchmodat,fchmodat2                
                              are collected
                  6.2.3.19    Ensure discretionary access control permission
                              modification events chown,fchown,lchown,fchownat are               
                              collected
                  6.2.3.20    Ensure discretionary access control permission
                              modification events
                                                                                                 
                              setxattr,lsetxattr,fsetxattr,removexattr,lremovexattr,fremo
                              vexattr collected
                  6.2.3.21    Ensure successful file system mounts are collected                 
                  6.2.3.22    Ensure session initiation information is collected                 
                  6.2.3.23    Ensure login and logout events are collected                       
                  6.2.3.24    Ensure unlink file deletion events by users are collected          
                  6.2.3.25    Ensure rename file deletion events by users are collected          
                  6.2.3.26    Ensure events that modify the system's Mandatory
                                                                                                 
                              Access Controls are collected
                  6.2.3.27    Ensure successful and unsuccessful attempts to use the
                                                                                                 
                              chcon command are collected
                  6.2.3.28    Ensure successful and unsuccessful attempts to use the
                                                                                                 
                              setfacl command are collected
                  6.2.3.29    Ensure successful and unsuccessful attempts to use the
                                                                                                 
                              chacl command are collected

                                         Recommendation                                    Set
                                                                                         Correctly
                                                                                         Yes No
                  6.2.3.30    Ensure successful and unsuccessful attempts to use the
                                                                                              
                              usermod command are collected
                  6.2.3.31    Ensure kernel module loading unloading and modification
                                                                                              
                              is collected
                  6.2.3.32    Ensure kernel "init_module" and "finit_module" loading
                                                                                              
                              unloading and modification is collected
                  6.2.3.33    Ensure kernel "delete_module" loading unloading and
                                                                                              
                              modification is collected
                  6.2.3.34    Ensure kernel query_module loading unloading and
                                                                                              
                              modification is collected
                  6.2.3.35    Ensure the audit configuration is loaded regardless of
                                                                                              
                              errors
                  6.2.3.36    Ensure the audit configuration is immutable                     
                  6.2.3.37    Ensure the running and on disk configuration is the same        
                  6.2.4.1     Ensure audit log files mode is configured                       
                  6.2.4.2     Ensure audit log files owner is configured                      
                  6.2.4.3     Ensure audit log files group owner is configured                
                  6.2.4.4     Ensure the audit log file directory mode is configured          
                  6.2.4.5     Ensure audit configuration files mode is configured             
                  6.2.4.6     Ensure audit configuration files owner is configured            
                  6.2.4.7     Ensure audit configuration files group owner is
                                                                                              
                              configured
                  6.2.4.8     Ensure audit tools mode is configured                           
                  6.2.4.9     Ensure audit tools owner is configured                          
                  6.2.4.10    Ensure audit tools group owner is configured                    
                  6.3.1       Ensure AIDE is installed                                        
                  6.3.2       Ensure filesystem integrity is regularly checked                
                  7.1.1       Ensure access to /etc/passwd is configured                      
                  7.1.2       Ensure access to /etc/passwd- is configured                     
                  7.1.3       Ensure access to /etc/group is configured                       
                  7.1.4       Ensure access to /etc/group- is configured                      
                  7.1.5       Ensure access to /etc/shadow is configured                      
                  7.1.6       Ensure access to /etc/shadow- is configured                     
                  7.1.7       Ensure access to /etc/gshadow is configured                     
                  7.1.8       Ensure access to /etc/gshadow- is configured                    
                  7.1.9       Ensure access to /etc/shells is configured                      
                  7.1.10      Ensure access to /etc/security/opasswd is configured            
                  7.1.11      Ensure world writable files and directories are secured         
                  7.1.12      Ensure no files or directories without an owner and a
                                                                                              
                              group exist
                  7.1.13      Ensure SUID and SGID files are reviewed                         

                                         Recommendation                                Set
                                                                                     Correctly
                                                                                     Yes No
                  7.2.1       Ensure accounts in /etc/passwd use shadowed
                                                                                          
                              passwords
                  7.2.2       Ensure /etc/shadow password fields are not empty            
                  7.2.3       Ensure all groups in /etc/passwd exist in /etc/group        
                  7.2.4       Ensure shadow group is empty                                
                  7.2.9       Ensure local interactive user home directories are
                                                                                          
                              configured
                  7.2.10      Ensure local interactive user dot files access is
                                                                                          
                              configured

                Appendix: CIS Controls v8 Unmapped
                Recommendations
                                        Recommendation                                   Set
                                                                                      Correctly
                                                                                      Yes No
                  1.2.1.2     Ensure weak dependencies are configured                       
                  1.2.1.3     Ensure access to gpg key files are configured                 
                  1.2.1.4     Ensure access to /etc/apt/trusted.gpg.d directory is
                                                                                           
                              configured
                  1.2.1.5     Ensure access to /etc/apt/auth.conf.d directory is
                                                                                           
                              configured
                  1.2.1.7     Ensure access to /usr/share/keyrings directory is
                                                                                           
                              configured
                  1.2.1.8     Ensure access to /etc/apt/sources.list.d directory is
                                                                                           
                              configured
                  1.2.1.9     Ensure access to files in /etc/apt/sources.list.d are
                                                                                           
                              configured
                  1.5.8       Ensure kernel.kptr_restrict is configured                    
                  1.7.2       Ensure GDM login banner is configured                        
                  1.7.3       Ensure GDM disable-user-list option is enabled               
                  1.7.7       Ensure GDM disabling automatic mounting of removable
                                                                                           
                              media is not overridden
                  2.4.1.7     Ensure access to /etc/cron.yearly is configured              

                Appendix: Change History
                  Date        Version   Changes for this version
